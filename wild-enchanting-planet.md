> **⚠️ 本文已被取代，仅作历史记录保留。当前权威设计见 [`DESIGN.md`](./DESIGN.md)。**
>
> 本文是 v1 设计。2026-07-26 需求细化后（番茄钟形态、多应用白名单、活动组勾选列表），
> 其核心机制「点击开始时把当前活跃窗口记为唯一目标」已不再适用。完整变更对照见
> `DESIGN.md` §11。下面的内容不要拿来当实现依据。

# 专注计时提醒器 —— 软件框架设计（v1，已取代）

## Context

用户想克制自己看 PDF（或做其它任何单一任务）时中途切走的惰性：开一个计时器窗口，选定当前活跃窗口为"目标"，点击开始后计时；如果计时结束前切换到了别的窗口，这个计时器窗口就跳到最前面（置顶显示，不抢键盘焦点）提醒用户切回去。

已确认的关键决策（通过前面几轮讨论 + AskUserQuestion 确认）：
- **检测方式**：纯轮询 ActivityWatch 本地 REST API（`http://127.0.0.1:5600`），不直接调用系统原生的窗口监听 API。可接受 1~5 秒的检测延迟。
- **提醒行为**：窗口置顶显示，**不抢键盘焦点**——只是视觉提醒，不打断用户在别的 App 里正在做的操作。
- **目标窗口选择**：v1 只做"点击开始时，把当前活跃窗口记为目标"这种简单方式（不做拖拽选择）。
- **平台与语言**：这一步**只做平台无关的框架设计**，不涉及具体开发语言/工具。用户后续可能先从 Windows 端用 VC++ 实现（他更熟悉、Windows 是主力机），macOS 版本以后再说。

这份设计的目的是：给出一份两边（未来 Windows/macOS 实现）都能直接对照着写的架构说明，核心逻辑只写一遍，不用等语言/工具选定之后再返工一次概念设计。

## 核心架构洞察：检测层完全平台无关

因为「捕获目标窗口身份」和「持续监测是否切走」这两件事，**全部**通过轮询 AW 的 REST API 完成（不调用任何系统原生窗口 API），所以整个检测层的逻辑在 Windows/macOS 两边可以是完全相同的代码/伪代码，唯一需要平台特定实现的地方只有一处：**把提醒窗口置顶显示、且不抢焦点**（Windows 上对应 `WS_EX_TOPMOST` + `SetWindowPos(HWND_TOPMOST, ...)`，不调用 `SetForegroundWindow`/`SetFocus`；macOS 上对应 `NSWindow.level = .floating`，不调用 `makeKey`）。

这意味着实现顺序上，Windows 版写完之后，macOS 版基本是"把置顶显示那一小块换成 AppKit 写法，其余监测逻辑原样搬过去"。

## AW 接口约定（两边共用）

- 服务发现：`GET /api/0/buckets/` → 找 `type == "currentwindow"` 的 bucket，拿到它的 `id`（形如 `aw-watcher-window_<hostname>`）。程序启动时做一次，缓存住；如果 AW 重启过导致 bucket 找不到，允许重试。
- 读取"当前活跃窗口"：`GET /api/0/buckets/{id}/events?limit=1`，取返回数组里的第一条（AW 按时间倒序返回最新事件）。事件结构里关心两个字段：
  - `data.app`：进程名/App 名（比如 `Preview`、`AcroRd32.exe`）——用来判断"是不是目标程序"的主要依据。
  - `data.title`：窗口标题——浏览器这类"一个进程开多个不同内容窗口"的场景下用来做更细的区分（v1 可以先只比对 `app`，把 `title` 的比对留成可选的严格模式）。
- **AW 不可达时的行为**：不能静默失败。轮询请求失败（连接拒绝/超时）时，程序应该明确提示"连不上 AW，请确认它在运行"，暂停计时（不能假装"没检测到切换"就当作一直没切走），等 AW 恢复后再继续——这跟 `ActivityWatchJournal` 项目里"AW 不可达要如实报告，不能默默跳过"是同一个原则。

## 状态机

```
┌────────┐   点击"开始"，记录当前活跃窗口为 target   ┌───────────┐
│  Idle   │ ─────────────────────────────────────→ │ Counting  │
│(未计时)  │                                          │ (倒计时中) │
└────────┘                                          └─────┬─────┘
    ▲                                                       │
    │ 计时结束，正好一直停留在 target 上（成功）              │ 轮询发现 app != target
    │ ←─────────────────────────────────────────────────────┤
    │                                                       ↓
    │                                                 ┌───────────┐
    │              用户点击窗口上的"知道了/重新开始"     │ Triggered │
    └─────────────────────────────────────────────── │ (提醒中)   │
                                                       └───────────┘
```

- **Idle**：显示"开始"按钮和一个时长设置（比如分钟数输入框/下拉）。
- **Counting**：显示倒计时；每次轮询（建议默认 2~3 秒一次，跟 AW 自身窗口事件的更新粒度匹配，没必要更频繁）比对 `当前 app == target app`：
  - 相符 → 继续倒计时。
  - 不符 → 立即进入 **Triggered**（v1 不做"容忍几秒钟的短暂切走"这种宽限期，检测到就触发；这个宽限期可以留成以后一个可调参数，不是这次的必需项）。
  - 倒计时天然走完（没触发过 Triggered）→ 回到 Idle，可以给个"完成"的提示音/动画。
- **Triggered**：提醒窗口置顶显示（不抢焦点），倒计时停止。用户需要主动点一下"重新开始"（回到 Idle，不自动重新计时——避免用户切回去之后计时器在背后不知不觉又开始跑）。

## 目标窗口的捕获与比对

- 点击"开始"这一刻，直接复用同一个 AW 轮询逻辑读一次 `currentwindow` 最新事件，把它的 `data.app`（未来要做更严格模式的话，也存 `data.title`）存为 `target`。**不需要额外调用任何系统窗口枚举 API**——这跟监测阶段用的是同一份数据源，逻辑复用，实现量最小。
- 比对策略 v1：只比 `app` 字段是否完全相等。够用且实现简单，覆盖用户描述的主要场景（PDF 阅读器整体作为目标，不关心具体哪一页/哪个文档）。
- 已知局限（写进设计里，不是这次要解决的问题）：如果目标程序不是独立进程而是浏览器里的一个标签页（比如网页版 PDF 阅读），`app` 字段只会是"Chrome"/"Safari"，没法区分标签页——这种场景需要启用 `title` 比对，v1 不做，先支持独立 App 的场景。

## 界面元素（框架层面，不涉及具体控件库）

- 主窗口：时长设置控件 + "开始"按钮 + 倒计时显示。常驻后台运行（可以做成"最小化到系统托盘/菜单栏"，但主窗口本身不是一次性对话框，是持续存在的）。
- Triggered 状态下：窗口内容切换成提醒文案（比如"你切走了，回去继续吧"）+ "重新开始"按钮，同时触发置顶显示。

## 配置项（建议做成可调，但给出默认值）

| 配置项 | 默认值 | 说明 |
|---|---|---|
| 轮询间隔 | 2~3 秒 | 太短没意义（AW 自己更新也没那么快），太长会拉长检测延迟 |
| 计时时长 | 用户每次自己设，不设全局默认 | 主界面上直接可调 |
| 比对严格模式（title） | 关闭 | v1 先不做，留接口 |
| 违规宽限期 | 0（立即触发） | 以后可以加"容忍N秒短暂切走" |

## 后续可扩展项（明确不在这次设计范围内，只是记录，避免以后忘了讨论过）

- 拖拽窗口来选目标，而不是"点开始时的当前活跃窗口"。
- 违规宽限期可调。
- 结束时的历史统计（配合用户现有的 ActivityWatchJournal 项目，说不定以后能把"专注会话"也当成一种事件写回某个地方，但这次不做）。

## 验证方式

这一步只交付设计文档，没有可运行代码，因此不涉及"跑起来验证"。如果用户之后决定拿这份设计去实现 Windows 版（VC++），建议实现完第一版后至少手动验证一遍完整状态机的四条路径：开始→正常走完、开始→切走触发提醒、AW 服务没启动时的报错提示、提醒后点"重新开始"能否正确回到 Idle 并重新捕获新的 target。
