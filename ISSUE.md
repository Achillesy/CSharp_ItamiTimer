  * 1. 当 Ticking 开启时，播放其它声音都会被中断，需要修改为不同线程播放声音，可以叠加声音。
  * 2. 修改闹钟的设定，这是个整体的设计修改。
      - 2.1 首先是 Setting 界面，仅仅显示 Shutdown on/off，去除 Alarm on/off，显得和其它设置一致。
      - 2.2 注意这里也存在和(1)同样的bug，如果此时Ticking开启，预听的声音是播放不完整的。
      - 2.3 每次重新启动应用的时候，Shutdown 重置为 off
      - 2.4 当用户滚轮改变黄针位置的时候，设定闹铃具体的时间点相当于原来的Alarm开启
        （新设计中Alarm变量删除，不必要存在）
      - 2.5 程序退出时，只需要记住黄针最后的位置，不再关心具体的时间点。(可以保留当前方案，只是说明不必要)
      - 2.6 程序开启以后，除非用户动了滚轮，或者Setting中开启了Shutdown，才会计算新的闹钟时间点。
  * 3. 我不明白为什么那么多绘制，为什么不直接用icon或者emoji，请说明理由。
    这个程序大小固定，不会被拉伸变形。
    好多画面都是固定的，比如钟的表盘，比如骨牌，就那么几个固定的内容。
    如果是我提的需求不使用图片，请告诉我改正。
  * 4. Setting 界面应该等于或略小于应用程序大小
    如果高度做不到，就把宽度调整到略小于应用程序
  * 5. Setting 的 Tick volume 修改为 强制（翻译为适当的英文） on/off
    默认为off，不影响当前设计。如果on， 主界面的 Ticking 强制为开，点击不能关闭，播放 Ticking 声音。
    强制开关 on 以后，只有在点击 Start 按钮开始学习后，直到休息时间结束期间没有 Ticking。
    点击Ignore按钮或者退出程序都会保存 强制开关 状态。
    处理冲突，如果用户先点击了Ticking，然后点击 Start，然后进入Setting界面开启 强制 on，那么以强制规则优先。
  * 6. 进行一个大修改，开启程序时，界面不随aw是否启动退化
    把专注判定留给判定模型
  * 7. 我觉得你的判定模型过于复杂，
    假设用户点击开始按钮的时间为09:01:25，
    那么设定开始时间为整分钟时间09:01:00，
    当前时间为 09:10:00，预定结束时间为 09:26:00，
    进入判定模型
      - 7.1 以秒为单位，预定结束时间 - 开始时间 = 25*60
        150 个存储空间预置为 0
        预置空间(可变长队列，可设计为60秒一个单位的链表)
        状态列表： 
        0（灰色，预计任务时间）
        1 (专注时间)
        2 (aw脱机时间，默认也算专注时间)
        3 (afk记录的离线时间)
        4 (非专注时间)
      - 7.2 连接aw
        - 7.2.1 连不上
        代表用户可能在做任务，只是连接不上aw
        对应的存储空间 480到540 标记为 3 (aw无效)
        - 7.2.2 连接上
        取aw的时间记录，从 09:06:00 到 09:10:00
        使用 rules.json 对aw记录打标
        对应事件的存储空间设置为1，用户专注时间
        非对应事件的存储空间设置为4，用户非专注时间
        使用 afk 记录
        对应离开时间的存储空间设置为3, 用户离线时间
        - 7.2.3 特殊时段，[09:02:00, 09:03:00, 09:04:00]
        这几个时间点如果连接上aw也是取前4分钟的记录
        判断模型是统一的，仅仅是对存储空间的写入长度不同
        - 7.2.4 向前4分钟的优劣
        由于默认aw的afk时间是向前3分钟，所以取4分钟肯定可以覆盖
        缺点是如果aw的afk被调整长于3分钟，例如5分钟
        按照现在判定模型，也只是误算了一次 5-4 = 1分钟的用户离开时间
        - 7.2.5 最后是画彩色圆弧
        首先，计算当前存储空间中所有>2的数目，这是需要补时的秒数
        补充到60的倍数后添加存储空间长度
        接着就可以60s一个时间段绘制每一分钟的圆弧图像了
        每一分钟内，如果有专注时间和aw脱机时间，就按专注比例画彩色圆弧
        如果一分钟内没有专注时间和脱机时间，按照占比大的状态画对应圆弧
        0 灰色
        3 空白
        4 红色
  * 8. 把Shutdown修改为Command，增加灵活性
      具体就是根据当前操作系统执行 rules.json 中预设好的一条命令
  * 9. 放弃在任务后画番茄(当前设计)，改用简单的计量方法
      初始化时，每个任务的 during = accumulateMinutes
      累计时间目前是分钟，请修改以秒为计量单位
      完成一个番茄钟或者点击ignore时就从存储空间获得累计值
      得到结果 during/3600 保留两位小数靠右显示在任务后面
      界面上不要显示单位，不要做任何说明
      退出的时候持久化during，仅仅从存储空间累计
  * 10. 当前版本开启或者退出时如果有任何查询aw的动作，
      都和我讨论是否可以移除，尽量精简逻辑和减少用户误操作
