# 决策护栏（DECISIONS）

> 这份清单存在的理由：这个项目在 2026-07-26 ~ 07-30 的密集开发里推翻过自己很多次，
> 每一条推翻都有代价、都是用户**知情后选择**的。后来者（人或 AI）读当前代码时会觉得
> 某些地方「显然可以改进」——**先查这里**。每条都写明：定了什么、代价是什么、为什么
> 不要翻案。要翻案必须先跟用户重新确认。
>
> 完整的推导过程在 git 历史里（本文件之前的 DESIGN.md 是编年体决策日志，
> 2026-07-30 拆分为「当前设计」+ 本清单）。

## ⚠️ 已失效的条目（2026-07-31 那一轮主动推翻，读下文前先看这里）

下面这些条目当时是有效护栏，**现在已经被用户明确推翻**，不要再拿它们当依据：

| 条目 | 原内容 | 现状 |
|---|---|---|
| A2 | 勾选集合用并集、中途补勾追溯生效 | **作废**——小目标改成 Radio **单选**，Start 后锁定，`GroupChanges` 已删 |
| A5 | `Neutral` 计入、`SelfApps` 自身豁免硬编码 | **作废**——`Neutral`、`ignore` 名单、自身豁免全部删除，分类只剩 OnTask / OffTask |
| A3 | 不做缓存，每轮全量重放 | **部分作废**——`Replay` 全量重放已被 `JudgmentBuffer`（秒级增量 + 4 分钟重写窗口）取代 |
| B3 | 模式启动时锁死，绝不自动降级 | **作废**——Pomodoro 退化路径整个删除，永远是约束模式，AW 脱机由判定模型内部兜底 |
| E3 | 拨针一格 5 分钟 | **改为 1 分钟/格** + 滚轮加速档位（加速那半条 2026-08-02 才真正生效，见 H12） |
| E8 | Shutdown 开关 | **改名 Execute**，执行 `rules.json` 的 `executeCommand`，与提示音互斥 |

## ⚠️ 已失效的条目（2026-08-02 那一轮，判定引擎第二版）

| 条目 | 原内容 | 现状 |
|---|---|---|
| A8 | 休息 = ⌊focus/5⌋+1，+1 补「发现延迟」 | **改为 `⌈focus/5⌉`**——达成时刻不再回溯推导（H5），+1 补的那个延迟已经不存在。见 H6 |
| A3 | 不做缓存、每轮全量重放 | **彻底作废**——`Replay` 连同它的测试一起删（DESIGN §15.7） |
| D1 | 纯度**连续**编码进格子高度 | **部分作废**——渲染改成三档（100/80/60%）。原始计数仍保留在 `MinuteCell` 里，随时能回到连续编码。见 H10 |
| D3 | 「人不在」什么都不画 | **作废**——`Afk` 改画**虚线空心框**（满高）。「什么都不画」现在只剩 `Init`（还没画过） |
| D（螺旋） | 色环螺旋**三圈**（lane 2 [0.14,0.26]） | **作废**——只有两圈，buffer 120 格够不到第三圈。见 H8 |

仍然有效的：A1、A4、A6、A7、B1、B2、B4、C 全部、D2、D4~D7、E1、E2、E4~E7、F 全部、G 全部。
新增：H 全部（判定引擎第二版）。

## A. 判定与核算

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| A1 | **`Absent` 完全照搬 AW 的判断**（2026-07-27，用户两次选择） | 安静读 PDF 超过 180 秒不动键鼠 → 判离开、不计入、任务拖长（AWJ 已实测丢过真实学习记录） | AFK 段长度分析、每组容忍阈值。**旋钮在 AW 自己的 `aw-watcher-afk.toml` 里**，不在本程序 |
| A2 | **勾选集合用当前并集、追溯生效**（2026-07-27，用户明确否决时间线方案） | 先玩 20 分钟 Blender 再补勾，红格当场变绿 | 带时间戳的 `groupTimeline`。用户原话：「学习一样厌倦了，切换到学习另一样」不算作弊 |
| A3 | **不做缓存、不做水位线**（2026-07-27）：每轮全量重查重放 | 每分钟一次本机 HTTP 往返 ×2 | 增量缓存——「必须永远与重算一致」的不变量错了以后症状极隐蔽（色块对、账不对） |
| A4 | **afk bucket 必需**，缺失即无法工作 | 没装 afk watcher 的用户进不了约束模式 | 「缺失时视为在座」的降级——那把最省力的作弊路径（停在目标应用上走开）变成默认行为 |
| A5 | **`Neutral` 计入**专注时长；**自身豁免硬编码**在判定第 1 步 | — | 把豁免挪进配置（漏一行就死循环）；改 `Neutral` 不计入（番茄钟模式会**静默停摆**，`PomodoroFallbackTests` 守着） |
| A6 | **`startedAt` 截断到整分钟**，不进位（2026-07-27 用户改定） | 点击前最多 59 秒白送 | 进位（用户不想点完干等） |
| A7 | **`Gap` 推截止线**这个矛盾按用户公式字面实现，记录在案 | Gap 实质被惩罚，与 §「不惩罚」冲突 | 别悄悄「修好」它——要改先确认，改法是 `ProjectedEnd` 里减 `GapSeconds` |
| A8 | ~~**休息 = ⌊focus/5⌋+1，+1 与 60 秒节拍绑死**~~ **2026-08-02 作废，见 H6** | 最多多歇 1 分钟 | 「实际耗时÷5」（激励反了）；「偷懒扣休息」（二次惩罚会让人干脆放弃任务）——**这两条仍然有效** |

## B. 生命周期与持久化

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| B1 | **任务不落盘，退出 = 放弃**（2026-07-27 砍掉 v3 招牌的「可恢复」） | 崩溃/关机丢当前轮（最长 50 分钟） | current-task.json、history.jsonl、TaskStore、任何恢复逻辑。历史统计是姊妹项目 AWJ 的活 |
| B2 | **绝不自动开始下一轮**（原则 1） | — | 休息结束自动续杯。程序代替用户提交任务 = 强制加任务 |
| B3 | **模式启动锁死**（2026-07-28） | AW 后启动要重开程序 | 「AW 断了自动降级成番茄钟」= 杀 aw-server 免费刷时长；空闲重探；重试按钮 |
| B4 | **界面任何时候都不给账单**（2026-07-27） | 拿不到「chrome 偷了 8 分钟」的归因 | 在 GUI 里列数字。痛感载体是表盘本身；CLI 保留账单（它没有表盘可看） |

## C. 提醒与窗口

| # | 决策 | 背景 | 不要做成 |
|---|---|---|---|
| C1 | **整套自动置顶已废弃**（2026-07-28，用户：「不要再纠结窗口置顶这种事情了。逻辑混乱，又容易出错」） | 实机连塌三次，每次塌在新地方。根因：「弹出来」和「绝不抢焦点」这两条约束本身矛盾，在文档里并存了很久没人发现；且置顶是有状态的东西，撤销条件跨会话生命周期 | 任何**自动**置顶/收起/弹出。右上角图钉是**手动**开关、无状态机，跟被砍的那套不是一回事 |
| C2 | **跑偏没有任何主动提醒**，只写日志 | 与不给账单同一思路 | 跑偏音、跑偏弹窗、任务栏图标变色报警 |
| C3 | **键鼠空闲声只在 `[60,180)` 窗口响**；性质是**补救**不是通报 | 最坏安静 ~120 秒才被发现 | 过 180 秒还响（已救不回来，纯噪音）；靠 AW 事后判断（T5：回填写死，事后干预类方案全部无效） |
| C4 | **喇叭只管滴答**，不是总静音（2026-07-28 用户修正） | — | 合并成总静音。这个程序 = 一个钟 + 一个督工：滴答/图钉属于钟（开关在钟上），三声通知属于督工（开关在设置里） |
| C5 | **秒针一秒一跳**（2026-07-28，推翻「亚秒连续扫」定稿） | — | 改回连续扫。扫秒针的钟不会响——滴答来自步进擒纵，物理上互斥 |

## D. 视觉

| # | 决策 | 背景 | 不要做成 |
|---|---|---|---|
| D1 | **纯度编码进格子高度**（木桶短板，2026-07-28） | 取代「低纯度加刻线」——8.7px 宽的格子被刻线+边框吃剩 6px，「注解把被注解的东西吃掉了」。方向性教训：**别再加记号，把量编码进长度** | 任何往颜色上贴补丁的色盲方案 |
| D2 | 短板**下限 1/2，绝不取 0** | 用户：「不要红色短板矮到看不到」 | 取 0——零高度与「人不在（不画）」混淆，而那是最不该混淆的一对：一个不怪你，一个全怪你 |
| D3 | **「人不在」什么都不画**（2026-07-28） | 惩罚只体现在截止弧变长这一处 | 画灰块（数偷懒时会把上厕所数进去）。`DialPalette.Absent` token 留着不用，语义占位 |
| D4 | **休息扇形**取代色环淡出（2026-07-28） | 淡出实现了但用户从没看见（当时短任务休息=0），整套删净 | 恢复淡出。扇形从圆心切出（形状是唯一空着的通道）、蓝色（灰=欠账、绿琥珀红=成绩，都有主）、不缩不淡不画倒计时（分针扫出扇形即结束） |
| D5 | **仓库不放位图、不放音频**；图标全部矢量、滴答运行时合成 | Segoe Fluent Icons 在 macOS 是豆腐块（已换矢量）；`C:\Windows\Media` 没有滴答 | 引入图标字体、打包 wav/png |
| D6 | 滑块不给数字、分割线以下不给提示字、骨牌不解释、齿轮混进骨牌行 | 「能猜的就让人猜」是刻意的设计语言 | 加说明文字、加「当前是番茄钟模式」提示（列表整个消失就是信号） |
| D7 | **骨牌只在启动和点 Start 时更新**日期 | 跨午夜不点 Start 就不更新——故意保留以增加神秘感 | 挂定时器实时刷新 |
| D8 | 侧面亮面宽度按 `j + max(0, Fallen−1)` 移窗查表（2026-08-02，原 ISSUE #12） | 原来按固定屏幕位置查表，最左边那块骨牌（世界坐标恒为 `Count-1`）整周宽度死等于 6px；移窗后它随一周推进逐日变窄，且宽度用尽正好是它倒下那天，不会有「还没倒但已经没侧面」的空窗期 | 直接按 `j + Fallen` 移窗——会在它倒下**前一天**就已经宽度归零 |

| D9 | 小目标右侧的累计时长**任务期间每分钟刷新**，显示值 = `已落盘 checkpoint + 本轮实时`；**允许它往回退**，不做单调化（2026-08-03；2026-08-06 随 I1 调整了后一项的含义） | 原来只在完成/ignore 时才动，跑一小时界面纹丝不动。显示端只做加法，**一个字都不写**——`during.json` 只在任务启动那一刻被回填改写一次（I1）。回退有两个来源，都是知情接受的：① 判定引擎的既有行为，每拍重画最近 4 分钟，AW 的 afk 迟 180 秒回填，离座后已计的分钟会被撤销——表盘的红格和截止弧本来就这么退；② 本轮那一项带 fail-open 水分，而下次启动的回填是 fail-closed 的（I2） | 为了「好看」记一份「本轮见过的最大值」把它变成只增不减——那是给界面加一份会说谎的状态，直接违背原则 4 |
| D10 | **任务栏 / Dock 图标不做动态跟随，两个平台都保持 exe 的静态番茄图标**（2026-08-04） | `RingIcon` 生成的彩色进度环通过 `Window.Icon` 实时更新，Windows 上标题栏、Alt-Tab 能正确显示彩色圆环（真机验证过），但任务栏那个具体位置只认 Avalonia 内部为 `ICON_SMALL` 生成的小尺寸 HICON（16~24px），会直接忽略、继续显示 exe 静态图标——这是 Avalonia 框架内部限制，`Window.Icon` 这层公开 API 没有开关能覆盖，要修只能自己 P/Invoke 绕开、另发一条更大尺寸的 `WM_SETICON`（跟 `WindowPin.cs` 绕开 `Window.Topmost` 是同一类手法，但换成 GDI 图标那套 API，工作量不小）。macOS 对应的是 Dock tile，由 `NSApplication.setApplicationIconImage:` 驱动，与 `Window.Icon` 是两套完全不同的机制，要做得走 Objective-C 运行时消息发送——仓库里目前一行 ObjC 互操作代码都没有，是全新的一类维护成本，比 Windows 那边更重。两边工作量都不小，只做一边会造成「Windows 有、macOS 没有」的不对称体验，所以两边都不做 | 只给 Windows 一侧写原生 `WM_SETICON` 绕过代码——不对称比两边都没有更别扭；给 macOS 加一半的 ObjC 互操作又不完整实现。真要做，两边一起做 |

## E. 闹钟（2026-07-30）

| # | 决策 | 背景 | 不要做成 |
|---|---|---|---|
| E1 | **响铃时刻在拨针那一刻算死**（`NextRing`），之后单调比较 | 每帧重判黄针位置会在「拨到与时针重合」时当场触发 | 角度容差判定（0.5°/分钟，1.5° 容差 = 提前 3 分钟响） |
| E2 | **三级判断全部严格小于**：恰好重合 = 12 小时后 | 用户明确：「刚好相等，说明希望的是 12 个小时后再响」 | `<=`（拨着拨着突然响） |
| E3 | **拨针输入只有滚轮**（2026-07-30 晚定稿）：前滚逆时针、后滚顺时针，一格 5 分钟 | 点击拨针做过两版（独立按钮 → 钟面左右键+长按加速），当天全部撤掉——用户：「免得影响以后的功能扩展」，钟面点击留白 | 恢复任何点击/长按拨针；把滚轮方向按自己的「直觉」翻转（方向是用户点名的） |
| E4 | 闹钟交互在**钟面本身**上，无独立按钮 | 独立按钮时代撞过：Button 内部把 PointerPressed 标 Handled，普通 `+=` 订阅收不到 | 恢复独立按钮；给钟面挂静态 Tooltip（会盖住闹钟时间提示） |
| E5 | 闹钟是**一次性**的，响过即撤 | — | 每日重复闹钟（要做先确认） |
| E6 | 调整期静默用**截止时刻**不用布尔量 | — | 布尔 + `Task.Delay` 复位——滚轮连续来时早到的复位会掐断晚到的调整期 |
| E7 | **闹钟持久化只有一个值：响铃时间点**，退出时写一次；黄针位置 = 时间点对 12 小时取余的**推导值**（`AlarmClock.Position` 是计算属性）（2026-07-30 定稿，推翻了同日早些的「位置+时刻各存一份」） | 关着程序时错过的闹钟不补响（只剩黄针残影）；崩溃退出丢本轮拨针 | 单独存黄针位置（冗余，两个值会漂）；滚轮每拨一下就写盘；补响过期闹钟；「机械钟语义」（位置常设、每 12 小时都响——配合 Shutdown 会每天关两次机） |
| E9 | **`executeCommand` 是个列表，永远只执行第 0 条**（2026-08-02） | 想换命令要动文件、重排顺序 | 加界面去选（D6：能猜的就让人猜）；执行全部（一串关机+锁屏同时跑）；按名字查找（那就成了配置格式设计，而这只是个收藏夹） |
| E10 | **齿轮按钮不挂 Tooltip**（2026-08-03，另一台机器上删的） | 点开的是模态 `SettingsWindow`。模态窗一起来，按钮就再也收不到 `PointerExited`——气泡卡在对话框上面，关掉对话框还赖着不走。在 `OnSettings` 里显式 `SetIsOpen(false)` 也不稳（用户实测）。喇叭和图钉没这问题：它们是就地切换，指针始终在同一个窗口里 | 把 `ToolTip.Tip="Settings"` 加回来（齿轮本来就不用解释，D6）；改成「打开前手动关一次气泡」——试过，不可靠 |
| E11 | **响铃遍数：闹钟 4 遍、三声通知各 2 遍**（2026-08-03），间隔 = 音频文件自身时长（`Sound.Duration` 直接读文件头算，见 §10） | 系统音一两秒就完了，走个神就错过。闹钟给 4 遍是因为它是唯一没有第二次机会的声音——响完什么都不留（黄针不动、不弹窗，§8.1a）；三声通知给 2 遍就够，它们各自落在一个留在屏幕上的状态上（表盘满了、蓝扇形、格子不再长），漏听还能看回来。2 遍也是被节拍卡住的上限：三声通知挂在整分钟节拍上，Idle 可能连着两分钟都响，最长的系统音（Ring05，12.8 秒）响 2 遍 26 秒，响 4 遍就压到下一次提醒上了 | 当成 E5 的「每日重复闹钟」翻案（仍是一次性：`MarkFired` 照旧在第一遍之前就跑了，变长的只是这一次的响铃）；用固定间隔——winmm 的 `PlaySound` 只有一个通道，下一遍早到会把上一遍从中间掐断，macOS 那边则是叠着一起响，两种都不是「响了 N 遍」；把设置里的试听也改成响 N 遍（试听是挑音色，不是排练打扰） |
| E12 | **连响不加锁、不排队**（2026-08-03 用户问过，当场定死） | 这条路径只碰局部变量；音频层唯一的共享可变状态是 `MacAudio` 的 SoundID 缓存，它自己有 `Gate`。锁真正能买到的是「声音不重叠」，那是策略不是正确性：代价只能是**丢**一声（重要的输给不重要的）或**排队**（迟到，且要维护一份跨 Give Up、跨关窗的状态）。何况撞不上——三声通知同挂一个整分钟节拍，`TaskSession` 每拍最多选一个（`focusDone` / `idleNudge` 是 `else if`），真能交叉的只有闹钟 × 通知。技术上还有一条：`lock` 跨不了 `await`，换成 `SemaphoreSlim` 抱着睡 5~50 秒，那已经是队列了 | 加「通道忙就跳过」（那是滴答的策略，滴答可以丢，通知不能丢）；加播放队列 |
| E13 | **`Tick.Stop()` 只挂在喇叭点击里，不放 `ApplyChrome()`**（2026-08-03） | Windows 上 `Tick.Stop()` 是 `PlaySound(null)`，停的是**本进程在 winmm 单通道上正在放的任何东西**，不管是谁起的头。而 `ApplyChrome()` 有四个调用点（构造 / 喇叭 / **图钉** / 关 Settings），放在那里等于「点图钉把正在响的闹钟掐了」——图钉跟声音毫无关系。响铃只有一遍、窗口一秒时撞不上；改成连响 2~4 遍、窗口几秒到几十秒之后就撞得上了。截断本身用户不在意（E12：不排队不加锁），触发点乱才是问题 | 挪回 `ApplyChrome()`「反正幂等」；为了「只停自己那一声」去换 `waveOut`（单通道模型下没有干净做法，代价远大于收益） |
| E8 | **Shutdown 开关不跨会话**：每次启动在 `Settings.Load` 里强制复位，退出时不用管 | 想到点关机必须本次会话里手动打开 | 把它持久化——重启后被上一个会话的关机命令拍死是事故；复位放退出时（崩溃路径覆盖不到，启动复位一道就够） |

## F. 身份标识与构建（改错不报错、只把账算错的一类）

| # | 决策 | 后果 |
|---|---|---|
| F1 | `App.axaml` 的 **`Name="ItamiTimer"`** 永不改名/本地化 | macOS 上 AW 报的 `data.app` 就是它。不设 → `Avalonia Application` → 自身豁免失效 → 看一眼进度就违规的死循环。修法**绝不是**往 `SelfApps` 塞 `Avalonia Application`（豁免所有 Avalonia 应用） |
| F2 | csproj **`AssemblyName=ItamiTimer`** 钉死 | Windows 上 `data.app` 是 exe 名，同上 |
| F3 | Core 保持 **`net10.0`**、零 UI 包引用 | 边界纪律的执行机制本身 |
| F4 | App 的 TFM 是 **`net10.0`**，别把 `-windows` 加回去 | 后缀从没起过作用；去掉后 CA1416 真的开始报没守住的平台调用 |
| F5 | **`StripPdbFromPublish` target 不许删**，也不许退回文档手工步骤 | 「必须记得做」的步骤已经被忘过一次（127MB 发布） |
| F6 | 三个调试出口走 **headless**，正常启动路径不动 | 构建（pack-macos.sh 的 --export-iconset）不该依赖图形会话；`UsePlatformDetect` 在无图形会话崩 -6661 |
| F7 | 查询窗口**放宽 6 小时**再自己裁（T1） | AW 只按事件起点过滤，跨起点事件静默消失。没有可证明安全的宽度，失败模式良性（判 Gap），不为它加机制 |
| F8 | `HttpClient` **`UseProxy = false`** | 系统代理吞 localhost，表现成莫名连不上 |
| F9 | `AwEvent.End` 用 **`AddTicks`** 不用 `AddSeconds(double)` | 后者按毫秒取整，微秒精度的事件碎成亚毫秒区间，偷懒 8 次膨胀成 54 次 |
| F10 | AW 时间戳**在 `AwClient` 边界上 `ToLocalTime()`** | 「同一个量两种表示，要在边界上归一，不要指望每个使用点各自记得转」——时区 bug 犯过两次 |
| F11 | `pack-macos.sh` 里 **`$VAR` 紧跟中文标点一律加花括号写成 `${VAR}`**（2026-08-02） | macOS 自带的 bash 是停在 3.2 的老版本（GPLv3 之后苹果不再升级）。`set -u` 下 `$RID，` 这种写法，标点的字节会被误算进变量名，报 `RID: unbound variable`——报错信息就在眼前，但指向的原因是假的（变量明明赋过值）。属于 G 节那一族「编译/运行不报真正的错，只报一个看着像但方向错的错」 |

## G. 方法论教训（不针对具体功能）

- **一个功能连塌三次、每次塌在新地方，通常不是实现烂，是需求自相矛盾**（C1 的置顶：
  「弹出来」和「绝不抢焦点」并存了很久没人发现）。
- **更好的设计通常不是把旧方案改好，而是让旧方案的整个问题消失**（D1 的木桶短板）。
- **凡是「必须记得做」的手工步骤，就是迟早会被忘掉的步骤**（F5）。
- **凡是同一个量有两种表示，就要在边界上归一**（F10：圈号/时区/偏移量都栽过）。
- **改动的说服力来自实测，不是推断**（headless 改造用 SHA-256 逐字节对比 + DYLD
  库加载清单验证；UIA 测「抢不抢焦点」必须先用无关控件做对照——Invoke 本身会抢）。
- **被否掉的常常是表达方式，不是那件事本身**（键鼠空闲检测被删又加回：否的是置顶
  这个表达，检测本身用户要）。
- **根因描述也要按代码核对，不能只按印象写**（§15.1：第一版的根因分析写得言之凿凿，
  但跟代码对不上——它决定了后来所有人往哪个方向修）。
- **「更好的设计让整个问题消失」在这一轮连中两次**：把「无记录」从不计入改成计入之后，
  「断线兜底会被下一拍撤销」那个问题自己没了；把逐秒查找改成按秒落格之后，T4 的桥接
  不需要了。两次都不是把旧方案改好，是换个问法让旧问题不成立。
- **同一个现象要先分清它会不会自愈**（DESIGN §7.4）：AW 滞后的尾巴和零时长事件的空洞
  在盘面上长得一模一样，一个下一拍就好、一个永远不会好，处理方式完全相反。
- **换类型时要拿改动前的输出逐字对比**（2026-08-02：`MinuteCell` 从 `double` 改成 `int`，
  CLI 账单里的 `off / 60` 当场变成整数除法，`52.6 min` 打成 `52.0 min`）。编译过、
  测试全绿、数字还像模像样——**只有对比才看得出来**。跟 F9 的 `AddSeconds` 同一族：
  数值类型的改动不会报错，只会安静地少几位。
- **对比之前先确认输出是可复现的**（2026-08-02：想用 SHA-256 比对表盘样张验证重构，
  结果 10 张全不同——`DrawHands` 读 `DateTime.Now`，同一份代码隔几秒渲染两次就不同。
  是那三张「零色块、只有休息扇形」的也不同，才暴露了原因）。**先证伪你的对比方法，
  再用它下结论。**
- **一个文件只能有一个解析器**（§15.4 的 `executeCommand`）。两条读取路径是不同时间、
  不同 AI 各写一半留下的，两套设置只能靠人手动对齐——咬过两次，症状都是「文件的一半
  好用、另一半安静地不工作，程序照常启动」。**多个 AI 先后改同一个项目时，这类
  「各自实现一半」的重复格外容易长出来**，而它不报错。
- **别把别的框架的常识搬进 Avalonia**（H12 的滚轮 `Delta/120`）。这类错的共同特征是
  **编译过、不抛异常、功能安静失效**——跟 F1/F2 的改名、F9 的 `AddSeconds` 是同一族。
  判断方法很朴素：只要写下的是一个**魔数**（120、96、0.5f 之类），就去查它在**这个**
  框架里到底是多少。

## H. 判定引擎第二版（2026-08-02 定稿，规格见 DESIGN §3/§4）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| H1 | **码值 = 覆盖优先级**（值越小越晚画、越权威），**不是**「对用户有利的程度」 | `Afk`(2) 排在 `OffTask`(3) 之下，看起来反直觉 | 按「有利程度」调换 Afk / OffTask——afk 优先级会**悄悄失效且不报错**。判「计入」请用不变量 `码 ≥ Focused(4)`，别列举具体码值 |
| H2 | **「这一秒没有任何记录」→ `AwOffline`，计入专注**（知情的 fail-open） | `aw-watcher-window` 单独死掉 = 全盘绿、白拿满分（DESIGN §16.5 记录在案，用户明确暂不处理） | 改成不计入——AW 每拍末尾恒有 6~12 秒滞后，那会变成恒定扣时 |
| H3 | **整拍连不上 = 空事件列表**，没有单独的兜底代码路径 | — | 加 `if (awDown)` 分支；保留 `AwOfflineFallback` |
| H4 | **连不上时只写最后一分钟**，不铺满 4 分钟 | — | 用「没有信息」去覆盖「已有的信息」——前 3 分钟已有真实判定，铺满会把红的抹绿 |
| H4a | **漏拍漏到查询窗口之外的那段填 `Init`（不计入），不是 `AwOffline`**（2026-08-02 实现时补） | 挂起期间既不算专注也不算偷懒，只把任务拖长 | 一律填 `AwOffline`——那样**睡一觉就能刷满任务**。`AwOffline` 的语义是「查了，AW 拿不出数据」，漏拍是我们自己没查，两件事不该同一个待遇。也不能什么都不填：上一拍的承诺弧还铺在那儿，会被染成「还没走到」 |
| H5 | **达成是事件，不是推导值**：算出缺口 ≤ 0 的那一拍就是达成时刻，记下来不再重算 | 最晚比真实达成晚 60 秒发现，而这 60 秒不再吃休息 | 恢复 `FocusCompletedAt()` 那类「回头找跨过阈值那一格」的算法——账本一重写它就往回跳（§15.1 就是这么来的） |
| H6 | **休息 = ⌈focus/5⌉，只读提交时锁定的 `FocusMinutes`** | 正式量程每档比原来少 1 分钟（10→2、25→5、50→10） | 用「剩余目标」算休息（拖得越久歇得越少，激励反了）；恢复 +1（它补的那个延迟已经不存在）；只用 5 的倍数验证公式（护栏测试的取值必须覆盖滑块实际能出的值） |
| H7 | **归档结算范围以 padding 为基准**：`[180, 3780)` | — | 写成 `[0, 3600)`——多算 padding、漏算 180 秒，归档瞬间账会跳且毫无提示（§15.5） |
| H8 | **盘面只有两圈**；归档时内圈内容跳到外圈、内圈清空（首次满 2 小时，之后每小时一次） | 视觉塌缩；超过两小时的早期记录从盘上消失（账已进 `during`） | 恢复 lane 2（buffer 只有 120 格，够不到）；为了「看起来连续」而不让圈号跟 buffer 位置走——归档存在的理由**就是**让盘面只画最近一到两小时 |
| H9 | **查询区间与写入偏移一律从整分钟算**，绝不掺 `now` 的亚秒零头 | — | 用 `DateTimeOffset.Now` 直接减 4 分钟——相位每拍不同、边界秒来回翻面，AW 响应慢 10 秒还会整段错位 |
| H10 | **CLI 用离散档、表盘可以用连续**；`MinuteCell` 保留四个原始计数 | `> 40` 这一档下偷懒 19 秒仍然满绿满高 | 把渲染档位固化进判定层——那样表盘再也回不到 D1 的连续编码 |
| H11 | **承诺弧每拍重算**（先清 `[H,7380)` 再填 `[H,H+D)`），不记「上次画到哪」 | 每分钟一次 7KB `Fill` | 记住最后一个 `Gray` 的位置再增量推动——afk 收缩会让弧末端**前移**，增量做法会留下残渣 |
| H12 | **滚轮加速按「连续滚了几格」算，不按单次 `Delta` 幅度**（2026-08-02）；一串下来 1 → 3 → 8 → 15 → 30 分钟/格，停手 300ms 断串 | 快滚容易冲过头，但松手就回到 1 分钟/格 | 拿 `Delta.Y / 120` 当格数——**120 是 Win32 `WM_MOUSEWHEEL` 的单位，Avalonia 一格就是 1.0**，那个除法恒等于 0.008，被 `Math.Max(1, …)` 拉回 1，于是 E3 记的加速阶梯**一次都没跑起来过**，而且不报错 |

## I. 累计时长改成 checkpoint 模型（2026-08-06 定稿，规格见 DESIGN §11.2）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| I1 | **`during.json` 从「账本」降级为 checkpoint**：只在**任务启动那一刻**写一次，回填 `[recordedThrough, StartedAt)` 这一段（= 上一场任务全程 + 中间的空隙），然后把 `recordedThrough` 推到本次 `StartedAt`。任务运行期间一秒都不落盘 | 界面显示的实时项在下次启动前不落盘，程序崩了这段显示就没了——但**数字本身不会丢**，下次启动照样从 AW 重新数出来 | 恢复「每一秒专注只入账一次」那套：`Settled` 事件 / `UnbankedSeconds` / `TakeUnbankedSeconds()` / `_banked` 幂等标志 / 三条终结路径各记一次账。它们防的是「这一秒现在不记就永远没了」，**AW 是地面真相，那个前提不成立了**。加回任何一条都会和回填重复计费，而且是静默的 |
| I2 | **回填 fail-closed（底色 `Init`），运行期继续 fail-open（底色 `AwOffline`）** | 同一段时间在下次启动后**可能变小**（跟 D9 的 afk 追认回退同方向，用户知情接受） | 「为了一致」把两边统一成同一个底色。fail-open 站得住的前提是**程序正在跑、拍子正在走**——机器摆明开着、人摆明在，AW 哑了是 AW 的锅。回填时程序**没在跑**，AW 没数据最大的可能是机器关着，照 fail-open 记账**一次跨周末的回填能凭空记 48 小时**。⚠️ `Judgment.Paint` 自己从不填底色（fail-open 是 `JudgmentBuffer.Cover` 用水位线加的），所以回填复用它一行都不用改——**别顺手改 `Paint` 去"统一"** |
| I2a | **也不要反过来把运行期改成 fail-closed** | — | AW 每拍末尾恒有 6~12 秒滞后，运行期 fail-closed 会变成恒定扣时（H2 原文）。两边规则不同**是对的**，不是待清理的不一致 |
| I3 | **「从没统计过」用 `recordedThrough: null` 哨兵**，不把 bucket 的 `created` materialize 进 during.json | 首次回填要走完整段 AW 历史，可能几十秒 | 播种时查 AW 把 `created` 写进去。三条理由，第三条是决定性的：① `created` 是派生值且会变（重装 AW / 迁 datastore）；② 回填窗口的起点**不需要准确，只需要足够早**，裁剪是 AW 自己的本职工作；③ **播种在程序启动路径上，那一刻 AW 可能连不上**——「已播种但起点未知」这个状态**无论如何都省不掉**，materialize 只是在旁边多加一条会变质的脆弱路径。`created` 只作临时下界用（`FindBucketCreatedAsync`），查完就扔 |
| I4 | **从 1.0.x 迁移时，`recordedThrough` 必须盖「迁移这一刻」的戳** | 迁移前那段历史沿用老账本的数，不重新推导 | 留 `null`——那样下次启动会从 AW 历史开头重数一遍**再加到**已有的值上，**数字直接翻倍**，而且没有任何报错 |
| I5 | **凡是「纯逻辑 + 写盘」的方法一律拆两层，测试只调纯的那层**（`FromLegacy`/`Migrate`、`Apply`/`Advance`） | 多一个 public 方法 | 在单元测试里调会 `Save()` 的方法——`During.Save()` 写的是**用户真实的** `%LOCALAPPDATA%\ItamiTimer\during.json`。2026-08-06 就是这么把用户的累计时长冲成测试数据的，**跑完测试全绿，一声不响**。这跟 F1/F2 改名、H12 的 `Delta/120` 同族：编译过、不抛异常、安静地造成损失 |
| I6 | **回填按天切片 + 120 秒的回填专用超时**，运行期仍然是 10 秒（2026-08-06 真机干跑打回来的） | 请求数 ×7；`FetchEventsAsync` 的 6 小时放宽让按天切多拉 1.25 倍数据（按周只有 1.04 倍）。实测 150 天全量 4 分 11 秒、增量 1.3 秒 | 沿用 `AwClient` 默认的 10 秒超时去拉周级的片子——真实历史里一周的窗口事件量足以超时，而回填失败 = 不推进 checkpoint，那段历史会**永远卡在同一个地方**：每次启动重试、每次同样超时、累计时长再也不前进，只有日志里一行 WARN。两个场景的取舍是相反的：运行期宁可短超时 fail-open 也不能拖住整分钟节拍，回填**慢一点无所谓，失败才是问题** |
