# 决策护栏（DECISIONS）

> 这份清单存在的理由：这个项目在 2026-07-26 ~ 07-30 的密集开发里推翻过自己很多次，
> 每一条推翻都有代价、都是用户**知情后选择**的。后来者（人或 AI）读当前代码时会觉得
> 某些地方「显然可以改进」——**先查这里**。每条都写明：定了什么、代价是什么、为什么
> 不要翻案。要翻案必须先跟用户重新确认。
>
> 完整的推导过程在 git 历史里（本文件之前的 DESIGN.md 是编年体决策日志，
> 2026-07-30 拆分为「当前设计」+ 本清单）。

## ⚠️ 已失效的条目（2026-07-31 那一轮主动推翻，读下文前先看这里）

下面这些条目当时是有效护栏，**现在已经被用户明确推翻**，不要再拿它们当依据：

| 条目 | 原内容 | 现状 |
|---|---|---|
| A2 | 勾选集合用并集、中途补勾追溯生效 | **作废**——小目标改成 Radio **单选**，Start 后锁定，`GroupChanges` 已删 |
| A5 | `Neutral` 计入、`SelfApps` 自身豁免硬编码 | **作废**——`Neutral`、`ignore` 名单、自身豁免全部删除，分类只剩 OnTask / OffTask |
| A3 | 不做缓存，每轮全量重放 | **部分作废**——`Replay` 全量重放已被 `JudgmentBuffer`（秒级增量 + 4 分钟重写窗口）取代 |
| B3 | 模式启动时锁死，绝不自动降级 | **作废**——Pomodoro 退化路径整个删除，永远是约束模式，AW 脱机由判定模型内部兜底 |
| E3 | 拨针一格 5 分钟 | **改为 1 分钟/格** + 滚轮加速档位（加速那半条 2026-08-02 才真正生效，见 H12） |
| E8 | Shutdown 开关 | **改名 Execute**，执行 `rules.json` 的 `executeCommand`，与提示音互斥 |

## ⚠️ 已失效的条目（2026-08-02 那一轮，判定引擎第二版）

| 条目 | 原内容 | 现状 |
|---|---|---|
| A8 | 休息 = ⌊focus/5⌋+1，+1 补「发现延迟」 | **改为 `⌈focus/5⌉`**——达成时刻不再回溯推导（H5），+1 补的那个延迟已经不存在。见 H6 |
| A3 | 不做缓存、每轮全量重放 | **彻底作废**——`Replay` 连同它的测试一起删（DESIGN §15.7） |
| D1 | 纯度**连续**编码进格子高度 | **部分作废**——渲染改成三档（100/80/60%）。原始计数仍保留在 `MinuteCell` 里，随时能回到连续编码。见 H10 |
| D3 | 「人不在」什么都不画 | **作废**——`Afk` 改画**虚线空心框**（满高）。「什么都不画」现在只剩 `Init`（还没画过） |
| D（螺旋） | 色环螺旋**三圈**（lane 2 [0.14,0.26]） | **作废**——只有两圈，buffer 120 格够不到第三圈。见 H8 |

仍然有效的：A1、A4、A6、A7、B1、B2、B4、C 全部、D2、D4~D7、E1、E2、E4~E7、F 全部、G 全部。
新增：H 全部（判定引擎第二版）。

## A. 判定与核算

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| A1 | **`Absent` 完全照搬 AW 的判断**（2026-07-27，用户两次选择） | 安静读 PDF 超过 180 秒不动键鼠 → 判离开、不计入、任务拖长（AWJ 已实测丢过真实学习记录） | AFK 段长度分析、每组容忍阈值。**旋钮在 AW 自己的 `aw-watcher-afk.toml` 里**，不在本程序 |
| A2 | **勾选集合用当前并集、追溯生效**（2026-07-27，用户明确否决时间线方案） | 先玩 20 分钟 Blender 再补勾，红格当场变绿 | 带时间戳的 `groupTimeline`。用户原话：「学习一样厌倦了，切换到学习另一样」不算作弊 |
| A3 | **不做缓存、不做水位线**（2026-07-27）：每轮全量重查重放 | 每分钟一次本机 HTTP 往返 ×2 | 增量缓存——「必须永远与重算一致」的不变量错了以后症状极隐蔽（色块对、账不对） |
| A4 | **afk bucket 必需**，缺失即无法工作 | 没装 afk watcher 的用户进不了约束模式 | 「缺失时视为在座」的降级——那把最省力的作弊路径（停在目标应用上走开）变成默认行为 |
| A5 | **`Neutral` 计入**专注时长；**自身豁免硬编码**在判定第 1 步 | — | 把豁免挪进配置（漏一行就死循环）；改 `Neutral` 不计入（番茄钟模式会**静默停摆**，`PomodoroFallbackTests` 守着） |
| A6 | **`startedAt` 截断到整分钟**，不进位（2026-07-27 用户改定） | 点击前最多 59 秒白送 | 进位（用户不想点完干等） |
| A7 | **`Gap` 推截止线**这个矛盾按用户公式字面实现，记录在案 | Gap 实质被惩罚，与 §「不惩罚」冲突 | 别悄悄「修好」它——要改先确认，改法是 `ProjectedEnd` 里减 `GapSeconds` |
| A8 | ~~**休息 = ⌊focus/5⌋+1，+1 与 60 秒节拍绑死**~~ **2026-08-02 作废，见 H6** | 最多多歇 1 分钟 | 「实际耗时÷5」（激励反了）；「偷懒扣休息」（二次惩罚会让人干脆放弃任务）——**这两条仍然有效** |

## B. 生命周期与持久化

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| B1 | **任务不落盘，退出 = 放弃**（2026-07-27 砍掉 v3 招牌的「可恢复」） | 崩溃/关机丢当前轮（最长 50 分钟） | current-task.json、history.jsonl、TaskStore、任何恢复逻辑。历史统计是姊妹项目 AWJ 的活 |
| B2 | **绝不自动开始下一轮**（原则 1） | — | 休息结束自动续杯。程序代替用户提交任务 = 强制加任务 |
| B3 | **模式启动锁死**（2026-07-28） | AW 后启动要重开程序 | 「AW 断了自动降级成番茄钟」= 杀 aw-server 免费刷时长；空闲重探；重试按钮 |
| B4 | **界面任何时候都不给账单**（2026-07-27） | 拿不到「chrome 偷了 8 分钟」的归因 | 在 GUI 里列数字。痛感载体是表盘本身；CLI 保留账单（它没有表盘可看） |

## C. 提醒与窗口

| # | 决策 | 背景 | 不要做成 |
|---|---|---|---|
| C1 | **整套自动置顶已废弃**（2026-07-28，用户：「不要再纠结窗口置顶这种事情了。逻辑混乱，又容易出错」） | 实机连塌三次，每次塌在新地方。根因：「弹出来」和「绝不抢焦点」这两条约束本身矛盾，在文档里并存了很久没人发现；且置顶是有状态的东西，撤销条件跨会话生命周期 | 任何**自动**置顶/收起/弹出。右上角图钉是**手动**开关、无状态机，跟被砍的那套不是一回事 |
| C2 | **跑偏没有任何主动提醒**，只写日志 | 与不给账单同一思路 | 跑偏音、跑偏弹窗、任务栏图标变色报警 |
| C3 | **键鼠空闲声只在 `[60,180)` 窗口响**；性质是**补救**不是通报 | 最坏安静 ~120 秒才被发现 | 过 180 秒还响（已救不回来，纯噪音）；靠 AW 事后判断（T5：回填写死，事后干预类方案全部无效） |
| C4 | **喇叭只管滴答**，不是总静音（2026-07-28 用户修正） | — | 合并成总静音。这个程序 = 一个钟 + 一个督工：滴答/图钉属于钟（开关在钟上），三声通知属于督工（开关在设置里） |
| C5 | **秒针一秒一跳**（2026-07-28，推翻「亚秒连续扫」定稿） | — | 改回连续扫。扫秒针的钟不会响——滴答来自步进擒纵，物理上互斥 |

## D. 视觉

| # | 决策 | 背景 | 不要做成 |
|---|---|---|---|
| D1 | **纯度编码进格子高度**（木桶短板，2026-07-28） | 取代「低纯度加刻线」——8.7px 宽的格子被刻线+边框吃剩 6px，「注解把被注解的东西吃掉了」。方向性教训：**别再加记号，把量编码进长度** | 任何往颜色上贴补丁的色盲方案 |
| D2 | 短板**下限 1/2，绝不取 0** | 用户：「不要红色短板矮到看不到」 | 取 0——零高度与「人不在（不画）」混淆，而那是最不该混淆的一对：一个不怪你，一个全怪你 |
| D3 | **「人不在」什么都不画**（2026-07-28） | 惩罚只体现在截止弧变长这一处 | 画灰块（数偷懒时会把上厕所数进去）。`DialPalette.Absent` token 留着不用，语义占位 |
| D4 | **休息扇形**取代色环淡出（2026-07-28） | 淡出实现了但用户从没看见（当时短任务休息=0），整套删净 | 恢复淡出。扇形从圆心切出（形状是唯一空着的通道）、蓝色（灰=欠账、绿琥珀红=成绩，都有主）、不缩不淡不画倒计时（分针扫出扇形即结束） |
| D5 | **仓库不放位图、不放音频**；图标全部矢量、滴答运行时合成 | Segoe Fluent Icons 在 macOS 是豆腐块（已换矢量）；`C:\Windows\Media` 没有滴答 | 引入图标字体、打包 wav/png |
| D6 | 滑块不给数字、分割线以下不给提示字、骨牌不解释、齿轮混进骨牌行 | 「能猜的就让人猜」是刻意的设计语言 | 加说明文字、加「当前是番茄钟模式」提示（列表整个消失就是信号） |
| D7 | **骨牌只在启动和点 Start 时更新**日期 | 跨午夜不点 Start 就不更新——故意保留以增加神秘感 | 挂定时器实时刷新 |
| D8 | 侧面亮面宽度按 `j + max(0, Fallen−1)` 移窗查表（2026-08-02，原 ISSUE #12） | 原来按固定屏幕位置查表，最左边那块骨牌（世界坐标恒为 `Count-1`）整周宽度死等于 6px；移窗后它随一周推进逐日变窄，且宽度用尽正好是它倒下那天，不会有「还没倒但已经没侧面」的空窗期 | 直接按 `j + Fallen` 移窗——会在它倒下**前一天**就已经宽度归零 |

| D9 | 小目标右侧的累计时长**任务期间每分钟刷新**，显示值 = `已落盘 checkpoint + 本轮实时`；**允许它往回退**，不做单调化（2026-08-03；2026-08-06 随 I1 调整了后一项的含义） | 原来只在完成/ignore 时才动，跑一小时界面纹丝不动。显示端只做加法，**一个字都不写**——`during.json` 只在任务启动那一刻被回填改写一次（I1）。回退有两个来源，都是知情接受的：① 判定引擎的既有行为，每拍重画最近 4 分钟，AW 的 afk 迟 180 秒回填，离座后已计的分钟会被撤销——表盘的红格和截止弧本来就这么退；② 本轮那一项带 fail-open 水分，而下次启动的回填是 fail-closed 的（I2） | 为了「好看」记一份「本轮见过的最大值」把它变成只增不减——那是给界面加一份会说谎的状态，直接违背原则 4 |
| D10 | **任务栏 / Dock 图标不做动态跟随，两个平台都保持 exe 的静态番茄图标**（2026-08-04） | `RingIcon` 生成的彩色进度环通过 `Window.Icon` 实时更新，Windows 上标题栏、Alt-Tab 能正确显示彩色圆环（真机验证过），但任务栏那个具体位置只认 Avalonia 内部为 `ICON_SMALL` 生成的小尺寸 HICON（16~24px），会直接忽略、继续显示 exe 静态图标——这是 Avalonia 框架内部限制，`Window.Icon` 这层公开 API 没有开关能覆盖，要修只能自己 P/Invoke 绕开、另发一条更大尺寸的 `WM_SETICON`（跟 `WindowPin.cs` 绕开 `Window.Topmost` 是同一类手法，但换成 GDI 图标那套 API，工作量不小）。macOS 对应的是 Dock tile，由 `NSApplication.setApplicationIconImage:` 驱动，与 `Window.Icon` 是两套完全不同的机制，要做得走 Objective-C 运行时消息发送——仓库里目前一行 ObjC 互操作代码都没有，是全新的一类维护成本，比 Windows 那边更重。两边工作量都不小，只做一边会造成「Windows 有、macOS 没有」的不对称体验，所以两边都不做 | 只给 Windows 一侧写原生 `WM_SETICON` 绕过代码——不对称比两边都没有更别扭；给 macOS 加一半的 ObjC 互操作又不完整实现。真要做，两边一起做 |

## E. 闹钟（2026-07-30）

| # | 决策 | 背景 | 不要做成 |
|---|---|---|---|
| E1 | **响铃时刻在拨针那一刻算死**（`NextRing`），之后单调比较 | 每帧重判黄针位置会在「拨到与时针重合」时当场触发 | 角度容差判定（0.5°/分钟，1.5° 容差 = 提前 3 分钟响） |
| E2 | **三级判断全部严格小于**：恰好重合 = 12 小时后 | 用户明确：「刚好相等，说明希望的是 12 个小时后再响」 | `<=`（拨着拨着突然响） |
| E3 | **拨针输入只有滚轮**（2026-07-30 晚定稿）：前滚逆时针、后滚顺时针，一格 5 分钟 | 点击拨针做过两版（独立按钮 → 钟面左右键+长按加速），当天全部撤掉——用户：「免得影响以后的功能扩展」，钟面点击留白 | 恢复任何点击/长按拨针；把滚轮方向按自己的「直觉」翻转（方向是用户点名的） |
| E4 | 闹钟交互在**钟面本身**上，无独立按钮 | 独立按钮时代撞过：Button 内部把 PointerPressed 标 Handled，普通 `+=` 订阅收不到 | 恢复独立按钮；给钟面挂静态 Tooltip（会盖住闹钟时间提示） |
| E5 | 闹钟是**一次性**的，响过即撤 | — | 每日重复闹钟（要做先确认） |
| E6 | 调整期静默用**截止时刻**不用布尔量 | — | 布尔 + `Task.Delay` 复位——滚轮连续来时早到的复位会掐断晚到的调整期 |
| E7 | **闹钟持久化只有一个值：响铃时间点**，退出时写一次；黄针位置 = 时间点对 12 小时取余的**推导值**（`AlarmClock.Position` 是计算属性）（2026-07-30 定稿，推翻了同日早些的「位置+时刻各存一份」） | 关着程序时错过的闹钟不补响（只剩黄针残影）；崩溃退出丢本轮拨针 | 单独存黄针位置（冗余，两个值会漂）；滚轮每拨一下就写盘；补响过期闹钟；「机械钟语义」（位置常设、每 12 小时都响——配合 Shutdown 会每天关两次机） |
| E9 | **`executeCommand` 是个列表，永远只执行第 0 条**（2026-08-02） | 想换命令要动文件、重排顺序 | 加界面去选（D6：能猜的就让人猜）；执行全部（一串关机+锁屏同时跑）；按名字查找（那就成了配置格式设计，而这只是个收藏夹） |
| E10 | **齿轮按钮不挂 Tooltip**（2026-08-03，另一台机器上删的） | 点开的是模态 `SettingsWindow`。模态窗一起来，按钮就再也收不到 `PointerExited`——气泡卡在对话框上面，关掉对话框还赖着不走。在 `OnSettings` 里显式 `SetIsOpen(false)` 也不稳（用户实测）。喇叭和图钉没这问题：它们是就地切换，指针始终在同一个窗口里 | 把 `ToolTip.Tip="Settings"` 加回来（齿轮本来就不用解释，D6）；改成「打开前手动关一次气泡」——试过，不可靠 |
| E11 | **响铃遍数：闹钟 4 遍、三声通知各 2 遍**（2026-08-03），间隔 = 音频文件自身时长（`Sound.Duration` 直接读文件头算，见 §10） | 系统音一两秒就完了，走个神就错过。闹钟给 4 遍是因为它是唯一没有第二次机会的声音——响完什么都不留（黄针不动、不弹窗，§8.1a）；三声通知给 2 遍就够，它们各自落在一个留在屏幕上的状态上（表盘满了、蓝扇形、格子不再长），漏听还能看回来。2 遍也是被节拍卡住的上限：三声通知挂在整分钟节拍上，Idle 可能连着两分钟都响，最长的系统音（Ring05，12.8 秒）响 2 遍 26 秒，响 4 遍就压到下一次提醒上了 | 当成 E5 的「每日重复闹钟」翻案（仍是一次性：`MarkFired` 照旧在第一遍之前就跑了，变长的只是这一次的响铃）；用固定间隔——winmm 的 `PlaySound` 只有一个通道，下一遍早到会把上一遍从中间掐断，macOS 那边则是叠着一起响，两种都不是「响了 N 遍」；把设置里的试听也改成响 N 遍（试听是挑音色，不是排练打扰） |
| E12 | **连响不加锁、不排队**（2026-08-03 用户问过，当场定死） | 这条路径只碰局部变量；音频层唯一的共享可变状态是 `MacAudio` 的 SoundID 缓存，它自己有 `Gate`。锁真正能买到的是「声音不重叠」，那是策略不是正确性：代价只能是**丢**一声（重要的输给不重要的）或**排队**（迟到，且要维护一份跨 Give Up、跨关窗的状态）。何况撞不上——三声通知同挂一个整分钟节拍，`TaskSession` 每拍最多选一个（`focusDone` / `idleNudge` 是 `else if`），真能交叉的只有闹钟 × 通知。技术上还有一条：`lock` 跨不了 `await`，换成 `SemaphoreSlim` 抱着睡 5~50 秒，那已经是队列了 | 加「通道忙就跳过」（那是滴答的策略，滴答可以丢，通知不能丢）；加播放队列 |
| E13 | **`Tick.Stop()` 只挂在喇叭点击里，不放 `ApplyChrome()`**（2026-08-03） | Windows 上 `Tick.Stop()` 是 `PlaySound(null)`，停的是**本进程在 winmm 单通道上正在放的任何东西**，不管是谁起的头。而 `ApplyChrome()` 有四个调用点（构造 / 喇叭 / **图钉** / 关 Settings），放在那里等于「点图钉把正在响的闹钟掐了」——图钉跟声音毫无关系。响铃只有一遍、窗口一秒时撞不上；改成连响 2~4 遍、窗口几秒到几十秒之后就撞得上了。截断本身用户不在意（E12：不排队不加锁），触发点乱才是问题 | 挪回 `ApplyChrome()`「反正幂等」；为了「只停自己那一声」去换 `waveOut`（单通道模型下没有干净做法，代价远大于收益） |
| E8 | **Shutdown 开关不跨会话**：每次启动在 `Settings.Load` 里强制复位，退出时不用管 | 想到点关机必须本次会话里手动打开 | 把它持久化——重启后被上一个会话的关机命令拍死是事故；复位放退出时（崩溃路径覆盖不到，启动复位一道就够） |

## F. 身份标识与构建（改错不报错、只把账算错的一类）

| # | 决策 | 后果 |
|---|---|---|
| F1 | `App.axaml` 的 **`Name="ItamiTimer"`** 永不改名/本地化 | macOS 上 AW 报的 `data.app` 就是它。不设 → `Avalonia Application` → 自身豁免失效 → 看一眼进度就违规的死循环。修法**绝不是**往 `SelfApps` 塞 `Avalonia Application`（豁免所有 Avalonia 应用） |
| F2 | csproj **`AssemblyName=ItamiTimer`** 钉死 | Windows 上 `data.app` 是 exe 名，同上 |
| F3 | Core 保持 **`net10.0`**、零 UI 包引用 | 边界纪律的执行机制本身 |
| F4 | App 的 TFM 是 **`net10.0`**，别把 `-windows` 加回去 | 后缀从没起过作用；去掉后 CA1416 真的开始报没守住的平台调用 |
| F5 | **`StripPdbFromPublish` target 不许删**，也不许退回文档手工步骤 | 「必须记得做」的步骤已经被忘过一次（127MB 发布） |
| F6 | 三个调试出口走 **headless**，正常启动路径不动 | 构建（pack-macos.sh 的 --export-iconset）不该依赖图形会话；`UsePlatformDetect` 在无图形会话崩 -6661 |
| F7 | 查询窗口**放宽 6 小时**再自己裁（T1） | AW 只按事件起点过滤，跨起点事件静默消失。没有可证明安全的宽度，失败模式良性（判 Gap），不为它加机制 |
| F8 | `HttpClient` **`UseProxy = false`** | 系统代理吞 localhost，表现成莫名连不上 |
| F9 | `AwEvent.End` 用 **`AddTicks`** 不用 `AddSeconds(double)` | 后者按毫秒取整，微秒精度的事件碎成亚毫秒区间，偷懒 8 次膨胀成 54 次 |
| F10 | AW 时间戳**在 `AwClient` 边界上 `ToLocalTime()`** | 「同一个量两种表示，要在边界上归一，不要指望每个使用点各自记得转」——时区 bug 犯过两次 |
| F11 | `pack-macos.sh` 里 **`$VAR` 紧跟中文标点一律加花括号写成 `${VAR}`**（2026-08-02） | macOS 自带的 bash 是停在 3.2 的老版本（GPLv3 之后苹果不再升级）。`set -u` 下 `$RID，` 这种写法，标点的字节会被误算进变量名，报 `RID: unbound variable`——报错信息就在眼前，但指向的原因是假的（变量明明赋过值）。属于 G 节那一族「编译/运行不报真正的错，只报一个看着像但方向错的错」 |

## G. 方法论教训（不针对具体功能）

- **一个功能连塌三次、每次塌在新地方，通常不是实现烂，是需求自相矛盾**（C1 的置顶：
  「弹出来」和「绝不抢焦点」并存了很久没人发现）。
- **更好的设计通常不是把旧方案改好，而是让旧方案的整个问题消失**（D1 的木桶短板）。
- **凡是「必须记得做」的手工步骤，就是迟早会被忘掉的步骤**（F5）。
- **凡是同一个量有两种表示，就要在边界上归一**（F10：圈号/时区/偏移量都栽过）。
- **改动的说服力来自实测，不是推断**（headless 改造用 SHA-256 逐字节对比 + DYLD
  库加载清单验证；UIA 测「抢不抢焦点」必须先用无关控件做对照——Invoke 本身会抢）。
- **被否掉的常常是表达方式，不是那件事本身**（键鼠空闲检测被删又加回：否的是置顶
  这个表达，检测本身用户要）。
- **根因描述也要按代码核对，不能只按印象写**（§15.1：第一版的根因分析写得言之凿凿，
  但跟代码对不上——它决定了后来所有人往哪个方向修）。
- **「更好的设计让整个问题消失」在这一轮连中两次**：把「无记录」从不计入改成计入之后，
  「断线兜底会被下一拍撤销」那个问题自己没了；把逐秒查找改成按秒落格之后，T4 的桥接
  不需要了。两次都不是把旧方案改好，是换个问法让旧问题不成立。
- **同一个现象要先分清它会不会自愈**（DESIGN §7.4）：AW 滞后的尾巴和零时长事件的空洞
  在盘面上长得一模一样，一个下一拍就好、一个永远不会好，处理方式完全相反。
- **换类型时要拿改动前的输出逐字对比**（2026-08-02：`MinuteCell` 从 `double` 改成 `int`，
  CLI 账单里的 `off / 60` 当场变成整数除法，`52.6 min` 打成 `52.0 min`）。编译过、
  测试全绿、数字还像模像样——**只有对比才看得出来**。跟 F9 的 `AddSeconds` 同一族：
  数值类型的改动不会报错，只会安静地少几位。
- **对比之前先确认输出是可复现的**（2026-08-02：想用 SHA-256 比对表盘样张验证重构，
  结果 10 张全不同——`DrawHands` 读 `DateTime.Now`，同一份代码隔几秒渲染两次就不同。
  是那三张「零色块、只有休息扇形」的也不同，才暴露了原因）。**先证伪你的对比方法，
  再用它下结论。**
- **一个文件只能有一个解析器**（§15.4 的 `executeCommand`）。两条读取路径是不同时间、
  不同 AI 各写一半留下的，两套设置只能靠人手动对齐——咬过两次，症状都是「文件的一半
  好用、另一半安静地不工作，程序照常启动」。**多个 AI 先后改同一个项目时，这类
  「各自实现一半」的重复格外容易长出来**，而它不报错。
- **别把别的框架的常识搬进 Avalonia**（H12 的滚轮 `Delta/120`）。这类错的共同特征是
  **编译过、不抛异常、功能安静失效**——跟 F1/F2 的改名、F9 的 `AddSeconds` 是同一族。
  判断方法很朴素：只要写下的是一个**魔数**（120、96、0.5f 之类），就去查它在**这个**
  框架里到底是多少。

## H. 判定引擎第二版（2026-08-02 定稿，规格见 DESIGN §3/§4）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| H1 | **码值 = 覆盖优先级**（值越小越晚画、越权威），**不是**「对用户有利的程度」 | `Afk`(2) 排在 `OffTask`(3) 之下，看起来反直觉 | 按「有利程度」调换 Afk / OffTask——afk 优先级会**悄悄失效且不报错**。判「计入」请用不变量 `码 ≥ Focused(4)`，别列举具体码值 |
| H2 | **「这一秒没有任何记录」→ `AwOffline`，计入专注**（知情的 fail-open） | `aw-watcher-window` 单独死掉 = 全盘绿、白拿满分（DESIGN §16.5 记录在案，用户明确暂不处理） | 改成不计入——AW 每拍末尾恒有 6~12 秒滞后，那会变成恒定扣时 |
| H3 | **整拍连不上 = 空事件列表**，没有单独的兜底代码路径 | — | 加 `if (awDown)` 分支；保留 `AwOfflineFallback` |
| H4 | **连不上时只写最后一分钟**，不铺满 4 分钟 | — | 用「没有信息」去覆盖「已有的信息」——前 3 分钟已有真实判定，铺满会把红的抹绿 |
| H4a | **漏拍漏到查询窗口之外的那段填 `Init`（不计入），不是 `AwOffline`**（2026-08-02 实现时补） | 挂起期间既不算专注也不算偷懒，只把任务拖长 | 一律填 `AwOffline`——那样**睡一觉就能刷满任务**。`AwOffline` 的语义是「查了，AW 拿不出数据」，漏拍是我们自己没查，两件事不该同一个待遇。也不能什么都不填：上一拍的承诺弧还铺在那儿，会被染成「还没走到」 |
| H5 | **达成是事件，不是推导值**：算出缺口 ≤ 0 的那一拍就是达成时刻，记下来不再重算 | 最晚比真实达成晚 60 秒发现，而这 60 秒不再吃休息 | 恢复 `FocusCompletedAt()` 那类「回头找跨过阈值那一格」的算法——账本一重写它就往回跳（§15.1 就是这么来的） |
| H6 | **休息 = ⌈focus/5⌉，只读提交时锁定的 `FocusMinutes`** | 正式量程每档比原来少 1 分钟（10→2、25→5、50→10） | 用「剩余目标」算休息（拖得越久歇得越少，激励反了）；恢复 +1（它补的那个延迟已经不存在）；只用 5 的倍数验证公式（护栏测试的取值必须覆盖滑块实际能出的值） |
| H7 | **归档结算范围以 padding 为基准**：`[180, 3780)` | — | 写成 `[0, 3600)`——多算 padding、漏算 180 秒，归档瞬间账会跳且毫无提示（§15.5） |
| H8 | **盘面只有两圈**；归档时内圈内容跳到外圈、内圈清空（首次满 2 小时，之后每小时一次） | 视觉塌缩；超过两小时的早期记录从盘上消失（账已进 `during`） | 恢复 lane 2（buffer 只有 120 格，够不到）；为了「看起来连续」而不让圈号跟 buffer 位置走——归档存在的理由**就是**让盘面只画最近一到两小时 |
| H9 | **查询区间与写入偏移一律从整分钟算**，绝不掺 `now` 的亚秒零头 | — | 用 `DateTimeOffset.Now` 直接减 4 分钟——相位每拍不同、边界秒来回翻面，AW 响应慢 10 秒还会整段错位 |
| H10 | **CLI 用离散档、表盘可以用连续**；`MinuteCell` 保留四个原始计数 | `> 40` 这一档下偷懒 19 秒仍然满绿满高 | 把渲染档位固化进判定层——那样表盘再也回不到 D1 的连续编码 |
| H11 | **承诺弧每拍重算**（先清 `[H,7380)` 再填 `[H,H+D)`），不记「上次画到哪」 | 每分钟一次 7KB `Fill` | 记住最后一个 `Gray` 的位置再增量推动——afk 收缩会让弧末端**前移**，增量做法会留下残渣 |
| H12 | **滚轮加速按「连续滚了几格」算，不按单次 `Delta` 幅度**（2026-08-02）；一串下来 1 → 3 → 8 → 15 → 30 分钟/格，停手 300ms 断串 | 快滚容易冲过头，但松手就回到 1 分钟/格 | 拿 `Delta.Y / 120` 当格数——**120 是 Win32 `WM_MOUSEWHEEL` 的单位，Avalonia 一格就是 1.0**，那个除法恒等于 0.008，被 `Math.Max(1, …)` 拉回 1，于是 E3 记的加速阶梯**一次都没跑起来过**，而且不报错 |

## I. 累计时长改成 checkpoint 模型（2026-08-06 定稿，规格见 DESIGN §11.2）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| I1 | **`during.json` 从「账本」降级为 checkpoint**：只在**任务启动那一刻**写一次，回填 `[recordedThrough, StartedAt)` 这一段（= 上一场任务全程 + 中间的空隙），然后把 `recordedThrough` 推到本次 `StartedAt`。任务运行期间一秒都不落盘 | 界面显示的实时项在下次启动前不落盘，程序崩了这段显示就没了——但**数字本身不会丢**，下次启动照样从 AW 重新数出来 | 恢复「每一秒专注只入账一次」那套：`Settled` 事件 / `UnbankedSeconds` / `TakeUnbankedSeconds()` / `_banked` 幂等标志 / 三条终结路径各记一次账。它们防的是「这一秒现在不记就永远没了」，**AW 是地面真相，那个前提不成立了**。加回任何一条都会和回填重复计费，而且是静默的 |
| I2 | **回填 fail-closed（底色 `Init`），运行期继续 fail-open（底色 `AwOffline`）** | 同一段时间在下次启动后**可能变小**（跟 D9 的 afk 追认回退同方向，用户知情接受） | 「为了一致」把两边统一成同一个底色。fail-open 站得住的前提是**程序正在跑、拍子正在走**——机器摆明开着、人摆明在，AW 哑了是 AW 的锅。回填时程序**没在跑**，AW 没数据最大的可能是机器关着，照 fail-open 记账**一次跨周末的回填能凭空记 48 小时**。⚠️ `Judgment.Paint` 自己从不填底色（fail-open 是 `JudgmentBuffer.Cover` 用水位线加的），所以回填复用它一行都不用改——**别顺手改 `Paint` 去"统一"** |
| I2a | **也不要反过来把运行期改成 fail-closed** | — | AW 每拍末尾恒有 6~12 秒滞后，运行期 fail-closed 会变成恒定扣时（H2 原文）。两边规则不同**是对的**，不是待清理的不一致 |
| I3 | **「从没统计过」用 `recordedThrough: null` 哨兵**，不把 bucket 的 `created` materialize 进 during.json | 首次回填要走完整段 AW 历史，可能几十秒 | 播种时查 AW 把 `created` 写进去。三条理由，第三条是决定性的：① `created` 是派生值且会变（重装 AW / 迁 datastore）；② 回填窗口的起点**不需要准确，只需要足够早**，裁剪是 AW 自己的本职工作；③ **播种在程序启动路径上，那一刻 AW 可能连不上**——「已播种但起点未知」这个状态**无论如何都省不掉**，materialize 只是在旁边多加一条会变质的脆弱路径。`created` 只作临时下界用（`FindBucketCreatedAsync`），查完就扔 |
| I4 | **从 1.0.x 迁移时，`recordedThrough` 必须盖「迁移这一刻」的戳** | 迁移前那段历史沿用老账本的数，不重新推导 | 留 `null`——那样下次启动会从 AW 历史开头重数一遍**再加到**已有的值上，**数字直接翻倍**，而且没有任何报错 |
| I5 | **凡是「纯逻辑 + 写盘」的方法一律拆两层，测试只调纯的那层**（`FromLegacy`/`Migrate`、`Apply`/`Advance`） | 多一个 public 方法 | 在单元测试里调会 `Save()` 的方法——`During.Save()` 写的是**用户真实的** `%LOCALAPPDATA%\ItamiTimer\during.json`。2026-08-06 就是这么把用户的累计时长冲成测试数据的，**跑完测试全绿，一声不响**。这跟 F1/F2 改名、H12 的 `Delta/120` 同族：编译过、不抛异常、安静地造成损失 |
| I6 | **回填按天切片 + 120 秒的回填专用超时**，运行期仍然是 10 秒（2026-08-06 真机干跑打回来的） | 请求数 ×7；`FetchEventsAsync` 的 6 小时放宽让按天切多拉 1.25 倍数据（按周只有 1.04 倍）。实测 150 天全量 4 分 11 秒、增量 1.3 秒 | 沿用 `AwClient` 默认的 10 秒超时去拉周级的片子——真实历史里一周的窗口事件量足以超时，而回填失败 = 不推进 checkpoint，那段历史会**永远卡在同一个地方**：每次启动重试、每次同样超时、累计时长再也不前进，只有日志里一行 WARN。两个场景的取舍是相反的：运行期宁可短超时 fail-open 也不能拖住整分钟节拍，回填**慢一点无所谓，失败才是问题** |

## J. Alarms 清单（2026-08-06 定稿并落地，规格见 DESIGN §9）

| # | 决策 | 已接受的代价 | 不要做成 |
|---|---|---|---|
| J1 | **跟手动闹钟彻底独立、互不共享状态**——各自的持久字段、各自的音色、各自的表盘元素（黄针 vs 小红圈） | 两套几乎并行的小逻辑 | 复用闹钟的 `_fired`/单值持久化模型。清单是"多条外部生成的时间戳"，闹钟是"一次手拨的意图"，语义不同，硬塞进同一个模型只会把两边都搞复杂 |
| J2 | **`alarms.md` 用 Markdown 清单**（`- [ ] YYYY-MM-DD HH:mm 文字`），不是自造语法的纯文本，也不是 INI | 需要一条正则 | 最初想用 `.txt` + 自造的 `#` 注释——没有任何工具认得这个语法。Markdown 是真实存在的格式，且跟用户日记本来的写法一致；不认识的行（标题/空行/随笔）一律当装饰忽略，不需要专门定义注释 |
| J3 | **`[x]`（勾选）恒被跳过，不管是不是还没到** | 无 | 给清单加一个独立的"静音某一条"开关——勾选本身就是"改数据源"这个动作的一种，不需要在程序里再造一条路 |
| J4 | **不解析周期规则，只认展开好的具体时间戳**；程序只读 `alarms.md`，从不回写、不清理过期行 | 上游（日记/日历/脚本）必须自己把"每天 14:00"展开成一串具体日期 | 在 `AlarmsList` 里加"每天/每周"这类周期语法——那是 E5 明确拒绝过的"每日重复闹钟"的变种，会破坏"程序只管执行不管建立"这条边界 |
| J5 | **时间戳不做任何时区换算，只认本机当前时钟** | 换时区后，清单里的时刻跟着"这台电脑现在几点"重新解释 | 加时区字段/换算逻辑——吃药、打卡这类事跟着的是人在哪儿、墙上的钟指几点，不是固定的 UTC 瞬间，这本来就是语义上对的行为，不是简化 |
| J6 | **Settings 的 Alarms 开关只管响不响铃，是普通持久化设置（不像 `commandEnabled` 那样每次启动强制复位）；检查清单/挑出到点条目/弹系统通知这条主链路强制无条件、每分钟都做，不受这个开关控制** | 关掉这个开关不能让提醒完全消失，只能消音 | 做成"武装/未武装"的开关（照抄 Execute）——这是这轮讨论里推翻过一次的方案：检查逻辑本身应该跟判定引擎一样不记状态、每拍重算，而不是需要用户"打开"才生效 |
| J7 | **去重水位线纯内存、不持久化，初始化成程序启动那一刻**（`_alarmsProcessedThrough`），不是闹钟那种跨会话持久的 `_fired` | 程序关闭期间错过的条目，重开后直接跳过，不倒回去补 | 跨会话持久化"上次数到哪一分钟"——已经定了"不补响、只看未来"，持久化这个水位线只是白白多一个要维护、可能跟文件内容对不上的状态 |
| J8 | **表盘小红圈只在下一条落在未来 12 小时以内时才画** | 清单里更远的下一条不会在表盘上体现，想看要去翻文件 | 直接照抄黄针的 mod-12 换算不加这个门槛——黄针的换算从不骗人是因为 `NextRing` 保证被取余的数从没超过 12 小时；清单的"下一条"没有这个保证，5 天后的时刻直接取余会画出"2 点钟方向"这种误导性的位置 |
| J9 | ~~系统通知走一个短命的 `powershell.exe`/`osascript` 子进程，AppId 用裸字符串 `"Windows PowerShell"`~~ **（当天两次翻案，最终结论见 J13：不是走不通，是 AppId 选错了）**。第一次翻案：真机验证时屏幕上什么都没弹出来，误判成"这条路整个走不通"，改成方案 B——程序自己在骨牌区域画一条提示条，`Platform/Notify.cs` 一度被删掉 | — | 见 J13，不要重复这次的误判过程：**不要因为屏幕上没弹出来就断定 AppId 没注册**，先去通知中心（不是只看有没有横幅）翻一遍历史 |
| J10 | **同一分钟内清单和闹钟都到点：清单先判断、闹钟后判断**，响铃遍数清单是 2 遍（不是闹钟的 4 遍） | 无新增机制——直接并入 E12/E13 的"不加锁不排队"。J11 改成提示条之后，"清单响完还有系统通知兜底"这条理由改成了"清单响完还有 `itami.log` 那行日志兜底"，结论不变 | 加播放队列/锁去保证两边不重叠——那不是这里要买的东西。真正要买的是"谁输了代价更小"：闹钟响完什么都不留，清单响完还留着一分钟的提示条，所以让闹钟的调用在后，Windows 单通道上闹钟赢 |
| J11 | **提示方式改成程序自己画一条提示条，叠在骨牌所在的 Grid 里**，显示时间 + 文字，**刚好显示一分钟**（到点起、到下一分钟整点止），到点自动收起 | 不再有"关掉程序也能事后翻看"的通知历史——`itami.log` 里 `Alarms 清单到点: ...` 那一行是唯一留痕 | 单开一个置顶悬浮窗——骨牌区域本来就在"表盘和骨牌，给眼睛看"这个区域里，复用现成的位置和尺寸不需要处理任何新窗口的显示/层级问题；也不要在提示条隐藏后额外做"清除"动作，跟表盘上其它一切一样，是"这一拍该不该显示"的条件判断，不是要擦掉的状态 |
| J12 | **视觉定稿：深色文字（`#1B222A`）打底、亮蓝色（`#63A6E0`）文字错位 2px 叠在上面**，两层内容一致，只有颜色和位置不同；时间行 18 号加粗，文字行 16 号不加粗 | 骨牌全程透出来，不再整块遮住 | 同一天现场反馈迭代了五轮，中间形态**都不要回头捡**：①整块矩形色块（蓝底白字，挡住骨牌）②贴着文字的小背景（深色半透明底白字，还是块状）③纯深色文字，无底色但两层字号/字重统一成时间那档（18 号加粗），字重偏重——每一版都是当场截图/真机观察后被下一轮反馈否掉的，不是猜的，别为了"看起来更像正经通知"倒退回带底色的版本，也别把文字行的字重加回时间那一档 |
| J13 | **`Notify.cs` 加回来，AppId 从裸字符串 `"Windows PowerShell"` 换成 PowerShell 控制台宿主真正的 AppId**（`{1AC14E77-02E7-4E5D-B744-2EB1AE5198B7}\WindowsPowerShell\v1.0\powershell.exe`，Windows 装 PowerShell 时自动登记的固定值，不是这个项目分配的），**跟提示条并存，不是二选一** | 会不会弹横幅取决于 Windows 自己的"请勿打扰"/通知设置，这个不确定性接受，不追求代码层面消除 | 用户自己翻通知中心发现的：J9 里判定"走不通"的那次测试其实用的是裸字符串，从没在系统里注册过；同一天早些时候用这个正确 AppId 测试的那条通知**其实送达了**，只是"请勿打扰"开着、横幅被吞掉，通知安静地躺在通知中心里没被人看到——两次测试用的是不同的 AppId，结论不能混着用。**别再为了"提示条已经够用"把 `Notify.cs` 第二次删掉**：提示条保证屏幕上一定看得见（不受 Windows 通知设置影响），系统通知额外给一份"关掉程序也能翻看"的记录，两者互补，没有谁能顶替谁 |

## K. 无边框透明主窗口（2026-08-08 讨论定形，同日实现，改过一次方向；规格还没写进 DESIGN.md）

> K1~K2 是讨论阶段被排除的路径，仍然有效。**K3~K8 曾经是第一版的方案和实现记录——
> 整个方向错了，已作废**，见下面「⚠️ 已失效」；K9~K11 是改过方向之后真正落地的东西。
> 真正定稿时记得把这一节的内容搬一份规格进 DESIGN.md。

### ⚠️ K3~K8 已失效（2026-08-08 当天用户指出理解错了）

第一版把"无边框透明"做成了另开一扇独立的悬浮钟面窗口（`ClockWindow`，只画表盘，
跟 MainWindow 镜像状态），代码写完、编译通过、单元测试全绿、Debug 产物冒烟启动过。
**用户随后纠正：要的是整个主窗口本身无边框、背景透明，所有现有功能原样保留，
不是另开一扇只有表盘的新窗口。** `ClockWindow.axaml`/`.axaml.cs` 连同
`Settings.ClockWindowEnabled`/`ClockWindowX`/`ClockWindowY`、SettingsWindow 里
的"Desktop widget"卡片、MainWindow 里 `ApplyClockWindow`/`_clockWindow`/
`_shuttingDown` 那一整套已经**全部撤销删除**。不要因为"代码已经写好、测试也过了"
就想把这套方案捡回来——问题不是实现质量，是从一开始就理解错了需求范围。

### K. 现行方案

| # | 决策 | 已接受的代价 / 背景 | 不要做成 |
|---|---|---|---|
| K1 | **不做 Rainmeter 那种真·桌面嵌入**（Windows 上的 `WorkerW` 技巧：`Progman` 发未公开的 `0x052C` 消息 → `SetParent` 挂到派生出的空 `WorkerW`） | 技术上可行，但那是 explorer.exe 内部实现细节、非公开契约，历史上跟着 Windows 功能更新碎过好几次（Rainmeter 自己也在追着修），维护成本不可控；且真嵌入意味着必然被其它应用窗口完全遮住、通常还要点击穿透，跟"可交互"的诉求冲突。用户是双屏，不担心遮挡，没必要为了「贴在图标层下面」这一点付出这个代价 | 真去实现 `SetParent`/`WorkerW` 那套——除非以后明确需要「压在桌面最底层、`Win+D` 也不消失」这个具体效果，且愿意承担跟 Windows 更新打地鼠的成本 |
| K2 | **不做 Windows 11 Widgets 面板**（Adaptive Cards + `IWidgetProvider` COM + MSIX 打包接入官方挂件板） | Adaptive Cards 只能声明式布局，画不出矢量表盘，会退化成周期性截图糊上去——直接踩中 D5/16.2「维持矢量」这条护栏；而且 Widgets 面板默认收起、要用户主动点开才可见，跟"常驻可见"的诉求南辕北辙；接入还要求 MSIX 打包 + 大概率要 Store 上架，跟现有唯一对外发布路径（`pack-windows.ps1` → Inno Setup 单个 exe，DESIGN §14）完全是两条链路 | 为了「官方认证」这个名义去追这条路——它解决的是"按需瞥一眼"的问题，不是这里要的"常驻可见" |
| K9 | **无边框透明是 `MainWindow` 自己的属性**（`WindowDecorations="None"` + `Background="Transparent"` + `TransparencyLevelHint="Transparent"`），不是另开的第二扇窗口。表盘、骨牌、Start 按钮、滑块、目标列表、齿轮/喇叭/图钉、版本号——**所有现有控件原封不动**，只是外框和窗口底色没了 | 文字/图标原来假设背景是浅灰色 `#D7DBE0`，现在直接叠在用户桌面壁纸上，能不能看清是用户自己的桌面决定的，不是代码能兜底的事——用户已经知情要这个效果 | 只把表盘部分做透明、其余控件继续套一层不透明底色"保证可读性"——用户明确要的是整扇窗口一起透明，不是部分透明 |
| K10 | **拖动范围收窄到表盘**（用户 2026-08-08 明确要求："仅仅点在表盘区域拖动才有效"）：`PointerPressed → BeginMoveDrag` 直接挂在 `Dial`（`DialControl`）上，不是整个 `Window` | ~~用 Dial 的矩形 `Bounds` 当命中范围，四个角也能拖~~ **这个描述是错的，见 K21** | 反过来在整个 Window 上监听、再排除按钮/滑块/单选框——最初就是这么做的，用户嫌拖动范围太大，改成只挂在 Dial 上，比维护一份排除列表更简单也更符合"钟面点击留白"这条既有护栏（DECISIONS E3）的精神 |
| K11 | **macOS 上：置顶复用 `WindowPin.cs` 已验证的 `Window.Topmost`**（不带激活语义，直接能用，见该文件注释）；**`BeginMoveDrag` 和透明背景的实际观感未在 macOS 真机验证过**，只是 Avalonia 文档承诺三个桌面后端都支持 | — | 假定"Windows 测过等于 macOS 也通"——这个项目已经在 H12（滚轮 `Delta/120`）上吃过一次"编译过、不报错、功能安静失效"的亏，同一类风险，做完必须在 macOS 真机上跑一遍再定稿，不能只凭文档 |
| K12 | **图标/文字的可读性靠"双层描边"（浅色加粗垫底 + 深色正常叠在上面），不是加背景色块**（`ChromeIcons` 的 Halo 描边、新增的 `HaloTextBlock`）——用户 2026-08-08 明确要求这个方向，跟提示条的双层文字（DECISIONS J12）同一个思路 | Ticking/Pin/Settings 三个图标、目标列表的文字、累计时长数字、版权/版本号这几处都套了这个技巧；`AlarmBanner` 已经有自己的双层文字方案（DECISIONS J12），原样没动 | 反过来给这些地方套 `Border` 背景色块——用户明确排除过；给单个 `Path` 同时设 `Fill` 和 `Stroke` 图省事——那样描边会画在填充**之上**啃掉半圈填充边缘，必须分成两个叠放的 `Path`（浅色描边在下、深色纯填充在上），细节见 `ChromeIcons.Speaker` 的注释 |
| K13 | ~~`RadioButton.Content` 换成 `HaloTextBlock` 之后，目标名字改存进 `Tag`~~ **2026-08-08 当天被 K14 撤销，见下** | — | — |
| K14 | **Start 按钮以下整体套一张半透明背景卡片**（`#CCD7DBE0`，80% 不透明度，`CornerRadius=10`），**目标列表/累计时长/版权/版本号的 `HaloTextBlock` 双层描边整个撤销，改回普通 `TextBlock`；`RadioButton.Content` 也改回普通字符串，K13 那个 `Tag` 间接层一并撤销**（用户 2026-08-08："不用加重字这些了"）——卡片本身已经给足对比度，双层描边变得多余。`HaloTextBlock.cs` 文件直接删掉，不留死代码 | Ticking/Pin/Settings 三个图标**不在这次撤销范围内**——它们坐在 Start 按钮以上（表盘/骨牌区域），卡片只包住 Start 以下，图标继续吃 K12 的描边光晕 | 反过来把卡片也往上包到图标那一层——用户没有这么要求，图标那层保持全透明+描边的方案，不要顺手统一成"整个窗口一张卡片"，那是被明确排除过的路（K3/K9："所有功能原样保留，只是外框和窗口底色没了"，只有 Start 以下这次改了主意） |
| K15 | **卡片的 `Border` 用 `Grid.RowSpan="2"` 铺在骨牌 Grid 和 Controls 上面两行，但**声明顺序在两者**之前**（Grid 子元素的层叠顺序看声明顺序，跟 `Grid.Row`/`RowSpan` 无关），画在骨牌和 Controls **下面**——骨牌本身、`AlarmBanner`、齿轮照常盖在它上面 | — | 把 Border 放到骨牌 Grid **之后**声明——那样会盖住骨牌 |
| K16 | **卡片范围（2026-08-08 当天改了四版，最终定稿）：从骨牌最下边开始，包住 Start 按钮、滑块、目标列表、版权/版本号**；`Grid.Row="2"` 紧接骨牌那一行，行与行默认贴着排，不用手算 Margin 去够骨牌下边缘 | 改到定稿走了四版：① 顶边伸进骨牌阴影下半段（Margin 60px）→ 圆角在骨牌旁探出来、宽度对不上（骨牌视觉宽度比卡片窄）；② 贴骨牌底部留 8px、包住 Start（Margin 96px）→ 用户纠正"是Start底部以下留间歇"；③ Start 完全不进卡片、卡片从 Start 下方 14px 开始 → 用户再次纠正"Start 按钮也应该是卡片的一部分"；④ 定稿如左 | 再去揣摩"应该更好看"的方向自己改卡片边界——这条改了四版才对，每一版都是用户看到真机效果后纠正的，不是能靠推理得出的东西 |
| K17 | **卡片内部统一 12px 留白**：`Border` 自己的 `Padding="12"`（四边），内层 StackPanel 的 `Spacing` 也从 10 提到 12——外边距和控件间距用同一个数字，卡片内部是一套统一的间距，不是"外圈 12、内部 10"两个数 | 滑块、`RadioButton`、靠右对齐的版本号原来直接顶着卡片左右边缘（用户 2026-08-08 反馈"不好看"） | 用内层 StackPanel 的 `Margin` 去实现内边距——`Border.Padding` 才是这件事的正主，`Margin` 那种写法是上一版的做法，已经换掉；也不要只加左右不加上下（用户原话没区分方向） |
| K18 | **卡片不透明度 90%**（`#E6D7DBE0`，`E6` = 230/255 ≈ 90%），底色仍是原来的窗口底色 `#D7DBE0` | 从最初的 80%（`#CC`）提上来，用户 2026-08-08 定的 | — |
| K19 | **表盘上挂一个右键菜单，只有"Close window"一项**（图标是 `ChromeIcons.Close()` 画的矢量 X），点了走 `Window.Close()` | 无边框之后没有标题栏、没有系统菜单，任务栏图标的右键菜单一度是唯一能关窗口的地方，补一个跟它对齐的入口；跟左键拖动挂在同一个控件上不打架（一个右键一个左键） | 做成一整套窗口菜单（最小化/置顶/设置都塞进去）——用户只要了"关闭窗口"这一项，跟任务栏那个菜单对齐；也不要绕开 `Close()` 直接退进程，那样任务没结束时不会弹 §9 的放弃确认 |
| K20 | **菜单文字是英文 "Close window"，不跟随系统语言** | 用户原话是"要根据系统文字，如果不行，就统一为英文"——跟随系统语言要引入一整套本地化资源机制（这个项目目前一行都没有），为一个菜单项不划算；而且"界面文字英文"本来就是 CLAUDE.md 里的硬约束 | 为了这一个菜单项引入 `.resx`/`CultureInfo` 那一套——真要做本地化是整个界面一起做的事，不是单点开个口子 |
| K21 | **Avalonia 对自绘控件的命中测试按「实际画出来的绘制操作」逐个判定，不是按控件的矩形 `Bounds`**（2026-08-08 用户实测推翻了 K10 原来的描述）——所以表盘拖动的真实生效范围**是圆的**：四个角（矩形内但一笔没画过）拖不动，圆盘外沿再往外一点能拖，因为那儿画着表盘投在"墙上"的阴影（`DialControl` 的第一层）。用户看过之后认为圆形**更好**，**维持现状不改** | 写 K10 那句"用矩形 Bounds 当命中范围"时是**凭对框架的印象推断的，没有验证**——正是 CLAUDE.md 里"别把别的框架的常识搬进 Avalonia"警告的那一类，而且这次连"编译不报错"都不算数：功能是好的，只是我对它为什么好的解释是错的 | 为了"让方框范围也能拖"去 override `HitTest`/给 Dial 套一层 `Background="Transparent"` 的容器——用户已经明确表示圆形更好。**旁证**：`Panel`/`Border` 想让整个矩形可点必须显式写 `Background="Transparent"`，正是因为那个背景本身就是一个绘制操作，没有它就没有可命中的东西——同一套机制 |
| K22 | **记住窗口位置**：`settings.json` 的 `windowX`/`windowY`（物理像素），拖动停下来时写一次；没存过就走 axaml 的 `CenterScreen` 居中。恢复在 `Opened` 里做（那之前 `Screens` 拿不到可靠信息），**恢复完立刻 `ClampIntoScreen()` 过一遍** | 无边框之后没有标题栏，位置全靠拖表盘定，每次启动跳回正中太烦人 | 直接信任存下来的坐标——显示器拔掉/分辨率变了之后那个位置可能整个在屏幕外，窗口就再也看不见也够不着了。**已实测**：把 `windowX/Y` 手改成 `99999,99999` 启动，程序把它拉回了 `2093,620` |
| K23 | **拉回屏幕内只在拖动停下来之后做**（`_settle`，250ms 没再动就算松手），不在 `PositionChanged` 里当场做；拉回的目标是 `ScreenFromWindow` 给出的那块屏的**工作区**（扣掉任务栏） | Windows 只替我们管住了上边缘，左/右/下要自己来（用户 2026-08-08 提出） | **一边拖一边往回拉**——那样窗口永远被摁在当前这块屏的边界内，就永远到不了"一半以上落在另一块屏上"那个状态，而 `ScreenFromWindow` 正是按这个判断归属的，结果是**窗口再也拖不到第二块显示器上去**。用户是双屏，这不是理论风险。也不要拿"所有屏幕拼起来的外接矩形"当边界——多屏排布不一定是个完整矩形，那样会把屏与屏之间的空洞也算成合法位置 |
| K24 | **`Position`/`WorkingArea` 是物理像素，`FrameSize`/`ClientSize` 是 DPI 无关的逻辑单位**，两者之间差一个 `Screen.Scaling`，`ClampIntoScreen` 里用 `PixelSize.FromSize(FrameSize, screen.Scaling)` 换算 | — | 直接拿 `FrameSize` 的数去跟 `WorkingArea` 比——缩放 100% 的屏幕上恰好对，一到 125%/150% 就把窗口算小了，边界判断跟着错，而且**不报错**，跟 H12 那个 `Delta/120` 同一族 |

| # | 决策 | 已接受的代价 / 背景 | 不要做成 |
|---|---|---|---|
| K1 | **不做 Rainmeter 那种真·桌面嵌入**（Windows 上的 `WorkerW` 技巧：`Progman` 发未公开的 `0x052C` 消息 → `SetParent` 挂到派生出的空 `WorkerW`） | 技术上可行，但那是 explorer.exe 内部实现细节、非公开契约，历史上跟着 Windows 功能更新碎过好几次（Rainmeter 自己也在追着修），维护成本不可控；且真嵌入意味着必然被其它应用窗口完全遮住、通常还要点击穿透，跟"可交互"的诉求冲突。用户是双屏，不担心遮挡，没必要为了「贴在图标层下面」这一点付出这个代价 | 真去实现 `SetParent`/`WorkerW` 那套——除非以后明确需要「压在桌面最底层、`Win+D` 也不消失」这个具体效果，且愿意承担跟 Windows 更新打地鼠的成本 |
| K2 | **不做 Windows 11 Widgets 面板**（Adaptive Cards + `IWidgetProvider` COM + MSIX 打包接入官方挂件板） | Adaptive Cards 只能声明式布局，画不出矢量表盘，会退化成周期性截图糊上去——直接踩中 D5/16.2「维持矢量」这条护栏；而且 Widgets 面板默认收起、要用户主动点开才可见，跟"常驻可见"的诉求南辕北辙；接入还要求 MSIX 打包 + 大概率要 Store 上架，跟现有唯一对外发布路径（`pack-windows.ps1` → Inno Setup 单个 exe，DESIGN §14）完全是两条链路 | 为了「官方认证」这个名义去追这条路——它解决的是"按需瞥一眼"的问题，不是这里要的"常驻可见" |
| K3 | **最终方案是"看着像"贴桌面，不是真嵌入**：无边框（`SystemDecorations=None`）+ 透明背景（`TransparencyLevel=Transparent`）+ 固定位置（读记住的坐标）+ 复用现成的 `WindowPin.cs` 做置顶。**不加**点击穿透、不加 `WS_EX_NOACTIVATE`、不隐藏任务栏/Dock——保留完整的普通窗口交互能力（用户明确要求："其它限制没有必要"） | 不是真嵌入，`Win+D`（显示桌面）会把它跟其它窗口一起收起，真·嵌入桌面的挂件在这个动作下不会消失，这里会——双屏、不常按这个手势，代价可接受 | 反过来加那些限制"让它看起来更像挂件"——用户已经明确排除过，点击/置顶都要正常生效 |
| K4 | **拖拽用 `Window.BeginMoveDrag(PointerPressedEventArgs)`**，挂在表盘（`DialControl`）的 `PointerPressed`（左键）上，整块表盘可拖 | 这不跟 E3「钟面点击留白」冲突，是兑现它——当时特意不处理点击就是为了给以后的功能扩展留空间。跟现有钟面上唯一挂着的 `PointerWheelChanged`（闹钟滚轮，`MainWindow.axaml.cs:139`，绑定在 MainWindow 自己那个 `dial` 实例上）互不相关，因为桌面挂件用的是新窗口里独立的一个 `DialControl` 实例。表盘渲染每拍整个重画、跟指针事件无耦合（跟 Alarms §9.1 一路的"没有清除这回事"同一模型），拖拽不会影响承诺弧/达成弧怎么画 | 恢复任何跟 E3 冲突的点击/长按拨针逻辑；给这块拖拽区域加命中测试排除，除非以后表盘上真长出了别的可交互控件 |
| K5 | **macOS 上：置顶复用 `WindowPin.cs` 已验证的 `Window.Topmost`**（不带激活语义，直接能用，见该文件注释）；**`BeginMoveDrag` 和透明背景的实际观感未在 macOS 真机验证过**，只是 Avalonia 文档承诺三个桌面后端都支持 | — | 假定"Windows 测过等于 macOS 也通"——这个项目已经在 H12（滚轮 `Delta/120`）上吃过一次"编译过、不报错、功能安静失效"的亏，同一类风险，做完必须在 macOS 真机上跑一遍再定稿，不能只凭文档 |
| K6 | **`Settings.ClockWindowEnabled` 默认 `false`**，只能在 Settings 窗口里手动打开；MainWindow 在构造函数和 `OnSettings` 的 `ShowDialog` 返回之后各查一次这个值（`ApplyClockWindow`），没有第三个改它的入口 | 第一次升级到这个版本的用户看不到任何变化，得自己去 Settings 里翻出来打开 | 默认开——跟 `WindowPin`「手动 pin，不自动决定」是同一个态度（K3 的原话），不要因为"功能已经做完了"就顺手把默认值改成开 |
| K7 | **`ClockWindow.SyncFrom` 每帧（跟 `MainWindow.OnFrame` 同一个 33ms 心跳）把主表盘当前的 `Palette`/`Cells`/`StartedAt`/`RestFrom`/`RestMinutes`/`AlarmMinutes`/`AlarmsDotMinutes` 整个复制一遍**，不管 MainWindow 自己当时是否可见（不受 `visible` 那个判断限制），也没有任何"只在变化时才同步"的差分逻辑 | 每帧多几次属性赋值 + 一次 `InvalidateVisual`，量级跟表盘自己的重画一样，可以忽略 | 加一层"上次同步过的值"来判断要不要同步——跟 H11（承诺弧不记"上次画到哪"）、Alarms 清单表盘小红圈（DESIGN §17）同一个理由：这扇窗口该显示什么，永远由源头**此刻**的样子决定，不需要一个可能跟源头对不上的中间状态。挂件之所以要在 MainWindow 被最小化/被挡住时还照常更新，就是靠这一行不受 `visible` 限制换来的，别把它套进那个判断里 |
| K8 | **关掉挂件的两条路径分开处理**：整个程序退出时级联关闭（`MainWindow.Closed` 里置位 `_shuttingDown` 再关 `ClockWindow`），不碰 `Settings.ClockWindowEnabled`；用户绕开正常路径单独关掉挂件本身（比如挂件拿到焦点时按 Alt+F4）时，`ClockWindow.Closed` 里检测到 `!_shuttingDown` 才把这个开关一并翻回 `false` 并存盘 | 多一个 `bool` 字段和一次判断 | 不分这两种情况、统一在 `ClockWindow.Closed` 里把开关翻回 `false`——那样正常退出程序后重新打开，Settings 里的"桌面挂件"开关会莫名其妙变成关闭，跟用户实际的偏好（"我一直开着这个功能，只是这次退出了程序"）对不上 |

## L. `executeCommand` 交给 shell 的方式（2026-08-08 修，用户在 macOS 真机报的 bug）

| # | 决策 | 已接受的代价 / 背景 | 不要做成 |
|---|---|---|---|
| L1 | **Unix 上把命令作为单独一个 argv 元素交给 `sh -c`**（`psi.ArgumentList.Add("-c")` + `psi.ArgumentList.Add(cmd)`），**绝不拼成 `Arguments = $"-c \"{cmd}\""` 那样一整条字符串** | 一个平台一条分支，比原来那行三元表达式长 | 拼字符串。拼了之后 .NET 还要按 C 运行库规则把它切回 argv，而 `rules.json` 里默认那几条 macOS 命令**自己就带双引号**（`osascript -e 'tell application "System Events" to ...'`）——内层的 `"` 把外层引号提前闭合，切出来是 `["-c", "osascript -e 'tell application System", "Events to restart'"]`：脚本被截断、单引号没闭合，剩下半截还成了 `$0`。sh 报 `unexpected EOF while looking for matching '`、退出码 2。**这个 bug 最阴的地方是闹钟那一侧全对**（到点了、也真调了 `Command.Execute`、日志里"Alarm fired; running executeCommand: …"照常打印），只有命令自己安静地失败，不翻到日志里 stderr 那一行根本看不出来——跟 H12（滚轮 `Delta/120`）、K21（命中范围）同一族「编译通过、不报错、功能安静失效」 |
| L2 | **Windows 分支保持 `Arguments = $"/c {cmd}"` 不动** | 两个平台走两条不同的写法，看着不齐整 | 图省事把 Windows 也一起换成 `ArgumentList`——`cmd.exe /c` 有它自己一套引号解析规则，跟 `ArgumentList` 用的 C 运行库转义规则**对不上**（单个元素会被加上引号，cmd 对带引号的 `/c` 参数又有另一套剥离规则）。Windows 这条路是真机验证过的，这次 bug 也不在它身上，没有理由顺手动它 |
| L3 | **回归测试断言的是「命令的副作用真的发生了、且双引号原封不动」**（`CommandQuotingTests`），不是「进程起来了」 | 测试要起真实子进程、轮询等副作用落地，比纯函数测试慢一点（~0.2s） | 断言「`Process.Start` 返回非 null」或「`Command.Execute` 没抛异常」——出 bug 的那一版**这两条全是过的**，拿它们当断言等于什么都没测。也不要拿真实的 `osascript`/关机命令去测：要么有破坏性，要么依赖 macOS 的自动化授权，测试环境里必然是红的 |
| L4 | **闹钟到点时重读 `rules.json`**（`Command.ExecuteFresh`），不用启动时那份快照；读失败退回快照并记 Error 日志 | 每次响铃多一次磁盘读（一年也读不了几次） | 以为这违反 §15.4 而不敢做——§15.4 的护栏是「**一个类型模型、一个解析器**」，当年的事故是两处各用不同 JSON 设置（一边不跳注释、一边区分大小写）导致文件一半好用一半安静失效；这里读的仍是 `GroupRules.Load`，同一个解析器，只是时机从"启动一次"变成"每次到点"。反过来也别做成"读失败就什么都不执行"：到点该关机却一声不响，比用一份稍旧的命令更糟，前提是日志里写清楚用了旧的 |
| L5 | **`executeCommand` 的浏览/换序/试跑收口在 `itami commands` 里**，App 界面上不加任何选择器（D6：能猜的就让人猜）；CLI 通过 csproj 的 `<Compile Include>` **link 同一份 `Command.cs`**，不是各写一份 | CLI 因此依赖 App 项目里的三个文件（Command/Log/AppData，都零 Avalonia 依赖） | 在 CLI 里另写一遍"怎么调 shell"——那样 CLI 测过了 App 照样能坏，L1 那个引号 bug 恰恰只在 App 那条路上犯，独立实现的 CLI 根本测不出来。**试跑的全部价值就在于它跑的是同一份代码**，为了"CLI 不该依赖 App"的洁癖把它拆成两份，等于把这个工具唯一的意义拆掉了 |
| L6 | **换序走文本搬运（`RulesText.MoveToFront`），不做 JSON 反序列化再序列化**；数组里出现注释就**拒绝改写**，让用户手工挪 | 多一个只会搬字符、不懂 JSON 语义的小模块 | 图省事用 `JsonSerializer` 往返——`rules.json` 是**用户手写**的文件（DESIGN §11），解析器明确允许注释，往返一次会把注释、缩进、键序全部抹掉：文件仍然合法、程序照常运行，用户下次打开才发现自己写的说明没了。宁可在拿不准的形状上拒绝改写，也不要"尽力而为"地猜 |
| L7 | **`itami commands` 默认操作 `AppData.RulesPath()`（程序真正在用的那一份），不是 `./rules.json`**，且每次都把完整路径打在第一行 | 跟其它子命令的默认值不一致（那几个仍是 `./rules.json`） | 沿用 `./rules.json` 默认值图个整齐——要改命令的人要改的必然是**生效的那一份**，而这个项目的 rules.json 有三级查找链（`AppData.RulesPath`），"我到底在改哪一份"本来就是个反复要查的问题。路径打出来是顺手把这个问题一并回答掉 |
| L8 | **所有以分钟为单位的功能收进 `MainWindow.OnMinute` 一段直线代码**，`_frame`（33ms）成为程序里唯一的钟；`TaskSession` 不再持有自己的定时器，由 `OnMinute` 在分钟边界调 `TickMinuteAsync` 一次 | `TaskSession` 从"自己会走"变成"被调用"，构造之后到第一个分钟边界之间它不动 | 以为「把闹钟从每秒改成每分钟」就解决了并行——**解决不了**。原来分钟级的事分散在两个各走各的定时器上：闹钟/清单挂在 33ms 的 `_frame`（边界后 ≤33ms 触发），AW 查询挂在 `TaskSession` 自己的 1 秒定时器上，而那个定时器**锚在任务开始那一刻**，所以在边界后 0~1000ms 的任意位置触发。两个钟之间没有任何协调，结果就是闹钟已经把关机命令发出去了、同一分钟里 AW 查询随后才到（用户 2026-08-08 报：两次重启后 AW 报警）。**根因是两个时钟没合并，不是某一行写了并行。** |
| L9 | ~~**`OnMinute` 里的顺序是定死的**：① 闹钟判断（Execute 开着就执行命令并**等它跑完**）② AW 查询+判定+三声通知 ③ Alarms 清单 ④ 闹钟响铃 ⑤ 提示条到期收起~~ **2026-08-08 当天被 L12 部分推翻**：命令执行从第 ① 步挪到了**最后一步**。仍然有效的部分：闹钟**判断**（`MarkFired`）还在第 ① 步；闹钟**响铃**仍排在 Alarms 清单之后 | — | 把「闹钟响铃」也提到第 ① 步——**「闹钟先判断」和「闹钟后响铃」是两件事**，不矛盾：响铃必须排在 Alarms 清单之后，因为 Windows 的 winmm 是单通道，谁后响谁截断前一个，闹钟响完什么都不留、清单响完还留着一分钟提示条（DESIGN §17） |
| L10 | **命令要 `await` 到子进程退出**（`Command.ExecuteAsync`），不再是 `Task.Run` 的 fire-and-forget | 关机/重启命令永远等不到结果——进程被系统杀掉，`await` 后面那几行根本不会执行。**这是正常情况，不是 bug** | 保留 fire-and-forget：那正是「一边把机器拆掉、一边还在敲 aw-server」的来源。另外读流和 `WaitForExit` 的顺序不能反——先 `WaitForExit` 再读流，管道缓冲区满时子进程会阻塞在写上，等退出就成了等一件永远不会发生的事 |
| L11 | **休息阶段从每秒改成每分钟**（原来 `RestFrom = done` + `Updated?.Invoke()` 每秒跑一遍） | 无 | 担心「休息结束会晚一分钟」——不会：`_focusDoneAt` 是整分钟、`RestMinutes` 是整数，`done + rest` 必然落在整分钟上，按分钟判断跟按秒判断**结果完全一样**。原来每秒那一遍是纯白做：休息期间 `RestFrom` 恒等于 `done`，每秒在重复赋同一个值，再触发一次重画表盘 + 重算任务栏图标（`Icon = RingIcon.Make(...)`） |
| L12 | ~~**命令执行挪到 `OnMinute` 的最后一步**，但闹钟**判断**仍留在第 ① 步（靠 `runCommand`/`ringAlarm` 两个标志跨步传递）~~ **同日被 L13 取代**：闹钟整体（判断 + 执行/响铃）一起挪到最后。仍然有效的部分：**命令必须是这一分钟的最后一件事**，理由见右栏 | — | **再翻回「命令优先」**：L10 记的理由（「关机命令永远等不到结果，进程被系统杀掉，`await` 后面几行根本不会执行」）**只在 macOS 成立**。Windows 的 `shutdown /s /t 0` 是提交关机请求就返回，`await` 立刻回来，后面的 AW 查询照跑，正好落在系统正在关机的时候——**那就是 2.0.7 本来要修的那个 bug，在 Windows 上等于没修**。命令放最后之后，这个修复不再依赖「进程会被杀掉」这个平台特性 |
| L13 | **`OnMinute` 的最终顺序**（用户 2026-08-09 提出）：**① 提示条到期收起 ② AW 查询+判定+三声通知 ③ Alarms 清单 ④ 闹钟（判断 + 执行命令/响铃，同一处）**。相比 L12 那一版，闹钟的判断和动作不再拆到两头，`runCommand`/`ringAlarm` 两个跨步传递的标志一并删掉 | 闹钟的判断时刻比 L12 那版晚了最多一次 AW 查询（10 秒）——但判断用的是传进来的同一个 `now`，**结论完全相同**，只是期间用户如果拨了滚轮，`_alarmQuietUntil`/`FireAt` 会取到更新的值，那正是调整期静默想要的行为（E6） | ① **把闹钟判断挪回第 ① 步**：那一版的失败模式是「第 ②~③ 步抛异常 → `MarkFired` 已经执行 → 闹钟被消费掉 → 预约的关机永远不发生」；闹钟整体在最后时，异常发生在 `MarkFired` **之前**，下一分钟 `ShouldFire` 仍为真会重试，**不加任何 `try` 就没有这个问题**。② **把提示条收起挪回最后**：现在是「先清旧的、再画新的」，反过来只是因为 `hideAt = now + 1 分钟` 恰好躲过判断才没出事，改成 59 秒就会把第 ③ 步刚画上的提示条当场擦掉。③ 给中间几步加内层 `try`——用户明确否掉过（见 L12 原文，「加了 log 反而让我忽略了」），而且 L13 之后也不再需要 |
| L14 | **分钟边界就是命令的截止线**（用户 2026-08-09）：下一分钟发现 `_minuteBusy` 还是 `true`，就 `Cancel()` 掉上一分钟的 `CancellationTokenSource`，然后**本分钟仍然跳过**（上一次调用要等控制权回到消息循环才真正退栈复位标志，接着往下走会跟它抢）。**取消的是我们的等待，绝不杀子进程**——它可能正是那条执行到一半的关机命令 | ① 不引入超时常量、不引入第二个计时器：这个程序只有一个钟（L8）。② **取消不一定能打断已经阻塞住的管道读**：.NET 的 `Process` 标准流不是重叠 IO，令牌只在两次读之间被检查——`WaitForExitAsync` 可取消是确定的，卡在 `ReadToEndAsync` 上则未必（推断，未实测）。所以最坏情况仍可能卡住，只是从"必然永久"降成了"小概率" | **不加取消、只靠 `_minuteBusy` 挡着**——那是这次修掉的 bug：`_minuteBusy` 的复位在 `finally` 里，而那个 `finally` 属于还卡在 `await` 上的那次调用，命令不退出它就永远复位不了，之后每分钟只打一行日志就 return，**AW 查询和判定再也不跑、表盘冻住，而界面按设计全程沉默**。也不要改成杀子进程 |
| L15 | **两个流仍然串行读，已知的死锁死角保留**（用户 2026-08-09：「不能两个流并发读」，理由是不想在每分钟的异步链上引入更多并发面） | 子进程把 stderr 写满一个管道缓冲（Windows 上通常 4KB）就会阻塞在写上，于是永远不关 stdout，我们卡在读 stdout 上等一个不会来的 EOF。兜底只有 L14 的分钟边界取消，而那一条恰恰在"读已经阻塞住"的场景下未必生效。现有清单里的命令（关机/锁屏/休眠）输出接近于零，实际撞上概率很低 | 顺手改成 `Task.WhenAll` 并发读「反正是标准写法」——用户明确否过。真要根治且能接受代价，另一条路是**干脆不重定向 stdout/stderr**（没有管道就没有这类死锁，`WaitForExitAsync` 的取消也就百分百可靠），代价是日志里丢掉 `stdout:`/`stderr:` 两行，而那跟 §8.1a「界面可以沉默，但原因不能消失」有冲突——**要改先跟用户确认这个代价** |
| L16 | **`itami commands` 收成三个互不重叠的模式**（用户 2026-08-09）：`--list` 只看（`*` 标出 #0）、`--select [N]` 只改文件不执行、`--execute` 只跑 #0 且不带下标。想试哪条就先 `--select` 成 #0 | 上一版的 `--test`（光标上下选 + y/N）整个删掉，包括那段 ANSI 光标重画代码 | 给 `--execute` 加回下标参数（`--execute N`）——**上一版最大的问题正是"测的那条"和"闹钟真会跑的那条"可以是两回事**，那是最容易骗过自己的地方；现在只剩一个 #0，这个歧义在设计上就不存在。也不要去掉 `--execute` 的 y/N 确认：清单里躺着 shut down / restart，而不带参数的 `--execute` 最容易从命令行历史里翻出来再敲一次（沿用用户 2026-08-08 对 `--test` 的同一条拍板） |
| L17 | **`executeCommand` 的退出码不可信，程序无法判断命令是否真的生效**（2026-08-08 实测）：Windows 上 `shutdown /h` 在休眠被禁用的机器上**退出码 0、stdout 空、stderr 空**，机器纹丝不动。`cmd /c shutdown /h` 单独跑一遍复现，三个信号完全一致 | 这一类失败**程序侧无解**：能观察到的信号全都在说成功。日志里那三行（`running executeCommand` / `exited with 0`）已经把拿得到的都记了 | **别把这当成日志的 bug 去"修"**——不是漏记，是根本没有可记的东西。也别去加"执行后验证是否真的休眠/关机了"那类检查：那要为每条命令写一份平台相关的验证逻辑，而 `executeCommand` 的设计前提正是"用户写什么就跑什么，程序不理解命令的语义"（E9）。用户侧的排查手段是 `powercfg /a`（看休眠到底可不可用）和 `powercfg /h on`（启用，需管理员） |
| L18 | **`itami commands` 的未知参数一律报错退出**（2026-08-09），认识的只有 `--list`/`--select`/`--execute`/`--rules` | 多一个白名单要跟着维护 | 沿用"不认识就当没看见"——`--test` 删掉之后正是这个行为：它会**静默掉进 `--select` 的交互分支**，用户以为在试跑，实际在改 rules.json。删掉一个开关就该让它报错，不是让它变成另一条命令 |
