// 命令行原子层的入口。目前只是个占位，让解决方案能构建。
//
// 计划中的形态（接口尚未设计，见 DESIGN.md §7 重放算法、§8 模块划分）：
//   itami --since <ISO8601> --until <ISO8601>
// 输出每分钟一个色块，绿 = 计入、红 = 偷懒、灰 = 离开、空心 = AW 无数据。

Console.WriteLine("itami —— 命令行原子层，尚未实现。");
Console.WriteLine("设计见 DESIGN.md §7 / §8。");
return 0;
