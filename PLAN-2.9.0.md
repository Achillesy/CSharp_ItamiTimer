# 规划 · 只剩两件事：日志归因 + 可选主题

> **不做长远规划**（用户 2026-08-27 明确）。这份文件只追踪当前两件已拍板的事，
> 完成后把该沉淀的内容搬进 `DESIGN.md` / `DECISIONS.md`，本文件删除。
>
> 原本更大的"跑偏时主题联动"设想（阶段三、四）**已搁置**，等这两件做完再谈。

## 已完成

### ✅ 2.8.0 · 日志归因

`TaskSession` 每分钟 tick 时，用同一次已经在手的窗口事件算出"这一分钟主要撞在
哪个不命中的窗口上"，写进日志。新增 [`OffTaskAttribution.Attribute`](src/ItamiTimer.Core/OffTaskAttribution.cs)
（Core，纯函数，8 个单元测试）。

**顺带修了一个从没触发过的 bug**：这条诊断日志一直用错索引（`cells[^1]`），生产
环境里一次都没打印过。详见 `DESIGN.md` §16.5.3、`DECISIONS.md` L31。

## 进行中

### 2.9.0 · 可选主题（用户可切换 Light/Dark，两态）

**位置**：主窗口新按钮，跟齿轮（`SettingsBtn`）同一行，右边距对齐图钉（`PinBtn`）
的 -4，凑成右上角 2×2：

```
[喇叭]  [图钉]
[齿轮]  [主题]   ← 新按钮
```

**两态，不是三态**：点一下在 Light / Dark 之间切换，不保留"跟随系统"。代价几乎
为零——现有的 `ApplyTheme()` 只在构造函数里调用过一次（`MainWindow.axaml.cs:122`），
启动后系统主题再变也不会跟着变，所以今天并不存在一个真正在运作的"跟随系统"功能
可丢。

**已有基础**（2026-08-27 摸底，直接复用）：

| 东西 | 状态 |
|---|---|
| `DialPalette.Dark` | 完整（"Night face: a deep grey face plus a dark metal bezel"） |
| `DialControl.Palette` / `DominoRow.Palette` | `StyledProperty` + `AffectsRender`，赋值即重绘 |
| `ApplyTheme()` | 已存在，需要改成可重复调用、由用户设置驱动而非系统主题 |
| `--dial-specimens` | headless 出图，暗色验收不用开真实窗口就能存档 |

**范围**（分类处理，不是"15 处颜色全改"）：

- **语义色保持不变**：Start 绿 `#2FA36B`、Give Up 红 `#D6453F`——跟表盘"绿=专注、
  红=偷懒"是同一套语义，暗色下改了反而破坏一致性
- **中性色必须跟着翻**：Start 以下那张卡片 `#E6D7DBE0`、禁用态灰
- **必须修**：提示条两行文字 `#1B222A`（深色），压在暗色骨牌上会看不清——这是
  Alarms 清单（用药提醒）的显示面，不修等于让它安静失效
- **Settings 窗口自己**：`#D7DBE0` 背景 + `#E8EBEE` 卡片
- **Give Up 确认框**（`Confirm.cs`）

**验收**：

1. 点按钮能在 Light/Dark 之间来回切换，立即生效
2. **人眼看真实窗口**确认两个模式下每个元素都看得清（不能只看代码/出图）——
   暗色调色板目前只被 `--dial-specimens` 用过，从没在真实窗口里出现过
3. Alarms 提示条在暗色下清晰可读
4. Settings 窗口、Give Up 确认框在暗色下正常
5. `--dial-specimens` 出一版暗色图存档

## 搁置（等上面两件做完再谈）

跑偏时主题自动翻转、5 秒轮询检测焦点漂移。之前那版规划的摸底结论（主题模型该是
推导值、翻转必须与用户操作解耦、频闪风险等）没有作废，只是先不推进——真谈的时候
从这次对话记录里捞就是了，不单独维护一份等着过时的文档。
