# 一袋米要扛几楼（ItamiTimer）—— 架构设计 v3

> 本文是当前**权威设计文档**。历史版本：`wild-enchanting-planet.md`（v1）已废弃。v1 → v2 → v3 的变更对照见 §12。
>
> v3 相对 v2 是**核心模型的根本变更**：计时不再由进程内的倒计时累加，而是由「已提交的任务记录 + AW 事件历史」**推导**得出。这一条改变了几乎所有其它章节。

## 0. 当前待定决策（2026-07-26 记录，动手前先解决）

按优先级。**前两条不解决就不要写核心代码。**

### 0.1 ✅ `Absent` 怎么判 —— 已定稿（2026-07-27）：完全照搬 AW 的判断

用户 2026-07-27 决定：**AW 说 `afk` 就是 `Absent`，不加任何解释层。** 不管当时是在看学习视频还是在读书，一律照 AW 的裁决。

- **不做**「按不间断 AFK 段的长度判」，**不做** 每组一个 `afkToleranceSeconds`。`rules.json` 保持 §5.2 的样子，不加新字段。
- 用户明确知道自己接受了什么：读 PDF 超过 afk 阈值不动键鼠，那几分钟就是灰的、不计入、任务被拖长。这个取舍在 §6.1.1a 提过两次，用户两次都选了简单方案。**不要再翻案。**
- **容忍阈值没有消失，它搬到了底座上**：想放宽就去改 AW 自己的 `aw-watcher-afk.toml`（默认 180 秒）。旋钮在 ActivityWatch 那边，不在本程序里——这正是原则 0 的字面落实。

**§4 的 `Absent` 判定和 §7 的重放算法现在可以实现了。** 判据就一句：区间被 `status == "afk"` 的事件覆盖 → `Absent`。

副产品（见 §7.2）：缓存水位线因此是**固定**的 `now − 180s`，不需要退到某段未闭合 AFK 的起点。

### 0.2 UI 框架：转 Avalonia（已验证，倾向确定）

用户 2026-07-26 明确对 Avalonia 有兴趣，并要求先确认框架还活着。**已核实，状态健康：**

| 证据 | |
|---|---|
| **Avalonia 12** | **2026 年发布**，把 **.NET 10 作为核心要求**——正好是本项目的 target |
| 发布节奏 | VS 扩展 12.1（2026-05-06）、12.0（2026-04-10）、11.12（2026-01-28 加入 .NET 10 模板） |
| 规模 | 31.2k stars / 2.8k forks / 27,917 commits |
| 商业支撑 | 公司赞助（Devolutions）+ 商业产品线 Avalonia XPF（按 app/平台授权）——**有营收，不是爱好项目** |
| 采用方 | Schneider Electric、Unity、**JetBrains**、**GitHub** |
| v12 技术 | 新的 composition-based 渲染管线成为默认——对模拟表盘的平滑动画有利 |

**VS2026 + .NET 10 的兼容坑已经没了**（早期讨论里"模板不能选 .NET 10、需手改 csproj"是 .NET 10 RC 阶段的情况，11.12 起已修）。

转栈的实际影响：

- `TargetFramework` 从 `net10.0-windows` 改成 `net10.0`（去掉 `-windows`）才能跨平台；只做 Windows 也可以留着。
- Avalonia **自己用 Skia 画每个像素**，不用系统原生控件 → 界面在 Windows/macOS 上长得一样。§3.1 那句"macOS 版只需重写第 9 层（置顶不抢焦点）"会变得几乎字面成立。
- WinForms 的知识不迁移，WPF 的知识迁移得很好。**WPF 的纯 XAML 表盘项目是最可直接复用的参考**（Shape/Transform/Path 模型两边一致）。
- 代价：可视化设计器不如 WinForms 成熟，主要手写 XAML（对"画一个好看的表盘"来说无所谓，好看的 UI 基本都是手写的）；生态比 WPF 小（本项目只要一个表盘 + 几个勾选框，够用）。

**不再需要找"开源番茄钟基底"。** 原因：风格定下来之后，用 Avalonia 的 XAML 画一个表盘是几十行的事，而所有现成番茄钟的计时引擎都必须扔掉（清一色是「进程内 `remaining` 秒数递减」，正是 §2 和原则 4 禁止的），能拿的只有视觉层。所以走"看样板选风格 → 自己画"这条路，比"挑基底 → 拆引擎"更省。

视觉参考留一条最有价值的线索：**Flutter Clock Challenge**（Google 2020 办的钟面设计比赛，850+ 参赛、前 30 全开源，**评审第一条标准就是 visual beauty**）。虽然是 Dart，但获奖作品的做法是"用 Canvas 把每个像素画出来"，跟 Avalonia 的 Skia 绘图同一路子。视觉设计不像代码那样会过时。

### 0.4 表盘与色环规格 —— ✅ 已定稿（2026-07-27）

用户 2026-07-27 早上答复，6 项全部落定：

| # | 选择 | **定稿** |
|---|---|---|
| 1 | 表盘底 | **跟随主题**：白天素白（A）、夜里深灰（B）。见下方修正说明 |
| 2 | 色环位置 | **内圈色带**（选项 B） |
| 3 | 配色刻度 | **经琥珀色三段过渡**（选项 B） |
| 4 | 离开与无数据 | 采纳原方案：`Absent` 灰色实心 / `Gap` 空心虚线 |
| 5 | 超圈行为 | **内缩一圈（螺旋）**（选项 A） |
| 6 | 秒针 | **要**，且是**亚秒间隔连续重绘**的扫秒针（sweep），目的是制造紧张感 |

**正式渲染规格见 §8.2**，常驻形态（任务开始后收进任务栏、仅偏离时跳出）见 §8.3，休息阶段见 §8.4。

#### ⚠️ 第 1 项的修正：那个选择从来没有真正呈现过

用户 2026-07-27 反馈"看不出素白和深灰的区别"。查了样板页，**是样板页的 bug**：那个 HTML 里画盘面的那一行把填充写死成 `var(--dial-face)`，完全没有分支到 `o.face`——

```js
svg.appendChild(el("circle", { cx: C, cy: C, r: rFace, fill: "var(--dial-face)" }));
```

`o.face` 实际只影响三处：`minimal` 时去掉外圈与高光、`minimal` 时只画五分刻度、`plain|dark` 时画数字。所以「素白盘面」和「深灰盘面」两张卡生成的 SVG **逐字节相同**，看到的差别只来自当时的系统主题。

**但这个 bug 顺带把 §0.5 问题 1 也回答了**：用户一直在看的其实就是"盘面跟随主题"，并且觉得可以。于是 A 和 B 不是互斥项，而是同一套东西的日面与夜面。**定为盘面跟随主题**，`--dial-*` 四个 token 各有浅深两份（样板页里已经调好）。原先"盘面恒为深灰"的推荐作废。

下面保留原始的待定记录作为背景。

### 0.4-旧 原始待定记录（已由上表取代）

用户 2026-07-26 定的视觉方向：**Win7 边栏那种简约挂钟**。计时可视化的概念：

> 提交任务时在盘面上预画一段扇形（比如 25 分钟 = 蓝色），之后每分钟查一次 AW，
> 这分钟有效就是绿色，做了其它事就按占比往绿色里掺红。

**这个概念有个很好的副产品**：按 v3 模型偷懒不停表、而是让任务变长，所以那段蓝色扇形的**终点会随偷懒往前滑**——绿色只攒到 25 分钟，红斑把整段弧撑到 30、40 分钟。用户会亲眼看着截止线离自己越来越远。比数字账单直观得多，是"痛"的可视化载体。

**⚠️ 样板页已作废，不要拿它当实现依据**（2026-07-27）：它有个 bug（画盘面那行没分支到 `o.face`，「素白」和「深灰」渲染结果逐字节相同），色带位置也跟 §8.2.1 定稿的 [0.50, 0.68] 不符，更没有休息态的淡出和真的在扫的秒针。正式规格以 §8.2 为准；视觉迭代直接在 Avalonia 里做，那才是最终产物。

**样板页**（2026-07-28 已删除，见下方说明）曾发布为 Artifact：
**https://claude.ai/code/artifact/8b803438-9eba-41c0-a296-5c98848abbe6**
（顶部表盘会 60× 速度自动跑一遍 30 分钟的一轮，可重播、可切深浅色。）

**待定的 5 个选择**（用户 2026-07-26 晚说"明天再定"）：

| # | 选择 | 选项 | 我的倾向 |
|---|---|---|---|
| 1 | 表盘底 | 素白 / 深灰 / 极简无数字 | 素白最接近 Win7 边栏 |
| 2 | 色环位置 | 饼形扇区（用户字面描述）/ **内圈色带** / 外沿细环 | **内圈色带**——饼形会压住指针，表盘就没法当钟读了 |
| 3 | 配色刻度 | RGB 直接插值 / **经琥珀色三段过渡** | **三段过渡**——RGB 直插在 50% 处会出现发脏的橄榄绿，看着像画错了 |
| 4 | 离开与无数据的表现 | 见下 | 灰色 + 空心虚线 |
| 5 | 超圈行为 | **内缩一圈（螺旋）** / 原地覆盖 / 换纯进度条 | 内缩一圈——还能看出"绕了一圈多"，本身就是羞耻感 |

外加一条：**秒针要不要**（挂钟的灵魂，但每秒重绘，而 v3 的判定是 3 秒一轮——纯装饰，可以要，只是别让人误以为它代表判定频率）。

#### 0.4.1 已确认要补的：一分钟有四种结局，不是两种

用户原描述只有"有效→绿"和"做了其它事→掺红"，但 §4 里不计入的有**三种**，账单里本来就分开报，盘面也必须分开——**把"起身离开"画成红色等于冤枉自己**：

| 状态 | 计入 | 盘面表现 |
|---|---|---|
| `OnTask` / `Neutral` | ✔ | 纯绿 |
| `OffTask` | ✘ | 按该分钟占比掺红（深浅能区分"瞄了一下"和"彻底跑了"） |
| `Absent` | ✘ | **灰色，不是红色** |
| `Gap` | ✘ | **空心 / 虚线描边，不填色** |

另：红绿是最常见的色盲混淆对（约 8% 男性），补救不必换配色——让明度跟着变，或给重度偷懒的格子加细斜纹，颜色之外留第二个信号。方案 3 的琥珀过渡本身已带明度变化。

#### 0.4.2 每分钟的颜色也必须是重放算出来的

"每分钟查一次 AW"要落成**每分钟的纯度 = 重放结果的一个投影**，不是一个逐分钟累加的数组。判据：关掉界面再打开，**每一格的颜色都要能原样重建**（§2）。

另外注意两个频率是**不同的关注点**，不要混成一个：

- **色环粒度** = 1 分钟
- **置顶提醒的响应** = 约 3 秒（§10 的轮询间隔）

一个轮询循环、两个消费者。只按分钟查会让提醒最多迟 60 秒，那就失去意义了。

### 0.3 AWJ 的定位收窄为「参考」

用户决定（2026-07-26）：**不把 AWJ 的 Python 原子层搬进本项目，也不做成跨进程调用**，而是**参考它的设计**（原子分解、踩过的坑、算法思路），**需要的部分用 C# 重新实现**。

这跟 §2.1 的结论一致（兄弟而非上下层），但更进一步：连 §2.1 里那个"可选地调 `awj stats --json` 做统计面板"的方案也暂时不做了。`ActivityWatchJournal\ATOMS.md` 从"待实现的协议对象"变成**纯参考资料**——读它是为了不重犯它踩过的坑、不漏掉它已经识别出的原子，不是为了调用它。

推论：§8 模块表里的第 7 层 `AwjStats` 暂时移出范围；顶部统计面板如果要做，也是 C# 自己按 `ATOMS.md` 的原子清单实现。

### 0.5 ✅ 由 0.4 衍生的 5 个问题 —— 已全部定稿（2026-07-27）

| # | 问题 | **定稿** |
|---|---|---|
| 1 | 盘面跟不跟主题变？ | **跟随主题**（素白 = 日面，深灰 = 夜面）。原推荐"恒为深灰"作废，理由见 §0.4 的修正说明 |
| 2 | "进入任务栏"是任务栏按钮还是系统托盘？ | ~~任务栏按钮~~ **整条作废**：窗口从头到尾放在原地，不收起（2026-07-27），提醒也不再靠窗口（§8.3） |
| 3 | 人回到正轨后，跳出的窗口自动缩回吗？ | ~~只撤销置顶~~ **整条作废**：不再有置顶，跑偏也不再有任何主动提醒（§8.3） |
| 4 | 专注达成 / 休息结束要不要也跳出来？ | **各响一声系统音，可在设置里关**（2026-07-28 改，§8.3.1）。原答案"弹出但不置顶"跟"绝不抢焦点"自相矛盾，实机上等于什么都没发生 |
| 5 | AW 连不上要不要跳出来？ | **不跳**。任务栏图标不再动态重画（§8.3.4），界面上的信号是分割线以下整块变灰 |

## 1. 核心原则

四条，后面所有设计都是从这里推出来的：

0. **AW 是底座，不是零件。** ActivityWatch 是一个多平台、已经做好了的工具，负责如实记录行为。ItamiTimer 的全部意义是**借这个已有的、客观的记录来约束自己**，而不是自己去实现行为监测。所以它是硬依赖：AW 访问不了就直接说无法工作，不猜测、不降级、不自己找替代信号。（**这条约束的是"约束模式"本身**——不许拿别的信号冒充 AW 的记录。至于"没装 AW 时整个退化成纯番茄钟"，那是另一回事，见 §11.1。）
1. **任务一旦提交就已确定。** 点下「开始」的那一刻，任务的开始时间就固定了，这项任务就是要执行的。程序**永远不会代替用户提交一个新任务**——休息结束不自动开始下一轮（那相当于强制给用户加了个任务）。
2. **计时完全基于 AW。** 程序自己不累加时间。所有"过了多久、有没有偷懒、完成了没有"都是拿任务开始时间去查 AW 的事件历史算出来的。
3. **窗口只是外在表现，但进程就是任务本身。**（2026-07-27 修订，见 §2）窗口最小化、被遮挡、收进任务栏，都不影响任何东西。但**退出程序 = 放弃当前任务**——任务只活在内存里，不落盘。
4. **状态是推导出来的，不是攒出来的。** 进程内不持有"剩余多少秒"这种可变累加值。任何时刻的状态 = 纯函数(任务记录, AW 事件历史, 当前时间)。

姊妹项目 `ActivityWatchJournal` 是同一个底座的另一个读者（回溯记账），跟本项目是**兄弟关系而非上下层**——边界、复用什么、协议怎么定，见 §2.1。

## 2. 任务不落盘：退出程序就是放弃任务（2026-07-27 重大变更）

> **⚠️ 这一节整个重写过。** v3 原本的招牌性质是「关掉界面、重启电脑都不影响结果」，任务记录会落盘、重启后重放接上，文中称其为"本次需求最本质的收获"。**用户 2026-07-27 决定砍掉它**：「这个项目本身是要求短时间的专注，简化需求。」
>
> 变更前我明确提示过代价（程序区分不了"用户点了关闭"和"进程被杀"，所以崩溃/关机也会丢任务），用户知情后仍选择最简方案。**不要改回去。**

### 现在的模型

**任务只活在内存里。** 没有 `current-task.json`，没有 `history.jsonl`，没有 `TaskStore`。程序从头到尾**一个字节都不写盘**（`rules.json` 只读）。

| 动作 | 结果 |
|---|---|
| 窗口最小化 / 被遮挡 / 收进任务栏 | 无影响，任务照常 |
| **退出程序**（点 X、Ctrl+C） | **放弃当前任务**，退出前弹窗确认并给出账单 |
| 崩溃 / 关机 / 进程被杀 | 同上，任务没了。**这是明知的代价** |
| 下次启动 | 干净状态，等用户开新任务 |

**为什么能接受**：任务最长 50 分钟（§8.4.2a），崩溃最多丢这一轮。而换来的是整个持久化层消失——`TaskStore`、启动时的恢复逻辑（原 §9）、僵尸任务的 12 小时兜底，全部不需要。

### 原则 4 完好，而且仍然是这个设计的根

不落盘**不等于**回到 v2 那套"进程内 remaining 秒数递减"。状态依然是推导出来的：

- **历史数据仍然在 AW 里，不在本程序里。** 每一轮都拿 `startedAt` 向 AW 查 `[开始时间, 现在]` 整段重放，不是攒一个计数器。
- **轮询间隔不影响正确性**，只影响提醒的及时性。哪怕 60 秒查一次，算出的专注时长依然精确到 AW 自己的事件粒度。
- **进行中的临时性 AW 不可达不损坏任何东西**——没有"未落盘的累加值"可丢，恢复之后重查那段区间就补齐了（§6.2）。
- **重放是纯函数**，喂合成事件就能穷举测试，不用真等 25 分钟（§7）。

丢掉的只有**跨进程的持久性**这一条。

## 2.1 与 ActivityWatchJournal 的关系：兄弟，不是上下层

### 设计思想

AW 是一个**多平台、已经做好了的工具**，它负责如实记录行为。它是共享的**底座**。`ActivityWatchJournal`（以下 AWJ）和 ItamiTimer 都是这个底座的**读者**：

```
       ┌──────────────┐        ┌──────────────┐
       │ ItamiThimer  │        │     AWJ      │
       │  当下·约束    │        │  回溯·记账    │
       │   (C#/WinForms)│      │   (Python)   │
       └───────┬──────┘        └──────┬───────┘
               │  ← 共享 classify_rules.yaml（规则，不是代码）
               │     可选：按需取一份统计快照 ────┐
               ↓                                ↓
       ┌──────────────────────────────────────────┐
       │      ActivityWatch  本地 REST API         │
       │      （多平台、已做好、如实记录）             │
       └──────────────────────────────────────────┘
```

**不做成 `ItamiTimer → AWJ → AW` 这种串联**，理由是：串联之后链条上最脆的一环（Python 虚拟环境）会决定整个程序最重要的功能（约束）能不能用。AW 本身是硬依赖没办法，但不该再加第二个。两个项目做兄弟、共享**规则文件**、按需交换**算好的统计结果**，才符合"用一个已经做好的工具来约束自己"这个初衷。

### 为什么「完全调用 AWJ 的原子功能」这个形态撑不住

读过 AWJ 的代码之后，三个具体原因：

**1. AWJ 是为"已经结束的整天"设计的，ItamiTimer 需要"还没结束的任意区间"。**

AWJ 每个 CLI 命令都是 `--date <YYYY-MM-DD>`，而且有一个显式的 `ensure_day_finished()` 守卫会直接抛 `DayNotFinishedError`（"本工具目前只整理已经结束的日子，明天再来试试"）。`day_bounds_utc` 算的是日界。而 ItamiTimer 要的是 `[任务开始时刻, 现在]`——一个任意起点、**still open** 的区间。

这不是加个参数的事：`dayproc` 整套 session 构建逻辑都建立在"闭合的一天"之上（按 AFK 空档切段、按 `min_duration` 过滤噪音）。"调用原子功能"预设了这些原子是**区间形状**的，而它们实际是**天形状**的。

**2. 真正可复用的原子很小，而且最关键的那个用不了。**

| AWJ 模块 | 规模 | 对 ItamiTimer |
|---|---|---|
| `aw_api.py` | 90 行 urllib 包装 | C# 写等价物也是这个量级。为它跨一个进程边界不值得 |
| `classifier.py` | 79 行纯函数 | **粒度不对**。它算的是 5 大类，而 §5.1 已论证白名单需要更紧的粒度。约束逻辑用不了，**统计面板正好用** |
| `_not_afk_intervals` + `_overlap_seconds` + `clip_to_not_afk` | 约 60 行 | 真正的算法价值所在。但 §7 需要的是它的**变体**（按区间分类、边界集合不同），不是原样调用 |

合计"省下不用重写"的量级约 150 行直白代码，代价是一个进程边界 + 一个 Python 虚拟环境依赖。不划算。

**3. ItamiTimer 真正要自己拥有的东西，恰恰不是界面。**

v3 的核心是**任务记录 + 针对它的重放核算**：固定的开始时刻、锁定的时长、勾选集合的并集语义、`Absent` 优先级、`Gap` 处理、以及 §5.4 那条"一切改动追溯生效"的规则。AWJ 里**没有任何**与此对应的概念——它总结日子，它不裁决一个已提交的承诺。这部分逻辑就是 ItamiTimer 的本体，也正是微妙的正确性所在，没有可委托的对象。

所以"这个项目主要是界面"这个判断低估了它。界面确实不是重点（原则 3 也说了界面只是外在表现），但剩下的那部分不是"调用别人"，是这个项目自己的核心。

### 按延迟和用途切分，这才是这个思路真正成立的地方

| | 归属 | 延迟要求 | 为什么 |
|---|---|---|---|
| 实时 on-task 判定、置顶提醒 | **ItamiTimer（C# 直连 AW）** | 60 秒（§8.3.5） | 每轮起一个 Python 进程不合理；而且这是核心功能，不能被 Python 环境问题拖down |
| 任务记录 + 重放核算 | **ItamiTimer（C#）** | 按需 | AWJ 没有这个概念，无从委托 |
| **工作/学习/生活/娱乐/其它 的比例、当天活动列表** | **AWJ（Python，按需调用）** | 秒级、按需刷新即可 | 天然是"天"形状的、容忍延迟、而且**用的就是那份一直在维护的 `classify_rules.yaml`**，单一事实来源。这正是你想放在界面顶部的东西，契合得很好 |

**关键纪律：AWJ 那侧不可用时必须优雅降级。** 统计面板显示"统计暂不可用"，计时器照常工作。这样硬依赖数量保持为 1（只有 AW），Python 环境出问题不会把专注计时器搞瘫。

### 交互协议

推荐 **给 AWJ 加一个面向机器读的子命令**，比如：

```
awj stats --since <ISO8601> --until <ISO8601> --json
```

输出一份稳定的 JSON 契约（各分类秒数、活动列表、未识别应用等）。ItamiTimer 按需 `Process.Start` 调一次，读 stdout 解析。

- AWJ 唯一依赖是 pyyaml，没有 pandas/aw-client 之类重量级包，进程启动开销约 150~300ms——对"按需刷新的统计面板"完全够用，**不需要**常驻服务或本地 HTTP 端口那套机具。
- **需要在 AWJ 侧改的关键一点**：现有命令被 `ensure_day_finished()` 挡着，没法查"今天"。而统计面板要看的恰恰是进行中的今天。所以这个新子命令必须显式支持**未结束的、部分的**区间——这是唯一一处真的需要动 AWJ 的地方。
- 这属于**对 AWJ 项目的改动**，有它自己的设计取舍（要不要复用 `dayproc` 的 session 构建、`min_duration` 过滤在部分天上怎么处理），应该在那个项目里单独讨论定稿，不在本文范围内。
- 备选（零代码耦合）：直接读 AWJ 已经写出来的产物（`diary/<device>/<date>.ics` 或 Obsidian 日记的 md 块）。好处是完全不碰 Python，坏处是只覆盖已结束的天、且依赖用户先跑过 `awj day`。可以作为 fallback，不作为主路径。

### 已经在共享的东西：规则文件

`classify_rules.yaml` 里攒了两百多条真实 app 名（含 macOS 变体和踩过的坑），ItamiTimer 的 `rules.json` 应该拿它当**字典**抄（§5.1、§5.4）。这是"共享规则、不共享代码"的具体体现——两个项目对"什么应用叫什么"的认知保持一致，但各自的匹配语义和粒度独立演进，不会一边改动把另一边搞坏。

AWJ 里那些**AW 行为特性的硬知识**同样值得搬过来（不是搬代码，是搬结论）：心跳续期会让 duration 自己长大（§6.1.1）、系统代理会吞 localhost 请求（§6.1.2）。这两条都是别人已经用真实数据换来的教训。

## 3. 任务的生命周期

```
        用户勾选活动组、设定时长、点「开始」
                    │
                    ↓
        ┌───────────────────────┐
        │  写盘：任务记录已提交    │  startedAt 固定，focus/rest 时长锁定
        │  status = Committed    │
        └───────────┬───────────┘
                    │
        （以下全部是"查 AW 算出来的"，不是"跑出来的"）
                    │
                    ↓
    累计专注时长 = 重放 [startedAt, now] 的 AW 历史，
                  把判定为 on-task 的区间时长加起来
                    │
        ┌───────────┴────────────┐
        │                        │
   累计 < 专注时长            累计 ≥ 专注时长
   【专注阶段】                     │
        │                        ↓
   当前这一刻在干什么？      focusCompletedAt = 累计跨过阈值的那一瞬间
   ├ on-task  → Focusing     （从历史里插值算出精确时刻）
   ├ off-task → Slacking          │
   │            置顶提醒 ⚡        ↓
   ├ absent   → Away          restEndsAt = focusCompletedAt + 休息时长
   └ AW 挂了  → Unknown            │      ← 纯算术，休息阶段不需要访问 AW
                             ┌─────┴─────┐
                             │           │
                        now < restEndsAt │ now ≥ restEndsAt
                        【Resting】       │
                        随便干什么        ↓
                                  ┌──────────────┐
                                  │ status =     │
                                  │  Completed   │  ← 到此终结，
                                  └──────────────┘     绝不自动开下一轮
```

**一个任务 = 一段专注 + 一段休息，然后结束。** 想再来一轮，用户自己再点一次「开始」——那是一个新任务，有新的开始时间。这是原则 1 的直接落地。

用户也可以中途放弃：写 `status = Abandoned` + `abandonedAt`。

### 3.1 休息阶段几乎不存在

`restEndsAt = focusCompletedAt + 休息时长`，是纯算术。休息期间什么都不受约束，所以**休息阶段完全不需要访问 AW**。

推论：**整个依赖 AW 的逻辑只涉及专注阶段。** 这是个不小的简化。

2026-07-27 进一步确认：休息区间**连事后的账单也不进**（用户明确否决了"免费记一行"的提议）。所以这里说的"不访问 AW"是彻底的，不只是"不实时轮询"。完整规格见 §8.4。

## 4. 判定模型

沿用 v2 的两个正交的门，但语义从"倒计时是否推进"改成"这段区间是否计入累计专注时长"：

> **区间计入累计专注时长 ⟺ on-task（活跃应用命中当前已勾选的组）且 present（AW 的 afk 数据说在座）**

| 区间分类 | 判定条件 | 计入累计？ | 实时提醒？ |
|---|---|---|---|
| `OnTask` | 命中当前已勾选的小目标（§5.4 并集） | ✔ | — |
| `Neutral` | 命中 ignore 名单，或是 ItamiTimer 自己 | ✔ | — |
| `OffTask` | 其它（含规则文件里没有的应用） | ✘ | **置顶提醒** |
| `Absent` | afk 数据说 `afk` | ✘ | 不提醒 |
| `Gap` | 这段区间 AW 根本没有数据 | ✘ | 不提醒 |

几点定义要说清楚：

- **`Neutral` 计入累计**。文件选择对话框、`explorer.exe`、瞄一眼 ItamiTimer 自己——这些是干活的附带动作，为它们停表既琐碎又制造噪音。
- **`Absent` 优先级高于一切**。一个区间如果同时是"中性应用"和"afk"（典型场景：锁屏，`LockApp.exe` 在 ignore 名单里而 afk 也变成 `afk`），必须判成 `Absent` 不计入。否则会出现"锁屏一小时专注时长照涨"的漏洞。
- **`OffTask` 不计入但也不惩罚**。偷懒的时间单纯不算专注时长，任务因此被拖长——痛感来自时间惩罚，不来自作废重来。
- **`Gap` 既不计入也不惩罚**，见 §6.3。

## 5. 活动组规则文件（JSON）

### 5.1 为什么不复用 `classify_rules.yaml`

姊妹项目 `ActivityWatchJournal/config/classify_rules.yaml` 的用途是把**每一个**窗口事件归入 5 个大类（工作/学习/生活/娱乐/其它）做时长统计，粒度太粗：勾一个「工作」等于放开 `explorer.exe`、`Taskmgr.exe`、`cmd.exe`、`EXCEL.EXE`……四十来个应用，"限制在少数几个应用之间切换"的约束就没了。

ItamiTimer 要的是**紧的白名单组**：`读书学习` 只是几个阅读器，不是整个「学习」类。所以独立文件、独立语义。也顺便避开两个项目共用一份配置的耦合——一边改规则把另一边搞坏是要刻意避免的。

但那个文件里攒了两百多条真实的 app 名（含 macOS 变体和踩过的坑，比如 `Minecraft` 同时匹配 `Minecraft.exe` 和 `Minecraft.Windows.exe`），可以当**字典**照抄；它的 `其它` 整段（锁屏、系统弹窗、安装包、`dwm.exe`……）几乎能整体搬进本文件的 `ignore`。

### 5.2 格式

`rules.json`，放在 exe 同目录（用户手改）。

**这个文件是逐渐生长的**（2026-07-27 用户明确）：一个组 = 一个**小目标**，随着目标变化不断新增，旧目标用 `disabled` 屏蔽而不是删掉。大计划（"学习 ETF"）不进这个文件——那是用户日记的事，本项目只管把小目标落实到"别再切来切去"。

```json
{
  // 每组一个易记的名字 = 一个小目标。
  // rules 是条件列表：组内任一条命中即算命中（【或】）。
  // 单条规则里 app 和 title 都写，则两个都要命中（【与】）。
  "groups": {
    "学习经济学": {
      "rules": [
        // 不管用什么应用，标题里有这些字就算在学经济学
        { "title": "(?i)经济学|曼昆|Mankiw" },
        // 或者：打开这个专门的应用，本身就说明在学
        { "app": "EconReader\\.exe" }
      ]
    },
    "写代码": {
      "rules": [
        { "app": "devenv\\.exe|Code\\.exe|^Code$" },
        { "app": "WindowsTerminal\\.exe|^Terminal$" }
      ]
    },
    "网页学习": {
      "rules": [
        // app 和 title 都写 = 必须同时命中，用来卡浏览器标签页
        { "app": "chrome\\.exe|msedge\\.exe|^Safari$", "title": "(?i)ComfyUI|教程|tutorial" }
      ]
    },
    "上一季度的目标": {
      "disabled": true,          // 屏蔽：不出现在勾选列表里，也永不命中
      "rules": [{ "title": "(?i)Blender" }]
    }
  },

  // 中性：既不算专注、也不算违规，计入累计专注时长
  "ignore": [
    "LockApp\\.exe|^loginwindow$",
    "SearchHost\\.exe|StartMenuExperienceHost\\.exe",
    "PickerHost\\.exe|OpenWith\\.exe",
    "explorer\\.exe|^Finder$",
    "dwm\\.exe|^Dock$",
    "^unknown$"
  ]
}
```

用 `System.Text.Json`（内置，不引入外部依赖），解析时开 `ReadCommentHandling = Skip` 和 `AllowTrailingCommas = true`，好让用户能在文件里写注释。

代价（相对 YAML）：正则里的反斜杠要双写，`SumatraPDF\.exe` 得写成 `"SumatraPDF\\.exe"`。文件顶部写一行注释提醒这件事。

#### 匹配语义（2026-07-27 修订：组内是「或」）

正则一律用 `search`（不要求全串匹配），大小写不敏感建议在正则里自己写 `(?i)`。

```
单条规则命中 ⟺ （无 app  或 app 正则命中 data.app）
             且（无 title 或 title 正则命中 data.title）

组命中       ⟺ 该组未 disabled，且【任一】条规则命中
```

于是自然长出三种形状，这三种缺一不可：

| 规则形状 | 含义 | 例子 |
|---|---|---|
| 只写 `app` | 用这个工具就算，标题无所谓 | 写代码：`devenv.exe` |
| **只写 `title`** | **做这件事就算，用什么工具都行** | 学习经济学：标题含「经济学」，可能是 PDF、可能是视频 |
| 两个都写 | 用这些工具做这件事 | 网页学习：浏览器 **且** 标题命中 |

**为什么必须是「组内或」而不是「apps 与 titles」**：用户的真实需求是「标题里有经济学 **或者** 打开了专门的经济学应用」。如果一个组只能表达 `apps AND titles`，这两个条件就没法并存——要么被迫枚举所有可能打开经济学内容的应用（漏一个就被判偷懒），要么专门的经济学应用必须改标题才能命中。

**空规则必须是加载期错误。** 一条 `app` 和 `title` 都不写的规则匹配一切，勾上那个组等于把约束整个关掉。fail-closed 要求**拒绝加载并报错**，不能静默放行。同理，`rules` 为空数组的组也报错。

这个形状跟 AWJ 的 `aw_atoms.py:classify()` 是同一个模型（`{app?, title?, tag}`，缺省即"匹配一切"），那边的注释里专门说明了这么设计是为了**让一条 title 规则能跨所有应用生效，而不是被困在某个 app 的规则树里**。

刻意不做 `classify_rules.yaml` 那种递归 `children` 下降——这里只需要回答"是否属于某组"这个布尔问题，不需要 5 路分类。附带好处：v1 设计里"浏览器标签页没法区分"那条已知局限被 title 条件直接解决了。

#### 5.2.1 `disabled`：旧目标屏蔽，不删

小目标会过期。删掉一个组会让 `rules.json` 失去历史，也容易手滑删错；加 `"disabled": true` 则是一行的事。

- 字段缺省 = 启用。新增组不用写这一行。
- `disabled` 的组**不出现在勾选列表里**，判定时**永不命中**。
- 屏蔽一个仍被当前任务勾着的组，那些时间会重新算成 `OffTask`。这是 §5.4「一切改动追溯生效」的自然结果，不是特例。

#### 5.2.2 改 `rules.json` 立即且追溯生效

任务进行中改规则文件，改动对**整段历史**生效——下一次统计（每分钟一次）重新读文件、重新打标全部事件。

这不是漏洞，是 §5.4 那条统一规则的一部分：**规则和小目标的任何改动，一律对整段历史生效。** 想在任务中途补一条"这个应用也算"，改完等一分钟就见效，不用放弃重来。

代价如实记下：`rules.json` 是 §7 重放的一个**隐藏可变输入**，所以重放严格说不是"只由任务记录和 AW 历史决定"的纯函数。测试时把规则当作显式参数传入即可（原子层本来就这么设计，见 §14.5）。

### 5.3 判定顺序（刻意避开一个坑）

```
1. 是 ItamiTimer 自己？               → Neutral（硬编码，不走配置）
2. 命中任一【当前已勾选】的组？         → OnTask（§5.4：并集，追溯生效）
3. 命中 ignore 名单？                  → Neutral
4. 其它（含规则文件里没有的应用）        → OffTask（fail-closed）
```

三点注意：

- **第 2 步只对已勾选的组求值**，而不是"先解析出它属于哪个组、再看那组勾没勾"。后者有个隐蔽 bug：某应用同时命中 A 组（未勾）和 B 组（已勾），按文件顺序先匹配到 A 就被误判成违规。按"是否命中任意已勾选组"来判，组之间的先后顺序完全不影响结果，配置里也就不需要定义优先级语义。
- **第 1 步必须硬编码，不能交给配置**。提醒窗口置顶后用户去点它、或者用户自己来看进度，ItamiTimer 就成了活跃窗口，AW 会如实上报 `ItamiTimer.exe`；要是它被判成 `OffTask`，就会"提醒 → 用户看提醒 → 又违规 → 再提醒"死循环。配置写漏一行就炸掉整个程序，所以这条不进配置文件。
- **第 4 步 fail-closed**（未知应用算违规）是刻意的：宁可误报让用户去补一条规则，也不要漏报把约束悄悄放空。误报代价很低——不作废、不惩罚，只是那段时间不计入累计。

### 5.4 中途加小目标：用当前勾选集合的并集，追溯生效

> **⚠️ 2026-07-27 重大变更。** 此前的设计是带时间戳的 `groupTimeline`，每个区间用「该区间当时生效」的勾选集合判定。**用户明确否决了它**，改为下面这条。变更理由和被放弃的论证见 §5.4.1，不要在没有跟用户重新确认的情况下改回去。

规则一条：

> **重放时，整段历史一律用【当前勾选的全部小目标的规则并集】打标。**

没有 `groupTimeline`，没有生效时刻，没有"当时"。任务记录里只存一个当前集合：

```json
"groups": ["学习经济学", "学习Blender"]
```

用户的原始场景：

> 本来定了 50 分钟学经济学，比较顺利，内容提前看完了。这时在界面里补勾一个「学习Blender」，
> 下一分钟统计时送进原子层的就是两个小目标规则的并集，剩下的时间不再算偷懒。

**这里有个容易被误读的点要说清**：这不是"放宽约束"，而是这个产品对「我提前干完了」的正确回应。**承诺的是时长，不是目标完成度**——经济学的内容看完了，但账还没结清（累计可能才 35 分钟）。任务不会因为书读完了就结束，它只认那 50 分钟。所以剩下的 15 分钟只有三条路：硬啃经济学、加一个小目标让别的学习也算数、或者放弃任务。加小目标存在的意义就是第二条。

**取消勾选同样追溯生效**（那段时间会重新变成 `OffTask`）。规则的对称性是刻意的：**一切改动，一律对整段历史生效**，没有例外——这也覆盖了改 `rules.json`（§5.2.2）和 `disabled` 一个组（§5.2.1）。整个系统只有这一条规则，不需要记住任何时间线语义。

**审计记录仍然保留**：每次增删小目标追加一条 `{at, groups}` 到 `groupChanges`，**只用于最终报告**（§7.1 那行"中途添加了「学习Blender」"），**绝不参与判定**。它是显示用的，删掉不影响任何数字。

#### 5.4.1 这么改放弃了什么（如实记录，别当成待修 bug）

带时间戳的方案挡住了一条路：

> 10:15 经济学看烦了，打开 Blender 瞎折腾 20 分钟。10:35 心想「Blender 也是学习啊」，勾上它。
> 用并集重放 → 那 20 分钟的红格子**当场变绿**，截止线往回缩。

用户 2026-07-27 的答复：**"不算作弊，这对我很正常，学习一样厌倦了，切换到学习另一样。"**

这个立场是自洽的：**约束的对象是「有没有待在学习范围里」，不是「有没有一直待在同一个小目标上」。** 学腻了换另一样学，本来就不是这个程序要惩罚的东西。

**约束因此变成**：在你**最终声明**的学习范围之内待满 N 分钟。仍然被惩罚的是那些你从头到尾都不会声明的东西——小说、无关视频、微信。这比原方案弱，但不是没有约束。

被明确接受的代价：红格子可以靠事后补勾变绿。**这是用户知情后选择的，不要"顺手改进"回时间线方案。**

（附带好处：`GroupsAt` 原子没了，`Boundaries` 少收集一类边界，任务记录少一个字段。约十几行代码。）

### 5.5 什么可变、什么不可变

| | 提交后可变？ |
|---|---|
| 开始时间 | ✘ 锁定（原则 1） |
| 专注时长 | ✘ 锁定。中途改时长等于移动球门，任务就不"确定"了 |
| 休息时长 | ✘ 锁定。由专注时长 ÷ 5 推导，提交时算出（§8.4.2） |
| 勾选的小目标 | ✔ 可变，**改动追溯作用于整段历史**（§5.4），另留一份纯审计的变更记录 |

## 6. AW 接口约定

### 6.1 两个 bucket，区间查询

| 用途 | bucket type | 关心的字段 | 必需 |
|---|---|---|---|
| 活跃窗口 | `currentwindow`（形如 `aw-watcher-window_<hostname>`） | `data.app`、`data.title`、`timestamp`、`duration` | ✔ |
| 是否在座 | `afkstatus`（形如 `aw-watcher-afk_<hostname>`） | `data.status`（`not-afk`/`afk`）、`timestamp`、`duration` | ✔ 见 §6.1.1 |

bucket 发现：`GET /api/0/buckets/` 找一次并缓存 id，失效时重新发现。

取事件用**区间查询**而不是 v2 的 `?limit=1`：

```
GET /api/0/buckets/{id}/events?start=<ISO8601>&end=<ISO8601>
```

**两个都是必需的**，`afkstatus` 尤其不能当成"可选的锦上添花"——原因见下。

### 6.1.1 为什么 afk bucket 必需：窗口事件的 duration 会靠心跳自己长大

AW 的窗口监听器**即使用户不在电脑前，也会持续心跳续期"当前聚焦窗口"这条事件**，只是那条事件的 `duration` 变得异常长，事件流里**不会留出任何空白间隔**。

这不是理论推测，是姊妹项目 `ActivityWatchJournal` 已经踩过并确认的坑（见该项目 `DESIGN.md` §13 与 `dayproc.clip_to_not_afk` 的注释：2026-07-14 确认，一个挂在后台的 SSH 终端因此在日记里显示了 6 个多小时）。

对 ItamiTimer 的直接后果很严重：**如果不看 afk 数据，"走开"这个动作是完全隐形的。** 用户把 SumatraPDF 停在前台，起身走 25 分钟，窗口事件会一路长到 25 分钟，程序会算出一次完美达成、零违规的专注任务。整个约束被"走开"这个动作彻底绕过——而且这是最省力的绕过方式。

所以：

- `currentwindow` **必需**——没有它无法判断在不在做正事。
- `afkstatus` **同样必需**——没有它无法判断人在不在，而 §4 里 `Absent` 优先级最高那条规则正是靠它落地的。缺任何一个都按 §6.2 处理：直接提示无法工作。

（v2 曾把 afk 定为"可选，缺失时降级为永远视为在座"。**那是个错误**，因为它把最容易的作弊路径变成了默认行为。降级方案在这里不存在。）

### 6.1.1a 反方向的真实证据（已知代价，2026-07-27 决定接受）

上面论证了"必须看 afk"。**但把 afk 当成非黑即白的开关是错的**，反方向有同样确凿的实测证据。

AWJ 项目 `AGENTS.md` 记载（2026-07-14 实测）：

> AW 分不清"人不在电脑前"和"人在认真看，只是没动键鼠"。这导致真实发生过的学习（教程视频、参考资料页面）经常在这两条流水线的最终结果里被连根丢掉（ETF 投委会视频、Blender 教程都有这种情况）。

AWJ 里 `awj goal-time` 这条完整的第三流水线，**存在的唯一理由就是绕开 afk 裁剪**。

对 ItamiTimer 来说这是个更尴尬的矛盾，因为**"专心读 PDF"正是本项目的原始动机**：

| 做法 | 后果 |
|---|---|
| 不用 afk 裁剪 | 停在目标应用上起身走开 → 无法察觉（§6.1.1 那个洞） |
| 用 afk 裁剪（按"这一刻 afk 与否"判） | **认真读 PDF 十分钟不动键鼠 → 被判 `Absent` 不计入专注时长** |

两个都是实测确认的，不是理论权衡。

曾倾向的解法是按「不间断 AFK 段的长度」判 + 每组一个 `afkToleranceSeconds`（认真读会被翻页打断成一串短段，真走开是一条长段）。

**这个方案已被否决（2026-07-27，见 §0.1）。** 用户选择完全照搬 AW 的判断：AW 说 `afk` 就是 `Absent`，不管当时在读什么。上面这一整段证据保留下来，是为了让接续的会话知道**这个代价是被明知而选中的，不是被漏掉的**——不要拿它当成一个待修的 bug 去"改进"。

真要放宽，正确的位置是 AW 自己的 `aw-watcher-afk.toml`（默认 180 秒），不是在本程序里加解释层。

### 6.1.2 别让系统代理吞掉 localhost 请求

`aw-server` 总是本机（或同一局域网），请求必须**绕过系统代理**。系统级的 SOCKS/HTTP 代理会把 localhost/LAN 流量一起吞掉，表现成莫名其妙的连不上。这是 AWJ 项目里已经处理过的坑（`aw_api._opener()` 显式传了空的 `ProxyHandler`）。

C# 侧对应的做法：构造 `HttpClient` 时用 `new HttpClientHandler { UseProxy = false }`，不要用默认的 `HttpClient()`。

### 6.2 AW 访问不了：直接说无法工作

> 注意区分：本节说的是**已经提交了约束模式的任务**、中途 AW 不可达。「用户根本没装 AW，于是整个程序退化成纯番茄钟」是另一件事，见 §11.1 —— 那条路的关键纪律恰恰是**不许在任务中途自动切过去**，否则杀掉 aw-server 就成了作弊路径。

- **提交任务时** AW 不可达 → 拒绝提交，明确报错"连不上 ActivityWatch，请确认它在运行"。不允许开始一个从一开始就没法核算的任务。
- **任务进行中** AW 不可达 → 界面明确显示无法工作（`Unknown` 状态），不显示任何编造的进度数字，不提醒。

绝不能把"读不到数据"当成"没检测到切换"——那样约束会在用户不知情的时候整个失效。这跟 `ActivityWatchJournal` 项目"AW 不可达要如实报告，不能默默跳过"是同一条原则。

**但要注意这个新模型带来的一个好性质**：进行中的临时性 AW 不可达**不影响最终结果的正确性**。因为事件历史存在 aw-server 里，不在本程序里——网络/进程恢复之后，重新查那段区间就能把历史补齐。所以"无法工作"的准确含义是「**此刻**没法告诉你状态、没法提醒你」，而不是「任务作废」或「这段时间的账丢了」。真正会留下窟窿的只有 aw-server 自己宕掉（watcher 写不进去），那种情况见 §6.3。

### 6.3 覆盖率检查与「窟窿」

只查 HTTP 是否成功是不够的，必须检查**区间覆盖率**：把查回来的事件时长加总，跟 `[startedAt, now]` 的墙上时间比。差额就是没有数据的**窟窿**（`Gap`）。

窟窿的来源：aw-server 或 watcher 宕掉的那段时间、AW 服务重装、系统休眠的边界等。

处理：**既不计入累计专注时长，也不算违规**——不奖励也不惩罚，如实报告"有 N 分钟 AW 没有数据，不计入"。这是唯一诚实的处理方式，也跟 fail-closed 原则一致（不确定的时间不算你专注过）。

顺带说明：v2 设计里那条"数据过期检查"（防止 `?limit=1` 拿到几小时前的旧事件被当成当前状态）在 v3 里**自动消失了**——因为改用区间查询，旧事件带着自己的 `timestamp`+`duration` 落在历史里的正确位置，末尾没有数据就自然表现为 `Gap`，不会被误当成"现在"。模型换对了，一整类 bug 不用专门防。

## 7. 重放算法

这是 v3 的核心，也是实现上最不显然的一段。两个 bucket 是**各自独立的时间线，边界不对齐**，不能只遍历一个列表——需要做区间求交。

```
输入：任务记录（startedAt, focusDuration, groups）、两个 bucket 的事件列表、now
输出：TaskState

1. 把两个事件列表都裁剪到 [startedAt, now]
2. 收集所有边界时刻，排序去重：
     - startedAt、now
     - 每个 window 事件的起点和终点
     - 每个 afk 事件的起点和终点
     （§5.4 改成并集之后，勾选集合不再产生边界）
3. 对每一对相邻边界构成的区间 [t_i, t_{i+1})：
     - 找覆盖它的 window 事件 → app/title；找不到 → Gap
     - 找覆盖它的 afk 事件   → status == "afk" 则 Absent；找不到 → Gap
                              （afk 是必需的，缺数据就是没数据，不能当成"在座"——§6.1.1）
     - 用【当前】勾选集合的并集（§5.4，全段统一，不随时刻变）
     - 按 §4 + §5.3 分类 → OnTask | Neutral | OffTask | Absent | Gap
4. 累计专注时长 = Σ (OnTask ∪ Neutral) 区间的时长
5. 若累计 ≥ focusDuration：
     沿时间顺序找到累计跨过阈值的那个区间，在区间内**插值**出精确时刻
     → focusCompletedAt；该时刻之后的区间不再参与专注核算（属于休息阶段）
6. 违规记录 = OffTask 区间的极大连续段（用于报告次数、总时长、都切去了哪些应用）
7. 窟窿 = Gap 区间的合计（§6.3）
8. 当前这一刻的分类 = 最后一个区间的分类 → 决定要不要置顶提醒
```

第 5 步的插值让完成时刻能精确算出来，`restEndsAt` 因此也是精确的，且重启后可重算。

**这个算法是纯函数**：喂进去一份任务记录 + 两个合成的事件列表 + 一个 `now`，断言输出。不需要 HTTP、不需要真的等 25 分钟，能穷举测试所有边界情况。这是 v3 模型在可测性上的最大收益。

### 7.1 最终报告

**⚠️ 2026-07-27 变更：界面【任何时候】都不给账单。** 用户原话：「简化，任何时候都不提供账单，就是钟盘面上的弧形，自己看，自己猜。」

这一条推翻了本节初版的结尾（"这份诚实的账单就是痛的载体"）。**痛感的载体换成了表盘本身**：

- 每一格的颜色（绿 / 琥珀 / 红）就是那一分钟的成绩单，一分钟一格，实时长出来；
- 灰色圆弧的终点（§14.1a 的预计结束时刻）随着偷懒一格一格往前滑，**看着它离自己远去**比读一行"偷懒 11 分钟"更直接。

数字既然全都能从盘面上看出来，再列一遍就是重复，而且列出来会把注意力从"看"拉回"读"。所以不列。**猜，是设计的一部分**——跟 §8.4.2a 把滑块上的数字也去掉是同一个决定。

代价说清楚：用户拿不到"chrome 偷了 8 分钟"这种归因明细。他知道并选了这一边。历史统计本来就是 AWJ 的活（§2.1）。

**报告本身仍然是免费的**（重放的副产品，不需要额外记账），所以命令行的 `itami` 仍然打印它——CLI 没有表盘可看，账单是它唯一的输出：

```
任务：读书学习
承诺专注 25 分钟，实际耗时 41 分钟

  偷懒 5 次，共 11 分钟
      chrome.exe        8 分钟
      Weixin.exe        3 分钟
  离开              3 分钟
  AW 无数据          2 分钟（不计入）
```

`TaskState.OffTaskSecondsByApp` 因此保留，只有 CLI 在用。

### 7.2 ❌ 不做缓存，不做水位线（2026-07-27 决定）

曾设计过一套增量缓存：水位线 `now − 180s` 以前的色块冻结、之后每轮重算，`Gap` 格子永不冻结，查询窗口要比目标区间放宽。

**用户 2026-07-27 决定砍掉：牺牲效率来简化代码。** 每一轮就老老实实重查 `[startedAt, now]` 整段、重放一遍。

理由站得住：

- 任务最长 50 分钟（§10），一次全量查询的事件量是**分钟级**的，不是天级的。AWJ 查一整天也就 1600~1900 条窗口事件，50 分钟这个量级的开销可以忽略。
- 缓存的**唯一**收益是省几次本机 HTTP 往返，而代价是引入一个「必须永远和重算结果一致」的不变量——那正是最容易悄悄写错、且错了以后症状极其隐蔽的地方（色块看着对，账却不对）。
- 无缓存反而让原则 4 落得更彻底：**没有任何进程内状态可以偏离事实。**

**保留下来的两条硬知识**（跟缓存无关，做全量查询时同样要遵守）：

1. **查询窗口必须比目标区间宽。** `aw_atoms.py` 的 **T1**（2026-07-26 实测）：AW 的 REST API 只按事件**自己的开始时间**过滤，不按区间重叠——一个开始于区间之前、延伸进区间的事件会**静默消失**。当天精确查询返回 1662 条，两边各放宽 6 小时返回 1927 条。所以查 `[startedAt, now]` 时要往前多要一截再自己裁。
2. **最近约 180 秒的 afk 判定是暂定的。** `aw_atoms.py` 的 **T3**：AFK watcher 要连续 180 秒零输入才翻成 `afk`，所以"此刻"的 afk 结论字面意义上还不存在，事后会翻。**不做缓存以后这条不再是正确性问题**（每轮全量重算，翻了自然跟着改），但它解释了一个正常现象：最后几个色块可能在下一轮变色。**不要**为此加"稳定化"逻辑去掩盖它。

## 8. 模块划分

| # | 模块 | 职责 | 依赖 |
|---|---|---|---|
| 1 | `AwClient` | bucket 发现、区间查询两个 bucket、返回结构化事件或失败原因 | HTTP |
| 2 | `GroupRules` | 读 `rules.json`、编译正则、暴露 `(app, title, 已勾选组) → OnTask/Neutral/OffTask`（§5.3） | 纯逻辑 |
| 3 | `TaskRecord` | 已提交任务的**内存**模型：`startedAt`、时长、`groups`、`groupChanges`、`status`。**不落盘**（§2） | 无 |
| 4 | `TaskEvaluator` | §7 的重放算法。**纯函数**，无 I/O、无时钟 | 2、3 |
| ~~5~~ | ~~`TaskStore`~~ | **2026-07-27 删除**。任务不落盘，历史统计是 AWJ 的活（§2.1），本项目不自己攒一份 | — |
| 6 | `Settings` | 轮询间隔、上次用的时长和勾选（作为下次的默认值） | 文件 |
| 7 | `AwjStats` | 可选：调 `awj stats --json` 取当天分类统计给界面顶部用（§2.1）。**失败必须优雅降级**，不影响 1~6 层 | 子进程 |
| 8 | `PainForm` | 把 `TaskState` 渲染出来；提交/放弃任务；勾选列表由 2 提供组名填充；顶部显示 7 的统计 | WinForms |
| 9 | ~~`TopmostNotifier`~~ | **2026-07-28 已过时**：平台特定代码现在是 5 个文件（放音、滴答、键鼠空闲、置顶、数据目录），不再只有置顶一处。清单见 §15.2。"第 1~6 层平台无关"那条纪律**完好无损** —— macOS 移植时 Core / Cli / 80 个测试一行没改就全过了 | 平台调用 |

四条边界纪律：

- **第 1~6 层不含任何 UI 代码、不含任何 Win32 调用、不依赖第 7 层。** 这是"检测层完全平台无关"这个从 v1 延续下来的核心洞察：所有判定输入都来自 AW 的 REST API，不碰原生窗口 API。macOS 版只需要重写第 9 层。
- **第 7 层是纯增益，不是依赖。** 约束和核算（1~6）绝不能经由 AWJ——那会让 Python 环境成为核心功能的单点故障（§2.1）。`AwjStats` 挂了就是统计面板显示"暂不可用"，计时器照常工作。
- **不要引入 `Process.GetProcesses()` / `EnumWindows`。** 界面上那个勾选列表是**预定义的活动组**，不是实时进程列表——这样列表项和监测判定用的是同一份规则、同一个 `app` 字段来源，不存在"选择器报 `chrome`、监测端收到 `chrome.exe`、永远匹配不上"这类错位；跨平台也由构造保证（同一组里同时写 Windows 和 macOS 的 app 名）。
- **计时器的「状态」和「渲染」分开。** `TaskEvaluator` 只吐"累计 1394 秒、当前 Slacking"，怎么画是第 8 层的事。这样 v1 的数字倒计时换成模拟表盘，改动关在一个控件里。

### 8.0 项目布局：把边界纪律交给编译器（2026-07-27 落地）

上面那四条边界纪律里，第一条（1~6 层不含 UI、不含 Win32）靠人自觉是守不住的。所以拆成三个 csproj，让**编译器**去执行：

```
ItamiTimer.slnx
src/
  ItamiTimer.Core/     net10.0        类库   模块 1~6，无 UI 无 Win32
  ItamiTimer.Cli/      net10.0        itami.exe        命令行原子层
  ItamiTimer.App/      net10.0        ItamiTimer      模块 8~9（界面，Avalonia）
```

- **`Core` 是 `net10.0`，没有 `-windows` 后缀**。这不是随手写的：一旦有人往 Core 里塞 WinForms / Avalonia / P/Invoke，**直接编不过**。绝不要给它加 `UseWindowsForms`、`UseWPF` 或任何 UI 包。
- **`App` 的 `AssemblyName` 钉死成 `ItamiTimer`**。AW 上报的 `data.app` 就是 exe 名，而 §5.3 第 1 步靠它硬编码自身豁免。改了这里 → 程序把自己判成 `OffTask` → 「提醒 → 用户看提醒 → 又违规 → 再提醒」死循环。csproj 里已写注释说明。
- ~~**`App` 目前还是 WinForms 空脚手架**~~ → **早已作废**：Avalonia 版在 2026-07-27 落地，表盘、骨牌、设置窗口都在里面。
- **`App` 的 TFM 2026-07-28 从 `net10.0-windows` 改成 `net10.0`**（去掉后缀）。那个后缀对本项目从来没起过作用（`DllImport` 在 `net10.0` 上照样编得过，见上一条 Core 的注释），换来的只有平台分析器上下文；去掉之后 CA1416 **反而开始真的报**没守住的平台调用。理由和收益见 §15.1，**别加回去**。
- `Cli` 的产物叫 `itami.exe`，是原子层的可执行入口，同时充当 Core 的测试夹具和 App 渲染层的数据源。

测试项目 `tests/ItamiTimer.Core.Tests/`（xUnit）已就位。§7 的重放算法是纯函数，喂合成事件就能穷举边界，不用真等 25 分钟——这是这个模型最大的可测性收益。

### 8.1 完全不写盘（2026-07-27 变更）

原来是"只在提交任务、改勾选、任务终结三个时刻写盘"。**现在一个字节都不写**——任务只活在内存里（§2）。`rules.json` 只读，程序对磁盘是只读的。

顺带没了：崩溃时"半写状态"的可能、文件锁、并发两个实例互相覆盖、以及旧任务文件格式的兼容负担。

### 8.1a 唯一的例外：诊断日志（2026-07-27）

界面对用户是**沉默的**（§8.6：分割线以下一个提示字都没有，配置坏了也让用户猜）。
但**沉默不等于把原因丢掉**——出错的理由必须留下来，否则程序坏了就是个黑箱：
用户只看到按钮灰着，而谁也说不出为什么。

所以有一份日志：`%LOCALAPPDATA%\ItamiTimer\itami.log`，超过 1MB 滚一次、只留一份旧的。

**这跟 §8.1「完全不写盘」不冲突**，因为那一条禁的是**任务状态**落盘（不要
`current-task.json`、不要累加值、退出即放弃），目的是让状态永远由 AW 历史推导。
日志是另一类东西：**只往后追加、从不被读回来参与任何判定，删掉它不改变程序的任何行为**。

必须记的四处（都是原本会把异常吞掉的地方）：

| 时机 | 记什么 |
|---|---|
| 启动 | 一行，附日志路径本身 |
| AW 探活 | 成功；或**失败的完整异常**（界面上只是置灰，一个字都不说） |
| 读 `rules.json` | 成功 + 小目标清单；或**是哪个组的哪条正则坏了**（`GroupRules` 抛的异常里带着组名和那条正则） |
| 提交任务 | 小目标、时长、起算时刻 |

另外接住了 `UnhandledException` 和 `UnobservedTaskException`——界面既然沉默，
**崩溃的原因更要留下来**，否则程序凭空消失，谁也说不出发生了什么。

**日志本身绝不能把程序搞挂**：写不进去就算了，那会把一个小毛病升级成崩溃。

## 8.2 表盘渲染规格（v1 正式规格，2026-07-27 定稿）

全部长度归一化到 `rFace = 1.0`（盘面内圆半径）。角度：12 点为 0°，顺时针，分钟 × 6°。

### 8.2.0 空盘本身就得是一个好看的桌面钟（2026-07-27 用户提出）

用户原话：

> **即使不连接 AW、不统计任务时长，这个程序界面就是一个很好看的钟，就会让人把它开在桌面上。**

**这是需求，不是感想。** 它改变优先级——表盘的好看不是"以后再打磨的门面"，而是产品的一部分：§8.4.5 已经定了「色环褪尽之后剩下的就是钟本身」，如果那个钟不好看，"空盘即邀请"的闭环就没有说服力；而且没人会把一个丑东西留在桌面上，留不住就没人提醒你开始下一轮。

第一版画出来是"一个能用的时钟控件"，不是"愿意开在桌面上的钟"。用户拿一张实物木质挂钟的照片指出，**差的不是几何而是材质和光**：

| 缺的 | 怎么补 |
|---|---|
| 带渐变的木质边框 | 边框圆用 `LinearGradientBrush`（左上受光 → 右下背光），外缘再加一道亮边模拟圆润的转角——少了这道边，整圈会读成"一块扁的褐色圆盘"而不是"一圈有厚度的木头" |
| 钟落在墙上的阴影 | 下方一个压扁的椭圆，`RadialGradientBrush` 由黑到透明 |
| 边框投在盘面上的影 | 盘面内缘一圈内阴影，越靠外越深 |
| 指针投在盘面上的影 | 指针几何偏移几像素、填半透明黑，画在指针之前。三根一起推一次 `PushTransform` |

**光源统一假定在左上。** 四层影子的方向都得跟着它，否则会互相打架。

同时修掉的几处（都是"看着像控件"的来源）：指针改成带锥度而不是等宽直线、刻度收细、暗角减弱、盘面提白。

### 8.2.1 分层与半径

| 层 | 半径 | 说明 |
|---|---|---|
| 外圈 bezel | 1.02 | 描边 `--dial-edge`，3.5px @ 210px |
| 盘面 | 1.00 | 填 `--dial-face` |
| **色环 lane 0** | **[0.50, 0.68]** | 内圈色带，环宽 0.18 |
| **数字 1~12** | 基线圆 **0.70** | 无衬线粗体。**2026-07-27 改**：原为「只画 12/3/6/9、基线圆 0.755」，那是照抄已作废的样板页；用户给的实物参考是十二个齐全的，四个数字配一圈密刻度显得空，而且 0.755 会跟刻度挤在一起。色带在 [0.50, 0.68]，数字在 0.70，两边不打架 |
| 分钟刻度 | 0.94–0.965 | 细，`--dial-tick` |
| 五分刻度 | 0.90–0.965 | 粗，`--dial-ink` |
| 时针 / 分针 / 秒针 | 0.50 / 0.72 / 0.80 | 画在色环之上 |
| 中心毂 | 0.045 | |

**这里对样板页做了一处修正**：样板的色带是 [0.60, 0.86]，而数字在 0.755 —— 数字正压在色带上。浅色样板看着还行，换成深灰盘面加饱和色块之后数字会糊掉。所以色带整体内移到 [0.50, 0.68]，数字（0.755）与刻度（≥0.90）都在色带之外。代价是时针尖（0.50）正好贴住色带内缘，分针与秒针仍会横穿色带 —— 这是"内圈色带"方案固有的，指针是细线且画在上层，可接受。

### 8.2.2 色环的映射：分针就是写入头

第 i 个色块占据钟面分钟刻度 `[m₀+i, m₀+i+1)`，`m₀` = `startedAt` 的分钟数（跨小时自然绕回）。

于是**分针指到哪一格，哪一格就是此刻正在被填的那一分钟**。表盘同时是钟、是进度条、是账本，三者共用一套坐标，不需要额外的进度指示器。这是整个设计最值钱的性质。

### 8.2.3 每一分钟格子的四种结局（§0.4.1）

| 结局 | 画法 |
|---|---|
| `OnTask` / `Neutral`（纯度 1.0） | `--focus` 实心 |
| 混入 `OffTask`（纯度 p<1） | 三段过渡取色 `ramp(1-p)`：focus → amber → slack |
| `Absent` | ~~`--absent` 实心~~ → **什么都不画**（2026-07-28，§8.2.3a） |
| `Gap` | 不填色，仅 1px 虚线描边（`dasharray 2 2`，左右各留 0.4° 缝） |

- 格与格之间用 0.6px 的 `--dial-face` 描边分隔，保证 60 格边界始终可数。
- ~~**色盲第二信号**：纯度 < 0.34 的格子在色带中线画一根径向短刻线，重度偷懒可以**数**出来。~~ **整条作废**（2026-07-28，§8.2.3a）：改成用**格子的高度**编码纯度。

### 8.2.3a 木桶与短板：纯度编码进**高度**（2026-07-28 晚定稿）

用户的原话，一句话把设计立住了：

> 你把这圈弧线想象成一个木桶，红色就是短了的木桶边……反正这一分钟没有专注，木桶的短板就出现了。不管什么样的色盲，他都能看懂。

**这一圈是个木桶，每一分钟是一块板。** 专注满了就是满高的一块；走神了，板子短一截 —— **短板自己会露出来**。

```
高度 = rIn + (rOut − rIn) × (0.5 + 0.5 × 纯度)
```

颜色照旧（绿 → 琥珀 → 红）。**两个通道编同一个量**：一个给正常视觉，一个给所有人。

**满高的灰色承诺弧正好成了"这一分钟满格"的参照线** —— 绿板齐了它就是满的，短了一眼看得见。这是"承诺弧与色块共用同一条 lane"这个几何约定第三次白送东西（前两次见 §8.4.4、§14.1a）。

**下限取 1/2**，用户看过 0.18 那一版后定的：「不要红色短板矮到看不到。」一格径向只有约 25px，0.18 时最短那块只剩 4~5px，快贴到内圈上了。取半高之后纯度 0 的那一分钟仍是一块显眼的红板，而满格与最短之间还有一倍落差，参差照样一眼可辨。

**绝不能取 0**：「人不在」现在什么都不画，零高度会让"全程走神"跟它撞在一起 —— 而那正好是最不该混淆的一对：**一个不怪你，一个全怪你。**

#### 它取代了什么，以及为什么值得记

原做法是往颜色上贴补丁：纯度 < 0.34 的格子在中线加一根径向刻线，好让人**数**出重度偷懒。用户看到真实界面的反馈是「红色的格子会是一个空心的小块」。量出来他是对的：

```
表盘控件高 330 → rFace ≈ 140，一格（6°）的弧宽只有 8.7px
  − 刻线 0.008 × rFace          1.1px
  − 左右边框 0.005 × rFace × 2   1.4px
= 剩下的红 6.1px，还被白线从正中劈成两条各 3.1px
```

**注解把被注解的东西吃掉了。** 第一版的修法是把刻线改成外缘上的一个缺口 —— 治了症状，没治病：那仍然是在颜色之外**另加一个记号**。

用户给的方向在另一层：**别再加记号，把量本身编码进长度。** 长度不需要辨色、不需要数、也不占用颜色的地盘。

> **一个更好的设计通常不是把旧方案改好，而是让旧方案的整个问题消失。**

#### 顺带修掉的：「人不在」什么都不画

量刻线的时候发现「离开」那格也带同一根线 —— 因为 `Purity = 0` 也 `< 0.34`。这直接绕开了 §0.4.1 立的规矩（「把起身离开画成红色等于冤枉自己」）：你数"偷懒了几分钟"时，会把上厕所那几分钟一起数进去。**规矩立在文档里，实现悄悄绕过去了。**

用户的裁决比"离开格不画刻线"更彻底：

> 离开我觉得应该什么都不画，就是预计完成的圆弧加长而已啊。

**离开不是你的错，惩罚只该体现在截止弧变长这一处，盘面上不必再记一笔。** 留下的空白不会跟"还没走到"混淆 —— 写入头之前的每一分钟都已经发生过，中间的空白只可能是离开；而"AW 没数据"另有虚线框，分得开。

`DialPalette.Absent` 因此没有任何地方在用了，token 留着（四种结局之一的语义位置），注释里写明。

样张 `09-barrel-purity-from-full-to-zero`（连续渐变，验下限和色带）和 `10-barrel-mostly-full-with-a-few-short-staves`（偶有短板，验读法成不成立）就是为这一节加的。

### 8.2.4 承诺弧与截止线

- 承诺弧：从写入头起，长度 = `max(0, 承诺专注分钟 − 已获得纯度之和)`，填 `--pending`，22% 不透明。
- **截止线必须显式画**：在承诺弧终点画一条径向细实线（`--pending` 满不透明，`rIn−0.02` → `rOut+0.02`）。只靠蓝弧的边缘不够 —— 偷懒时这条线往前滑，正是 §0.4 说的那个"看着截止线离自己越来越远"，它得是一条**线**，眼睛才追得住。

### 8.2.5 超圈：螺旋内缩（选项 A）

| 圈 | 半径 | 覆盖 |
|---|---|---|
| lane 0 | [0.50, 0.68] | 0–60 分钟 |
| lane 1 | [0.31, 0.46] | 60–120 分钟 |
| lane 2 | [0.14, 0.26] | 120–180 分钟 |

lane 2 内缘（0.14）仍在中心毂（0.045）之外。**超过 180 分钟不再内缩**，第 4 圈起在 lane 2 上原地覆盖，并在 12 点外沿标一个圈数「×4」。理由：三小时还没做完 25 分钟，羞耻已经饱和，再加视觉复杂度没有边际收益。

### 8.2.6 秒针：⚠️ 2026-07-28 由「亚秒连续扫」改为「一秒一跳」

**本节原来定的是亚秒连续扫，作废。** 改的原因是滴答声（§8.3.6）：

> **扫秒针的钟是不会响的。** 连续扫来自连续驱动的机芯，滴答来自步进擒纵 —— 真实世界里这两件事互斥。既然要滴答，秒针就得跟着步进，否则声音和画面各说各话。

`DrawHands` 里就一行：`var sec = (double)now.Second;`，不再加毫秒。

**33ms 的重绘保留下来了**，作用变了：原来是为了画出亚秒的连续位置，现在是为了让**跳变及时**（延迟 ≤33ms）以及**在秒边界上放那一声滴答**。下面这一节原有的论证只在"连续扫"的前提下成立，留作记录。

### 8.2.6a 原「亚秒连续」的论证（已作废，留作记录）

- **角度是纯函数**：`deg = (now.Second + now.Millisecond / 1000.0) * 6`。**不用累加器** —— 原则 4 在渲染层同样成立，而且这样天然免疫掉帧、系统休眠后的漂移。
- 帧率：窗口可见时约 60fps。Avalonia 的 composition 渲染管线上这只是重设一个 `RotateTransform`，代价可忽略。**窗口不可见时完全停掉**（§8.3 收进任务栏之后白捡的好处）。
- 分针角度同样带秒的小数，跟着一起平滑走 —— 秒针扫而分针跳会显得廉价。
- **颜色不用 `--slack`**（样板页里是红的）。红是"偷懒"的语义色：色带全红时秒针会消失，而且会教育眼睛"红 = 正常"。新增独立 token **`--sweep`**（深盘上取蓝灰高光，如 `#C9D4E0`）。
- 秒针**纯装饰**，不代表任何判定频率（判定频率见 §8.3.5）。

### 8.2.7 两套配色

- 两份 token 最终落在 `DialColors.cs` 的 `DialPalette.Light` / `DialPalette.Dark`，实现时按需微调过（木框、骨牌、休息扇形都是后加的）。
- 盘面四个 token（`--dial-face/-edge/-ink/-tick`）**两套主题共用深色那份**（待 §0.5 问题 1 确认）：表盘是一个恒定的物件，白天它是浮在浅色窗口上的一块深色仪表；顺带绿/琥珀/红只需为一种底色调一次。
- 切换跟随系统（`ActualThemeVariant`），设置里可强制。

## 8.3 ❌ 整套「置顶提醒」已废弃，改为两声系统音（2026-07-28）

**本节原有的 8.3.1~8.3.6 六小节全部作废**，包括：收进任务栏常驻、任务栏图标动态重画、置顶不抢焦点、看表不是专注、键鼠空闲督促。它们的实现已从代码里删干净（`Win32Topmost.cs`、`InputIdle.cs` 两个文件删除，`itami idle` 子命令删除）。

用户 2026-07-28 的两句话：

> 不要再纠结窗口置顶这种事情了。逻辑混乱，又容易出错。直接使用系统提供的声音。

> 像所有通用程序一样，多一个 Setting 界面，参考 Clock 应用程序的设置，只有任务结束声音，休息结束声音。极致简化，把检测鼠标是否不动的相关逻辑也去掉。

**为什么这是对的，而不是半途而废。** 置顶提醒在实机测试里连着塌了三次，而且每次修完都塌在新地方：

1. 达成那次**根本没置顶** —— §0.5 问题 4 定的是"弹出但不置顶"，可我们同时刻意不抢焦点（§13 第 6 条）。「不抢焦点 + 不置顶」= 窗口根本浮不上来，用户坐在 PDF 里什么都没看见。**这两条约束本身是矛盾的**，文档里并存了很久没人发现。
2. 改成置顶之后，达成的那次置顶**永远撤不掉** —— `EndSession()` 抹掉了"我顶着呢"的记号，看门狗再也不去撤，窗口压在所有应用上面，除了重开程序没有办法。
3. 撤销的时机本身反复改了三轮：等 AW 确认下一格干净 → 只认键鼠 → 挂到渲染帧上。每一轮都是因为上一轮在真机上表现不对。

根子在于**置顶是一个有状态的东西**：顶上去了要记得撤，撤的条件要跨越会话生命周期，还要跟"用户自己点进来了""对话框被主窗口盖住了"这些情况互动。声音没有状态 —— 响过就完了。

**跑偏从此没有任何主动提醒，只写一行日志。** 表盘上那格是红的、灰弧往前滑了一截，自己看，自己猜 —— 跟 §7.1 不给账单、§8.4.2a 滑块不给数字是同一条思路。

### 8.3.1 三声

| 事件 | 声音 | 备注 |
|---|---|---|
| 专注达成 | 「任务结束声音」，可关 | 对应 Clock 的 *End of session sound* |
| 休息结束 | 「休息结束声音」，可关 | 对应 Clock 的 *End of break sound* |
| 键鼠安静满 60 秒 | 「键鼠空闲声音」，可关 | 见 §8.3.5。用户 2026-07-28 想过之后加回来的 |

没有「任务开始」音，**也没有「跑偏」音** —— 跑偏只写日志。

三者的性质不同，值得分开看：前两声是**通报**（事情已经发生了），第三声是**补救**（再不动键鼠，这段时间就要作废了）。所以第三声的默认音色要更轻：它一分钟可能响两次，而且它问的是"你还在吗"，不是"完成了"。

**只用系统自带的 wav**（`C:\Windows\Media\*.wav`），下拉框在运行时枚举那个目录，不打包任何音频资源 —— 跟表盘不用位图是同一条纪律。播放走 `winmm.dll` 的 `PlaySound`，`SND_ASYNC | SND_FILENAME | SND_NODEFAULT`：异步别卡界面，找不到文件就安静（不要退化成"叮"）。**提示音绝不能把程序搞挂**，跟日志同一个原则。

### 8.3.2 设置窗口

照 Windows 时钟应用的「专注时段」设置页排：一张卡片一条设置，卡片里是标题 + 右侧开关，下面是音色下拉框。

**一个字的说明都没有**（用户 2026-07-28）：标题 + 控件，剩下的自己猜 —— 跟主界面分割线以下不给提示字、不给数字（§8.4.2a）是同一条。

四张卡：任务结束声音、休息结束声音、键鼠空闲声音、**滴答声音量**（只有滑块，没有开关）。

**没有「确定 / 取消」**：改一下存一下，跟系统设置一个路数。**选中音色立刻试听一次** —— 从一串文件名里盲选是选不出来的。

入口是主窗口小目标那一行右端的一个齿轮，30px、四成半透明、悬停变实。它是分割线以下唯一一个不属于"任务"的控件，所以画得尽量轻。

### 8.3.3 设置要落盘，这是第二个例外

`%LOCALAPPDATA%\ItamiTimer\settings.json`，跟日志同一个目录。

跟 §8.1「完全不写盘」不冲突，理由和日志那条例外（§8.1a）一模一样：**§8.1 禁的是任务状态落盘**（不要 current-task.json、不要累加值、退出即放弃），目的是让状态永远由 AW 历史推导。设置不是任务状态 —— 它不参与任何判定，删掉它程序照样跑，只是回到默认音色。

读写失败一律吞掉走默认值。设置读不出来不该让程序打不开。

### 8.3.4 连带删掉的东西

- **收进任务栏 + 任务栏图标动态重画**。窗口从头到尾就放在那儿（用户 2026-07-27 已定），图标保持番茄不变。
- `itami idle` 子命令和 CLI 里的督促输出。命令行层不再做空闲检测——它本来就没有声音可放。

> **键鼠空闲检测不在此列。** 它一度被删掉，用户当天又加了回来：「我应该保留键鼠空闲检测，也是用声音提醒，需要在设置里面加一条。」这个来回本身值得记：被否掉的从来不是**空闲检测**，而是它原来那个**表达方式**（置顶）。换成声音之后，同一件事既保住了又不再有状态要维护。

### 8.3.6 滴答声：合成的，不带资源文件

用户 2026-07-28：「我还希望有时钟持续的滴答声」→ 选了**运行时合成**。

**为什么必须合成**：`C:\Windows\Media` 那七十个 wav 里**一个滴答都没有** —— 全是通知音和铃声（ding / chord / chimes / tada / Alarm01~10 / Ring01~10 / 各种 Windows Notify）。所以 §8.3.1「只用系统自带的音」这条纪律在这里第一次走不通。

合成反而更合这个项目的路子：表盘、番茄、骨牌、图标全是代码画出来的，**凡是能算出来的东西就算出来**。而滴答恰好是最好算的一类声音 —— 它就是一个短促的宽带脉冲。

```
白噪声 × exp(-t/τ_click)        ← 那一下"咔"
      + 阻尼正弦 exp(-t/τ_body)  ← 木头腔体的余韵
      × 0.5ms 淡入               ← 不淡入会多出一个直流爆音
```

35 毫秒、16bit 单声道 44.1kHz、约 1500 个采样点，连 44 字节的 WAV 头一起写进内存，用 `PlaySound(SND_MEMORY | SND_ASYNC)` 播，**不落盘**。

**「滴」和「答」是两个不同的缓冲区**（2800Hz / 2300Hz，衰减常数也不同），按秒的奇偶交替放。真钟的擒纵机构两边不对称 —— 这一点是它听起来像钟而不像节拍器的关键。

**触发挂在已有的 33ms 渲染帧上**，检测秒跨越就放。不另起定时器：天然对齐墙钟、不会漂，而 33ms 的抖动对一声 35ms 的"咔"完全听不出来。另一条路是做个整 1 秒的缓冲交给 `SND_LOOP` 循环（零 CPU），但音频时钟会跟系统时钟慢慢漂开，一小时后秒针和声音就对不上了。

**窗口最小化时照样响**（用户 2026-07-28 明确）：滴答是钟本身在走，跟你有没有在看它没关系。只有重绘会因为看不见而跳过，那纯粹是省电。

**音量是烤进缓冲区的**（`PlaySound` 本身没有音量参数），改音量就重新合成一遍，很便宜。四条声音里**只有滴答有音量滑块**：另外三声一小时响不了几次，滴答一小时响 3600 次。

**滴答的开关不在设置窗口里**，在钟的右上角那个喇叭上（§8.3.7）。设置窗口只管它的音量。理由是分类：**滴答属于「钟」，另外三声属于「督促」** —— 混在一页里会让人以为滴答也是任务机制的一部分。拖动音量滑块时**无视开关状态照样试听**：用户正在调音量，就是要听见。

### 8.3.8 Debug / Release，以及发布出去之后文件都在哪

**两个配置都写日志**（用户 2026-07-28 傍晚：「让 Release 版本也写 log」）。

⚠️ 这条**推翻了同一天早些时候的决定**。当天上午定的是「log 文件只有 Debug 才写，Release 版本不必了」，实现用 `[Conditional("DEBUG")]`，让整个调用连同实参在 Release 编译时一起消失——省掉了那行每分钟一次的日志里一大串字符串插值。

推翻的理由，其实就写在当时那条决定自己的「代价」小节里：

> 界面对用户是沉默的（分割线以下一个提示字都没有），日志是**唯一**能让人事后看出"它到底怎么了"的地方（§8.1a）。Release 下出了错——AW 连不上、`rules.json` 写坏、抛异常——屏幕上只有一个灰按钮，没有任何线索，用户和维护者都无从查起。

而省下的开销本来也不值一提：**一分钟一行，一轮任务最多五十行**。写入量另有上限兜底——超过 1MB 滚一次、只留一份旧的，长期开着也不会把盘吃光。

> 想回到"只在出事时留痕"的中间档：给 `Log.Info` / `Log.Warn` 加回 `[Conditional("DEBUG")]`、`Log.Error` 不加即可。正常一轮任务零写入（没有 ERROR 就没有行），出事时又有据可查。

**发布形态**：框架依赖 + RID 限定（`-c Release -r win-x64 --self-contained false`），**27 MB / 35 个文件**。

两个尺寸陷阱，都实测过：

| 做法 | 结果 |
|---|---|
| 不指定 RID | **560 MB** —— Skia / HarfBuzz **所有平台**的原生库和调试符号全进来，光三个平台的 `libSkiaSharp.pdb` 就 244 MB |
| 指定了 RID，但不删 `.pdb` | **127 MB / 39 个文件** |
| 指定 RID + 删 `.pdb` | **27 MB / 35 个文件** |

**`dotnet publish` 自己不会剔除调试符号**——它只是把 NuGet 原生包里的内容照搬到输出目录。那 100 MB 的差额就两个文件：`libSkiaSharp.pdb`（80.1 MB）和 `libHarfBuzzSharp.pdb`（19.9 MB）。

**这一步已经自动化了**，不需要人记得做：`ItamiTimer.App.csproj` 里的 `StripPdbFromPublish` 挂在 `AfterTargets="Publish"` 上，Windows 和 macOS 两条线一起受益。

> **为什么值得做进构建，而不是写进文档。** 它先以"文档里的第二条命令"的形式存在过几个小时，而在那之前它压根没被写下来 —— 第一版发布文档只有一条 `dotnet publish`，照着跑的人拿到 127 MB，没有任何提示。**一个"必须记得做"的手工步骤，就是迟早会被忘掉的步骤**；这一条已经被忘过一次，没理由赌第二次。
>
> 顺带删掉的还有本项目自己那两个 pdb（各几十 KB）。现在没有任何东西读它们 —— `Log.Error` 记的是异常类型和消息、不记堆栈。哪天想让 Release 的日志带上行号，要改的是 `Log.Error`，并在那个 target 里把自家的 pdb 排除掉。

**文件都在哪**：

| 东西 | 位置 | 谁写 |
|---|---|---|
| 程序本体 | `%LOCALAPPDATA%\Programs\ItamiTimer\` | 发布时覆盖 |
| 默认规则 | 同上，`rules.json` | 发布时覆盖 |
| **用户的规则** | `%LOCALAPPDATA%\ItamiTimer
ules.json` | **只有用户**，发布碰不到 |
| 设置 | `%LOCALAPPDATA%\ItamiTimer\settings.json` | 程序（§8.3.3） |
| 日志 | 同上，`itami.log` | 程序，**仅 Debug** |

`rules.json` 按三级找（见 `AppData.RulesPath`）：用户那份 → exe 旁边那份 → 当前工作目录。**绝不只看工作目录** ——快捷方式的"起始位置"可以是任何东西，按工作目录找会时灵时不灵。这个坑是 2026-07-28 建快捷方式时才暴露的：此前所有测试都恰好从仓库根目录启动，工作目录一直正好是对的。

**exe 的图标也是代码画的**。`tomato.ico` 由 `IconExport` 从 `TomatoIcon` 导出九档尺寸（16~256，PNG 负载），csproj 的 `ApplicationIcon` 引用它。仓库里那个 `.ico` 只是导出结果——**位图不是手画的**，跟表盘、骨牌、番茄一以贯之。番茄形状要改就改 `TomatoIcon.cs` 再导一次。

### 8.3.7 右上角两个即时开关：喇叭、图钉

用户 2026-07-28 指定的位置和顺序：窗口**右上角**，**先喇叭后图钉**，点图标就变。

| | 关 | 开 | 作用 |
|---|---|---|---|
| 喇叭 | `E74F` 打叉的喇叭 | `E767` 喇叭 | **只管滴答声**（§8.3.6） |
| 图钉 | `E718` 空心图钉 | `E840` 实心图钉 | 窗口置顶 |

**喇叭不是总静音**（用户 2026-07-28 修正）：「界面上的喇叭只控制滴答声，这是钟本身的功能，和监督学习无关。」督促那三声各有各的开关，在设置窗口里。这条分界值得记住 —— **这个程序其实是两样东西装在一个窗口里**：一个你愿意开在桌面上的钟，和一个盯着你学习的督工。喇叭属于前者，设置窗口那三张卡属于后者。

图钉那一格**刻意不用 `E77A`（打叉的图钉）**：打叉在满不透明度下会读成"置顶被禁用"，跟"已置顶"正好相反。喇叭打叉没有这个歧义 —— 划掉的喇叭全世界都认得是静音。状态同时靠**图形**和**明度**表达（关着淡、开着实），不用一个字。

~~字形来自 Windows 自带的 `Segoe Fluent Icons` / `Segoe MDL2 Assets`~~ → **2026-07-28 改成矢量绘制**（`ChromeIcons.cs`）。那个字体只有 Windows 装机自带，macOS 上是两个豆腐块。改完反而把纪律补齐了 —— 图标字体本来是"仓库里不放位图"这条链上唯一一个外部美术依赖，现在喇叭和图钉跟表盘、番茄、骨牌一样都是代码画的，两个平台长得一模一样。

（齿轮仍然是 emoji `⚙`，没跟着换 —— 它在两个平台上都渲染得好好的，没有换的理由。）

两个状态都**落盘**（§8.3.3），下次打开保持。

**齿轮不在这一排，它在骨牌行的顶部、喇叭正下方**（用户 2026-07-28 晚挪的）。

理由不是省地方，是**增加迷惑性**：把一个功能按钮塞进骨牌那一行，骨牌就更像某种控件的一部分 —— 用户原话「让用户自己更加不明白骨牌什么意思」。这跟滑块不给数字、分割线以下不给提示字（§8.4.2a）、不给账单（§7.1）是同一条路子：**能猜的就让人猜。**

横向落点又往左挪了一次，从图钉正下方挪到**喇叭**正下方，理由是用户看图之后的一句：「看上去骨牌就像被它推倒的。」齿轮正好压在倒下那几块的上方，成了那串多米诺的因 —— 一个纯粹的视觉玩笑，但它把"这七块是什么"这个问题又搅浑了一层。

> ⚠️ 那个右边距（26）是**量出来的，不是算出来的**。图钉那一档是 −4，两者理应相差一个喇叭宽 + 间距，但屏幕缩放会把 DIP 和物理像素岔开（本机 125%，UIA 报的 38px 差实际是 48 物理像素），所以按实测调到对齐为止。改这个数之前先量，别靠推。

三个图标现在都是**矢量画的**（`ChromeIcons`）。齿轮原来还留着 Segoe Fluent 的字形 `E713`，挪位置时才暴露 —— 那个字体只有 Windows 有，而 `chrome` 样式的 `FontFamily` 早在喇叭图钉改矢量时就被删了，于是齿轮渲染成一个豆腐块。八齿齿轮的 32 个顶点由外径 / 内径 / 齿宽三个数算出来，中心孔用 `EvenOdd` 挖。

> ⚠️ **图钉把置顶带回来了，但它跟 §8.3 砍掉的那套不是一回事。** 被砍掉的是**自动**置顶：程序自己判断该顶、该撤，撤的条件还要跨越会话生命周期 —— 那套在实机上连塌三次。现在这个是**手动开关，没有状态机、没有"什么时候该撤"的判断**，所以那三类 bug 一个都不会回来。同一个 `SetWindowPos` 调用，完全不同的东西。

### 8.3.5 键鼠空闲：唯一一件"来得及补救"的提醒

AW 要安静满 180 秒才把人翻成 `afk`，而且那条 afk 事件的**起点会回填到最后一次输入**（§14.4a T5）。所以这段时间不是等它判完再说——**判完就救不回来了**。这一声必须赶在截止线之前响。

```
最后一次输入
     │
     ├─ 60 秒 ──→ 计时点上响一声「键鼠空闲」        ← 还来得及
     │
     └─ 180 秒 ─→ AW 回填 afk，这段时间作废          ← 太晚了，闭嘴
```

**只在 `[60, 180)` 这个窗口里响。** 过了 180 秒就不再响了：那时人多半真的离开了房间，每分钟响一声只是噪音，而且已经救不回任何东西。

判断放在**整分钟的计时点**上（用户 2026-07-28：「键鼠有无动作，是和查询 aw 一样，在整分钟的时候做」），而且算在查 AW **之前** —— 这样 AW 连不上时也不会把它一起吞掉。

代价：最坏要安静将近 120 秒才被发现（上一个整分钟差一点没到 60 秒），离 180 秒仍有 60 秒余量。

**它救不了的那种情况仍然救不了**：安安静静读 PDF 超过 180 秒不动键鼠，那几分钟照样判 `Absent`、不计入、任务被拖长——这一声只是让你有机会在作废之前动一下。要真正放宽，旋钮在 `aw-watcher-afk.toml`（§0.1），不在本程序里。

## 8.4 休息阶段：色环褪去，空盘就是下一轮的邀请（2026-07-27 定稿）

用户 2026-07-27 定的原则一句话：**休息不要设计复杂。**

### 8.4.1 休息不是任务，也不被观察

| | |
|---|---|
| 休息是任务吗 | **不是**。它是"任务尚未终结但已无约束"的尾巴 |
| 休息期间访问 AW 吗 | **不访问**。用户去哪、焦点回不回原应用、本程序在不在前台，**全都不重要** |
| 休息区间进账单吗 | **不进**。曾提议免费记一行"休息期间你又回到了 devenv.exe"，**已被用户否决** —— 记录它就是在暗示它重要，而它不重要 |

约束休息会把这个产品的激励反过来：痛感的来源是「承诺了专注却偷懒」，休息是兑现而不是承诺。如果休息也被监视，用户会学会一件事——**别把任务做完**，因为做完之后紧接着是另一段被监视的时间。

原则 1 也在这里保持完好：因为休息不是任务，`focusCompletedAt` 那一刻程序并没有代替用户提交任何东西。

### 8.4.2a 时长范围：10~50 分钟，步进 5（2026-07-27 定）

> **代码里就是这个量程**（`MainWindow.axaml` 的 `Slider x:Name="Minutes"`）。
> 2026-07-27~28 期间它临时是 2~10 分钟、步进 1、默认 5，为了不用每次真坐 25 分钟
> 才看到达成那一刻；核心循环在真机上跑通若干轮之后，2026-07-28 已改回正式量程。
>
> 那段临时量程留下一个副产品：它暴露了 `RestMinutes` 用整除会让 ≤4 分钟的任务
> 算出 **0 分钟**休息，休息阶段整个不存在 —— 于是休息时长改成了向上取整（§8.4.2）。


界面上用一个**可拖动的滑块**选专注时长，只有 9 个档：10 / 15 / 20 / 25 / 30 / 35 / 40 / 45 / 50。

**步进 5 分钟带来一个刚好的性质**：休息时长 = 专注 ÷ 5 在正式量程下恒为整数分钟（10→2、25→5、50→10），永远不会出现「休息 4 分 24 秒」这种数。（取整规则仍然保留，见 §8.4.2 —— Core 允许任意时长，短任务照样要算得出休息。）

**⚠️ 这个范围属于 UI 层，不属于 Core。** `TaskRecord` 和 `TaskEvaluator` 必须接受**任意**时长——理由是 §13 的手动验证清单要求"把时长临时设成 1~2 分钟"跑一遍，尤其是第 3 条（休息结束不自动开始新任务）需要真的跑完一个专注阶段。如果 10 分钟下限被硬编码进 Core，每验一次就要枯坐 10 分钟。约束加在滑块上，正常使用时用户拿不到 10 分钟以下的任务，而命令行原子层和测试可以随便喂。

### 8.4.2 休息时长 = ⌊承诺专注时长 ÷ 5⌋ + 1

用户 2026-07-28 定稿，原话：「现在就定，一次性解决，（设定任务时间/5）向下取整+1」。

滑块九个档全是 5 的倍数，所以实际效果就是"五分之一，再多给一分钟"：**10→3、25→6、50→11**。

**那个 +1 不是慷慨，是补偿。** 它一次关掉两个口子：

**① 补上「发现延迟」**（§14.0a，本条至此结案）。专注是在某个**真实时刻**攒够的，但程序要到**下一个整分钟的计时点**才发现；而休息是从真正达成那一刻起算的（`restEndsAt = focusCompletedAt + 休息时长`）。于是用户实际能歇的时间少了一截 —— 实测过一次：01:14:19 达成、01:15:00 才发现，5 分钟的休息只剩 4 分 19 秒。

这个延迟**有上界，且上界正好是计时点间隔 60 秒**（§14.1b）。所以补整整 1 分钟，就能保证：

```
实际能歇 = 休息时长 − 发现延迟 ≥ (⌊n/5⌋ + 1) − 1 = ⌊n/5⌋ = 名义时长
```

**永不少于名义时长**，且最多多歇 1 分钟。这是个闭式解，不需要引入"发现时刻"这种非纯函数的东西 —— §14.0a 里备选的方案 (b) 会破坏原则 4，方案 (c) 要把节拍改细、代价大得多。

**② 任何非零时长都有休息。** 原来用整除，`FocusMinutes ≤ 4` 全部算出 **0 分钟**，休息阶段整个不存在、休息扇形（§8.4.4）永远看不见 —— 用户确实一次都没看见过，还以为淡出没做。Core 必须接受任意时长（§8.4.2a：范围约束属于 UI 层，§13 的手动验证会用 1~2 分钟的任务跑）。

> ⚠️ **这个 +1 跟计时点间隔是绑死的。** 哪天把查询节拍从 60 秒改成别的，`RestMinutes` 必须跟着改 —— 补偿量等于延迟的上界，而延迟的上界就是节拍长度。`TaskRecordTests.最坏的发现延迟也吃不掉名义休息` 是这条不变量的守卫。


「承诺专注时长」指提交时锁定的那个数（25 分钟），**不是实际耗时**。25 → 5 分钟，50 → 10 分钟。不设下限（§13 把时长调成 1~2 分钟做验证时，短休息正是想要的）。

**明确排除两种算法**：

- ~~实际耗时 ÷ 5~~ —— 偷懒 16 分钟就多得 3.2 分钟休息，激励完全反了。
- ~~偷懒多久就从休息里扣多久~~ —— 很诱人，但违背 §4 已定的「`OffTask` 不计入但也不惩罚」。惩罚**已经发生过了**：截止线往前滑，你花 41 分钟才换到 25 分钟的绿。二次惩罚的真实后果是让人在偷懒之后干脆放弃整个任务（反正休息也没了）。

### 8.4.3 完整时序

```
累计专注 ≥ 承诺时长
      ↓
【弹出，不置顶】"任务完成，进入休息 5 分钟"
      ↓                    ← 【不给账单】（2026-07-27，§7.1）：盘面上那圈色块
      │                      和滑到最后的灰弧已经把话说完了，再列一遍数字
      │                      只会把注意力从"看"拉回"读"
【休息中】窗口留在前台显示，不收回任务栏，不置顶
      │   色环从 100% 不透明【线性淡出】到 0，淡完 = 休息结束
      │   淡出本身就是倒计时，不另画倒计时弧、不显示秒级倒计时数字
      ↓
now ≥ restEndsAt → status = Completed，归档 history.jsonl
      ↓
【弹出】"休息结束了，要再来一轮吗" —— **纯提示**
      ↓
空表盘 + 提示词 + 「新建任务」标签
      ↓
用户开新任务，或者关掉程序。**都是用户的选择**，程序停在这里等（原则 1）
```

### 8.4.4 休息扇形：你挣来的那块时间（2026-07-28 改）

**原来这一节写的是"色环线性淡出"，作废。** 用户原话：

> 原来的要求表盘上的记录淡出什么太麻烦，而且我也没看到 …… 我的需求改变为：当任务结束以后，根据原定完成计划时间/5 得到的休息时间，在表盘上画一个扇形即可，表示这是应该奖励的休息时间。休息时间结束，则把这个扇形删除。

（顺带澄清：淡出**是实现了的**，`RingOpacity` 每秒线性递减、`DrawRing` 里 `PushOpacity`。用户没看到是因为当时 `RestMinutes = 2/5 = 0` —— 两分钟的任务压根没有休息阶段，这也正是 §8.4.2 改成向上取整的直接原因。不过换掉之后整套淡出机制可以删干净，是净简化。）

```
专注达成
   ↓
【色环整个撤掉】 + 画一块扇形：[达成时刻, 达成时刻 + ⌈承诺时长÷5⌉]
   ↓
分针扫出扇形 → 休息结束 → 扇形消失，会话终结，空盘
```

**色环在达成那一刻就撤掉**（用户 2026-07-28）：任务已经结束、不再查 AW，那一圈记录没有继续画的理由。空出来的盘面本身就是"这一块归你了"的底子 —— 也跟 §7.1 不给账单一脉相承：过程走完了就不再复盘。

代码上要留意一处：`Cells` 清空之后写入头退回 `StartedAt`，`RemainingMinutes` 会算出一整段承诺弧重新冒出来。所以 `TaskSession.RemainingMinutes` 里必须显式挡一下 —— `FocusCompletedAt` 有值就直接返回 0。

**为什么是从圆心切出来的整块扇形，而不是又一条细环带。** 盘面上所有别的东西都是细带（色块、承诺弧），再加一条就只能靠颜色区分，而颜色四档（绿 / 琥珀 / 红 / 灰）已经占满、各有含义。**剩给我们的只有形状。** 一整块从圆心切出去的扇形读起来就是"这一块归你了"，跟"记录"根本不是一类东西，不会看错。

**为什么不缩、不淡、不画倒计时。** 分针本来就在扫。**分针扫出这块扇形的那一刻就是休息结束** —— 倒计时是免费的，不需要任何动画状态。这是"分针即写入头"（§8.2.2）那条几何约定第二次白送东西。

**为什么不是灰。** 用户说"灰色的扇形即可"，但同时说了颜色可以给建议。灰在这个盘面上**已经有含义**：`Absent`（人不在）和承诺弧（还欠着的时间）都是灰。拿灰画奖励，等于让奖励长得像欠账。绿 / 琥珀 / 红各有其主，**蓝是唯一还空着的色相**，而且天然读作"歇一歇"。

有意思的是 `Pending` 那个蓝当初就是为承诺弧定义的，后来被否掉，理由写在 `DrawPendingArc` 里：「灰色，不是蓝色：这段是"还欠着的时间"，它不该有任何情绪」。**而休息扇形要的正是情绪** —— 同一个理由，两个相反的结论，都成立。

渲染细节（`DialControl.DrawRestWedge`）：

| | |
|---|---|
| 外缘半径 | `0.70`，压在刻度圈里侧，不盖数字 |
| 填充 | 径向渐变，圆心处全透明 → 盘沿 `0x4E`。像一束光落在盘面上，而不是一块补丁；也避免糊住指针轴和时针 |
| 收口 | 盘沿再叠一道 `0x88` 的窄环带，不然渐变外缘会糊成一团 |
| 层次 | 画在**色环之下**（`DrawFace` 之后、`DrawRing` 之前）—— 它是背景上的一块光，不是记录 |
| 色号 | 日面 `#4E8CC8`，夜面 `#63A6E0` |

`RestFrom` 在会话终结那一刻置 null，扇形随休息一起消失。

### 8.4.5 空盘即邀请

用户的原话：**"色环就是正在努力的标记。没有色环、显示提示词、显示新建任务标签，都是默默提示用户进入下一轮自我约束。"**

于是整个产品是一个闭环，而且这个闭环只靠色环的有无来表达，不需要额外的状态指示器：

```
空盘（邀请） → 色环生长（正在努力） → 色环褪去（释放） → 空盘（邀请）
```

#### 8.4.5a 不变量：色环是当前任务的投影

用户 2026-07-27 补充：**新开任务，色环清除是必做的**——包括休息还没结束就直接开下一轮的情况。

这条**不是一个要实现的功能，而是一条不能写错的不变量**：

> 色环 = 当前任务重放结果的投影。**没有当前任务，就没有色环。**

淡出的不透明度同样是纯函数（`1 − 已过 / 休息时长`）。所以新任务一提交，`focusCompletedAt` 是 null、格子列表是空的，色环**自动**归零——前提是实现里**不留任何跨任务的缓存**。缓存住淡出进度或上一轮的格子列表，就会带着半透明的旧环进新任务。这是这条不变量唯一可能被写坏的地方，要有测试盯着。

同理，`stop`（放弃）之后也没有色环——不需要为它单写清理逻辑。

### 8.4.6 两个边界

- **休息期间用户想立刻开新任务** → 「新建任务」按钮在休息期间就是可点的，点了即把当前任务判 `Completed` 并开新的。**不需要单独做「跳过休息」按钮——新建任务本身就是跳过休息**，而且这是用户在*终结*任务，不是程序在*开始*任务，原则 1 不受影响。反方向的「延长休息」不做：不点就是了。
- **休息期间关程序** → 任务就此结束（§2）。休息本来也没有约束，所以这里没有损失——账单在专注达成那一刻就已经给过了。

### 8.5 开工 Avalonia 之前：按风险排序，不按好玩排序（2026-07-27）

命令行原子层跑通之后，Core（模块 1~4）已经被真实数据验证过了。界面层要新做的东西里，**只有两件有可能推翻设计**，其余都是画画：

| # | 要验的 | 不成立的话 |
|---|---|---|
| **1** | **置顶但不抢焦点**，且能盖住浏览器/播放器的全屏（`ShowWindow(SW_SHOWNOACTIVATE)` + `SetWindowPos(HWND_TOPMOST, SWP_NOACTIVATE)`，从 Avalonia 调） | §8.3.3 的提醒、§8.3.6 的督促窗**全部失效**——这两个是这个产品干预用户的唯一手段 |
| **2** | **任务栏按钮图标能按分钟动态重画**（Avalonia 运行时换 `Window.Icon`，看 Windows 任务栏是否真的跟着变） | §8.3.2 整节要重做；§0.5 问题 2 选任务栏按钮而不是托盘的前提也没了 |

#### ✅ 两条都实测通过了（2026-07-27）

用 Avalonia **12.1.0** 建了一次性 spike 实测，两条**全部成立**，§8.3 不需要重写：

| 验的 | 结果 |
|---|---|
| 任务栏按钮图标动态重画 | ✅ 运行时给 `Window.Icon` 赋新的 `WindowIcon`，任务栏按钮**实时跟着变**。§8.3.2 成立 |
| 置顶不抢焦点 · 浏览器全屏 | ✅ `ShowWindow(SW_SHOWNOACTIVATE)` + `SetWindowPos(HWND_TOPMOST, SWP_NOACTIVATE)` 从**最小化状态**弹出，盖住 F11 全屏视频，键盘输入仍落在浏览器 |
| 置顶不抢焦点 · **MAME 全屏游戏** | ✅ **同样盖得住**。原先担心的"独占全屏盖不住"在实测中没有出现 |

实测中发现并修掉的两个问题，都是产品本身要的，不是 spike 的问题：

1. **双屏时必须每次摆回主屏正中。** 用户把窗口拖到副屏之后，后续每次弹出都出现在副屏——**提醒弹到你没在看的那块屏上等于没提醒**。所以位置不能沿用"上次用户放的地方"，每次弹出前重新定位。（实现坑：`Position` 是物理像素而 `Width`/`Height` 是 DIP，高 DPI 下不乘 `Scaling` 会偏向左上角。）
2. **exe 不能带控制台窗口。** `<OutputType>WinExe</OutputType>` 已保证 PE subsystem = 2（Windows GUI，不分配控制台）；测试时看到的小黑窗来自 `dotnet run` 的宿主进程，不是应用本身。

保留下来的产品代码：`Win32Topmost.cs`（模块 9）、`RingIcon.cs`（§8.3.2）。spike 窗口本身已删除。

**先花半天把这两个做成能跑的最小 spike，再动表盘。** 表盘是几十行 XAML 加已经定死的几何数字（§8.2），画错了改一下就行；而上面两条要是做不到，§8.3 整章都要重写——那时候再发现，漂亮的表盘就白画了。

**替换 App 项目时最容易手滑的一条**：`<AssemblyName>ItamiTimer</AssemblyName>` 必须保留。AW 上报的 `data.app` 就是 exe 名，§5.3 第 1 步的自身豁免靠它；改掉就会「提醒 → 用户看提醒 → 又违规 → 再提醒」死循环。换 csproj 正是最容易把这行弄丢的时刻。

**平台特定代码的位置**：Win32 调用写进 `ItamiTimer.App`，用 `OperatingSystem.IsWindows()` 或 `[SupportedOSPlatform("windows")]` 守住。Core 保持 `net10.0` 且不含任何 P/Invoke——注意那半条是**纪律**不是编译器强制（`DllImport` 在 `net10.0` 上能正常编译，csproj 注释里已写明）。

### 8.6 明确不再议的两条（2026-07-27）

用户在命令行版本跑通后表示「不用再议」，**维持现状，不要重新提出**：

- **任务没有时间上限。** 承诺 25 分钟结果拖到 3 小时也不会有任何机制介入。这跟 §4「`OffTask` 不计入但也不惩罚」一致，视觉上的荒诞由 §8.2.5 的螺旋三圈 + 圈数标记承担。
- **`rules.json` 维持只有「学习经济学」一个小目标。** 「做 ItamiTimer 这个项目」不单列成小目标——所以用 Claude 写这个工具的时间，在学经济学的任务里就是偷懒（实测一段 104 分钟的历史里 `claude.exe` 占了 59 分钟）。文件本来就是逐渐生长的（§5.2），用户想加的时候自己加。

## 9. 启动与退出（2026-07-27 重写）

任务不落盘（§2），所以原来那套"启动时读 `current-task.json`、重放、判断上次任务处在哪个阶段"的恢复逻辑**整节删除**，连带"12 小时僵尸任务兜底"也不需要了。

### 启动

**永远是干净状态**：空表盘 + 小目标勾选列表 + 时长滑块，等用户开任务。没有"上次那个任务还在跑"这种情况。

### 退出 = 放弃当前任务

| 时机 | 行为 |
|---|---|
| 空闲时（没有任务） | 直接退出 |
| **专注中** | **弹窗确认**：告诉用户已经攒了多少、退出就作废，并附上此刻的账单。确认后 `status = Abandoned` 然后退出 |
| 休息中 | 直接退出（专注已达成、账单也给过了，没有损失） |

**确认弹窗不是礼貌，是必要的**：用户可能只是想把窗口收起来。而收起来的正确操作是最小化——那不影响任何东西（原则 3）。两个动作长得很像、后果差很远，必须问一次。

**崩溃 / 关机 / 进程被杀**：任务没了，没有恢复。程序区分不了"用户点了 X"和"进程被杀"，这是选最简方案时明知的代价（§2）。

## 10. 配置项

| 配置项 | 默认值 | 说明 |
|---|---|---|
| 专注时长 | 25 分钟 | **范围 10~50 分钟，步进 5 分钟**，界面用可拖动滑块。提交时设定，之后锁定（§5.5） |
| 休息时长 | **专注时长 ÷ 5**（25 → 5 分钟） | 提交时由专注时长推导并锁定，不单独设。不设下限（§8.4.2） |
| 轮询间隔 | **60 秒** | 整个程序只有这一个节拍（§8.3.5）。**只影响提醒的及时性，不影响核算精度**（§2） |
| 键鼠空闲督促阈值 | 60 秒 | 超过就弹窗催用户动一下（§8.3.6）。**必须小于 `aw-watcher-afk` 的 `timeout`（默认 180 秒）**——那个值在另一个程序的配置文件里，API 读不到，改了这边不会报错 |
| 弹窗触发下限 | 5 秒 | 刚走完的那一分钟里偏离超过这个数才弹，滤掉通知抢焦点之类的噪音（§8.3.5） |
| 违规宽限期 | 0 | 不作废不惩罚，误报代价很低，不需要宽限期。留接口 |
| 勾选上限 | 不硬限制 | 用户估计 ≤5 组，但规则粒度细的时候硬卡个数很烦；界面显示已选个数即可 |
| 上次的时长/勾选 | 持久化 | 作为下次提交的默认值，不影响已提交的任务 |
| 主题 | 跟随系统 | 可强制浅色/深色；盘面 token 不随主题变（§8.2.7） |
| 秒针帧率 | 60 fps | 仅窗口可见时生效（§8.2.6）。低配机器可降到 30 |
| 提醒回落延迟 | 1 个 tick（1 秒） | 动了键鼠之后多久撤销置顶（§8.3.4）。不再跟 AW 有任何关系 |

## 11. 明确不在 v1 范围内

- ~~模拟表盘/动画计时器~~ → **2026-07-27 移入 v1 范围**，规格见 §8.2。状态/渲染分离的边界照旧（§8 第四条纪律）
- ~~最小化到系统托盘 / 无界面后台常驻~~ → **2026-07-27 移入 v1 范围**，规格见 §8.3。它不再是"锦上添花"，而是这个产品的主要交互形态：平时只是任务栏上一个色环，偏离时才现身
- 经典番茄钟的长休息（每 4 轮一次）
- 违规宽限期可调
- 独立的小尺寸悬浮提醒窗（v1 直接把主窗口整个置顶）
- 规则文件的图形化编辑器（v1 手改 JSON）
- 跨机器合并（AW 的 bucket 是按 hostname 分的，v1 只看本机；AWJ 那边已有 `machines` 注册表可以借鉴）
- 把专注会话回写给 ActivityWatchJournal 做历史统计
- **界面顶部的分类统计面板**（§2.1）。设计上已经把位置和边界留好了（模块表第 7 层 + 优雅降级纪律），但它依赖 AWJ 侧先加一个支持「未结束的部分区间」的 `awj stats --json` 子命令——那是对另一个项目的改动，要在那边单独定稿。**v1 先把核心的约束/核算做对，统计面板作为第二步。**

### 11.1 没装 AW 时退化成纯番茄钟 —— ✅ 已实现（2026-07-28 晚）

用户 2026-07-28 傍晚确认这是**当初就有的设计意图**：「用户不装 aw 也就当个番茄钟用……只是不用 aw，需要自己计时（不考虑用户自己切换程序，纯计时）。」当晚把四个待定点定完并实现了。

**先说清楚为什么要单独写这一节**：原则 0、§6.2、§12 的变更表**三处都写着相反的规则**（「不猜测、不降级、不自己找替代信号」「不显示任何编造的进度数字」，v2 那套"AW 断了自己撑着"还是在 v3 里被主动删掉的）。所以后来人读到这里会以为纯计时是被否决的方案，从而拒绝实现。**它不是被否决，是还没做。**

#### 四条边界

1. **不破坏原则 4（状态是推导的，不是攒的）。** 纯计时模式下 `FocusedSeconds = now − startedAt`，**这仍然是 (任务, now) 的纯函数**。实现上不用碰 `Replay`：喂一条覆盖 `[startedAt, now]` 的合成窗口事件 + 一条 `not-afk` 的合成 afk 事件，整段重放出来就是计入。**Core 一行不用改。**

   **合成事件用自身豁免最省事**：`GroupRules` 里 `ItamiTimer` 是硬编码判 `Neutral` 的，而 `Neutral` **计入**专注时长。所以那条窗口事件写 `app = "ItamiTimer"` 即可 —— 不需要 `rules.json`、不需要勾任何小目标。语义上也不牵强：番茄钟模式下本来就是一切都算数。

2. **⚠️ 模式在【程序启动那一刻】锁死，整个会话不再变**（用户 2026-07-28 定，比本节初版的"提交任务时锁死"更严）。

   做成"AW 断了就自动降级成纯计时"的话，**杀掉 aw-server 就成了免费刷时长的作弊路径** —— 跟整个项目 fail-closed 的方向正相反，也会毁掉 §6.2 那个好性质（临时不可达不影响最终正确性，因为历史在 aw-server 里）。任务开始后 AW 挂了，仍然按 §6.2 报「无法工作」。

   **AW 后启动的情况不做任何补救**：用户原话「用户以后启动了 AW，那就重新启动一次本程序」。不做空闲重探、不做手动重试按钮 —— 少一个状态机，就少一类 bug（§8.3 那套自动置顶连塌三次的教训）。

3. **界面剩下什么。** 每一格永远满高、永远绿，灰弧永远不往前滑，**「痛感全部来自表盘」这个命题就没了** —— 那没关系，它此时本来就只是个钟。连带：

   - 小目标勾选列表**整个隐藏**（不是变灰 —— 它没有意义了）
   - 「开始」按钮去掉"至少勾一个"的启用条件
   - **不执行** §6.2 那套"分割线以下整块变灰" —— 那是给"AW 本该在但连不上"用的
   - 键鼠空闲那一声关掉（它存在的唯一理由是抢救即将被 AW 判成 afk 的时间，没有 AW 就没什么可抢救的，见 §8.3.5）
   - 表盘、骨牌、休息扇形、三条通知音、滴答、喇叭、图钉 —— **全部照常**

4. **`rules.json` 加载失败也退化成番茄钟**（用户 2026-07-28 定）。启动时跟 AW 探测一起检查，**番茄钟兜底，问题写进日志**。现在的行为是加载失败后程序基本不可用，那比退化成番茄钟糟得多。

#### 一条判据：**点击「开始」才表示用户希望被监管**

用户 2026-07-28 的原话：

> 用户打开番茄钟，可以只是好看，或者只是听听滴答的声音。他点击了「开始」按钮，那才表示他希望被监管。

**这句值得当尺子用。** 讨论过程中曾担心一个场景：平时装了 AW、习惯被约束的人，某天 AW 悄悄挂了，他打开程序没留意小目标列表不见了、照常点开始，于是以为自己在被盯着、其实全程放羊。

**结论是不额外加提示，这个残余风险已知并接受**，理由：小目标列表整个消失是个很硬的信号（约束模式下不勾就点不了开始），而且那种用户一眼就会发现盘面全绿、从不变红。加一行"当前是番茄钟模式"的提示，则会破坏"分割线以下一个提示字都没有"（§8.4.2a）。

#### AW 地址：从 `settings.json` 读，不写死在代码里

**现状是完全没有配置**：`AwClient(string baseUrl = "http://127.0.0.1:5600")`，三个调用点全是 `new AwClient()`，一处传参都没有。地址硬编码成 AW 的出厂默认端口 —— 不是"读了配置恰好是默认值"，是压根没提供过配置。

用户 2026-07-28 定：**默认值放进 `settings.json`，用别的文本编辑器改**。不做设置窗口里的输入框（那一页只放声音，§8.3.2）。所以 `Settings` 加一个字段、默认 `http://127.0.0.1:5600`，第一次保存时写出来让人看得见能改。

这条**跟番茄钟模式无关，可以单独做**。

#### 落点

| 文件 | 干什么 |
|---|---|
| `AppMode.cs` | 新增。两个值 + 那条"绝不自动降级"的警告 |
| `MainWindow.axaml.cs` | `_awReady` 布尔量升级成 `_mode`；`CheckAwAsync` 里裁决并写日志；新增 `ApplyMode()` 隐藏小目标列表；`RefreshStartButton` / `OnStart` 各加一条分支 |
| `TaskSession.cs` | `SyntheticSpan()` 造那两条合成事件；番茄钟模式跳过 HTTP、关掉键鼠空闲督促；AW 地址改从参数来 |
| `GroupRules.cs` | 新增 `Empty` —— `rules.json` 读不出来时给 `TaskSession` 用 |
| `Settings.cs` | 新增 `awBaseUrl` |
| `Core/Replay.cs` 等 | **一行没改**，承诺兑现 |

**`PomodoroFallbackTests` 五条**钉住第 1 条边界那个承诺。最要紧的是最后一条 `承诺的依赖_自身豁免判Neutral且Neutral计入` —— 它直接指着依赖本身。**"Core 一行不用改"这句话必须有测试兜着**：哪天有人动了 `Neutral` 的计入语义或自身豁免那几个名字，番茄钟模式会**静默地不再计时**，而界面上看不出来（盘面本来就该全绿）、日志里也不报错。这个项目已经出过好几个这种"不报错、不崩溃、只是安静把事情做错"的 bug（`Phase` 恒为 `NoData`、承诺弧跨整点跳圈、达成时刻打成 UTC）。

#### 实测（2026-07-28 20:24，把 `awBaseUrl` 指向不存在的 5999 端口模拟没装 AW）

```
ERROR  Cannot reach ActivityWatch; falling back to plain pomodoro mode: ... (127.0.0.1:5999)
INFO   Mode: Pomodoro
INFO   Task started [Pomodoro]. Goals:   focus 25 min  from 20:25:00  break 6 min
```

界面：小目标勾选框 0 个、`Start` 直接可点、齿轮仍在。87 个测试通过。

#### 它的价值

**对"扩大使用范围"的杠杆比代码签名大得多。** 有了它，没装 AW 的人下载下来就能立刻当钟和番茄钟用，装了 AW 的人才解锁约束模式 —— 上手台阶从"先装 AW、给两个系统权限、手写正则"降到"双击打开"。

## 12. 版本变更对照

### v2 → v3（本次，核心模型变更）

| v2 | v3 |
|---|---|
| 进程内 `remaining` 秒数递减 | **无可变累加值**；状态 = 纯函数(任务记录, AW 历史, now)（§2） |
| 关掉程序 / 重启就丢了 | v3 初稿改成落盘可恢复；**2026-07-27 又改回不落盘**——退出程序即放弃任务，任务最长 50 分钟，崩溃最多丢这一轮（§2）。原则 4 完好：状态仍是推导的，不是攒的 |
| `?limit=1` 读最新一条 | **区间查询** `?start=&end=`，重放整段历史（§6.1、§7） |
| focus → rest → focus 自动循环 | **一个任务 = 一段专注 + 一段休息，然后终结**；绝不自动开下一轮（原则 1、§3） |
| `AwaitingReturn` 状态（休息结束时人不在就等着） | 不需要了——休息结束就是任务结束，本来就在等用户 |
| AW 不可达 → 冻结、等恢复、自动续上 | AW 不可达 → **直接说无法工作**；但临时不可达不影响最终结果的正确性（§6.2） |
| afk bucket **可选**，缺失时降级为"永远视为在座" | afk bucket **必需**。那个降级方案是错的——窗口事件的 duration 会靠心跳自己长大，不看 afk 就等于"起身走开"是隐形的最省力作弊路径（§6.1.1，重要） |
| 没提 AWJ 的关系 | 明确为**兄弟而非上下层**：共享规则文件，统计面板可选地调 AWJ，但约束和核算绝不经由它（§2.1） |
| 需要"数据过期检查"防旧事件被当成现在 | **该类 bug 自动消失**，末尾没数据自然表现为 `Gap`（§6.3） |
| 无"窟窿"概念 | `Gap`：AW 没数据的区间，不计入也不惩罚，如实报告（§6.3） |
| 勾选集合是个当前值 | v3 初稿改成带时间戳的 `groupTimeline`；**2026-07-27 又改回当前值的并集、追溯生效**（§5.4、§5.4.1） |
| 规则文件 YAML（需 YamlDotNet） | **JSON**（`System.Text.Json`，零外部依赖）（§5.2） |
| 最终报告需要额外记账 | 报告是重放的副产品，免费（§7.1） |
| 轮询间隔影响计时精度 | 只影响提醒及时性（§2）。间隔本身 2026-07-27 从 3 秒改成 **60 秒**，全程序单节拍（§8.3.5） |
| 状态机是真的状态机 | "阶段"是推导出来的分类，不是机器状态（§3） |

### v1 → v2（历史，见 §14 之前）

单次计时 → 番茄钟；唯一目标窗口 → 多活动组勾选；只比 `app` → `app`+`titles`；违规停止需手动重启 → 违规暂停自动恢复；引入 afk 门；引入自身硬编码豁免。

## 13. 验证方式

把时长临时设成 1~2 分钟，跑这几条。**第 1~5 条是关键路径**——前 4 条验 v3 模型本身，第 5 条验那条最省力的作弊路径被堵住了：

1. **最小化不影响结果**（§2 修订后的版本）：提交任务，专注一会儿，**最小化窗口**，去别的应用摸鱼 1 分钟，再打开 → 那 1 分钟必须被算成偷懒并出现在账单里。注意这里是**最小化**不是关闭——2026-07-27 之后关闭程序等于放弃任务，原来那条"杀掉进程再打开、账要接得上"的验证**已作废**。
2. **退出要问一次**（§9）：专注中点关闭 → 必须弹窗确认并给出账单，不能默默丢掉。而最小化不能弹这个窗——两个动作长得像、后果差很远。
3. **休息结束不自动开始新任务**（本次需求的重点）：跑完一轮，休息结束 → 必须停在那里等用户，**不能**自己开始下一轮计时。
4. **中途加小目标追溯生效**（§5.4）：提交任务只勾「学习经济学」，去 Blender 里待 1 分钟，然后补勾「学习 Blender」→ 那 1 分钟**必须**变成专注时长，红格子转绿。（这与 v3 初稿的期望**相反**，2026-07-27 用户明确要求，见 §5.4.1。）
5. **实时提醒不抢焦点**：专注中切到未勾选的应用 → 窗口置顶弹到最前，但**在切走的那个应用里继续打字，字要落在那个应用**而不是 ItamiTimer。
6. **自身不触发死循环**（§5.3）：提醒弹出后去点 ItamiTimer 窗口本身 → 不应产生新的违规提醒，也不应累计成偷懒。
5. **「停在目标应用上走开」不能刷时长**（§6.1.1，**最容易漏测、后果最严重**的一条）：提交任务后把已勾选的应用（比如 SumatraPDF）停在前台，起身离开超过 afk 阈值 → 那段时间**不能**计入累计专注时长。注意这条**光看窗口事件是测不出来的**——窗口事件的 `duration` 会一路心跳长大，看起来像一直在专注。必须靠 afk 数据才能判对。这是最省力的作弊路径，务必单独验。
6. **实时提醒不抢焦点**：专注中切到未勾选的应用 → 窗口置顶弹到最前，但**在切走的那个应用里继续打字，字要落在那个应用**而不是 ItamiTimer。
7. **自身不触发死循环**（§5.3）：提醒弹出后去点 ItamiTimer 窗口本身 → 不应产生新的违规提醒，也不应累计成偷懒。
8. **AFK 不计入也不提醒**：专注中离开键盘超过 AW 的 afk 阈值 → 那段时间不计入累计**且没有**置顶提醒。
9. **锁屏不白涨时长**（§4 的 `Absent` 优先级）：专注中锁屏几分钟 → 那段时间不能计入累计专注时长。
10. **AW 没启动**：不开 AW 就想提交任务 → 拒绝提交并明确报错，不能装作在正常计时。缺 afk watcher（只跑了 window watcher）同样应该拒绝，不能降级（§6.1.1）。
11. **窟窿如实报告**（§6.3）：任务进行中把 aw-server 停掉几分钟再启动 → 那几分钟应报成"AW 无数据，不计入"，既不算专注也不算偷懒。

## 14. 时间模型与原子清单（2026-07-27）

这一节是命令行原子层（`src/ItamiTimer.Cli`）的开工依据。

### 14.1 一切对齐整分钟

用户 2026-07-27 定：**`startedAt` 强制落在整分钟上，此后所有核算查询也都落在整分钟上。**

**⚠️ 取整方向在 2026-07-27 当天被用户改过一次，现在是「截断」：**

```
点击「开始」 23:13:10
      ↓
startedAt = 23:13:00        ← 截断到【当前】这个整分钟
                            ← 不是进位到 23:14:00
```

用户的原话：「查询 aw 的时间起始点向前移，定为 23:13:00，不能往后移 23:14:00」。

**这条推翻了本节的初版**（初版写的是"绝不向后取整——那会把提交之前的时间也算进来，是追溯发放专注时长"）。两个方向各有代价，摆在这里，别再来回翻：

| | 进位 23:14:00（初版） | **截断 23:13:00（现行）** |
|---|---|---|
| 点完之后 | 干等最多 59 秒才真正开始 | 立刻开始 |
| 白送的时长 | 无 | 点击**之前**最多 59 秒 |
| 第一个计时点 | 23:15:00 才有第一格 | 23:14:00 就有第一格 |

用户选了不等。白送的那不到一分钟，是他刚决定要学习、正在打开材料的那段时间，认了。

**两个方向共有的收益仍然成立**（这才是"对齐整分钟"这件事本身的价值，跟方向无关）：

- **每个色块恒为完整的 60 秒。** 否则 `startedAt` 落在 10:08:37 时，第 0 格 `[10:08, 10:09)` 只有 23 秒属于任务；纯度分母若写成固定 60 秒，**第 0 格永远最多绿 38%**，看起来像一开局就在偷懒，而且极难看出错在哪。现在分母恒为 60，问题不存在。

推论：`focusDuration` 和休息时长都是整数分钟（§8.4.2a 的滑块步进 5 已保证），所以整条时间线除了 `focusCompletedAt` 之外全部落在整分钟上。

顺带一条：`TaskPhase.NotStarted`（`now < startedAt`）从此在真实任务里**不可达**了。枚举值留着，只为让重放对合成事件保持全函数。

### 14.0 三个 2026-07-28 实跑抓到的 bug（都不是逻辑错，都是"边界上没归一"）

一轮 5 分钟的真实任务，一次跑出三个。共同点：核算全对，**显示全错** —— 说明这一层缺守卫。

**① 承诺弧跨整点跳圈。** 圈号取的是表盘上的绝对分钟数（`start.Minute + cells.Count`），而色块取的是任务已走的分钟数（`cell.Index / 60`）。23:59 起算的任务走过 00:00，`60/60 = 1`，弧就跳到第二圈。**两套口径**。修法见 `DialControl.DrawPendingArc`。

**② `Phase` 恒为 `NoData`。** `DerivePhase` 取 `intervals[^1].Kind`，而 AW 的窗口监听总是落后 `now` 几秒（§14.4a T3），末尾**恒有**一小截 Gap。实跑 6 个计时点，6 行全是 `NoData`。后果：CLI 永远打「● AW 无数据」，§0.5 那条"断线换任务栏图标"接上去也会永远显示断线 —— 那个被动信号就废了。修法：从末尾往回找第一段有数据的区间，容差 `AwLagToleranceSeconds = 30`（实测心跳滞后 6~12 秒，而 aw-server 真宕掉时空洞会一路长过 30 秒）。**只影响 `Phase` 这个显示标签，不影响任何时长核算。**

**③ 「专注达成于 16:37:35」，实际是本地 00:37:35。** 就是 `ClockDisplayTests` 记的那个时区 bug 又犯了一次 —— 上次只把 CLI 的渲染收了口，App 的日志行漏在外面。**说明"每个显示的地方各自记得转本地"这个约定靠不住。** 现在改成**在边界上归一**：`AwClient` 解析时间戳那一步直接 `ToLocalTime()`，两种偏移量根本不流进核心。

三个都指向同一条经验：**凡是"同一个量有两种表示"，就要在边界上归一，不要指望每个使用点各自记得转。** 圈号 / 时区 / 偏移量都是。

### 14.0a ✅ 已结案：休息被"发现延迟"吃掉的那一截，用 +1 分钟补上

原问题（2026-07-28 实测）：

```
01:14:19  专注实际达成（重放算出来的时刻）
01:15:00  第 6 个计时点才【发现】达成 → 进入休息
01:16:41  休息结束        ← 休息只有 1 分 41 秒，不是 2 分钟
```

`restEndsAt = focusCompletedAt + 休息时长`，而 `focusCompletedAt` 是**真实达成那一刻**，不是发现那一刻。所以从用户视角看，休息被砍掉了"发现延迟"那么长的一截。

当时列了三条路，用户 2026-07-28 选了**第四条**，比三条都干净：

| | 方案 | 为什么没选 |
|---|---|---|
| (a) | 照旧，认这个损耗 | 名义 5 分钟实际 4 分钟，说不过去 |
| (b) | `restEndsAt` 从**发现**那一刻起算 | 要引入一个"发现时刻"——它不是纯函数的产物，直接破坏原则 4 |
| (c) | 把检查频率提细，让延迟变小 | 治标，而且要动整个 §14.1b 的节拍模型 |
| **(d)** | **休息时长 = ⌊n/5⌋ + 1** | ✅ 选它 |

(d) 成立是因为**延迟有一个确定的上界，而且这个上界就是计时点间隔（60 秒）**。补偿量取上界，不等式就永远成立：

```
实际能歇 = 休息时长 − 发现延迟 ≥ (⌊n/5⌋ + 1) − 1 = ⌊n/5⌋
```

**永不少于名义时长，最多多歇 1 分钟。** 闭式解，不引入任何新状态，也不用碰节拍模型。细节和守卫测试见 §8.4.2。

### 14.1a-1 计时点可以漏，账漏不了（2026-07-28 确认）

用户问的场景：点击「开始」的瞬间正好是 `23:59:00.999`，起算落在 `23:59:00`，可界面还没画完 `00:00:00` 就过去了 —— 第一个计时点岂不是丢了？

**丢不了,而且不需要任何补救逻辑。** 用户自己把答案说对了：

> 下一次 00:01:00 会查询到 23:59:00 到当前 00:01:00 内两分钟的活动，补上弧线的彩色。

**计时点不是"必须被打卡的时刻"，只是"抬头看一眼的时刻"。** 账在 aw-server 里，什么时候看都是同一本：

- 每一轮都重查 `[startedAt, now]` **整段**并重放（§7.2 不做缓存），所以晚看一次只是晚看，不是少看；
- 判据是 `FloorToMinute(now) > 已查过的那一分钟`，用的是 `>` 不是 `==`，跨过几个整分钟就补几格，一次吐出来；
- `ToMinuteCells` 按 `startedAt + i 分钟` 切格，跟"什么时候查的"无关。

这正是原则 4（状态 = 纯函数(任务记录, AW 历史, now)）在真实场景里第一次兑现。换成"每分钟往累加器里加一笔"的写法，漏一拍就永久少一分钟，而且**症状极其隐蔽**——色块看着对，账不对。

`tests/ItamiTimer.Core.Tests/CheckpointCatchUpTests.cs` 把这条钉死了，其中一条直接断言：补出来的第 0 格必须跟当初那一格**逐字段相同**，否则"补"就成了改历史。

### 14.1a-2 偷懒时间是累计值，不是每分钟的增量

用户 2026-07-28 的原话：

> 偷懒时间是个累计值，每次查询都会累计取整，加在"原定的计划完成时间"后面。

跟 §14.1a 的公式完全一致，这里只是把**为什么不能写成增量**记下来：

```
✅ 每次重算：   欠账 = (now − startedAt) − 已学到的        ← 从头算
❌ 每拍累加：   欠账 += 这一分钟没学到的                    ← 错
```

两者在"每拍都不漏"时数值相同，一旦漏拍或补拍就分道扬镳：增量写法会把补出来的两分钟**各记一笔**，也会在漏拍时永久少记。更要紧的是它是个**可变累加器**，正是原则 4 明令禁止的东西。

用户给的算例（已成为测试）：到 `00:02:00` 这个计时点，AW 说累计偷懒 75 秒 → 向上取整 **2 分钟** → 预计结束 = `23:59:00 + 05:00 + 02:00 = 00:06:00`。注意取整的是**累计值**，不是把每分钟的零头分别取整再相加 —— 后者会把 3 个 20 秒变成 3 分钟。

反过来也成立：欠账停止增长时截止线就**钉住不动**。测试里连查 5 拍、后 4 拍都老实学，预计结束一直是 `T0+6`；写成增量的话会一路滑到 `T0+9`。

### 14.1b 查询节拍锚在整分钟上，点击那一刻不查

用户 2026-07-28 纠正：「我 23:59:43 点击开始，这个时候，你要做的就是一件事，给表盘画上 5 分钟的灰色圆弧。查询 aw 的起始时间定为 23:59:00，接着你就应该在整分钟的时候查询了，到了 00:00:00，你就执行查询。」

```
23:59:43  点击开始   → 只做一件事：画出整段 5 分钟灰弧。【不查 AW】
23:59:00             → 查询区间的起点（§14.1）
00:00:00  第一个计时点 → 查 AW、重放、出第一格、按 §14.1a 重算截止线
00:01:00  第二个计时点 → 同上
```

**原来是错的**：`_lastAwAt` 初值取 `MinValue`，配上「距上次满 60 秒就查」，于是节拍跟着**点击时刻**漂。23:59:43 点的开始 → 23:59:44 查一次、00:00:45 再查，整整晚了 45 秒。用户看着表盘在整分钟毫无反应，而日志里那一行写在 45 秒之后。

现在的判据是 `FloorToMinute(now) > 已查过的那一分钟`，初值取 `StartedAt`（本来就是整分钟）。tick 是 1 秒，所以计时点的误差 ≤1 秒。

两条推论：

- **点击时刻不再产生任何日志行**。原来那行 `0.0/5 分钟 NotStarted 格子 0` 是节拍漂移的产物，没有信息量。
- **补勾小目标时例外**：那里要立刻重算（§5.4 追溯生效），所以把已查分钟设成 `MinValue`，下一拍就查，不等整分钟。

### 14.1a 预计结束时刻：偷懒多久，截止线就往前滑多久

用户 2026-07-27 定的核算模型。每到一个整分钟的计时点，重算一次：

```
应该学习时间 = 当前时刻 − 开始时刻
需要补的时间 = 应该学习时间 − 实际学到的时间
预计结束    = 开始时刻 + 承诺时长 + ⌈需要补的时间⌉      ← 向上取整到整分钟
```

用户给的算例（`Replay.ProjectedEnd` 和 `ProjectedEndTests` 第一条就是它）：

```
23:13:00 起算，承诺 10 分钟
23:14:00 第一个计时点：
    应该学 1 分钟，实际只学到 0.5 分钟   → 那一格填色偏红
    缺 0.5 分钟 → 向上取整 → 补 1 分钟
    预计结束 = 23:13:00 + 10:00 + 1:00 = 23:24:00
    灰色圆弧延伸到 23:24:00
```

后面每一分钟同理推演。**这就是 §0.4 说的痛感载体的具体机制**：用户不是看到一个数字变大，而是眼看着截止线在表盘上一格一格离自己远去。

几条细节：

- **向上取整是刻意的。** 整条时间线都落在整分钟上（§14.1），预计结束也该如此。代价：不满一分钟的零头会被算成一整分钟的欠账。精确值仍在 `FocusedSeconds` 里，只有这个**投影**取整。
- **缺口钳到 0，不能变负。** 起点截断会把点击之前最多 59 秒算进来，`FocusedSeconds` 因此可能短暂超过 `now − startedAt`；不钳的话截止线会往回缩。
- **截止线只往后滑，不往回缩。** 缺口是累计量（应该 − 实际），后面专心并不会把前面欠的还上——只是不再新增。
- **达成之后钉死。** `FocusCompletedAt` 一旦有值，预计结束就是那一刻。
- **第一个计时点不会撞上键鼠督促。** 用户不到一分钟前刚点过「开始」按钮，`GetLastInputInfo` 必然是新鲜的，直接进 AW 查询那一支。
- **⚠️ 一处已知的紧张：`Gap` 也会推截止线。** §6.3 写的是 AW 没数据「既不计入也不惩罚」，而按上面这个字面公式，Gap 算进"没学到"，于是会把截止线推后——那实质上就是惩罚。目前**按用户给的公式字面实现**，没把 Gap 摘出去。要改的话只需在 `ProjectedEnd` 里减掉 `state.GapSeconds`。T6 修掉之后 Gap 已经很少见，先记着。

### 14.2 四个时刻，不要混

| 概念 | 定义 | 注意 |
|---|---|---|
| `startedAt` | 任务提交后进位到的那个整分钟 | **UTC 存储**，界面显示本地时间 |
| **核算区间** | `[startedAt, floor_to_minute(now))` | **半开**。闭区间在边界上会双计。末端向下取整——**正在进行的那一分钟不画**，等它走完再画，否则格子会闪 |
| **查询区间** | `[startedAt − 6h, now)` | 必须比核算区间**宽**，见下 |
| **色环网格** | 第 i 格 = `[startedAt + i 分钟, +1 分钟)` | 与墙钟整分天然对齐，所以「分针就是写入头」（§8.2.2）成立 |

**查询区间为什么要放宽 6 小时**：AW 的 REST API 只按事件**自己的开始时间**过滤，不按区间重叠（`aw_atoms.py` T1，2026-07-26 实测）。一条开始于 `startedAt` 之前、延伸进任务的事件会**静默消失**。理论上没有可证明安全的宽度（心跳能让单条事件无限长，实测见过 6 小时的 SSH 终端），但**失败模式是良性的**：查不到就是开头几秒判 `Gap`，不计入也不惩罚。所以取 6 小时，如实接受残余风险，**不要为它加机制**。

### 14.3 两条查询路径，节奏不同、用途不同

「查询落在整分钟」和「3 秒轮询才能及时提醒」（§8.3.5）不矛盾——它们是**两条路径**：

| | 区间 | 节奏 | 用途 | 权威性 |
|---|---|---|---|---|
| **核算路径** | `[startedAt, floor_to_minute(now))` | 1 分钟 | 重放全段 → 累计时长、色块、账单 | **唯一权威** |
| **提醒路径** | `[now − 30s, now)` | 3 秒 | 只判"此刻在干什么" → 要不要跳出来 | 仅供提醒，**永不写入核算** |

提醒路径只取一个很短的尾窗、只关心最后一个区间的分类，不做全段重放。它算错了最多是提醒早了或晚了几秒，**动不了账**。

（v3 曾用 `?limit=1` 读最新一条，§6.3 已论证那是错的。这里用的仍然是区间查询，只是区间很短。）

### 14.4 `focusCompletedAt` 不进位，末格画短一截

累计专注时长是各个 `OnTask ∪ Neutral` 区间之和，而这些区间的边界由用户什么时候切换应用决定，**不落在整分钟上**。所以累计跨过阈值的那一刻（§7 第 5 步插值算出的 `focusCompletedAt`）一般在某分钟中间，比如 10:33:17。

**建议不进位**，色环最后一格按实际长度画短一截（终止于 `focusCompletedAt` 对应的角度）。理由：

- 进位到 10:34:00 意味着"账已经结清了但还得再撑 43 秒"，那 43 秒里切走算不算违规会变成一个没有好答案的问题。
- 色环画在连续的表盘上，末段短一截是自然的，纯度按它自己的实际长度算，不产生 §14.1 那种分母失真。
- `restEndsAt = focusCompletedAt + 休息时长` 因此也不在整分钟上——但休息期间只有一条淡出动画（§8.4.4），本来就不需要分钟对齐。

### 14.4a 实现时撞到的两个坑（2026-07-27，真实数据上确认）

写完原子层拿本机历史干跑时撞出来的，两条都**只影响正确性、不影响能不能跑**，所以特别容易蒙混过去。

#### T4：窗口事件的 `duration` 可以是 0

窗口标题**每秒都在变**时，AW 没法把相邻事件合并，于是每秒产生一条 `duration = 0.0s` 的事件。实测触发源就在本机：**Claude Code 在 Windows Terminal 里的转圈动画**（`⠂ ⠐ ⠂ ⠐ ETF 学习`）——这一个来源贡献了 3 天 5796 条终端事件里的大头。

零时长事件的 `[start, end)` 是**空区间**，"找覆盖时刻 t 的事件"永远找不到它，那段时间被整片误判成 `Gap`。实测把一段 22 分钟重放里的 **2 分钟**报成了"AW 无数据"，而当时终端明明一直聚焦着。

修法：把每条事件的终点延伸到下一条的起点，**但只在空隙 ≤ 5 秒时**。这个上限是关键——无上限地延伸会把 aw-server 宕掉造成的真实窟窿也一起填平，§6.3 的 `Gap` 检测就废了。

#### T5：afk 是**回溯**写入的，发现时损失已经写死

**实测（2026-07-27，本机 5 天数据）：239 条 afk 事件，最短 182.6 秒，没有一条短于 180 秒。**

```
最短的 10 条: 183, 183, 184, 184, 185, 185, 186, 186, 188, 189
```

底线精确卡在 `aw-watcher-afk` 的默认 `timeout = 180`。这证明 **afk 事件的起点是"最后一次动键鼠的那一刻"，不是"判定为 afk 的那一刻"**——它按定义不可能短于超时阈值。

时间轴：

```
T+0     最后一次输入
T+180   watcher 才翻成 afk，并把事件起点【回填到 T+0】
T+183   ItamiTimer 这才可能看到 Absent
        → [T+0, T+183] 已经整段记成 afk，一秒都追不回来
```

**推论（重要，能省掉一整类无效设计）**：任何"靠界面在事后干预来挽救 afk"的方案都被这条事实堵死。弹窗必然在 AW 出结论之后才到，此时损失已写死；用户动一下，再安静 180 秒，又丢一块。唯一成立的方向是**在 AW 的截止线之前动手**，也就是自己读键鼠空闲时间（§8.3.6）——因为 AW **只存算好的 afk/not-afk 结论，不存原始输入时间戳**，这个信号必须自己取。

顺带更正一条过时记录：AWJ 的 `aw_atoms.py` 注释说本机"没有 aw-watcher-afk.toml"。文件其实存在（`%LOCALAPPDATA%\activitywatch\activitywatch\aw-watcher-afk\aw-watcher-afk.toml`），只是所有设置都注释着走默认值——**旋钮一直在那儿，只是关着**。

#### T6：afk 桶在「尚未判定」的那段时间是**空的**（最严重的一个）

**2026-07-27 实机撞到**：用户在 B 站看《经济学原理》（曼昆），窗口标题明明含「经济学」，
表盘却一格都不长，日志显示 `0.0/10 分钟  无数据 0.2 分` —— 既不算专注，也不算离开，
而是「AW 没数据」。

根因是 T5（afk 回溯写入）的另一半，当初没想透：

```
22:32:16  最后一次动键鼠 → not-afk 事件到此为止，不再延长
22:32:16 ~ 22:35:16      → afk 桶里【什么都没有】，AW 还没判呢
22:35:16  超时 180 秒，AW 才写下 afk 事件，并【回填】到 22:32:16
```

**只要人安静下来，afk 桶的末端就出现一个最长 180 秒的空洞。** 而 §6.1.1 那条
「afk 缺数据不能当成在座」把这个空洞也一并打成了 `Gap` —— 可那条规矩防的是
「afk watcher 根本没在跑」，不是「AW 还没来得及判」。

**后果**：坐着看学习视频的头三分钟，表盘一格都不长。而看视频恰恰是最不需要动键鼠的
学习方式，也正是这个项目 §0.1 已经吃过一次亏的地方。

**修法**：区分两种「没有 afk 事件」——

| 情形 | 判定 |
|---|---|
| 末尾那条 **not-afk** 刚结束、还没到超时（180 秒） | **尚未判定**，延续为在座 |
| 一条 afk 事件都没有（watcher 没启动） | `Gap`（fail-closed，不变） |
| 空洞超过超时还没等到 afk 事件（watcher 死了） | `Gap` |
| 空洞前面那条是 afk | `Gap` |

**这个「暂定」会自我纠正，所以没有打开作弊路径**：真走开了，180 秒后 AW 会把 afk
事件回填下来，下一轮重放就正确判成 `Absent`。最终账目不受影响 —— 跟 §6.2
「临时不可达不影响最终结果」是同一个道理。

**实现上有个坑**：分类是按区间**起点**算的，而「暂定期」只有 180 秒。所以必须把
**每条 not-afk 事件的 `End + 180 秒`也加进边界集合**，否则那条事件之后的整段空洞会被
它起点处的判断整个裹走（实测：一段 9 分钟的空洞全被当成在座）。

#### `AddSeconds(double)` 会把值四舍五入到毫秒

.NET 的 `DateTimeOffset.AddSeconds(double)` 是**按毫秒**取整的（文档行为），而 AW 的时间戳是**微秒**精度。于是每条事件的终点偏差最多 0.5 毫秒，相邻事件不再严格首尾相接，重放切出一堆亚毫秒碎片区间，**连续的偷懒段合并不起来**。

症状极隐蔽：同一段 22 分钟历史，偷懒**总时长 11.9 分钟是对的**，但"偷懒次数"从 8 次膨胀成 **54 次**。只有次数错，很容易被当成"确实切换得很频繁"。

修法：`Start.AddTicks((long)Math.Round(DurationSeconds * TimeSpan.TicksPerSecond))`。

### 14.5 原子清单

| 层 | 原子 | 职责 |
|---|---|---|
| **L0**<br>有 I/O | `Probe()` | 探活，返回 hostname/版本或失败原因。提交任务前用（§6.2 拒绝提交） |
| | `FindBucketId(type)` | 按 bucket 的 `type` 字段找 id，缓存，失效重发现 |
| | `FetchEvents(id, since, until)` | 区间查询。**放宽由调用方负责**（§14.2） |
| **L1**<br>纯 | `Clip(start, dur, since, until)` | 把一条事件裁到半开区间；不相交返回 `null` |
| | `Boundaries(...)` | 收集所有边界并排序去重：`startedAt`、`until`、两个 bucket 每条事件的两端（§7 第 2 步） |
| | `CoveringAt(events, t)` | 在已排序事件里找覆盖时刻 `t` 的那条；找不到返回 `null` → `Gap` |
| **L2**<br>纯 | `RuleMatches(rule, app, title)` | 单条规则：`app` 与 `title` 都写则求与，缺省即通过（§5.2） |
| | `GroupMatches(group, app, title)` | 组内**任一**规则命中即命中；`disabled` 的组永不命中 |
| | `Classify(app, title, activeGroups, rules)` | §5.3 的四步顺序；**自身豁免硬编码在第 1 步，不走配置**。`activeGroups` 是全段统一的并集（§5.4），不随时刻变 |
| **L3**<br>纯 | `Replay(task, winEvents, afkEvents, now)` | §7 全部八步 → `TaskState`。**无 I/O、无时钟，`now` 是参数** |
| | `ToMinuteCells(state)` | 投影成每分钟一格（§8.2.3）。**是投影，不是逐分钟累加的数组**（§0.4.2） |
| **L4**<br>不在 Core | ANSI 色块 | `ItamiTimer.Cli` |
| | 表盘 | `ItamiTimer.App` |

**`Replay` 与 `ToMinuteCells` 之间那条线，就是 §8 第四条纪律「状态与渲染分开」的落点**——命令行和表盘消费同一个 `MinuteCell` 列表，一个渲染成终端色块，一个渲染成色环。

L1~L3 全是纯函数：喂一份任务记录 + 两个合成的事件列表 + 一个 `now`，断言输出。**不需要 HTTP、不需要真的等 25 分钟**，能穷举测试所有边界（这是 v3 模型在可测性上的最大收益，测试项目 `tests/ItamiTimer.Core.Tests` 已就位，xUnit）。

---

## 15. macOS 移植（2026-07-28）

> **一句话结论**：判定层"完全平台无关"这条从 v1 一路延续下来的设计，在实测里成立了 ——
> **`Core`、`Cli` 和 80 个测试一行没改就在 macOS 上全过**，全部工作量在第 8/9 层。
> 但界面层比预想的多一处：**AW 上报的应用名不是免费的**，见 §15.3。

### 15.1 目标框架：去掉 `-windows`

`ItamiTimer.App` 的 TFM 从 `net10.0-windows` 改成 `net10.0`。

那个后缀对本项目**从来没起过作用**：§8.0 的 csproj 注释里早就写明「net10.0 挡不住 P/Invoke」，
所以它换来的只有平台分析器上下文。去掉之后收益反而是正的 —— **CA1416 开始真的报没守住的
平台调用**，移植第一次编译吐出的那 15 条警告，恰好就是完整的待办清单：

```
Sound.Play / Sound.Available / Sound.PreferredOrFirst
Tick.Play / Tick.Stop
Win32Topmost.Set
Confirm.DisableMinimize
```

一条不多、一条不漏。**不要把 `-windows` 加回去。**

### 15.2 平台差异的落点

每一处差异都收口在**单个文件**里，没有散落的 `#if`：

| 文件 | Windows | macOS |
|---|---|---|
| `Sound.cs` | `C:\Windows\Media\*.wav` + winmm `PlaySound` | `~/Library/Sounds` → `/Library/Sounds` → `/System/Library/Sounds` 三级 `*.aiff`（高优先级同名文件盖住低的，这是系统约定）+ AudioToolbox |
| `Tick.cs` | 合成的 PCM 走 `SND_MEMORY`，不落盘 | **合成那半段一个字不差**（纯算术，字节流通用）；AudioServices 只认文件 URL，所以先落一次临时 wav |
| `MacAudio.cs` | — | `AudioServicesCreateSystemSoundID` + `PlaySystemSound`，SoundID 建一次留着 |
| `InputIdle.cs` | `GetLastInputInfo` | `CGEventSourceSecondsSinceLastEventType(0, 0xFFFFFFFF)` |
| `WindowPin.cs`（原 `Win32Topmost.cs`） | `SetWindowPos` + `SWP_NOACTIVATE` | Avalonia 的 `Window.Topmost` |
| `AppData.cs` | `%LOCALAPPDATA%\ItamiTimer` | `~/Library/Application Support/ItamiTimer` |
| `ChromeIcons.cs` | 两边共用（新增） | — |
| `Confirm.cs` | 清 `WS_MINIMIZEBOX` | **什么都不用做**：macOS 的模态窗口天生没有最小化按钮 |

几条判断的理由：

- **滴答不用 `afplay`。** 它是每秒一次的：子进程一小时 fork 三千六百次，而且 afplay 几十毫秒的
  启动延迟加在一声 35 毫秒的"咔"上，钟会听起来像在喘。AudioServices 是进程内调用。
- **`Tick.Stop()` 在 macOS 上是空操作。** 系统音接口没有"停"这个动作。代价正好是 Windows 那边
  注释里早就写着的那句 —— 最多多响 35 毫秒。
- **`AppData` 不能直接用 `SpecialFolder.LocalApplicationData`。** .NET 在类 Unix 上把它映射到 XDG 的
  `~/.local/share`，那是 Finder 里看不见的隐藏目录；而 §8.1 那条三级查找链**只读不写**，
  本来就指望用户自己去编辑那份 `rules.json`，藏起来等于把路堵死。
- **右上角两个图标改成矢量绘制。** 原来是 Segoe Fluent Icons 的字形，那个字体只有 Windows 装机
  自带，macOS 上是两个豆腐块。改完反而把纪律补齐了 —— 图标字体本来是"仓库里不放位图"
  这条链上唯一一个外部美术依赖。

### 15.3 ⚠️ 自身豁免：`Application.Name` 是正确性的一部分

**这是移植里唯一一个真正的坑，而且是静默的。**

§5.3 第 1 步的自身豁免靠 AW 上报的 `data.app` 认出自己。Windows 上那是 exe 名，所以
`AssemblyName` 钉死成 `ItamiTimer` 就够了。**macOS 上不是**：

```
data.app  ==  Avalonia 的 Application.Name
          ≠  进程名
          ≠  Info.plist 的 CFBundleName
```

不设 `Name` 时 Avalonia 默认叫 **`Avalonia Application`** —— 2026-07-28 实测，AW 就老老实实这么记了
下来（`app='Avalonia Application'`, `title='一袋米要扛几楼'`）。而 `SelfApps` 里那四个名字一个都对不上，
**自身豁免直接失效**，触发的正是 §5.3 注释里警告的那个死循环：用户看一眼进度 → ItamiTimer 成为
活跃窗口 → 判成 OffTask → 格子变红。

修法是在 `App.axaml` 上设 `Name="ItamiTimer"`。**绝不是**往 `SelfApps` 里塞 `Avalonia Application` ——
那会把**所有** Avalonia 应用一起豁免掉，等于开了一个后门。

这个坑值得单独记一笔的理由：它**不报错、不崩溃**，只是安静地把账算错，而 Release 下日志是关的
（§8.3.8）。要不是移植时特意去查了一次 AW 实际记了什么，它会一直藏着。

### 15.4 必须打成 `.app`

`pack-macos.sh` 做四件事：发布（限定 RID）、从 `TomatoIcon` 导 `.iconset` 再用 `iconutil` 压成
`.icns`、组 bundle、ad-hoc 签名。图标仍然是**代码画出来的**，仓库里不放位图。

除了图标和 Dock，bundle 还解决一个很能骗人的问题：**框架依赖的 apphost 要找到 .NET 运行时，
而双击启动的 GUI 应用不继承 shell 的环境变量**。.NET 装在 `/usr/local/share/dotnet` 之外时，
在终端里跑得好好的二进制，从 Finder 点开就是「You must install .NET to run this application」。
脚本用 `LSEnvironment` 把 `DOTNET_ROOT` 写进 Info.plist 解决 —— 路径在**打包这一刻**定死，
所以挪动 .NET 安装位置之后要重新打包。

### 15.5 规则文件：两个平台写在同一份里

AW 报的 app 名两边完全不同：

| | Windows | macOS |
|---|---|---|
| 锁屏 | `LockApp.exe` | `loginwindow` |
| 文件管理器 | `explorer.exe` | `Finder` |
| 开始菜单 / 聚焦 | `StartMenuExperienceHost.exe` | `Spotlight` |

但**不拆文件**：一条在另一个平台上永远匹配不上的正则是无害的，拆了反而要维护两份、还会漏同步。
这也正是 §8 第三条纪律「同一组里同时写 Windows 和 macOS 的 app 名」的落点。

macOS 那段名字**取自本机 aw-watcher-window 的真实上报，不是猜的**。加之前实测过代价：
一段 4 分钟的干跑里 `Finder` 1.5 分钟被算成偷懒。

### 15.6 实测记录（2026-07-28，M1 Mac mini / macOS 26.5.2 / arm64）

| 验证项 | 结果 |
|---|---|
| `dotnet test`（未改一行代码） | **80 / 80 通过** |
| `itami replay` 拿真实历史干跑 | 通过，账单正确 |
| `--dial-specimens` 离屏渲染 | 8 张全对（木框、落影、色带、螺旋内缩） |
| 中文字体 | 正常（Skia 走系统 fallback，无需配置） |
| 矢量图标开 / 关两态 | 正常 |
| `.icns` → Dock 图标 | 正常 |
| 滴答声（合成 → 临时 wav → AudioToolbox） | 正常 |
| 通知音列表 + 试听 | 14 个系统音全部列出，默认挑中 Glass / Submarine / Tink |
| 真机跑一轮 10 分钟任务 | 灰弧、红格、放弃确认、色环撤除全部正确 |
| **自身豁免** | **正确**：CLI 独立重放交叉验证，偷懒清单里只有 `Claude`，ItamiTimer 自己判 Neutral 计入 |
| 从 macOS 交叉编译 win-x64 | 通过，产出正确的 PE32+ GUI 可执行文件 |
| 三个调试出口不再需要图形会话 | 通过（§15.7）。`ssh localhost` 能跑完整 `pack-macos.sh`；`libAvaloniaNative.dylib` 不再被加载 |

**尚未验证**：专注达成那一声和休息阶段（要真的连续专注满 10 分钟才会走到，本轮没等）。
代码路径跟已验证的试听是同一条（`Sound.Play` → `MacAudio`），但**达成 → 休息 → 结束**
那段状态流转在 macOS 上确实还没跑过。

### 15.6a 实测记录（2026-07-28 晚，Codex-Win11 / Windows 11 26200 / x64）

`105183d` 那七处 Windows 专属路径是**在 macOS 上重构、交叉编译出 exe 但从未运行过**的。
这一轮在 Windows 上逐条跑通：

| 验证项 | 结果 |
|---|---|
| `dotnet build` / `dotnet test` | 零警告，**80 / 80 通过** |
| 三个调试出口走 headless 后仍然渲染正确 | 8 张样张全对；`--export-icon` 产物与仓库里的 `tomato.ico` **逐字节一致** |
| `Sound.cs` 的 `Library` / `Resolve` 重构 | 设置里三个下拉框列出真实音色（`Alarm01` / `Windows Unlock` / `Windows Message Nudge`），说明枚举 `C:\Windows\Media` 成功 |
| `Sound.Play`（winmm，换音色触发） | 无异常 |
| `Tick.Play`（`SND_MEMORY` 合成，拖音量滑块触发） | 无异常 |
| `WindowPin.Set`（`SetWindowPos` + `SWP_NOACTIVATE`） | 置顶生效；**焦点未被抢**（见下） |
| `Confirm.DisableMinimize` | 确认框 `WS_MINIMIZEBOX` = false，最小化与最大化按钮都不在，只剩 × |
| 确认框 `WS_EX_TOPMOST` | true —— 主窗口被图钉钉住时模态框仍压得住 |
| `ChromeIcons` 矢量图标（28px） | 喇叭 / 图钉两态都画得对 |
| `InputIdle.GetLastInputInfo` | 通过：`19:24:00.659  No input for 77s, nudging (in another 103s this time is written off)` —— 落在整分钟、77s 在 `[60,180)` 窗口内、余量 180−77=103 也对 |
| 按钮文字垂直居中 | `Start` / `Give up` / `Yes` / `No` 四个都正常 |
| 计时点对齐 | 日志落在 `19:17:00.813` / `19:18:01.005`，误差 ≤1 秒 |
| 休息时长 | 承诺 25 分钟 → `break 6 min`（⌊25/5⌋+1，§8.4.2） |

⚠️ **只能证明没抛异常，证明不了声音真的从喇叭出来** —— 那需要人耳，自动化测不了。

空闲督促那条花了三轮才验成，原因值得记：**这台机器有人在用**，每个计时点之前空闲都被
真实输入清零，从没进过 `[60, 180)` 那个窗口。先直接量 `GetLastInputInfo`（读到 15.8 秒）
才看清是测试条件没满足，不是功能坏了 —— 换句话说，**这一条只能在机器真的闲着的时候验**。

**一个差点被误判成 bug 的观察，值得记下来。** 用 UIA 的 `InvokePattern.Invoke()` 点图钉，
测出"焦点被抢走了"。但这是**测试工具的假象**：换成点喇叭（它的处理函数完全不碰窗口层级）
同样会把窗口拉到前台，说明抢焦点的是 UIA 的 `Invoke()` 本身。单独调用 `WindowPin` 用的那组
标志（`HWND_TOPMOST | SWP_NOACTIVATE`）时，置顶状态翻转而前台窗口不变 —— `SWP_NOACTIVATE`
是生效的。**用 UIA 验"抢不抢焦点"这类问题时，必须先用一个不碰该行为的控件做对照。**

---

### 15.7 三个调试出口改走 headless（2026-07-28 晚）

`--dial-specimens` / `--export-icon` / `--export-iconset` 原来都调 `BuildAvaloniaApp().SetupWithoutStarting()`，也就是走 `UsePlatformDetect` —— 那会去**初始化原生窗口平台**。

可这三条路径只往 `RenderTargetBitmap` 上画，**压根不需要窗口**。多出来的这个依赖在有图形会话的机器上看不出区别，一旦没有（CI、纯 ssh 会话）就崩：

```
Avalonia.Native was not able to start the RenderTimer   (-6661)
```

**这条不是洁癖，是真的会拦路**：`pack-macos.sh` 里那句 `--export-iconset` 是**构建的一环**，意味着「打包」这件事依赖有没有人登录着桌面。放到 CI 上必然栽。构建步骤依赖图形会话本来就是错的。

改法：加 `Avalonia.Headless` 包引用，这三条路径换成

```csharp
AppBuilder.Configure<App>()
    .UseHeadless(new AvaloniaHeadlessPlatformOptions { UseHeadlessDrawing = false })
    .UseSkia()
```

`UseHeadlessDrawing = false` 是关键的一半：headless 平台**默认连绘制都是空的**，关掉它再挂 `UseSkia`，画出来的才是真实像素、跟正常启动同一个后端。**正常启动那条路径一个字没动**，仍然是 `UsePlatformDetect`。

#### 验证（结论都是测出来的，不是推断的）

| 手段 | 结果 |
|---|---|
| `--export-icon` + `--export-iconset` 共 11 个产物的 SHA-256 | **改前改后逐字节一致** —— 渲染结果没有任何变化 |
| 8 张表盘样张 | **不能用校验和比**：盘上画着当前时刻的指针，同一个构建连跑两次哈希就不同（实测）。改为看图确认 |
| `DYLD_PRINT_LIBRARIES` 对比加载的原生库 | 调试出口只加载 `libSkiaSharp.dylib`；正常启动才加载 `libAvaloniaNative.dylib` |
| `ssh localhost` 跑完整 `pack-macos.sh` | 通过 |

⚠️ **第四行那个测试的证明力有限，别被它骗了**：本机图形登录着的时候，同一用户的 ssh 会话仍然够得到 window server —— 改之前的旧二进制在 `ssh localhost` 下**照样能跑**，原始崩溃在本机复现不出来。真正的证据是第三行：`libAvaloniaNative.dylib` 在调试出口上根本不再被加载，而 `-6661` 就是从那个库里抛的，**故障模式从结构上消失了**。要复现原始崩溃得找一台没有人登录桌面的机器（真 CI）。

## 16. 界面语言：英文（2026-07-28 傍晚）

用户原话：「除了玩梗的『一袋米要扛几楼』，其余全部用英文，实际上不是多语言版，是用英文版来扩大本程序的使用范围。」

所以这**不是 i18n**：没有 `.resx`、没有卫星程序集、没有语言切换开关、没有 `CultureInfo` 判断。英文**直接写死**在代码里，跟原来中文写死的方式一模一样。要加第二种语言的时候再搭架子，现在搭是提前优化。

### 16.1 四条边界

| 范围 | 语言 | 理由 |
|---|---|---|
| 界面文案（XAML + code-behind） | 英文 | 用户看得见 |
| CLI 输出（账单、图例、帮助） | 英文 | 同上 |
| 日志（`Log.Info/Warn/Error`） | 英文 | §8.3.8 之后 Release 也写，出问题时是给人看的 |
| Core 的异常消息 | 英文 | 会冒到 CLI 的 stderr 和日志里 |
| **窗口标题 / 确认框标题** | **中文** | `一袋米要扛几楼` 是**产品名**，是个梗。翻译它是起名决策，不是字符串替换 |
| **`rules.json`** | **中文** | 见下 |
| 源码注释、`CLAUDE.md`、本文档 | 中文 | 开发记录，不是产品 |

### 16.2 为什么 `rules.json` 不翻

它是**用户数据**，不是界面文案。三条具体理由：

1. `{ "title": "经济学" }` 匹配的是**窗口标题**。翻成 `"Economics"` 之后，本机那本《曼昆经济学原理第五版》就再也匹配不上了——规则的语言由**用户读什么书、用什么软件**决定，跟界面语言无关。
2. 组名（`学习经济学`）会原样显示成勾选框的标签。它是用户自己起的名字，程序没有资格改。
3. `ignore` 名单里是 **app 名**（`Finder`、`explorer\.exe`、`loginwindow`），本来就没有"语言"这回事。

换句话说：**界面语言和规则语言是两根独立的轴**。一个英文界面的用户会自己写一份英文的 `rules.json`，那是他的数据，不是我们的文案。

### 16.3 ⚠️ 一个未验证的风险：`ignore` 可能跟着**系统**语言变

macOS 的 aw-watcher-window 上报的 app 名来自 `NSWorkspace` 的 **`localizedName`**——名字里就写着 localized。所以中文系统下 `Finder` 很可能上报成 `访达`、`System Settings` 上报成 `系统设置`，而现有 `ignore` 名单会**整段失效**。

症状会跟移植 macOS 时撞到的那次一模一样：`Finder` 的 1.5 分钟被算成偷懒（§15.5）。

**本机是 en-US，没法证实**：`Finder.app` 的 `Info.plist` 里 `CFBundleDisplayName` 就是 `Finder`，`zh_CN.lproj` 下也没有 `InfoPlist.strings`，所以"访达"那个名字是从别处来的。**必须在中文系统上实测才能定论。**

记在这里是因为它有两个容易被忽略的性质：

- 它影响的是**算账正确性**，不是好不好看；
- 它跟界面语言**完全独立**——一个用英文界面的用户跑在中文 macOS 上照样中招。

### 16.4 绝不能"顺手本地化"的一个东西

`App.axaml` 的 `Name="ItamiTimer"`。它是 §5.3 自身豁免的依据（§15.3 详述）。做语言相关的改动时，"把所有面向用户的名字都本地化"是个很自然的冲动，而这一个改了**不报错、不崩溃，只是安静地把账算错**。

同理还有 csproj 的 `AssemblyName`。
