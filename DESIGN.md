# ItamiTimer（一袋米要扛几楼）—— 系统设计

> **本文是唯一的设计文档**：第一部分是当前实现的系统设计，第二部分是已知 Bug，
> 第三部分是待办需求。**已经解决的条目只留一行结论**——过程在 git 里。
>
> 配套：[`DECISIONS.md`](./DECISIONS.md) 护栏清单（被推翻的方案、知情接受的代价、
> 「不要翻案」）；[`README.md`](./README.md) 英文对外介绍；[`CLAUDE.md`](./CLAUDE.md) 工作规则。

---

# 第一部分 · 当前设计

## 1. 定位：番茄工作法 + 客观审计

番茄工作法的骨架：选一个任务 → 承诺一段专注 → 挣得一段休息 → 停下。它的两个弱点是
计时器量的是**流逝时间**而非**专注时间**，且合规全靠自觉。

ItamiTimer 保留骨架，把自觉换成审计：**只有窗口活动命中所选小目标、且人在座的时间
才计入**。审计数据来自本机 ActivityWatch（AW）的事件历史，程序自己不做行为监测。

偷懒的后果不是警告、不是作废，而是**任务被拖长**——表盘上灰色的截止弧往前滑。

## 2. 核心原则

0. **AW 是底座，不是零件。** 判定输入只来自 AW 本地 REST API，不碰系统原生窗口 API。
1. **任务一旦提交就已确定。** 起点固定、时长锁定。程序**永远不代替用户提交任务**。
2. **计时完全基于 AW。** 程序自己不累加时间。
3. **窗口是外在表现，进程即任务。** 最小化不影响任何东西；**退出程序 = 放弃任务**。
4. **状态是推导出来的，不是攒出来的。** 状态 = 纯函数(任务记录, AW 历史, now)。

> ⚠️ 原则 4 的边界（第二版定稿后）：`JudgmentBuffer` 是一块会被增量写入的秒级数组，
> 不再每轮从头重放，靠「每个计时点重写最近 4 分钟」自我纠正。第一版让步过了头——
> 连**达成时刻**都是从这块可变账本上重新推导的，账本一改它就往回跳（§15.1）。
>
> 第二版划清了界线：**账本是推导的，决定不是。** 承诺弧、色块、剩余缺口每拍从
> buffer 重算（原则 4 成立，且不再需要任何额外状态）；而「专注达成」是一个**事件**
> ——它发生在算出缺口 ≤ 0 的那一拍，记下来就不再重算。

## 3. 判定模型

**没有退化模式**（2026-07-31 删除）：程序永远是约束模式，界面不随 AW 在不在而变形。

六个状态码。**值的大小就是覆盖优先级：值越小，画得越晚、越权威。**

| 码 | 名 | 含义 | 计入专注 | 盘面 |
|---|---|---|---|---|
| 0 | `Init` | 还没画过 | ✘ | 不画 |
| 1 | `Gray` | 预计还要花的时间（承诺弧） | ✘ | 灰 |
| 2 | `Afk` | AW 的 afk 说人不在 | ✘ | 虚线空心框 |
| 3 | `OffTask` | 有窗口事件，不命中小目标 | ✘ | 红 |
| 4 | `Focused` | 有窗口事件，命中小目标 | ✔ | 绿 / 橙 / 琥珀 |
| 5 | `AwOffline` | 这一秒 AW 没有任何记录 | ✔ | 同上 |

> ⚠️ **这张表排的是覆盖优先级，不是「对用户有利的程度」。**
> 按有利程度排，`Afk`（空白、不怪你）本该排在 `OffTask`（红、全怪你）之上；但 `Afk`
> 必须**最后画**才能盖住 `OffTask`（人不在的时候窗口是什么无所谓），所以它的值必须
> 更小。谁按「有利程度」去调换这两个，afk 优先级会**悄悄失效且不报错**。
>
> 代码里请按下面这条不变量写，不要列举具体码值：
>
> **计入专注 ⟺ 码 ≥ `Focused`(4)**

`Init` 和 `Gray` 不参与覆盖顺序——它们是「还没画过」和「还没走到」，永远不会去盖别人。

> **盘面上「空心」的东西只有一个**：虚线描边、不填充、满高的框 = `Afk`。
> 容易混是因为它刚刚换过手——第一版里虚线框属于 `gap`（AW 没数据）、而 `Afk` 是
> 什么都不画：
>
> | | 第一版 | 第二版 |
> |---|---|---|
> | 虚线空心框 | `gap`＝AW 没数据 | **`Afk`＝人不在** |
> | 什么都不画 | `Afk`＝人不在 | `Init`＝没画过 / 承诺弧之后 |
>
> 换手的原因是「AW 没数据」这个类别没有了（§3.1 判 `AwOffline` 且计入），虚线框空
> 了出来。给 `Afk` **空心**而不是实心：有形状、没实体，读作「这段时间存在，但不属于
> 任何一边」。

### 3.1 「没有任何记录」为什么算专注（知情的 fail-open）

AW 拿不出这一秒的数据，可能是它自己的毛病（服务没起来、查询超时、心跳还没落盘），
不该让用户替它挨罚。所以 `AwOffline` **计入**，而且「整拍连不上」和「连上了但这一秒
没记录」共用同一个码——见 §4.4，这两件事在覆盖语义上是同一件事。

**代价如实记**：`aw-watcher-window` 死掉而 `aw-server` 还活着时，每一秒都没有记录 →
全判 `AwOffline` → **盘面全绿、白拿满分**。`FindBucketId` 只检查 bucket 存在，而
bucket 一旦建过就永远存在，所以 watcher 死了照样找得到。**判定逻辑本身不堵这个洞**
（用户 2026-08-02：应用只是辅助，故意偷懒不予考虑），只在 `TaskSession` 加了一层
诊断日志，见 §16.5。

### 3.2 小目标是单选

Radio（2026-07-31 由多选 CheckBox 改），Start 之后锁定不可改——连带删掉了
「中途补勾、追溯生效」那一整套语义（`TaskRecord.GroupChanges` 已删）。
命中即 `Focused`，其余一律 `OffTask`（fail-closed，`Neutral` / `ignore` 名单 /
自身豁免全部删除）。

## 4. 判定引擎（第二版，2026-08-02 定稿）

> 第一版逐秒查找留下的三个 Bug——休息起点回退、`IsFocusComplete` 与 `FocusCompletedAt`
> 口径分裂、承诺弧另算一套——在这一版里**是被消解的，不是被修补的**。

### 4.1 存储

`byte[7380]` = 180 秒 padding + 7200 秒绘制区（120 分钟 = 表盘两圈）。
`buffer[i]` 对应绝对时刻 `WallClock + i 秒`，`buffer[180]` = 任务起点。

- **padding 的唯一用途**：第一拍要查起点前 3 分钟，那段数据得有地方落。它**永不计入、
  永不绘制**，`CountFocused` 从 180 开始数。
- **绘制区 7200 秒不是内存考虑，是画图考虑**：钟面一圈 60 分钟，螺旋只留两圈（§8），
  所以能画的就是 120 分钟。超出部分靠归档滚动（§4.4）。
- 类型用 `byte`（或 `enum : byte`），不要 `int`——6 个取值的小枚举，`int[]` 只会让
  「这是个枚举」这件事从类型上消失。

### 4.2 每个整分钟做的五件事

**触发严格锚在整分钟**：`FloorToMinute(now) > 已处理过的分钟`。查询区间、写入偏移
**全部从整分钟算，不碰 `now` 的亚秒零头**——否则每拍相位不同，同一个 buffer 秒会被
相差近 1 秒的两个采样点各写一次，边界秒来回翻面；AW 响应慢 10 秒时写入位置还会整段
错位。

```
1. 覆盖   §4.3   把 [整分钟−4min, 整分钟) 这 240 秒重画一遍
2. 归档   §4.4   ElapsedSeconds ≥ 7200 才做
3. Gray   §4.5   重算承诺弧
4. 达成   §4.5   缺口 ≤ 0 就是达成，时刻 = 这一拍
5. 染色   §4.6   投影成每分钟一格，交给渲染层
```

第 3 步顺手就把第 4 步算出来了（缺口就是判据），不需要第二套算式。

**为什么固定 4 分钟窗口**：AW 的 afk 默认 180 秒才出结论并**回填**，取 4 分钟必然覆盖。
代价如实记：用户把 afk 超时调长到 5 分钟的话，会误算 1 分钟的离开时间——已接受。

### 4.3 覆盖算法：分层画，不逐秒查

**不是**「对每一秒去事件列表里找覆盖者」，而是**按码值从大到小分层覆盖**，后画的盖前面的：

```
① 初始化   [上次写入头, 查询窗口起点)  → Init       （没查过，也永远不会查了）
           [查询窗口起点, 这次写入头)  → AwOffline  （查了，AW 没给数据）
② 画 4     命中小目标的窗口事件 → Focused
③ 画 3     其余窗口事件         → OffTask
④ 画 2     status == "afk" 的 afk 事件 → Afk
```

四条规则合起来把好几件事一次办了：

- **一秒里有多条事件**（alt-tab）→ 后画的赢 → `OffTask` 压过 `Focused` → **fail-closed**，
  跟 §3.2 同一条精神。
- **afk 优先于一切** → 它画在最后，天然盖住所有窗口判定。不需要单独写一条优先级规则。
- **AW 整拍连不上 = 事件列表为空** → 初始化完之后没东西可画，结果自然全是 `AwOffline`。
  **不需要单独的兜底代码路径**，这是整套设计里最省事的一处。而且因为初始化只盖新地盘，
  连不上时**只有最后一分钟**被填成 `AwOffline`，前 3 分钟已有的真实判定原样保留——
  「不能用没有信息去覆盖已有的信息」这条规则不用单独写，它是初始化范围的副产品。

**一条要留意的性质**：前 3 分钟只被**重画**、不被清空，所以一秒一旦被判成
`Afk`/`OffTask`/`Focused`，就不会再退回 `AwOffline`；它只会被后来的真实数据改判。
这是刻意的——结论可以修正，但不该退化成「没有信息」。别为了「一致」把整个 4 分钟
都清成 `AwOffline` 再重画：AW 万一返回一份不完整的响应，那就等于把已经判红的秒
一次性抹绿。

三条必须写死的细节：

1. **一条事件画哪几秒**：它**触碰到的每一秒**都画，即 `floor(start)` 到 `ceil(end) − 1`。
   零时长事件按 `duration = 0.001` 处理，正好画满它落进的那一秒。
   边界秒会被相邻两条事件同时认领，那正好交给覆盖顺序去裁决——**归属由优先级决定，
   不由四舍五入决定**。代价是跨秒切换最多算错 1 秒，方向恒定偏严。
2. **画之前裁到窗口范围**。`FetchEventsAsync` 会把"跨进区间的那几条"一并查出来（T1），事件列表里
   会有起点在几小时前的长事件，不裁会往 buffer 外面写。
3. **初始化范围是「上次写入头 → 这次写入头」，不是固定的最后一分钟**（用 `ElapsedSeconds`
   当水位线）。但这段要**在查询窗口边界上切一刀**，两边填的东西不一样
   （2026-08-02 实现时补的，原稿只写了一律填 `AwOffline`）：

   - **窗口之内**（正常运行时就是最后那一分钟）→ `AwOffline`。跟「连不上只写上一分钟」
     是同一条规则（H4），漏三拍以内也全在窗口里、会被事件重画。
   - **窗口之外**（漏得更久，比如机器睡了两小时）→ `Init`，**不计入、不绘制**。
     不能把「我根本没查过」当成「AW 没记录」白送出去，**否则睡一觉就能刷满任务**。
     也不能不管——上一拍的承诺弧还铺在那儿，不清就会被染成「还没走到」。

   这一刀是必须的：`AwOffline` 的语义是「查了，AW 拿不出数据」，那是 AW 的毛病；
   而漏拍是我们自己没查。两件事凭什么同一个待遇。

> **这一版为什么不再需要 T4 的桥接**（`Replay.Bridge`）：旧的逐秒查找问「哪条事件**盖住**了
> 这一秒」，零时长事件的 `[start, start)` 是空区间，答案永远是「没有」，于是标题每秒都在变
> 的窗口（播放器时间码、编译进度、`(3) Gmail`、Claude Code 的转圈动画）整段变成「无记录」。
> 新的问法是「这一秒里**出现过**什么事件」——零时长事件的时间戳实实在在落在某一秒里，
> 照样能画。同一份数据换个问法，洞就没了。

### 4.4 归档：等价于「1 小时前放弃、又立刻重新开始」

`ElapsedSeconds ≥ 7200`（buffer 写满）时滚动一次。**判断对错的标准是这句话**：
它相当于在 1 小时前把任务放弃掉、又在同一刻用剩余目标重新开始。所以被结算掉的
必须正好是「上一个任务的全部时间」：

```
结算  count(≥4, buffer[180, 3780))  ← 正好 3600 秒 = 旧起点到新起点
      DuringSeconds += 这个数         （只进累计统计，见 §11）
      剩余目标     -= 这个数         （唯一的达成判据）
左移  Array.Copy(buf, 3600, buf, 0, 3780)
      → 旧 [3780,7380) 成为新 [180,3780)：新任务的第一小时
      → 旧 [3600,3780) 成为新 [0,180)：新任务的 padding，已结算过，不再统计
清尾  buffer[3780, 7380) = Init
推进  WallClock += 3600；ElapsedSeconds -= 3600
```

**结算范围是 `[180, 3780)` 而不是 `[0, 3600)`**——差的这 180 就是 §15.5 那个 Bug：
偏了以后既把「点 Start 之前」的专注秒算进账，又漏掉 `[3600,3780)` 那 3 分钟，
归档瞬间「还差多少」会跳一下，正负都可能，跳负时快达成的任务会**突然倒退且毫无提示**。

改对之后归档是**完全连续**的：

```
归档前  还差 = 剩余目标 − count(buffer)
归档后  还差 = (剩余目标 − x) − (count(buffer) − x)     ← 恒等
```

**三个量各司其职，绝不能混**：

| 量 | 归档时 | 用途 |
|---|---|---|
| `FocusMinutes` 原始承诺 | **永不变** | **只**决定休息时长（§6） |
| 剩余目标 | 减去已结算的专注秒 | 达成判定 + 承诺弧长度 |
| `DuringSeconds` | 加上已结算的专注秒 | **只**进 per-goal 累计，不参与任何判定 |

「拖得越久休息越少」是激励反向（A8 否过一次），所以休息时长只读原始承诺。

**第一次归档在满 2 小时，之后每 1 小时一次**（归档后 `ElapsedSeconds` 回到 3600）。
任务因此没有时间上限。盘面的代价见 §8.3。

### 4.5 承诺弧与达成：一个量算两件事

**每拍重算，不记状态**（原则 4）：

```
F  = count(≥4, buffer[180, 7380))        ← 不含 padding
D  = 剩余目标 − F，向上取整到整分钟
H  = 180 + ElapsedSeconds                ← 写入头

Fill(buffer[H, 7380), Init)              ← 先清
Fill(buffer[H, H+D),   Gray)             ← 后填，H+D 裁到 7380

D ≤ 0  ⟺  专注达成，达成时刻 = 这一拍
```

**先清后填不能省**：`D` 会变小也会变大。afk 收缩（T5）把原本判 `Afk` 的几十秒改判
`Focused` 时，`F` 增加得比 `H` 前进得多，弧的末端会**前移**；不清就会留下一截残渣，
承诺弧比实际长。每分钟一次 7KB 的 `Fill`，开销可以忽略，但省掉了所有「该清哪一段」的判断。

行为是自动产生的，不用另写规则：

| 这一分钟 | H | D | 弧末端 `H+D` |
|---|---|---|---|
| 全专注 | +60 | −60 | **不变**：弧变短，终点不动 |
| 全偷懒 | +60 | 0 | **+60**：截止弧往前滑一格 |
| 全离开 | +60 | 0 | **+60**：离开也推弧（A7，知情保留） |

**达成是一个事件，不是一个推导值。** 这是第一版 §15.1 那个 Bug 的解药：旧的
`FocusCompletedAt()` 回过头去数格子、找「跨过阈值的那一格」，账本一被重写那一格就移动，
休息起点就往回跳。现在达成就是「这一拍算出 `D ≤ 0`」——不可能回退，也就不需要任何
latch 去钉住它。`FocusCompletedAt()` 整个删除。

顺带得到一条可以拿来自检的不变量：**承诺弧消失的那一刻 = 专注达成的那一刻。**

### 4.6 染色：把 60 个格子压成一块

**投影范围 `[180, H+D)`**（已走过 + 承诺弧），再往后全是 `Init`，不画。
每 60 个一块，先数五个计数：`nFocus`(≥4) / `nOffTask`(=3) / `nAfk`(=2) / `nGray`(=1) /
`nInit`(=0)。**按「等于」数，别写成「大于」**——`count_3` 这种「值 > 3 的个数」读起来
像「值等于 3 的个数」，而且会让后面的分支依赖前一个分支的前提。

```
nFocus > 40  →  绿    高 100%
nFocus > 20  →  橙    高  80%
nFocus >  0  →  琥珀  高  60%
nFocus == 0  →  在 Init / Gray / Afk / OffTask 里取【计数最大】的那个
                平局取【码值大】的（OffTask > Afk > Gray > Init，fail-closed）
                  OffTask  红        高 50%
                  Afk      虚线空心框 高 100%
                  Gray     灰        高 100%
                  Init     不画
```

- **高度下限 50%，绝不取 0**（D2）：零高度会跟「不画」撞车。
- **两层的优先级方向相反，这是对的**：秒级 `Afk` 盖住一切（那是关于*事实*），
  分钟级平局时 `OffTask` 赢（那是关于*怎么呈现两个同样真实的东西*）。不写清楚的话，
  将来一定有人来「统一」它，一统一 afk 优先级就没了。
- **`Gray` 那一档靠 argmax 自然分流**：承诺弧不需要另一套画法，它就是「这一格 60 个
  全是 `Gray`」。`D` 向上取整到整分钟保证了格子不会半 `Gray` 半数据。
- **`MinuteCell` 必须保留原始计数**，只让渲染层去分档。CLI 用这三档（终端里离散更
  清楚），表盘将来想回到连续编码（D1 的木桶短板）时数据还在，不用改判定层。
  代价如实记：`> 40` 意味着**偷懒 19 秒仍然满绿满高**，短板在 41~60 这一段看不出来。
- **色块只为好看，不是账**（用户 2026-08-02）。真正参与判定的只有 §4.5 那个缺口 `D`，
  染色怎么分档都不影响任务什么时候完成。

## 5. 规则文件 `rules.json`

三级查找（`AppData.RulesPath`，绝不只看工作目录）：用户数据目录 → exe 旁 → 工作目录。
允许注释和结尾逗号。正则 `search` 语义。

```json
{
  "groups": {
    "学习经济学": { "rules": [ { "title": "(?i)经济学|曼昆" }, { "app": "EconReader\\.exe" } ] },
    "上季度目标": { "disabled": true, "rules": [ { "title": "Blender" } ] }
  },
  "executeCommand": {
    "windows": [ "explorer", "shutdown /s /t 0" ],
    "macos":   [ "open -a TextEdit", "osascript -e 'tell application \"System Events\" to shut down'" ]
  }
}
```

- 组内规则是**或**；单条规则里 `app` 与 `title` 是**与**。
- 三种形状缺一不可：只写 `app`（用这个工具就算）、只写 `title`（做这件事就算，
  什么工具都行）、都写（卡浏览器标签页）。
- 空规则 / 空组是**加载期错误**（匹配一切 = 关掉约束）。
- `disabled: true` 屏蔽旧目标而不删除。
- 两个平台的 app 名写在同一份文件里，永不匹配的正则无害。
- `executeCommand` 供闹钟的 Execute 用（§9）。按当前 OS 取那一项，**如果是数组就只执行
  第 0 条**（用户 2026-08-02）：这里是一个常用命令的收藏夹，想换用哪条就把它挪到第一位，
  或者把上面几条用 `//` 注释掉——注释在解析时被 Skip 掉，等于从数组里消失，
  下一条自然变成第一条。**没有界面去选**，跟这个程序其余地方一样（D6）。
  单条字符串也仍然认，省得逼老配置改格式。

> **整个文件只有一个类型模型 `RulesFile`、一个解析器**（2026-08-02 统一，见 §15.4）。
> 加新的节就往 `RulesFile` 加字段——别再另起一条读取路径。

## 6. 任务生命周期

```
选一个小目标（Radio）、拖时长滑块、点 Start
  ↓  startedAt = 截断到当前整分钟（09:01:25 → 09:01:00）
  ↓  FocusMinutes 锁定；RestMinutes = ⌈focus/5⌉；立刻画整段灰弧，不查 AW
【专注】每个整分钟：覆盖 → 归档 → Gray → 达成 → 染色（§4.2）
  ↓  某一拍算出缺口 D ≤ 0 → 达成，达成时刻就是这一拍
【休息】画蓝色扇形 [达成时刻, +RestMinutes]；纯本地计时，零 AW 访问
  ↓  圆弧**不清除**（2026-07-31 #11 改）——扇形先画、圆弧后画，z-order 天然正确
  ↓  分针扫出扇形 → 响一声 → 会话终结、清圆弧与扇形
【空盘】停在这里等用户。空盘即邀请。
```

- **休息不被观察**：期间干什么都不重要，不进任何记录。
- 休息中点 Start（显示 "New round"）= 终结当前任务 + 开新一轮。
- 专注中点 Start（红色 "Give up"）或关窗口 → 确认框 → Abandoned。
- 崩溃 / 关机 / 进程被杀 = 放弃。程序区分不了「点了 X」和「被杀」。

### 6.1 休息时长 = ⌈focus / 5⌉（2026-08-02 改）

**只读提交时锁定的 `FocusMinutes`，跟这一轮实际拖了多久毫无关系**——哪怕跨过两次归档，
50 分钟的任务照样歇 10 分钟。归档扣减的是「剩余目标」，那是另一个量（§4.4 的三量表）。

原公式是 `⌊focus/5⌋+1`，那个 +1 有两个职责，现在**两个都没了**：

- ① 补「发现延迟」——旧的休息从**账本里推导出的达成时刻**起算，比发现时刻早最多 60 秒。
  新的达成时刻就是发现的那一拍（§4.5），没有延迟可补。
- ② 保证任何非零时长都有休息——`⌊focus/5⌋` 在 focus < 5 时算出 0。`⌈focus/5⌉` 在
  focus ≥ 1 时恒 ≥ 1，这个补丁不需要了。

所以现在是干净的「精确五分之一」。**正式量程下每一档比原来少 1 分钟**：10→2、25→5、
50→10（原来是 3 / 6 / 11）。这是有意的。

> ⚠️ 旧的 +1 还藏着一个从没写进文档的前提：**滑块只出 5 的倍数**。2026-07-31 把滑块
> 改成步进 1 之后，`⌊f/5⌋+1` 在 8 个取值里破了 6 个（focus=6 时名义休息 1.2 分钟、
> 补偿后实得 1 分钟），而守这条不变量的测试 `InlineData` 还停在 10/25/50，全是 5 的倍数，
> 恒成立。**护栏测试的取值必须覆盖滑块实际能出的值**，否则它守的是一个已经不存在的世界。

### 6.2 时长滑块的量程随构建配置变

| 构建 | 量程 | 用途 |
|---|---|---|
| Release | 10~50 分钟，步进 5，默认 25 | 正式量程 |
| Debug | 3~10 分钟，步进 1，默认 3 | 不用真坐 25 分钟才看到达成那一刻 |

落法：axaml 写**正式**量程，构造函数里 `#if DEBUG` 覆盖成测试量程（`MainWindow.
ApplySliderRange`）——忘了改也只会把正式量程发出去，方向是安全的。2026-08-02 随判定
引擎第二版一起落地。

Core 不设范围（`TaskRecord` 接受任意时长），约束只在滑块上。而且 `RestMinutes` 对
**所有整数**都必须成立，不能只对 5 的倍数成立——否则在 Debug 里验证的不是 Release
的行为，测试量程就白设了。

## 7. AW 接口约定

### 7.1 两个 bucket

| type | 用途 | 缺了会怎样 |
|---|---|---|
| `currentwindow` | 在不在做正事 | 无法判定 |
| `afkstatus` | 人在不在 | **「停在目标应用上走开」完全隐形**——窗口事件靠心跳续期自己长大，事件流不留空白。这是最省力的作弊路径 |

### 7.2 硬知识（全部实测得来，实现时必须遵守）

| # | 事实 | 应对 |
|---|---|---|
| T1 | AW 区间查询只按事件**自己的开始时间**过滤，跨起点的事件静默消失 | 查询窗口向前放宽再自己裁 |
| T3 | AW 窗口事件滞后 `now` 约 6~12 秒 | 末尾几秒恒为空 → 判 `AwOffline`（计入）。**下一拍必然重写它**，自愈窗口 60 秒远大于滞后 12 秒，放着不管即可 |
| T4 | 窗口标题每秒变时产生 `duration=0` 的事件（实测触发源：Claude Code 在 Windows Terminal 里的转圈动画，一个来源就贡献了 3 天 5796 条事件的大头） | **第二版不再需要桥接**：按「这一秒里出现过什么事件」画，零时长事件照样落格（§4.3） |
| T5 | afk 事件**回溯**写入，起点回填到最后一次输入 | 发现时损失已写死 → 唯一有效的提醒是赶在截止线之前自己读键鼠空闲（§10） |
| T6 | 人安静下来后 afk 桶末端有 ≤180 秒空洞 | 4 分钟重查窗口天然覆盖，会自我纠正 |
| — | 系统代理会吞 localhost 请求 | `HttpClientHandler { UseProxy = false }` |
| — | `AddSeconds(double)` 四舍五入到毫秒，AW 是微秒精度 | `AwEvent.End` 用 `AddTicks` |
| — | AW 时间戳是 UTC | **在边界上归一**：`AwClient` 解析时直接 `ToLocalTime()` |

### 7.3 时间模型

- `startedAt` 截断到整分钟 → 每个色块恒为完整 60 秒。
- 查询节拍**锚在整分钟**（`FloorToMinute(now) > 已查过的分钟`），不锚在点击时刻。
  **查询区间的两端和写入偏移量也一律从整分钟算**，绝不掺 `now` 的亚秒零头（§4.2）。
- 计时点可以漏：buffer 按绝对偏移写入，晚一拍只是晚一拍——前提是初始化范围用水位线
  而不是「最后一分钟」（§4.3）。

### 7.4 判断一个「洞」要不要管：AW 以后会不会补上这段数据

盘面上看起来一样的「这一秒没记录」，生命周期完全不同。这条判据一次分清：

| 洞 | 会不会补上 | 结论 |
|---|---|---|
| 每拍末尾 6~12 秒滞后（T3） | 会，下一拍就有 | **自愈，不用管** |
| aw-server 断线期间 | 可能补（`aw-client` 有心跳队列） | 补了按真实算，没补维持 `AwOffline`，两边都对 |
| 零时长事件（T4） | **永远不会**——那些秒本来就不属于任何事件 | 第二版靠「按秒落格」解决，不是靠桥接 |
| `aw-watcher-window` 死掉期间 | **永远不会** | 全判 `AwOffline` = 全绿。已知敞着，见 §3.1 / §16.5 |
| 宣布达成那一拍的末尾 ~10 秒 | 没有下一拍了 | 白送一次 ≤12 秒，可接受 |

### 7.5 AW 内存镜像：把区间事件摊平成每秒一个判定（3.2.0）

AW 记的是 `[start, start+duration)` 的区间；这个程序从判定到画表盘**处处按秒**
（`JudgmentBuffer` 就是一个秒级数组）。今天这个转换在**每次查询之后重做一遍**
（`Judgment.Paint`），于是"想多知道一点"就必须"再查一次 AW"——而查一次的代价是固定的、
跟你要的区间宽度无关（见下），频率一提高就撞墙。

镜像把这个转换**做一次、留下来**：以后任何人问"某一秒你在干什么"，是内存里一次数组
下标，零成本。

**为什么每次查询都那么贵**（实测 2026-08-29）：`AwClient.FetchEventsAsync` 为绕开 T1
（AW 只按事件自己的 start 过滤，跨起点的长事件静默消失）曾经**无条件把查询往前放宽
6 小时**——一次拉回 **337 条 / 65.7 KB**，只为裁出其中 4 分钟。**这一条 2026-08-29 已经
换掉了**，见 §7.6。

**规格**：

| | |
|---|---|
| 长度 | 245 秒 = `JudgmentBuffer.QueryWindowSeconds`(240) + 5 余量 |
| 粒度 | 秒。每格：`JudgmentCode` + 是哪个窗口（app/title，跑偏时写日志用） |
| 只会出现四种码 | `Focused` / `OffTask` / `Afk` / `AwOffline`。`Gray`（承诺弧，还没发生的未来）和 `Init`（buffer 的槽位状态）不属于观测，留在 `JudgmentBuffer` 里 |
| 生命周期 | **点 Start 时创建，任务结束丢弃** |

⚠️ **那 5 秒余量不是可有可无的冗余**：`Cover` 要的是 `[整分钟-240s, 整分钟)`，而镜像覆盖
`[now-240s, now]`，`now` 总是**略晚于**那个整分钟——不留余量的话 `整分钟-240s` 那一秒
刚好掉出环外，**每分钟固定丢最老一秒**，方向 fail-open，不报错。

**为什么在 Start 时创建而不是启动时**：`Focused`/`OffTask` 是**相对于选中的小目标**算出来
的，而小目标恰好在点 Start 那一刻锁定（之后单选框就禁用了）。任务期内目标不可能变，
镜像里的判定码全程有效；换了目标就是新任务、新镜像。副产品：空闲时一次 AW 查询都没有。

**三条规则**（`AwMirror`，Core 纯逻辑，可单测）：

1. **从新事件自己的 `start` 开始重写**，不是从某个固定窗口。AW 的滞后（T3，6~12 秒）和
   afk 的回溯写入（T5，起身 3 分钟后才写、start 回填到 3 分钟前）**被同一条规则一起吃掉**
   ——事件自带 start，重写范围由数据决定，**不需要任何常数**。
2. **推进即清空**：写第 S 秒时，把「上次写到的秒 → S」之间跳过的槽位全部填成
   `AwOffline`。睡眠、卡顿、系统时间被改全由这一条兜住，否则环里的旧数据会冒充新观测
   （跟 §15.6 同一类事故）。
3. **预测分两步：空隙沿用前一秒；末尾用最后一个真实观测整段覆盖，且只在取到新数据
   那一拍重铺。**

   - **空隙**：AW 的事件之间**不是首尾相接的**（实测：切窗口留 ~1 秒空隙，因为 watcher
     一秒一轮询、duration 只覆盖到最后一次确认的轮询）。这些洞沿用**前一秒**。
   - **末尾**：`_observedThrough`（真实画笔画到的最新一秒）到 `Newest` 之间那 3~10 秒，
     拿 `_observedThrough` 那一格**把整段覆盖掉**——不是逐格继承。
   - **节拍**：每秒只做 **O(1)** 的事——`Advance` 把指针挪一格，新格**沿用前一格**，
     外推就是这么发生的；上面①②两步**只在这一拍真的取到了新数据时才跑**。跟 AW 说话
     是 5 秒一次、信息本身 ~10 秒才产生一次，**每秒重算一遍前沿是在假装有每秒的精度**
     （用户 2026-08-31）。实测两种写法输出逐字相同，后者少做 4/5 的无用功。

   ⚠️ **`AwMirror` 里不许出现「5 秒」这个数字**：节拍由 `MirrorFeed.PollSeconds` 决定，
   镜像只看"观测有没有前进"。实测把轮询间隔换成 3 / 5 / 15 秒，这个类一个字不用改，
   最坏延迟 12 / 13 / 23 秒——正好是 AW 自身滞后加上轮询间隔。

   ⚠️ **`MarkUnavailable` 那条路不许沿用前值**：AW 连不上时新的秒一律写 `AwOffline`，
   否则钟面会拿着掉线前的判定一直闪下去（DECISIONS N8：AW 出问题一律回底色）。
   所以 `Advance` 有两种模式，见它的注释。

   预测天然有上限——只往后传、跨不过环的起点，所以 watcher 死掉最多延续 245 秒，
   不会像无上限外推那样画出几小时。

   ⚠️ **末尾那一步必须是「覆盖」，写成「只填还空着的秒」就会炸**（2026-08-31 实机事故，
   用户定位并指定算法）。AW 的 ongoing 事件 duration **恒定落后实时 ~10 秒**，所以环的最前沿
   **永远够不到真实画笔**，只能外推。若外推值写下去就锁死，切窗口那一刻外推的旧判定
   会被永久钉在前沿上：真实事件随后只能改正它身后那段，前沿始终差最后一两格。

   后果只砸在**逐秒读前沿**的消费者身上（钟面闪烁 §8.9、滴答 §10）——账本每个整分钟读
   四分钟的窗口，末尾那十秒早被重画对了，**所以现象是「计时完全正确、只有闪烁瞎」**，
   极难往镜像上想。延迟由 **AW 提交周期(~10s) 与轮询周期(5s) 的拍频**决定，**无上限**。

   拿用户 2026-08-31 那段真实历史离线重放（30 组「提交周期 × 相位」× 6 次真实切换）：

   | | 闪烁「从未跟上」 | 账本 vs 当天 `itami.log` |
   |---|---|---|
   | 旧算法（只填空的） | **59/180**，10.0 秒周期下八个相位整轮全瞎 | 最大差 0.1~0.2 分钟 |
   | 现算法（整段覆盖） | **0/540**（含轮询 3/5/15 秒三档） | 最大差 0.1 分钟 |

   护栏：`AwMirrorTests.切走之后前沿要跟着变_哪怕真实事件永远够不到最后十秒`
   （做过变异测试，撤掉修复当场变红）。

**休息期间不刷新**（2026-08-29）：那时候没有任何人读镜像——分钟节拍在休息阶段早早
return，闪烁被 `!InRest` 挡掉，滴答跟着也是关的。一次 5 分钟的休息因此省下 300 次往返。

**取数：`Core/MirrorFeed`，两个前端共用同一份**（3.5.0）。初始化、每秒推进、按节拍
取数、掉线兜底全在它里面；挂在调用方那唯一的钟上（GUI 是 `MainWindow.OnFrame` 的秒边界，
CLI 是 `itami start` 的秒循环），**不新开定时器**（DECISIONS L8）。Core 不打日志，状态
变化用回调抛给调用方（跟 `Backfill` 的 `progress` 同一路数）。

⚠️ **为什么驱动必须在 Core**：`AwMirror` 是纯转换、本来就共享，但驱动曾经只存在于 App
的 `TaskSession` 里——于是 `itami` 拿得到镜像这个类、却没有喂饱它的代码，只能退回"自己
查 AW → 直接喂事件"的老路，**两个前端走在两条数据路上**。而 §15.7 那次事故的结论正是
"验证工具和被验证对象必须是同一个引擎、同一个节拍"。这跟 DECISIONS L25（`Command.BuildShell`
收成一份定义）是同一个形状，区别是那次带平台味道所以用 `<Compile Include>` link，而镜像
驱动是纯 AW 管道，正经放进 Core 就行。

**节拍：每秒推进，每 5 秒才通信，对齐到 :00 :05 … :55**（判据 `now.Second % 5 == 0`，
读的是墙上时钟，所以 :00 那一格必定有一次轮询，分钟判定用的数据在它跑之前刚被刷过）：

| 每秒 | 把镜像推进到 `now` + 跑预测。**纯内存、零成本**——反色要的 1 秒粒度全靠它 |
|---|---|
| **每 5 秒** | `GET /api/0/buckets/`（**739 字节**，一个请求同时给出"活着没"和"变了没"），`last_updated` 前进了才去 `?limit=N` 拉事件 |

所以对外只有**一个节拍**，不多出时间点；稳态下真正的事件查询约每 10 秒才发生一次。

**5 秒这个数是实测定的**（2026-08-29）：AW 的窗口桶是**批量提交**的——`last_updated`
的前进间隔 40 个样本全部落在 **9.3~11.1 秒**，而且**不受活动驱动**（整整 100 秒没有窗口
切换时照样每 10 秒跳一次）。更硬的一条：捕捉到的那次真实窗口切换，**新事件第一次出现时
`duration` 已经等于它的全部年龄**（10.2 秒 / dur=10.2）——它不是"先以 0 秒诞生再长大"，
而是在提交那一刻**一次性写下、直接覆盖整整 10 秒**。也就是说**信息本身 10 秒才产生一次**，
每秒去问约 90% 的请求换回"没变"。取提交节拍的一半：保证每次提交最多 5 秒内被看到，
不会因为相位不巧而整整错过一个周期。
⚠️ 别改成 10 秒（跟提交周期同量级会撞相位，最坏逼近 21 秒），也别改回 1 秒（5 倍请求
只买到 ~2 秒平均延迟，而地板是 ~5 秒）。

初始化是一次 `[镜像起点, now]` 的范围查询——"跨进区间的那几条"由 `FetchEventsAsync`
自己负责（§7.6），不需要任何放宽。

**谁在读镜像**：`TaskSession.TickMinuteAsync` 通过 `AwMirror.EventsIn` 把秒级内容还原成
事件，喂给原样不动的 `Cover`/`Paint`——**判定链路一个字都没改**，只是数据源从"再查一次
AW"换成了"从镜像里读"。陈旧诊断（§16.5）的判据同时换成了探针的 `last_updated`：
⚠️ 镜像里的秒会被预测填满，拿它判"新不新鲜"**永远为真**，等于把那条诊断安静废掉。

**常驻期只有这一个查询**。例外只有 `Backfill`：它扫的是跨天历史（按 24 小时分块），
245 秒的镜像装不下，走自己的查询，一次性、跑完就扔。（`itami` CLI 是独立进程，不在此列。）

**实测的开销**（2026-08-29，`--mirror-probe` 跑真实链路）：初始化 4 个请求约 530ms，
之后每秒约 **130ms**——而单独 curl 一次 `/api/0/buckets/` 就要 **110ms**，也就是说
**这个时间基本全是 aw-server 自己的响应延迟**，不是我们的开销；而且它是 `await`，
不占 UI 线程。作为对比，改造前每分钟那一次判定查询要拉 **65.7 KB / 337 条**。

**实测依据**（都在 2026-08-29 这台机器上量的，不是推断）：

| 量到的 | 值 |
|---|---|
| ongoing 事件的 `duration` | 每 ~10 秒跳一次（1388.5 → 1398.6 → 停 → 停），所以"已知末端"落后 now 2.7~9.4 秒——T3 那个 6~12 秒的机制就是它 |
| 事件带 `id` | 单调递增，但**跨桶全局**（afk 桶 240294/240296 跟 window 桶 240333 共用一个序列），所以单桶 id 不连续，**没法用连续性判漏** |
| ongoing 事件的 id | **不变**，duration 一直长 → "只读新 id"永远看不到"你还待在当前窗口"，游标解决不了这件事 |
| 同一秒含 ≥2 个窗口的比例 | 3 小时 10795 秒里 **0 条**（事件之间有 ~1 秒空隙，根本不重叠）→ "每秒取最新那个窗口"这个简化的代价是 0 |
| 零时长事件 | 占 18.5%（T4 是真的） |
| afk 桶的脏数据 | 有 **start 相同、duration 不同的重叠事件**（308.1 / 313.1 / 293.0 / 298.0）→ 必须全画，只取最新那条会把在座时间算多 |
| 两个桶的 `last_updated` 节奏 | window 每 ~10 秒前进；**afk 卡住 38 秒还在涨**（只在状态变化或慢心跳时才写）→ 在线阈值**不能共用**，afk 的阈值待实测，在那之前不判死 |

**镜像的边界——这两件事永远在它之外，不是遗漏**：

| | 为什么进不了镜像 |
|---|---|
| **`Backfill`**（点 Start 时算累计时长，§11.2） | 它要的是几小时到几个月，镜像是 245 秒；而且判定方向相反——运行期 fail-open（无记录算专注），回填 **fail-closed**（无记录不计入）。它覆盖的正是"没开任务的那段时间"，恰恰是镜像永远看不到的部分 |
| **键鼠空闲提醒**（`InputIdle`，`[60,180)` 秒） | 它的全部意义是**在 AW 把这段时间写掉之前叫醒你**——比 AW 早 120 秒。而镜像的数据源就是 AW，结构上不可能提前知道（DECISIONS C3：T5 回填写死，事后干预类方案全部无效）。另外 `InputIdle` 只决定何时戳人，**绝不参与记账**，记账的在座与否只认 afk 桶 |

### 7.6 范围查询：头 + 精确区间，没有需要猜的常数（2026-08-29）

T1 说 AW **只按事件自己的开始时刻过滤**，不按区间相交——一条在 `since` 之前开始、延伸
进区间的事件会**凭空消失**。原来的对策是 `FetchEventsAsync` 内部**统一往前放宽 6 小时**。

那个 6 小时**既是猜的、又不可证充分**，两头都不好：

- **不够时不报错**：一个窗口连续保持同一标题超过 6 小时（挂机、长视频、盯着一个 PDF
  过夜）照样整条丢掉。而 `Backfill` 是 **fail-closed** 的（清零成 `Init`，只数画上的），
  丢掉的秒**不计入**——也就是这个洞一直在**少算用户的累计时长**，而且悄无声息。
- **够用时也很贵**：实测一次 337 条 / 65.7 KB；`Backfill` 按 24 小时分块，每块都白拉
  一遍那 6 小时的重叠（按天切 = 1.25 倍冗余）。

现在改成两步，**没有任何需要猜的常数**：

1. **谁跨进了这个区间** —— `?limit=N&end=since`，拿区间起点之前最近的几条。实测 367 字节
   就够，而且对"三天前打开的窗口"同样正确。
2. **区间本身** —— 精确查 `[since, until)`，不放宽。

⚠️ **第 1 步不能只取 1 条**：afk 桶里有 **start 相同、duration 不同的重叠事件**（实测
308.1 / 313.1 / 293.0 / 298.0 秒），只取最新开始的那条可能漏掉更长的那条，于是"人不在"
的一段被算成在座——正是最不能犯的那类错。取 5 条，多取没有代价（越界的部分 `Paint`
会裁掉）。

**实测对照**（4 分钟窗口，2026-08-29 本机）：旧法 220 条事件，新法 **7 条**，
**覆盖的秒完全一致**（232 秒）。

## 8. 表盘渲染规格

几何归一化到 `rFace = 1.0`，12 点为 0°，顺时针，分钟 × 6°。

| 层 | 半径 | 说明 |
|---|---|---|
| 木质边框 | 1.02 | 渐变模拟受光（光源统一在左上）。**两个主题同一套木色，不跟着翻**（§8.8） |
| 盘面 | 1.00 | 跟随主题：日面素白、夜面深灰 |
| 色环 lane 0 | [0.50, 0.68] | 超圈螺旋内缩一次：lane 1 [0.31,0.46]。**只有两圈**（§8.3） |
| 数字 1~12 | 基线 0.70 | |
| 刻度 | 0.90~0.965 | 分钟细、五分粗 |
| 指针 | 时 0.50 / 分 0.72 / 秒 0.80 | 带锥度，投影偏移在指针之前画 |
| 闹钟黄针 | 0.62 | 比分针短、比时针略长 |

- **分针就是写入头**：第 i 格 = 钟面分钟 `[m₀+i, m₀+i+1)`。表盘同时是钟、进度条、
  账本，共用一套坐标。休息倒计时因此免费——分针扫出扇形即结束。
- **木桶短板**：颜色与高度编同一个量，一个给正常视觉，一个给所有人。下限 1/2，
  **绝不取 0**（零高度会和「不画」撞车）。第二版把连续纯度换成 §4.6 那三档
  （100% / 80% / 60%，红 50%）——代价是短板在 41~60 秒这一段看不出来，所以
  `MinuteCell` 里必须保留原始计数，将来想回到连续编码时数据还在。
- **承诺弧不再单独计算**：它就是 buffer 里那段 `Gray`（§4.5），跟色块走同一条投影、
  同一套坐标。灰 22%，**不画径向截止线**（2026-07-27 定：弧自己的末端已经说清楚了）。
- **休息扇形**：从圆心切出的整块，蓝色径向渐变，外缘 0.70。**先画扇形再画圆弧**。

  **2026-08-02 改：任务一开始就画，不等达成**（用户：给一个希望）。起点不是「达成的
  那一拍」这个**历史**时刻，而是承诺弧末端对应的**投影**时刻——`TaskStart + Elapsed +
  Deficit`，每拍跟着缺口重算，构造 `TaskSession` 时就有初值（= 起点 + 专注时长）。
  跟判定引擎同一条原则（原则 4）：不记状态，每拍重算。

  这个投影值天然带一层痛感：拖延时缺口不减、`Elapsed` 照样 `+60`，投影出来的休息
  起点**跟着一起往后挪**——不只是灰弧变长，连挣来的休息也在实时后退。真正达成那一拍，
  投影值恰好等于达成时刻本身（此时 `Deficit=0`），交接给"实际值"时不会跳变。
  达成之后 `TaskSession.RestFrom` 才从"投影"变成"锁定"，不再跟着重算。
- **秒针一秒一跳**，不是连续扫——扫秒针的钟不会响，滴答来自步进擒纵，物理上互斥。
- **骨牌**：七块 = 星期几，倾角按凸多边形接触求解。启动构造、点 Start 时各查一次日期，
  **外加 `OnMinute` 分钟节拍第 ⑤ 步每分钟核对一次**（2026-08-13，取代"只在启动/Start
  时查"，DECISIONS D12：故意保留的"跨午夜不刷新"神秘感被用户直接否掉）。

### 8.3 只有两圈，以及每小时一次的「塌缩」

buffer 的 7200 秒绘制区**不是内存考虑，是画图考虑**：钟面一圈 60 分钟，螺旋只留两圈，
所以能画的就是 120 分钟。原来写的「螺旋三圈、lane 2 [0.14,0.26]」是**够不到的**
——`ToMinuteCells` 最多吐 120 格，`Index/60` 最大是 1。`Lanes[2]` 删掉。

于是归档（§4.4）在盘面上的表现是：**内圈的内容整体跳到外圈、内圈清空**，记录长度从
两小时缩回一小时。**第一次发生在满 2 小时，之后每 1 小时一次**（归档后 `ElapsedSeconds`
回到 3600）。

这不是 bug，是「1 小时前放弃又重开」那个语义的直接后果，用户 2026-08-02 知情接受：
**归档存在的理由本来就是让盘面永远只画最近的一到两小时**。所以圈号就该跟 buffer 位置
绑定（`cell.Index / 60`），归档后跟着外移一圈是对的，不要去「修」它。

### 8.7 窗口本身：无边框 + 透明背景（2.0.0 起）

**这一节讲的是表盘周围那一圈"窗子"，不是表盘内容。** 决策依据在 DECISIONS K 节，
那边记了被排除的两条路（Rainmeter 的 `WorkerW` 桌面嵌入、Windows 11 Widgets 面板）
和中途推翻过一次的方案（曾经做成"另开一扇只有表盘的悬浮挂件"，整个撤销了）。

主窗口自己就是无边框透明的，**所有既有功能一个没少**，只是外框和窗口底色没了：

| | 怎么做的 | 备注 |
|---|---|---|
| 无边框 | `WindowDecorations="None"` | Avalonia 12 起 `SystemDecorations` 标记为过时，用新名字 |
| 透明背景 | `Background="Transparent"` + `TransparencyLevelHint="Transparent"` | |
| 拖动窗口 | `Dial` 的 `PointerPressed`（左键）→ `BeginMoveDrag(e)` | 没有标题栏之后唯一的移动手段 |
| 关闭窗口 | `Dial` 上挂 `ContextMenu`，一项 `Close window`（矢量 X 图标） | 走 `Window.Close()`，任务没结束照样弹 §9 的放弃确认 |
| 可读性 | Start 以下套一张 90% 不透明的浅色卡片；卡片外的三个图标各自描一圈浅色轮廓 | |
| 记住位置 | `settings.json` 的 `windowX`/`windowY` | 见下 |

**拖动的真实命中范围是圆的，不是方的**（2026-08-08 实测，纠正了当初的错误注释）：
Avalonia 对自绘控件的命中测试**按实际画出来的绘制操作逐个判定**，不是按控件的矩形
`Bounds`。所以表盘四个角（矩形内、但一笔都没画过的透明区域）拖不动，而圆盘外沿再往外
一点点能拖——那儿画着表盘投在"墙上"的阴影。同一套机制也解释了为什么 `Panel`/`Border`
想让整个矩形可点必须显式写 `Background="Transparent"`：那个背景本身就是一个绘制操作。
用户看过之后认为圆形更好，**维持现状，不去 override `HitTest`**。

**卡片的几何**（都是现场逐轮试出来的，别当成随手写的数字）：

- 顶边紧贴骨牌，骨牌那一行的下边距是 **`-4`**——布局上的间隔早就是 0 了，那条缝来自
  `DominoRow` 自己的绘制：地面线落在控件高度的 96.5%（`baseY = k.H * 0.965`），76px
  最底下约 2.7px 它自己没画，再加上阴影渐变在底边淡出的尾巴。**这个 -4 跟 `DominoRow`
  的 0.965 绑在一起，那边动了这里要重算。**
- 卡片内部只有一个间距数字：`Border.Padding` 和 `StackPanel.Spacing` 都是 **8**，
  边框到控件的距离 = 控件之间的距离。
- 底色 `#E6D7DBE0`：`#D7DBE0` 是 2.0.0 之前的窗口底色，`E6` ≈ 90% 不透明。

**窗口位置**（`MainWindow.ClampIntoScreen` / `_settle`）：

- 拖动停下来（位置 250ms 没再变）时做两件事：拉回屏幕内、把坐标写进 `settings.json`。
  没存过就走 axaml 的 `CenterScreen`；恢复在 `Opened` 里做（更早的话 `Screens` 拿不到
  可靠信息），**恢复完立刻也过一遍 `ClampIntoScreen`**——上次那块屏可能已经拔掉了。
- ⚠️ **归位只在松手之后做，绝不能在 `PositionChanged` 里当场做**：一边拖一边往回拉，
  窗口就永远被摁在当前这块屏的边界内，到不了"一半以上落在另一块屏上"那个状态，而
  `ScreenFromWindow` 正是按这个判断归属的——结果是**窗口再也拖不到第二块显示器上去**。
- ⚠️ 单位：`Position`/`WorkingArea` 是**物理像素**，`FrameSize`/`ClientSize` 是 **DPI
  无关的逻辑单位**，差一个 `Screen.Scaling`。不换算的话缩放 100% 的屏幕上恰好对，
  125%/150% 就把窗口算小了，而且不报错（跟 H12 的 `Delta/120` 同一族）。

### 8.8 主题：两态，用户切换（3.0.0）

右上角凑成 **2×2**：上排喇叭 + 图钉，下排齿轮 + **主题**。点主题图标在日面/夜面之间
来回切，立即生效，写进 `settings.json` 的 `darkTheme`。

**两态，没有「跟随系统」这一档。** 代价接近于零：3.0.0 之前 `App.axaml` 写的是
`RequestedThemeVariant="Default"`（跟随系统），而读它的 `ApplyTheme()` **只在构造函数里
跑过一次**——启动之后系统主题再变也不会跟着变，所以并不存在一个真在运作的「跟随系统」
功能可丢。反倒是那个半吊子状态会把界面撕成两半：**在深色系统上启动，表盘和骨牌已经是
夜面，而卡片、提示条、Settings 窗口还是浅色的**。3.0.0 起变体由设置显式钉死，系统主题
不再有发言权。

**颜色分三类，各走各的路**：

| 类别 | 谁 | 怎么跟主题走 |
|---|---|---|
| 语义色 | Start 绿 `#2FA36B`、Give Up 红 `#D6453F` | **不跟**。跟表盘"绿=专注、红=偷懒"是同一套语义（§8.2.3），夜面翻掉等于把这套语义拆了 |
| 中性色 | 卡片、Settings 窗口底/卡片/描边、Start 禁用态、提示条底层那份墨、command 等宽字 | `App.axaml` 的 `ThemeDictionaries`（`Itami*` 八个键）+ XAML 里的 `{DynamicResource}` |
| 自绘 | 表盘、骨牌、右上角四个图标 | 代码赋值：`DialControl.Palette` / `DominoRow.Palette`（`AffectsRender`），图标整排重建 |

`ApplyTheme()` 做三件事，缺一不可：① 钉 `Application.RequestedThemeVariant`——Fluent
自带控件（单选、滑块、下拉、开关、默认文字前景）**全靠这一句**跟着翻，Settings 窗口和
Give Up 确认框一起白拿，两张 ThemeDictionaries 也是按这个变体选的；② 刷两个自绘控件的
`Palette`；③ 调 `ApplyChrome()` 重建四个图标 + 右键菜单那个关闭图标——**图标是构造出来
塞进 `Content` 的对象，不重建就不会变**。

**图标的墨和光晕不再是常量**（原来是写死的深墨 `#1B222A` + 白光晕，理由是"窗口背景固定
浅灰、不跟随主题"——这个前提 3.0.0 作废）。取值不新增色号：墨 = `Palette.Ink`（表盘数字
和指针的同一支笔），光晕 = `Palette.Face`（盘面色）。日面素白**恰好就是**原来那个白光晕，
所以浅色一列一个像素都没变；夜面自然拿到深灰光晕。这四个图标坐在卡片**以外**、直接压在
表盘和骨牌上，是全窗口最需要这一层对比的地方。

**木头还是木头**：夜面的骨牌不动（`DialPalette.Dark` 的木色 `DominoSide #937757` 是现成
的，夜面整体压下来之后它是全窗口最亮的一片，用户 2026-08-29 看过预览后拍板维持原样）；
**表盘的木质边框也改回跟日面一模一样**——`DialPalette.Dark` 原来给的是"深色金属"三档灰
（`#6E7A88` / `#444E5A` / `#1E242B`），3.0.0 换成日面那套木色（用户 2026-08-29：钟的木制
边框不要变）。换主题换的是**表盘的照明**，不是把这只钟换成另一只钟：盘面从素白变深灰是
同一块表的日面夜面，边框从木头变金属就成了两件不同的物件——而这只钟的"材质和光"本来
就是照着一张真实木壳挂钟的照片调出来的（`DialColors.cs` 顶部）。

**齿轮那个 `Margin=...,30,0` 作废了**：30 是当年用眼睛量出来的"落在喇叭正下方"（屏幕缩放
让 DIP 和物理像素不是一回事，只能试）。现在下排跟上排用完全相同的结构——同一个 Horizontal
`StackPanel`、`Spacing="2"`、右边距 `-4`——两列是**几何上精确**对齐的，不再靠试。

### 8.9 跑偏时钟面一秒一翻（3.3.0）

专注期间跑偏时，**钟面每秒在两档之间来回翻**——黑、白、黑、白，一直闪到你回来；
不跑偏就是用户自己设的那一档。不弹窗、不出声——跟表盘上的红格和越滑越远的截止弧
同一条路数（§2 原则）。

⚠️ **"闪"是这个功能的全部意义，不是"反色"**。3.1.0 那版做成了稳定反色（跑偏就一直反着），
用户装机后判定**视觉影响太小**——稳定反着的钟面看两分钟就变成壁纸了，抓不住眼睛。
一秒一翻 = 完整周期 2 秒 = **0.5 Hz**，远在光敏性癫痫的 3 Hz 风险区间之外。

**只翻钟面**（`DialPalette.WithFaceFrom`，五个色号）：

| 翻 | 不翻 |
|---|---|
| `Face`、`FaceRim`、`Ink`（数字 + 时针分针 + 轴心）、`Tick`、`Sweep`（秒针） | 木边框（换的是照明，不是换一只钟，DECISIONS M10）；色环的绿/黄/红、休息蓝扇形、闹钟黄针、Alarms 小红圈（**那是账本本身**，反了就把"绿=专注、红=偷懒"拆了）；骨牌；卡片及以下；右上角四个图标；Settings 窗口和确认框 |

`Application.RequestedThemeVariant` **不跟着动**：那是主题，这是提示，两件事。反色只作用于
`DialControl.Palette` 一处（`MainWindow.ApplyDialPalette`）。

**判据：镜像里上一秒是不是 `OffTask`。**

```csharp
var second  = session.MirrorAt(now.AddSeconds(-1));
var offTask = !session.InRest && second.Code == JudgmentCode.OffTask;

var next = offTask && !_inverted;   // 跑偏 → 倒一次个儿；不跑偏 → 回底色
```

**是「翻」不是「设成反色」**：跑偏期间每一秒都把钟面倒一次个儿，所以它在闪；
回到目标那一秒立刻回底色，不留半个相位。

就这么多。所有的难处都在镜像那一层解决完了（§7.5）：AW 的 6~12 秒滞后由预测填上，
afk 的回溯改写由"从事件自己的 start 重写"吃掉，掉线由"那些秒记为无记录"兜住。
**这条路一次 AW 请求都不发**——镜像已经由每秒那次刷新维护好了，这里只是一次数组下标。

读 `now-1` 而不是 `now`：当前这一秒还在被这一拍写，上一秒才是稳定的。

跟着每秒那次镜像刷新走（`MainWindow.RefreshMirror` 的续体里），所以**粒度是 1 秒**；
不新开定时器（DECISIONS L8）。

⚠️ **绝不能写成"不是 `Focused` 就反色"**（这条从初版保留，是最容易踩的坑）：
`AwOffline`（这一秒 AW 一条事件都没有）按约定**算专注**，写成"不是 Focused"的后果是
`aw-watcher-window` 一死整段全是 `AwOffline`，**钟面永久反色而账本这段时间全判绿**
——屏幕和账本对着说反话。`Afk`（人不在，不怪你）同理不该触发。

**AW 出问题一律回底色**，两条路殊途同归：连不上 → 那些秒被记成 `AwOffline` → 不反色；
watcher 死了但服务还在 → 镜像里也全是 `AwOffline` → 同样不反色。理由不是"没数据不好意思
判"，而是**不让提示说谎**——AW 挂了那一分钟本来就会被 fail-open 判成全绿。

**只在专注阶段**：休息（挣来的）、任务结束/放弃、空闲一律底色；结束那一刻立刻收回
（`EndSession`），不等下一秒。

**日志**只在**状态变化**那一下记一行，而且**带上是哪个窗口**——镜像每一秒都存着 app/title，
这条信息是白拿的（跟 §16.5.3 的跑偏归因同一类）。

**响应速度的下限来自 AW，不来自我们**：实测 ongoing 事件的 `duration` 每 ~10 秒才跳一次
（§7.5），所以你切到别的窗口后，AW 要 **3~10 秒**才吐出这条新事件；在那之前镜像的预测说
的是"你还在旧窗口"。改这边的任何参数都突破不了这个下限。

### 8.10 窗口尺寸：标准 / 紧凑两档（3.8.0）

**起因**：在 2560×1440 @123% 上实测 **467×841 物理像素，占工作区高度 61%**——用户嫌它
太占地方。

**只缩表盘和骨牌，控件和字号一个都不动**（用户 2026-09-05 明确要求，包括右上角那四个
图标）。尺寸全部在 `WindowLayout`，**XAML 里一个都不写死**：

| | 标准 | 紧凑 |
|---|---|---|
| 窗口宽 | 380 | **292** |
| 表盘 | 330 | **256** |
| 骨牌 | 76 | **56** |
| 骨牌行边距 | (2, −4) | **(8, −3)** |
| 骨牌行高 | 74 | **61** |
| 提示条 | 2 条 | **1 条** |
| 窗口总高 | 684 | **≈597** |
| 占屏高 | 61% | **53%** |

**为什么只有这两块能缩**：卡片那 250px 是**地板**——控件和字号不缩，它就一分都省不掉。
就算把表盘和骨牌全砍光，窗口也还有 280px（25%）。

**为什么宽度必须跟着缩**：只缩表盘会让它变成一个小圆浮在 344 宽的行里两侧留空，而且
**拖窗口绑在表盘 `Bounds` 上**（DECISIONS K10），可拖区域会跟着缩水。反直觉的是，宽度
跟着缩之后窗口的宽高比是 0.56 → **0.49**，**更瘦高而不是更矮胖**——因为卡片高度不变、
宽度却缩了。

**表盘 256 = 内容宽度**（292 − 36），所以 `box = Math.Min(宽, 高)` 两边相等，表盘刚好
填满那一行；标准档反而是 330 高、344 宽，左右各空 7px。表盘一切都从 `Bounds` 推导
（`rFace = box / 2.35`），**改这一个数就等比缩放，绘制代码一行不用动**。唯一有硬门槛的
是秒针宽度 `rFace × 0.008`：256 档是 0.87 DIP，100% 缩放的屏幕上略虚，125%/Retina 上是
1 像素以上。

**骨牌自己的规格一个字没动**（6:3:1、间距 = 高的一半），它跟表盘一样等比缩放。
⚠️ 中途考虑过「保持骨牌像素尺寸不变、只压间距和倾角」，**已否决**（DECISIONS K26）。

**提示条的高度预算**（headless 实测，不是估的）：一行 48px、两行 69px。标准档 74px 装得
下两行、**余量只有 5px**；紧凑档 61px 只装得下一行，余量 13px。装不下会撑高那一行、把
卡片顶下去，那个负边距当场作废，整个窗口为一条提示条跳一分钟。护栏是
`WindowLayoutTests.骨牌行必须装得下它那一档的提示条`（做过变异测试：紧凑档改成两行当场变红）。

**开关是配置目录下一个 `layout` 文件**，内容一行 `compact`，认不出一律标准档。
**只在启动时读一次**，运行中改了下次启动才生效——见 DECISIONS K25。

⚠️ **`ApplyLayout()` 必须排在 `RestoreWindowPosition()` 之前**：后者在 `Opened` 里调
`ClampIntoScreen()`，而它读的是窗口当时的 `FrameSize`；尺寸后设就会拿旧尺寸去夹。

## 9. 闹钟、Command 与 Alarms 清单

表盘即输入设备：**在钟面上滚滚轮**调闹钟，没有独立按钮，左右键点击刻意留白。

- **模型**：黄针停在 720 个格子上（0~719 分钟，**1 分钟一格**）。滚轮前滚逆时针、
  后滚顺时针；**连续快滚加速**：一串滚下来 1 → 3 → 8 → 15 → 30 分钟/格，
  停手 300ms 断串、回到 1 分钟/格。
  ⚠️ 加速依据是**滚动的节奏**（连续几格），不是单次 `Delta` 幅度——Avalonia 里
  快滚表现为事件更密而不是 Delta 更大。原来按 `Delta.Y / 120` 算（那是 Win32 的单位），
  阶梯一次都没生效过，见 DECISIONS H12。
- **响铃时刻在拨针那一刻算死**（`NextRing`），三级判断**全部严格小于**：
  `now < 今天的 T` → 今天 T；`now < T+12h` → T+12；否则明天 T。
  **恰好重合意为 12 小时后**，不是立刻响。之后只单调比较，不做角度容差。
- **黄针位置是推导值**：时间点对 12 小时取余（`Position` 是计算属性）。
- **持久化**：只存 `alarmFireAt` 一个值，退出时写一次。**启动时只显示不激活**
  （`Restore` 恒 `_fired = true`），要用户滚轮拨针或开 Execute 才 `Activate`。
- 一次性：响过即撤。**检查节拍每分钟一次**（2026-08-08 从每秒改，DECISIONS L8）——
  `FireAt` 恒等于 `now.Date.AddMinutes(整数)`，秒和毫秒必然是 0，分钟边界一定精确命中，
  每秒去问一次在语义上一次都没必要过。所有以分为单位的功能都收在 `MainWindow.OnMinute`
  一段直线代码里，顺序见 L9。
- **响铃连响 4 遍**（2026-08-03）：一次触发、连响四遍，**间隔取音频自身时长**
  （`Sound.Duration` 读文件头算，§10；三声通知同机制，2 遍）。仍是一次性——`MarkFired`
  在第一遍之前就跑完了，变长的只是这一次的响铃，不是 E5 的每日重复。
  Execute 开着时不响，命令只执行一次。
- **Execute 开关**（Settings **最后一张卡**，2026-08-09 从第 4 张挪到末尾，§10.1）：
  开 → 到点执行 `rules.json` 的 `executeCommand`，音色下拉变灰、**卡片底部多一行显示
  到点会跑的那条命令**（§10.1）；关 → 到点响铃、可选音色、预览行消失。**二者互斥。**
  Execute 每次启动强制复位为关（关机绝不跨会话）。
- **到点那一刻重读 `rules.json`**（`Command.ExecuteFresh`，2026-08-08，DECISIONS L4）：
  用的不是启动时那份快照，所以 `itami commands` 换掉第一条之后，**正在运行的程序立刻
  跟着变，不用重启**。读失败就退回快照并记 Error 日志。仍然只有 `GroupRules.Load` 这
  一个解析器，不违反 §15.4。
- **挑哪条命令生效、单独试跑一条**，都在命令行里（`itami commands`，DECISIONS L5/L16）：
  界面上不加选择器。三个模式互不重叠——`--list` 只看（`*` 标出 #0）、`--select [N]` 只改
  文件、`--execute [--yes]` 只跑 #0 且**不带下标**（想试别的先 `--select` 成 #0）。这样
  "试的那条"和"闹钟真会跑的那条"在设计上就是同一条。执行细节见 §9.3。

### 9.1 Alarms 清单（3.7.0 换成 crontab，DECISIONS J）

**跟上面的闹钟完全独立、互不干扰**——不同的持久字段、不同的音色、不同的表盘元素，
一行闹钟代码都没碰。处理的是「预约好的打卡时间」（吃药、日记/日历里设的提醒），跟
闹钟「手拨一个一次性意图」是两件不相关的事。

**3.7.0 起数据源是一份标准 crontab，不再是 Markdown 清单**（推翻 J2/J3/J4，理由见那里）：
用了一个月发现重复的事情占绝大多数，而 J4 假设的那个「上游会把每天 14:00 铺成一串具体
日期」的上游从来没存在过——那条规则实际上是在逼一个不存在的东西干活。

- **数据源**：`%LOCALAPPDATA%\ItamiTimer\alarms.cron`，UTF-8：

  ```cron
  # ItamiTimer alarms.
  # 第 6 列是提醒文字，不是命令，永远不会被执行。
  #
  # 分  时   日 月 周    提醒文字
    0   14   *  *  *     吃药
    30  21   *  *  1-5   晚间打卡
    */30 10-18 * * MON-FRI  起来走两步
  # 0   7    *  *  *     这条暂时不响
    @daily                每日回顾
  ```

  **严格照 Linux（Vixie/cronie）的语义**，一条方言都不加：`*` `5` `1-5` `1,3,5` `*/15`
  `1-9/2` `5/15`、星期和月份认三字母名（大小写不敏感）、星期 `0` 和 `7` 都是周日、
  `@daily @midnight @hourly @weekly @monthly @yearly/@annually` 照 crontab(5) 展开。
  `#` 注释和空行跳过。**第 6 列到行尾全是提醒文字**，文字为空的行跳过。

  ⚠️ **日/周两栏的 OR 语义照抄 Vixie，不许「修正」成 AND**：两栏都不以 `*` 开头时是
  OR（`0 0 1 * MON` = 每月 1 号**或**每周一），只要有一栏以 `*` 开头就是 AND。判据是
  **字段文本是不是以 `*` 开头**（`*/2` 也算），不是「掩码是不是全 1」——跟 Vixie 的
  `DOM_STAR`/`DOW_STAR` 标志位完全一致。在一个号称「通用格式」的东西上偷偷改语义，比
  这个坑本身更坏。

  **`@reboot` 不支持**：它是唯一一个不表示时刻的别名，这里没有能自圆其说的语义（水位线
  本来就初始化成启动那一刻），跟写错的行同一个下场。

- **不做任何语法检查**（用户 2026-09-03 定）：解析不了的行安静跳过，**不记日志、不提示、
  不统计加载了几条**。理由是"写错了在 Linux 下也一样错，是自己的能力问题"。
  **反馈只有一条，正向的**：成功触发时往 `itami.log` 写一行历史，带上命中的表达式原文——

  ```
  Alarms list fired 23:55 [55 23 1 * *] 月度对账
  ```

  提醒没响就去翻日志，查不到记录即可反推是自己写错了。**知情代价**：一个 typo = 这条
  提醒永远不响，屏幕上和日志里都零反馈。

- **第 6 列永远是文字，永远不执行**（3.7.0 明确）。这份文件长得跟真 crontab 一模一样，
  而这个程序另有一条真会跑命令的路（`executeCommand`，内容多半是关机，§9.3），两者离得
  太近——一旦合流，一份看着人畜无害的提醒文件就能关机器。将来若要向 Linux 看齐做成可
  执行，**必须单独设计、单独确认**，不许「顺手统一」。

- **编码**：`File.ReadAllText`，**跟 `rules.json`/`settings.json` 是同一条路**，UTF-8、
  BOM 可有可无，注释和提醒文字写中文跟 `rules.json` 里的中文组名是同一个机制。
  **刻意用宽容解码而不是严格解码**：万一存成了 GBK，宽容解码只是中文乱码、五个 ASCII
  字段照常解析、规则照常触发；严格解码会在非法字节上抛异常、整份文件当空清单，那是
  「一个字符存错、所有提醒集体消失」，比乱码坏得多。

- **程序只读**，从不回写、不清理、不生成。「勾选跳过」随 Markdown 一起没了——「这条暂时
  不响」现在就是 crontab 自己的注释语法：行首加 `#`。

- **视野统一成 12 小时**：`Next`/`NextDue` 只在 `(now, now+12h]` 里往前找，跟表盘红圈的
  门槛是同一个数。好处是「永不成立的表达式（比如 2 月 30 日）要能停下来」这类顾虑天然
  消失——720 次谓词测试封顶，不需要另设搜索上限。代价（知情）：钟面之外没有任何地方能
  看到更远的安排，想看去翻文件。

- **检查节拍**：每分钟一次，在 `MainWindow.OnMinute` 那段直线代码里（§9.2，DECISIONS L8/L13）。
  **文件每分钟整个重读重解析一遍，没有缓存、没有 FileSystemWatcher**——几 KB、几十行，
  解析是微秒级，换来「改完最多一分钟生效、不用重启」。
  去重靠一个纯内存的水位线（`_alarmsProcessedThrough`），**初始化成程序启动那一刻**
  ——不补错过的提醒，只看「启动以后新到点」的条目。所以 14:01:37 才打开程序，14:01:00
  那条不会响：`14:01:00 ≤ 14:01:37` 落在窗口外，这不是专门写的分支，是水位线的自然结果。
  **当场算当场响，没有「上一分钟预先标记」这回事**——两阶段讨论过并否决（DECISIONS）。
  `Due` 的回溯封顶 12 小时：正常窗口就是 1 分钟，但休眠唤醒或 `_minuteBusy` 跳拍之后可能
  落后很多，而 crontab 规则是无限的（`*/5` 一天 288 次），不封顶等于一次唤醒吐出上千条。

- **到点做三件事**：
  1. **无条件**在骨牌区域叠一条提示条，显示时间 + 提醒文字，**刚好显示一分钟**
     （到点那一刻起、到下一分钟整点为止，见下文）；
  2. **无条件**弹 Windows/macOS 系统通知，**一条事件一个通知，永远不合并**（`Notify.Show`）；
  3. **受 Settings 的 Alarms 开关控制**是否也出声（`Sound.Repeat`，2 遍，独立音色）。
     这个开关是普通持久化设置，**不像 Execute 那样每次启动强制复位**——它不武装
     任何危险的东西，只管「响不响」。铃**只响一次**，不是几条响几遍。

- **同一分钟多条怎么显示**（3.7.0，crontab 之后这变得寻常）：
  - **提示条一条一行**，不是 `A / B / C` 挤成一行——挤一行很容易漏掉后面那几条。
  - ⚠️ **正文最多 2 行，硬上限**。提示条跟骨牌叠在同一个 `Auto` 高的 Grid 格子里，而
    卡片那个 `-4` 的负边距是按骨牌 76px 死算的（DECISIONS K16）；第三行会把这一格撑高、
    把卡片顶下去，整个窗口为了一条提示条跳一分钟。预算：时间行 18 号 ≈22px + Spacing 2px
    + 正文 16 号 ≈20px/行 → 两行 64px 在 76px 内，三行 84px 超了。
  - 多出来的条数缀在**时间行**末尾（`23:55  +2`），不占新的一行；再加 `MaxLines="2"` +
    `TextTrimming` 兜住「单条文字太长自己折成两行」。
  - **屏幕上省掉的那几条，在系统通知和 `itami.log` 里一条不少**——这是省略的兜底。

- **提示条和系统通知并存，不是二选一**（2026-08-06 一波三折，见 DECISIONS J9/J13）。
  最初走 Windows 原生 toast——起一个短命的 `powershell.exe` 子进程调 WinRT 的
  `ToastNotificationManager`，AppId 传的是裸字符串 `"Windows PowerShell"`。真机
  验证时看着像完全走不通：API 不报错、`Show()` 正常返回，但屏幕上什么都没有，
  于是一度**整个放弃**，改成只靠程序自己画的提示条（`Platform/Notify.cs` 当时
  被删掉）。
  ⚠️ **后来发现判断错了**：问题不是「这条路走不通」，是**裸字符串根本不是一个
  真实注册过的 AppId**——用户自己翻通知中心，发现当天早些时候用另一个正确的
  AppId（PowerShell 控制台宿主真正的 AppId，装机时系统自动登记的一串
  GUID+路径：`{1AC14E77-02E7-4E5D-B744-2EB1AE5198B7}\WindowsPowerShell\v1.0
  \powershell.exe`）测试过的那条通知，其实**送达了**，只是当时「请勿打扰」开着、
  横幅被吞掉，通知本身安静地躺在通知中心里没被看到。`Notify.cs` 换成这个正确
  的 AppId 后原样加回来，**跟提示条一起触发，不是互斥关系**：提示条保证屏幕上
  一定看得见，不受 Windows 通知设置/请勿打扰状态影响；系统通知额外给一份「关掉
  程序也能事后翻看」的记录，但会不会弹横幅、弹不弹得出来终究要看 Windows 自己
  的通知设置——这个不确定性本来就在，不是 bug，也不是要靠代码解决的问题。
  **视觉定稿经过好几轮现场反馈**（同一天，均已在真机上截图/直接观察过）：
  整块矩形色块 → 贴着文字的小背景 → 纯文字（骨牌全程透出来，深色 `#1B222A`
  保证在浅灰窗口底色和浅木色骨牌两种背景上都读得清）→ 深色文字打底、亮蓝色
  （`#63A6E0`，DialPalette.Dark.Rest 现成色号）文字错位叠在上面（`Margin
  ="-2,-2,0,0"`，跟 `DialControl.DrawHands` 画指针阴影「先画一份偏移的，再在
  真实位置画一份」同一个手法，只是这里两层都是实色，没有主次之分）→ 定稿：
  时间行 18 号加粗，文字行 16 号不加粗，两层内容和字号完全一致，只有颜色和
  2px 的错位不同。

- **表盘红色圆环**：圆心落在表盘边缘（半径 1.0），**故意盖到木框上**——2026-08-06
  用户反馈原来 0.98 处的小实心点太小、太不显眼，改成更大的空心圆环，角度算法
  跟黄针同一个换算（目标时间对 12 小时取余）。**只在下一条落在未来 12 小时以内
  才画**——超过这个窗口直接取余会画出误导性的位置（比如「5 天后 14 点」看着像
  「还有 6 小时」），干脆不画，想看就去翻 `alarms.cron`。跟表盘上其它一切一样每拍
  整个重画，没有「清除」这回事。
  **下一条那一分钟不止一条时：外圈换成橙色，中心补一个红色实心点**（3.7.0）。
  **内缩不外扩**，外径原样不变，占位、命中范围、跟木框的关系全都不用重算。
  ⚠️ **两态靠两个独立信号：颜色（红 vs 橙）+ 中心那点**，任缺一个另一个仍然读得出来。
  最初那版是「里面再画一个同色圆环」，用户 2026-09-03 报「看不出是两个」——查下来根因
  不止是颜色：外圈占 `0.039~0.061`（半径 0.05 ± 半个描边 0.011），内圈那版 `0.032 ±
  0.011 = 0.021~0.043`，**0.043 > 0.039，两笔描边本来就是叠着的**，同色时必然读成一个
  粗环。而且表盘半径实测 ≈140px，外圈内缘只在 5.5px 处，想在里面塞「环 + 可见间隙」，
  两者各只能分到 1px 出头，非 Retina 屏上就是一团糊。实心点半径 0.020（≈2.8px）离外圈
  内缘还有 2.7px 空白，这个尺寸上只有它读得出来；要「真的两个环」只能整体放大，那正是
  J21 否掉的外扩式。橙色另起一个 token（`AlarmsDotOuter`），不复用色环的 `Amber`——那是
  「21-40 秒专注」那一档的语义色，跟提醒无关；取值偏艳橙是因为这个标记的圆心压在木框上，
  淡橙会糊进木色里。
  **只有「一条」和「不止一条」两态，不数到三**——表盘是扫一眼的界面，「2 件还是 3 件」
  这个精度换不来行动上的差别，而「不止一件」能：它正好告诉你这一下值得点开看。
  ⚠️ 顺带一个不明显但很有用的性质：**在这个 12 小时窗口内 mod-12 是双射**（窗口内任意
  两个时刻相差必然小于 12 小时，不可能取余到同一个角度）。所以表盘上两条重叠**当且仅当
  它们真的在同一分钟**——那一种由双圈表示，绝不会被误读成「两件不同时刻的事撞在一个角度
  上」。J8 那条 12 小时门槛当初是为了避免误导，顺带白送了这个性质。

- **跟闹钟共存**：不加锁不排队，直接并入 E12/E13 的模型。同一分钟内两边都到点，
  **清单先判断、闹钟后判断**——闹钟的响铃调用在后，Windows 单通道上闹钟赢，因为
  闹钟响完什么都不留，清单响完还留着一分钟的提示条，输了这一下代价更小。

**错峰（同一分钟多条按稀有度推迟到不同时刻）讨论过并否决**，理由和当时的完整推演见
DECISIONS——结论是把一个复杂机制换成了一个显示问题（多行提示条 + 双圈）。

### 9.2 这一分钟按什么顺序做事（2.0.9，DECISIONS L8~L13）

**程序里只有一个钟**：`MainWindow._frame`，33ms。⚠️ **33ms 是采样率，不是帧率**——
`OnFrame` 第一句就是 `if (sec == _tickedSecond) return;`，所以每 33ms 实际只做两件事：
读一次 `DateTime.Now.Second`、比一次 int。**重画秒针、滴答、镜像刷新、`OnMinute` 全部在
这个守卫之后，每秒恰好一次**。33ms 的作用纯粹是把「发现秒边界」的延迟压到 ≤33ms（实测
10~24ms）：直接用 1000ms 的定时器会漂移，触发点还可能停在秒内任意位置，秒针就会在肉眼
可见的错位上跳。`_frame` 这个名字有误导性，它是个采样器不是帧循环。

**其余所有以分为单位的功能全部收在 `MainWindow.OnMinute` 一段直线代码里**，
按下面的顺序，一件做完再做下一件。

```
整分钟边界（_frame 观察到 sec == 0，实测滞后 10~24ms）
  1. 上一分钟的提示条到期则收起        ← 先清旧的，第 3 步才画新的
  2. AW 查询 + 判定 + 三声通知          （§4.2 那五件事在 TaskSession.TickMinuteAsync 里）
  3. Alarms 清单 → 提示条 + 系统通知 + 响铃   （§9.1）
  4. 闹钟到点？ → MarkFired，然后二选一：
        Execute 开 → 起一个带窗口的 shell 去跑命令，起完就走（§9.3，不 await）
        Execute 关 → 响铃
```

**为什么必须收成一条直线**：2.0.6 之前分钟级的事分散在**两个各走各的定时器**上——
闹钟和 Alarms 清单挂在 33ms 的 `_frame`（边界后 ≤33ms 触发），AW 查询挂在
`TaskSession` 自己的 1 秒定时器上，而那个定时器**锚在任务开始那一刻**，所以它在边界后
0~1000ms 的任意位置触发。两个钟之间没有任何协调，结果是：闹钟已经把关机/重启命令发
出去了，同一分钟里的 AW 查询随后才到——等于一边拆机器一边还在敲 aw-server（用户
2026-08-08 报：两次重启之后 AW 报警）。**根因是两个时钟没合并，不是哪一行写了并行**，
所以光把闹钟从"每秒检查"改成"每分钟检查"解决不了。现在 `TaskSession` 不再持有定时器，
由 `OnMinute` 在边界调它一次。

**这个顺序的三条理由，每条都对应一个真踩过或差点踩到的坑**（2026-08-09 定稿，L13）：

- **闹钟整体排最后，判断和动作不再分开**。命令多半是关机/重启，让它当这一分钟的最后
  一件事，前面该做的都已经做完。
  中间存在过一版"第 1 步判断并执行命令、第 4 步响铃、第 6 步执行"的写法，理由是"关机
  会把本进程一起杀掉，`await` 后面那几行根本不会执行"——**那个前提只在 macOS 成立**：
  Windows 的 `shutdown /s /t 0` 提交关机请求就返回，`await` 立刻回来，后面的 AW 查询
  照跑，正好落在系统正在关机的时候，**也就是本节开头那个 bug 在 Windows 上等于没修**。
  排到最后之后，这个修复不再依赖"进程会被杀掉"这个平台特性，两个平台都成立。代价是
  关机比原来晚一点（最坏是这一分钟 AW 查询的 10 秒超时），对预约关机可以忽略。
- **闹钟响铃因此天然排在 Alarms 清单之后**，正合 §9.1 的要求：Windows 的 winmm 是单
  通道，谁后响谁截断前一个；闹钟响完什么都不留，清单响完还留着一分钟的提示条，让闹钟
  赢这一下代价更小。
- **提示条收起排在第 1 步**，即"先清旧的、再画新的"（新提示条在第 3 步画）。反过来也能
  工作，但只是因为 `hideAt = now + 1 分钟` 恰好躲过判断——哪天有人把它改成 59 秒，收起
  那步就会把刚画上的提示条当场擦掉。这个顺序**结构上不可能有那个 bug**。

**整段只有一层 `try`，不给中间几步单独包**：那样会把一次真正的故障粉饰成已处理，反而
让人略过日志里那行 Error（用户原话："加了 log 反而让我忽略了"）。**而且闹钟排到最后
之后，不加 `try` 反而更安全**：前面几步抛异常时 `MarkFired` 根本没执行，闹钟没被消费掉，
下一分钟 `ShouldFire` 仍为真、会重试，预约的关机不会被静悄悄吃掉——闹钟排第 1 步的那一版
才有"异常一次、关机永远不发生"的问题。

**2.2.0 起 App 根本不等命令**（§9.3：只负责起窗口，起完立刻返回），所以"命令跑多久"
再也影响不到这一分钟。闹钟排在最后这个决定本身仍然成立、而且理由没变：命令多半是关机，
让它当最后一件事，前面该做的都已经做完。

**`_minuteBusy` 闸门**：AW 慢或命令挂住不退时，下一个分钟边界照样会到；这时整分钟
直接跳过并记一条日志，**绝不让两份并排跑**——那正是这次要消掉的东西。

**休息阶段也在这里**（原来是每秒）：`RestFrom` 在休息期间恒等于 `done`，每秒重复赋
同一个值再触发一次重画表盘本来就是白做（当时还要连带重算一次任务栏图标，
那半边 2026-08-10 已随图标整个删掉，DECISIONS D11）；而 `done` 是整分钟、
`RestMinutes` 是整数，`done + rest` 必然落在整分钟上，按分钟判断"休息结束了没有"跟
按秒判断**结果完全一样**，一秒都不会晚。

### 9.3 命令怎么被执行：两个平台同一条路（2.2.4，DECISIONS L17/L21/L25~L29）

到点且 Execute 开着时，**两个平台都走 `Command.LaunchDetached`**：直接把命令跑起来、
**起完就返回**，输出交给后台任务收进 `itami.log`。**不起任何窗口。**

```
分钟序列第 ④ 步 → LaunchDetached
    ├─ 现读 rules.json，取 #0（刚 --select 换过的立刻生效，不用重启）
    ├─ Process.Start(cmd.exe /c … | /bin/sh -c …)   ← 解释器两边各自沿用，见下
    └─ 立刻 return；后台 Task 并发读两个流、等退出、写日志
```

**中途存在过一版"Windows 另开一个控制台窗口"**（2.2.0~2.2.3），唯一理由是
`shutdown /h` 在休眠被禁用时**退出码 0、stdout 空、stderr 空、机器纹丝不动**——那句
"此系统上没有启用休眠 (126)"只讲给控制台听（L17）。2026-08-09 把这个现象的**范围**
测清楚之后推翻了（L29）：

| 命令（`cmd /c`，两个流都重定向） | 退出码 | stdout | stderr |
|---|---|---|---|
| `shutdown /h`（真失败） | **0** | **0 B** | **0 B** |
| `shutdown /?` | 1 | 4390 B | 0 |
| `shutdown /x`（无效参数） | 1 | 4390 B | 0 |
| `nosuchcommand123` | 1 | 0 | 107 B |
| `dir C:\不存在的路径` | 1 | 91 B | 16 B |

**不是"Windows 的失败信息普遍绕过管道"，是一条命令的一个分支**——`shutdown.exe` 连
4390 字节的帮助文本都老实走 stdout。为这一个罕见分支让每次到点都闪一个黑窗、还多养
一条平台专有代码，不划算。macOS 侧本来就没有这个病（`pmset -x` → stdout、
`shutdown`（非 root）/ `open /nonexistent` / 找不到的命令 → stderr，**没有一条是两个流
都空的**）。

⚠️ **知情接受的代价**：`shutdown /h` 那一类失败在 App 日志里只剩 `exited with 0`。
**诊断手段是在真实终端里跑 `itami commands --execute`**——那里有真控制台，那句话就出来
了。**四份 README 都写了这条指引**（`README.md` / `README_ZH.md` / `installer/README.txt`
/ `pack-macos.sh` 里那段进 .dmg 的 heredoc——第四份漏过一次，见 DECISIONS L30）。

**几条不能动的实现细节**：

- **命令解释器一个字没改**：`cmd.exe /c`（Windows）/ `sh -c`（Unix），`rules.json` 里
  所有既有条目语义不变（`start notepad` 照旧是 cmd 的内建 `start`），L1/L2 那两条引号
  护栏继续有效，`CommandQuotingTests` 不用重写（L21）。中途认真考虑过换成默认 shell
  （PowerShell/zsh），代价是重新解释用户所有既有条目——**能不能看见输出跟解释器是谁
  无关**，这两件事被混为一谈过一次。
- ⚠️ **`CreateNoWindow` 必须跟着 `redirect` 走，不能无条件设 `true`**（L29，2026-08-09
  实测）：
  - App 那条路（`redirect: true`）**必须设**——App 是 GUI 进程、自己没有控制台，
    `UseShellExecute = false` 起 `cmd.exe` 时 Windows 会**给子进程新建一个控制台窗口**，
    正是这次要消掉的那个黑窗。
  - CLI 那条路（`redirect: false`）**绝不能设**——实测 `echo HELLO && dir C:\不存在`：
    设了之后**退出码 1 收得到、输出一个字都没有**；不设则 `HELLO`、目录清单、
    `File Not Found` 全在。而 CLI 的全部意义就是让输出落在真控制台上，设上去等于
    **把本节唯一的诊断手段废掉**。
- ⚠️ **后台收尾必须 `Task.WhenAll` 并发读两个流**（L27）：串行读（先读完 stdout 再读
  stderr）在 300KB stderr 下 **macOS 实测必死锁**；Windows 的管道缓冲区更小（约 4KB
  vs 64KB），**只会更容易撞上**。`redirect: false` 的 CLI 那条路没有管道，不受影响。
- **卡住时只记一行，不杀进程**：60 秒后日志出现 `still running`。命令合法地跑很久
  （关机流程）跟卡在权限框上，从外面分辨不了，杀错了比等着糟。
- **绝不 await**：分钟序列调完立刻往下走，命令挂死也只是线程池里停着一个任务。
  L14 那套"分钟边界取消"因此整个删掉；`_minuteBusy` 保留，但它守的只剩 AW 查询
  （本来就有 10 秒超时）。

**职责边界**：

| 谁 | 负责什么 | 不负责什么 |
|---|---|---|
| App | 把命令起起来；`Process.Start` 抛异常时记 Error（**这是唯一还能真判断的失败**）；后台收输出写日志 | 命令跑没跑成——退出码在 `shutdown /h` 上就是撒谎的（L17） |
| `itami commands --execute` | 读 rules.json、跑 #0、**输出留在真控制台上** | 窗口活多久（`itami` 自己永远不停顿，L20） |

**`--yes`**：`--execute` 默认先打印要跑什么再等 y/N（清单里躺着 shut down / restart）。
L29 之后 App 不再调用 CLI，所以 `--yes` 只剩"手动跑时跳过确认"这一个用途。

**CLI 仍然跟 App 一起装**（L22/L23/L25a）：两个打包脚本各多 publish 一次 CLI 到同一个
目录，App 的 csproj 用 `ProjectReference ReferenceOutputAssembly="false"` + 拷贝 target
让 `bin\Debug` 的布局跟安装目录一致。**L29 之后这不再是"不装就坏"**——App 到点已经不
调用 CLI 了——**而是"装了用户才有诊断手段"**：上面那条"命令看着没生效就去终端里跑
`itami commands --execute`"的指引，得有那个 exe 才成立。

## 10. 声音

**只用系统自带的音，不打包音频资源**。Windows 枚举 `C:\Windows\Media\*.wav` 走 winmm；
macOS 三级枚举 `*.aiff` 走 AudioToolbox。找不到、放不出一律安静收场——
**提示音绝不能把程序搞挂**。

- 滴答 `SND_NOSTOP`：通道占用时跳过本次滴答，让通知音优先，不再互相打断。
- **Windows 的 winmm 只有一个通道**：`Tick.Stop()`（= `PlaySound(null)`）会停掉本进程正在放的
  任何声音。所以它**只挂在喇叭点击里**（关滴答时），不放 `ApplyChrome()`——否则点图钉、
  关 Settings 都会掐断正在响的铃（E13）。macOS 那边 `Tick.Stop()` 是空操作，不存在这回事。
- 三声通知（专注达成 / 休息结束 / 键鼠空闲），各自可关、选中即试听。
- **响铃遍数**（2026-08-03）：闹钟 4 遍、三声通知各 2 遍，间隔取音频自身时长。
  **试听仍然只响 1 遍**——试听是挑音色，不是排练打扰。
  不加锁也不排队（E12）：这条路径没有共享可变状态，而「声音不重叠」只能拿丢一声或迟一声去换。
- 键鼠空闲声只在 `[60, 180)` 秒窗口响：过了 180 秒 AW 已回填 afk，救不回来了。
- 滴答**运行时合成**：白噪声 × 指数衰减 + 阻尼正弦，「滴」「答」两个缓冲区按秒交替。
- **`Sound.Duration`：只读文件头算时长**，不解码、不播放、不引第三方库
  （`.wav` 走 RIFF 的 `fmt` byteRate + `data` 长度，`.aiff` 走 FORM 的 `COMM`
  帧数 ÷ 采样率，采样率是 80 位扩展浮点，手工拆）。两种格式的块**顺序不固定、按偶数补齐**，
  所以是遍历块不是读固定偏移——`C:\Windows\Media` 里真有 `LIST` 排在 `data` 前面的。
  只有闹钟连响用它排间隔；读不出来退回 3 秒，读错顶多节奏怪，绝不会把闹钟弄哑。
- **Force Ticking**（Settings **第 1 张卡**，2026-08-09 从第 5 张挪到最前）：开 → 主界面
  喇叭图标隐藏、滴答**无条件**响，专注/休息期间也不例外——这就是"force"的字面意思，
  不再有任何静音例外（2026-08-13 用户推翻旧决策，见 DECISIONS C6）。
- **关（默认）时，滴答就是"你走神了，回来"这句提醒**（2026-08-29 用户推翻 C7/C8，
  见 DECISIONS C10）：喇叭图标照旧手动开关（`TickEnabled` 关着就永远不响，人为选择
  优先级最高，没有任何情况能覆盖）；开着时**跟钟面的闪烁共用同一个判据**——
  镜像里上一秒是 `OffTask`，就既闪又响（§8.9）。

  | 镜像上一秒 | 闪 | 响 |
  |---|---|---|
  | `OffTask` | ✓ | ✓ |
  | `Focused` / `Afk` / `AwOffline` | ✗ | ✗ |

  ⚠️ **这推翻了 2026-08-13 的"安静是挣来的"**（C7/C8）。那一版里滴答是"时钟本来就在走"
  的中性信号：默认响，只有上一整分钟真的专注过才换来这一分钟安静，AFK 和 AW 掉线照响。
  现在滴答有了明确的含义，于是**没有跑偏就没有理由响**。`FocusLow` 那条特例（C8）跟着
  作废——它防的是"整分钟 tier"在分钟边界上的 fail-open 假象，而逐秒读根本没有 tier。

  **连带效果（知情）**：没有任务在跑时程序完全不响。以前空闲时它像个普通时钟一直嘀嗒，
  现在只有"专注期间跑偏"这一种情况出声。想要一个一直走的钟，**Force Ticking** 就是
  干这个的。

### 10.1 设置窗口的卡片顺序（2026-08-09，用户指定）

自上而下：**Force Ticking → Focus complete → Break over → Idle Warning → Alarms →
Command**。两头是用户点名的：滴答最常动、放开头；Command 是唯一会对机器动手的一条，
放末尾。中间四张保持原有相对顺序。

代码里**没有任何地方依赖这个顺序**——`SettingsWindow` 全靠 `FindControl` 按名字取控件，
重排卡片只动 axaml。文档里凡是写"卡 N"的地方按上面这张顺序读。

**Command 卡最底下多一行预览**（同日）：开关拨到 On 时，卡片底部显示到点真正会跑的那条
命令（等宽字体、灰字、只读）；拨回 Off 就整行消失。没配好则显示
`(no executeCommand.{os} in rules.json)`——这一句原来只在日志里，用户开着 Command 却什么
都没发生时界面上一个字都没有。

- 取数走 `Command.Preview(fallback)`，**跟到点执行是同一条路**（同一个 `Reload` 重读
  rules.json、同一个 `OsKey`、同一个 `FirstCommand` 取 #0），只是不执行、不写日志。
  另写一份读法就又是 §15.4 那个"两条读取路径"的坑。
- **每次拨到 On 都重新问一遍**，不是打开窗口时算一次存着：`itami commands` 可能在程序
  开着的时候改过文件，而到点读的正是那时候的文件。
- 这不是给 E9 翻案：**只读、不可选**，换命令仍然只有重排 rules.json 一条路。也不违反
  D6——D6 管的是**说明文字**，这行是**值**本身，而这个值多半是关机命令。

## 11. 设置与数据

`%LOCALAPPDATA%\ItamiTimer\`（macOS `~/Library/Application Support/ItamiTimer`）：

| 文件 | 作者 | 内容 |
|---|---|---|
| `settings.json` | **程序**，随时整份重写 | 三声开关与音色、commandEnabled/Sound、forceTicking、tickEnabled/Volume、pinned、selectedGroup、alarmFireAt、awBaseUrl |
| `during.json` | **程序**，只在任务启动那一刻写一次 | 每个小目标的累计专注秒数 + 算到了哪一刻（§11.2）|
| `rules.json` | **用户手写**，程序只读 | groups（判定规则）、executeCommand |
| `alarms.cron` | **用户手写，程序只读**（3.7.0，§9.1）| Alarms 清单：一份标准 crontab，第 6 列是提醒文字（**永不执行**）；程序从不回写、不清理、不生成 |
| `itami.log` | 程序 | 1MB 滚动。界面全程沉默，日志是唯一能事后查的地方 |

**Settings 最底下的 `Open Config Folder` 按钮**（2026-08-13，DECISIONS C9）：点了就在
Explorer/Finder 里打开这个目录——`rules.json`、`during.json` 的清零/找回历史都要靠手改
这个目录下的文件（§8.1、DECISIONS I7），一个按钮比记住 `%LOCALAPPDATA%\ItamiTimer` 这串
路径更可靠。不放进任何卡片，因为它没有开关状态，是一次性动作。实现是
`AppData.OpenInFileManager()`，Windows 走 `explorer.exe`、macOS 走 `open`，用
`ProcessStartInfo.ArgumentList` 传路径而不是手拼 `Arguments` 字符串，绕开路径带空格时的
Windows argv 转义问题（§15.4 那类坑）。

**任务状态不落盘**：没有 current-task.json，退出即放弃。设置和日志删掉不改变行为。

### 11.1 ✅ 关于「把 settings.json 和 rules.json 合并」（2026-08-02 定稿：不合并，不改名）

**先纠正一个前提**：用户提出合并的理由是「两个文件启动后都会被修改」——这个前提
**现在已经不成立**。2026-07-31 删掉了 `GroupRules.Accumulate`，累计时长改存
程序自己的文件。所以现在 `rules.json` 是**纯只读**的（2026-08-02 起累计值在
独立的 `during.json` 里，见 §11.2）。

**真正的区别不是「谁会被改」，而是「谁是作者」**：

| | `rules.json` | `settings.json` |
|---|---|---|
| 作者 | 用户手写 | 程序 |
| 内容 | 正则、组名、命令行 | 开关、音色、累计值 |
| 注释与排版 | **有意义** | 无所谓 |
| 写坏的后果 | **约束本身失效**（判定规则是这个产品的本体） | 回到默认音色 |

**三个方案**：

| 方案 | 做法 | 代价 |
|---|---|---|
| **A（推荐）不合并** | 保持两个文件，把纪律写死：**程序绝不写用户手写的文件**。可把 `settings.json` 改名 `state.json` 让所有权一眼可见 | 仍是两个文件 |
| **C 合并 + 局部改写** | 一个文件，程序用 `JsonNode` 读整份、只改 `state` 子树再写回 | 一个文件；注释仍会丢（STJ 不能 round-trip 注释），但规则内容不经过类型模型往返 |
| **B 合并 + 整体重写** | 一个文件两个顶层节，程序序列化整份写回 | ❌ 用户手写的正则每次被程序重写；序列化一旦出错，**静默损坏的是判定规则本身** |

**结论（A）：不合并，也不改名。** 让程序去写用户手写的文件，等于把「注释会丢」变成
常态，而且赌的是序列化永远不出错——赌注是这个产品的核心（约束规则），赔率不划算。
**三个文件各司其职**：`rules.json` 用户改、`settings.json` 程序改、`during.json`
（§11.2）程序改——作者已经靠文件名分得够清楚，不需要再改名。如果目标只是
「备份/同步少管一个文件」，C 可以接受；**B 不要做**。

### 11.2 `during`：一个小目标上的累计时长（2026-08-06 重写为 checkpoint 模型）

用户要的语义（2026-08-06 重新定义）：**用钟点约束行为的同时，拿到这个小目标已经执行的
累计时间**。1.0.x 靠「任务终结时落盘」统计，**不准也嫌麻烦**——两次任务之间的时间是
彻底的盲区，崩溃还会整段丢。

#### 模型：AW 是地面真相，磁盘只是 checkpoint

```
任务 A 启动（StartedAt，整分钟）：
  1. 回填 [recordedThrough, StartedAt) 这一段  ← 覆盖「上一场任务全程 + 中间的空隙」
  2. seconds += 回填结果
  3. recordedThrough = StartedAt
  4. 写盘 ← 全程唯一的写入点
任务 A 运行中：  一秒都不落盘。界面 = 已落盘 + 本轮实时（纯显示）
任务 A 结束：    不落盘。
--- 空隙（没开任务，但可能仍在做这件事）---
任务 A' 启动：   回填 [A.StartedAt, A'.StartedAt) —— 上一场 + 空隙，一次补齐
```

**不会重复计费**：上一场 session 的秒数根本没进过 `seconds`。

**崩溃安全是白捡的**：回填失败（启动时 AW 正好不在）就不推进 `recordedThrough`，下次
启动自然重试同一个窗口。**推进 checkpoint 这个动作本身就是成功的唯一证明**，不需要任何
重试或恢复逻辑。

#### ⚠️ 回填必须 fail-closed（和运行期相反）

运行期「这一秒没有任何记录」判 `AwOffline`，而 `AwOffline(5) ≥ Focused(4)`，**计入专注**
（H2 的知情 fail-open）。那条规则**只在程序正在跑、拍子正在走的时候站得住**：机器摆明
开着、人摆明在，AW 哑了是 AW 的锅，不该由用户背。

回填的处境正相反：**程序当时没在跑。**AW 没数据最大的可能是机器关着、或者人压根不在
电脑前。照 fail-open 记账，一次跨周末的回填能凭空记 48 小时。

所以 `Backfill.CountSpan` 的底色是 `Init`（=0，不计入）：**有证据才算**。
`Judgment.Paint` 自己从不填任何底色（fail-open 是 `JudgmentBuffer.Cover` 用水位线额外加
上去的），所以这里直接复用那个纯函数，一行都不用改。

**afk 覆盖层在回填里比运行期更不能少**：AW 的窗口 watcher 会在人离开时持续拉长当前窗口
的 `duration`（AWJ 的 Note T2），没有 afk 盖顶，一个长驻的匹配窗口能记一整夜。

**这套设计顺带把 fail-open 的水分自动洗掉了**：宽松那次只喂表盘和完成判定、从来没落过
盘；同一段时间下次启动会被 fail-closed 地重新数一遍才进账本。**两者不需要对账。**

代价：**同一段时间在下次启动后可能变小**。已知情接受（DECISIONS I2），跟 D9 的 afk
追认回退是同一个方向。

#### 存哪儿：独立的 `during.json`

```json
{
  "goals": {
    "学习经济学": { "seconds": 12345, "recordedThrough": "2026-08-06T09:00:00+08:00" }
  }
}
```

**`recordedThrough: null` = 下次回填去 AW 全部历史里数一遍。** ⚠️ **2026-08-13 起这不再是
自动播出的值**（用户推翻 I3，DECISIONS I7）——见下面"自动补种"一段。`null` 本身的机制
原样保留，只是从"新目标的默认起点"变成了纯手动选项：想让某个目标找回历史，去
during.json 把那一条的 `recordedThrough` 手动改成 `null`（或者随便一个更早的日期）再点
Start。

当初选 null 而不是在播种时填一个具体日期，理由有三条，放在这里仍然成立，只是现在只在
**手动**触发全历史回填时才用得上：

1. `created`（bucket 创建时间）是**派生值且会变**——重装 AW、迁移 datastore 都会让它变，
   冻进持久层就再也不会被修正；
2. 回填窗口的起点**不需要准确，只需要足够早**——「历史从哪儿开始」这个问题根本不必回答，
   把 `[null, StartedAt)` 交给 AW，它有多少给多少，裁剪是它的本职工作；
3. 查 `created` 得先连上 AW，而这一步很可能发生在程序刚启动、AW 还没连上的时候。

`created` 唯一剩下的用处是**临时**的：给全历史回填的分块循环定个下界，免得从 1970 年开始
一周一周地空扫。查完就扔（`AwClient.FindBucketCreatedAsync`）。

**秒数是整数**：数的是 buffer / 回填 span 里的格子数，不是 AW 事件的 `duration`。
**改完要把每一处 `/ 60` 检查一遍**，整数除法会把小数位悄悄吃掉（DECISIONS G）。

**自动补种：每次启动，rules.json 里有、during.json 里还没有的目标名，各种一条
`(0, 启动这一刻)`**（2026-08-13 推翻 I3，DECISIONS I7）。旧版只在文件**从没存在过**的
那一次播种、且故意留 `null`——新目标第一次点 Start 会去 AW 全部历史里回填，图的是"不丢
掉之前已经做过的部分"。用户现在要的是反过来：**新目标默认不背历史包袱**，只从被发现这
一刻开始算——`"编程"` 这种后加进 rules.json 的目标，不该因为之前用 Claude Code / VS Code
干过别的事，一点 Start 就平白多出几十小时。用挂钟时间当种子而不是查 AW 的 `created`，
上面三条顾虑天然不存在——不需要问 AW，播种这一步也就不会因为 AW 没连上而卡住。

界面上那个数字没有单位、不解释（D6），用户只能去数据目录里翻。翻到 `{}` 什么也学不到，
翻到 `{ "学习经济学": { "seconds": 0, "recordedThrough": "2026-08-13T09:28:34+08:00" } }`
一眼就明白这表是什么——跟 rules.json「给例子不给注释」同一条思路。

⚠️ **只补种缺失的，不覆盖已有的、也不清理改过名字的旧条目**——状态文件依然不镜像配置
文件，只是"发现新目标"这个动作从"仅在文件第一次创建时"变成了"每次启动都查一遍差集"。
**清零也复用这同一套机制**：把某个目标的整条记录从 during.json 里删掉，重启程序，它会
被当成"没见过的目标"重新补种成 `(0, 重启这一刻)`——不需要专门写一个"重置"功能，跟
"给一个目标手动找回历史"是同一枚硬币的两面，都是手改这个文件、不需要界面。

**从 1.0.x 迁移**：老文件 `{ "accumulatedSeconds": { "X": 12345 } }` 的秒数原样继承，
**`recordedThrough` 必须盖上「迁移这一刻」的戳，绝不能留 null**——老账本的语义就是
「到此为止全部已入账」，留 null 会让下一次回填从 AW 历史的开头重数一遍再加上去，
数字直接翻倍。更老的文件里秒数可能还是小数（`double` 时代），四舍五入救回来，不归零。

#### 分块：**按天**，不是按周（2026-08-06 真机干跑打回来的）

首次回填可能要走完整个 AW 历史。一次性拉两年的窗口事件是几十 MB 的 JSON，span 也会涨到
60 MB 以上。所以切片（`Backfill.ChunkSeconds`），复用同一块数组，内存 O(1)。

**第一版按周，在真实数据上跑不过去**：150 天的历史走到第 17 周就抛
`AwUnavailableException`——那一周的窗口事件多到单次请求超过了 `AwClient` 默认的 10 秒
超时。而回填的失败语义是「不推进 checkpoint、下次重试」，于是那段历史会**永远卡在同一个
地方**：每次启动重试、每次同样超时，累计时长再也不会前进，且只有日志里一行 WARN。

改成两条一起上：

- **片子按天**（86400 字节的 span），比周小一个数量级；
- **回填专用的 120 秒超时**（`Backfill.ClientTimeoutSeconds`）。运行期那个 10 秒是按
  「AW 卡住宁可 fail-open 也不能拖住整分钟节拍」定的，回填的取舍正好相反——**慢一点无所谓，
  失败才是问题**。

代价：请求数 ×7（2026-08-29 起每片是 2 个请求，见 §7.6；那 1.25 倍的 6 小时重叠冗余
同时消失了）。纯粹是网络开销，不影响正确性——
重复拉到的事件会被 span 的边界裁掉。

**实测**（Codex-Win11，2026-08-06）：150 天全量 = 300 次请求，**4 分 11 秒**；
常态的增量回填（上一场任务到现在，几小时）**1.3 秒**。前者一辈子只发生一次，且是后台
fire-and-forget；后者才是每次启动都走的路径。

切片本身的代价是每个边界最多错 1 秒（边界秒的归属由覆盖优先级决定，偏严格一侧）——
两年 730 个边界 ≈ 12 分钟，相对于两年的总量可以忽略。

#### 1.0.x 删掉了什么

整套「每一秒专注只入账一次」的机制**全部删除**：`TaskSession.Settled` 事件、
`UnbankedSeconds`、`TakeUnbankedSeconds()`、`_banked` 幂等标志、`MainWindow.Bank` /
`BankRemainder`、三条终结路径上的记账调用。

它们防的是「这一秒现在不记下来就永远没了」。**AW 是地面真相，那个前提不成立了。**

顺带没了的还有：漏调一条终结路径就丢一段时间、崩溃丢当前这一段（最长 2 小时）、
以及「双重入账比漏记更难发现」这个需要专门加幂等标志去防的问题。

#### 已知性质（不是 bug）

- **从没启动过的小目标永远显示 0**，哪怕你在匹配的应用里泡了 100 小时。这个数字的含义是
  「被追踪到的时间」，没启动过就是没追踪过。代价是各项在各自首次启动前不在同一个基准上。
- **只回填当前启动的那个小目标**，其他目标的数字滞后到它们各自下次被启动。用户
  2026-08-06 明确接受。
- **回填用的是当前的 `rules.json`**，改了规则会让历史被重新判定。累计时长的语义因此是
  「按当前规则，AW 记录里属于这个目标的时间」。


## 12. 模块与项目布局

```
src/ItamiTimer.Core/   net10.0  判定与投影。无 UI、无平台调用（csproj 强制）
src/ItamiTimer.Cli/    net10.0  itami：start / backfill / commands，跟界面共用 Core
src/ItamiTimer.App/    net10.0  Avalonia 界面
  ├── Platform/        平台差异每处收口在单个文件，没有散落的 #if
  └── Drawing/         表盘、骨牌、图标——全部矢量计算
tests/                 xUnit
```

Core 的关键类型：`JudgmentBuffer`（秒级存储 + 覆盖/归档/承诺弧）、`Judgment`（分层覆盖，纯函数）、
`AwMirror` + `MirrorFeed`（区间事件摊平成每秒一个判定，§7.5）、`GroupRules`（规则编译与匹配）、
`AwClient`（唯一碰网络的地方）、`OffTaskAttribution`、`AlarmsList`、`TaskRecord`、
`MinuteCell`（渲染契约）、`TimeGrid`。

**数据路只有一条**：`AwClient` → `MirrorFeed` → `AwMirror` → `Judgment.Paint` →
`JudgmentBuffer` → `MinuteCell`。界面和 `itami` 挂在同一条路的末端，**谁也不许自己
再走一遍**（§15.7 那次事故的形状）。

**两条改错不报错、只把账算错的身份标识**：`App.axaml` 的 `Name="ItamiTimer"`、
csproj 的 `AssemblyName=ItamiTimer`。macOS 上 AW 报的 `data.app` 就是前者。

## 13. 跨平台

判定层平台无关，工作量全在 App 层。平台差异收口于：`Sound` / `Tick` / `MacAudio` /
`InputIdle` / `WindowPin` / `AppData` / `Command`。

- macOS **必须打成 `.app`**（`pack-macos.sh`）：bundle 用 `LSEnvironment` 写
  `DOTNET_ROOT`——双击启动的 GUI 不继承 shell 环境变量。
- ⚠️ **`--dial-specimens` 的输出不可复现**：`DrawHands` 读 `DateTime.Now`，样张上画着
  实时指针，所以同一份代码相隔几秒渲染两次字节就不同（2026-08-02 实测）。
  **它不能用来做回归的逐字节对比**——G 组里 headless 改造那次用的 SHA-256 对比在这儿
  不成立，只能用眼睛看。想让它可比，得给渲染注入一个固定时刻，目前没做。
- 调试出口 `--dial-specimens` 走 **headless**，不初始化窗口平台（构建不该依赖
  有没有人登录着桌面）。
- 未验证风险：macOS 的 `localizedName` 随系统语言变（中文系统 `Finder`→`访达`）。

### 13.1 2.0.x 的无边框透明窗口：macOS 验证进度（2026-08-08 更新）

§8.7 那一整套原本**只在 Windows 真机上验证过**。2026-08-08 在 macOS（M1、单屏
2560×1440、缩放 100%、Dock 在底部不隐藏）上过了一遍，结论分三档记下来——**"程序化实测"
和"人眼简测"不是一回事，别把后者当成前者引用**。

| 要验证的 | 状态 | 依据 |
|---|---|---|
| `ClampIntoScreen` / `WorkingArea` | ✅ **程序化实测通过** | `windowX/Y` 手改成 `99999,99999` 启动 → 被拉回 `2180,706`：右边缘 2180+380 = 2560 正好贴屏幕右界，下边缘 706+648 = 1354，屏高 1440 → 底部扣掉 86px = **Dock**。再改成 `-99999,-99999` → 落到 `0,30`，上边缘 30px = **菜单栏**（Quartz 报告的菜单栏窗口高度正是 30）。**菜单栏和 Dock 都正确扣除**，这条原本是清单里"最可能出偏差"的一项 |
| 位置记忆 + 不漂移 | ✅ **程序化实测通过** | 设成 `900,400` 连续启动三次，`settings.json` 和 Quartz 读回的实际窗口位置都是 `900,400`，零漂移 |
| `Position` 的坐标原点/Y 轴方向 | ✅ **程序化实测通过** | Avalonia 写 `Position=900,400`，Quartz（左上原点、Y 向下）读回也是 `900,400` —— Cocoa 的 Y 轴朝上确实被 Avalonia 抹平了 |
| 置顶（图钉） | ✅ **程序化实测通过** | `pinned: true` 时 Quartz 报告主窗口在 **layer 3**（浮动层）而非普通的 layer 0 |
| 分钟边界的触发精度 | ✅ **程序化实测通过** | 连续三个整分钟命中 `+24ms / +10ms / +18ms`，全部落在 33ms 帧内（§9.2） |
| `WindowDecorations="None"` + 透明观感 | ⚠️ **仅人眼简测，未细看** | 用户 2026-08-08 简单跑了一遍"暂时没有发现问题"。**边缘抗锯齿/圆角**在 Quartz 下跟 DWM 未必一致，这一条只能靠眼睛，没有程序化依据 |
| `BeginMoveDrag` 拖动手感 | ⚠️ **仅人眼简测** | 同上 |

**2.0.7/2.0.8 的分钟序列在 Windows 上的验证进度**（2026-08-08）：

| 要验证的 | 状态 | 依据 |
|---|---|---|
| 编译 | ✅ **通过** | Windows 上 `dotnet build` 0 警告 0 错误（2.0.7 的代码首次在 Windows 编译） |
| 单元测试 | ✅ **通过** | 134 个全过（Core 98 + App 36，含 macOS 侧新加的 `RulesTextTests`/`CommandQuotingTests`） |
| `itami commands --list` | ✅ **通过** | 正确解析到 `%LOCALAPPDATA%\ItamiTimer\rules.json`、正确走 `executeCommand.windows` 分支、正确提示"闹钟永远跑 #0"、`*` 标在 #0 上；`--select N` 实测把第 2 条挪到了 #0 并留下 `.bak`（用临时副本试的）；`--execute` 在非终端下正确拒绝执行并返回 1 |

⚠️ **`shutdown /s /t 0` 在 Windows 上会立刻返回**（提交关机请求就退出，关机由系统在后台
推进）——这是**推断，不是本机实测**。2.0.8 的排序改动正是冲着这个推断去的：原来"命令
优先 + `await`"的保护完全依赖"进程会被杀掉"，而那只在 macOS 成立（见 §9.2 / DECISIONS L12）。

Windows 侧已经实测过的两条，验证方法可以照抄：把 `settings.json` 的 `windowX`/`windowY`
手改成 `99999,99999` 再启动，程序应该把它拉回屏幕内（Windows 上实测 `99999,99999` →
`2093,620`）；再启动一次，位置应该原样保持、不漂移。

**2.2.4 / 2.3.0 的 macOS 验证进度**（2026-08-09）：这两版是在 Windows 上写的、Windows 上
测的（`CreateNoWindow` 那两个值全部来自 Windows 实测，§9.3），macOS 侧当天补了一轮。

⚠️ **测法要说清楚：走的是一个临时探针程序**（引用 `ItamiTimer.App`，用假 `HOME` 把
`AppData.Dir` 指到临时目录，因此**一个字节都没碰用户真实的 rules.json**），
调的是 App 自己那份 `Command.LaunchDetached` / `Command.Preview` / `SettingsWindow`。
**不是"打开 GUI、拨一个真闹钟、等它到点"**——分钟序列第 ④ 步怎么调用它，这一轮没覆盖。

| 要验证的 | 状态 | 依据 |
|---|---|---|
| 编译 + 单元测试 | ✅ **程序化实测通过** | macOS 上 `dotnet build` 0 警告 0 错误；134 个测试全过（Core 98 + App 36） |
| `LaunchDetached` 不阻塞 | ✅ **程序化实测通过** | 加了 `CreateNoWindow` 之后仍是 **37ms 返回**，落在原先记录的 60~80ms 同一量级 |
| App 路（`redirect: true`）输出收集 | ✅ **程序化实测通过** | `echo … && ls /不存在` → 日志里三样齐全：`exited with 1`、`stdout: STDOUT_MARKER_OK`、`stderr: ls: … No such file or directory`。**`CreateNoWindow = true` 没有吃掉 macOS 的重定向输出** |
| CLI 路（`redirect: false`）输出落在真控制台 | ✅ **程序化实测通过** | `itami commands --execute --yes --rules <临时文件>`：`STDOUT_MARKER_OK` 和 `ls` 的报错**都打在终端上**。L29 那半边（"CLI 绝不能设 `CreateNoWindow`"）在 macOS 上同样成立 |
| `--execute` 非终端下拒绝执行 | ✅ **程序化实测通过** | 不带 `--yes` 时打印 `(not a terminal — pass --yes to run unattended)`，退出码 1，命令没跑 |
| `Command.Preview` 跟执行同一条取数路径 | ✅ **程序化实测通过** | 预览文本与随后真正执行的命令逐字一致；`executeCommand.macos` 整个缺失时显示 `(no executeCommand.macos in rules.json)`；**预览没有往日志写任何一行**（`log: false` 生效） |
| Settings 卡片顺序（2.3.0 重排） | ✅ **程序化实测通过** | 逻辑树取出的标题依次是 `Force Ticking > Focus complete > Break over > Idle Warning > Alarms > Command`，与 §10.1 的规格一致 |
| `CommandPreview` 控件解析 + 显隐 | ✅ **程序化实测通过** | `FindControl<TextBlock>("CommandPreview")` 取得到（名字打错就是开 Settings 时抛异常，**编译不报**）；`CommandEnabled` 关时 `IsVisible=False`、开时 `True` |

## 14. 构建与发布

```bash
dotnet build ItamiTimer.slnx
dotnet test  ItamiTimer.slnx
./pack-macos.sh --dmg
.\pack-windows.ps1
```

- **必须限定 RID**：否则所有平台的 Skia/HarfBuzz 原生库全进来（实测 560MB）。
- csproj 的 `StripPdbFromPublish` 自动删掉两个巨型 pdb：127MB → 27MB。**别删这个 target。**
- 版本号唯一来源是 `Directory.Build.props` 的 `<Version>`：`pack-macos.sh`/`pack-windows.ps1`
  和运行时的 `MainWindow`（2026-08-07 从 `SettingsWindow` 挪过来，方便不开 Settings 就能
  确认当前跑的是哪个版本）都读它，改版本只改这一处。

**2026-08-03 起，本地调试验证和对外发布彻底分开**：本地验证就用项目目录内 `bin/Debug`/
`bin/Release` 的编译产物直接跑（`rules.json` 已经通过 csproj 的 `Content Include` 跟着
编译产物走，能独立跑起来），**不再**用 `dotnet publish -o "$LOCALAPPDATA/Programs/
ItamiTimer"` 或不带 `--dmg` 的 `pack-macos.sh` 把东西发到项目目录外面去测试。对外发布
只有一条路：`dist/` 目录里的成品——`pack-macos.sh --dmg` 的 `.dmg`、`pack-windows.ps1`
的 `.exe`，这两个脚本只负责产出这一份最终安装包。

**`pack-windows.ps1`**（2026-08-03 新增）：对应 `pack-macos.sh --dmg` 的 Windows 版本，
产物是 `dist/ItamiTimer-<版本>-win-x64.exe`。跟 macOS 的 `.dmg`（只在 Read Me 里让用户
自己去装 .NET）不一样——这个安装包会**主动检测**：装机时扫 `Program Files\dotnet\shared\
Microsoft.WindowsDesktop.App` 有没有 `10.*` 子目录，没有就提示用户、从
`https://aka.ms/dotnet/10.0/windowsdesktop-runtime-win-x64.exe`（官方 evergreen 链接，
实测能正确 302 到 builds.dotnet.microsoft.com）下载运行时安装器并帮忙装上（见
`installer/ItamiTimer.iss` 的 `[Code]` 段）。**ItamiTimer 本体走 `PrivilegesRequired=lowest`
按用户级安装**（`{autopf}` 因此落在 `%LOCALAPPDATA%\Programs`，跟项目手动 publish 那条命令
是同一个目录），全程不弹 UAC；只有真的要装 .NET 运行时那一步——它本身必须机器级、需要
提权——单独用 `ShellExec('runas', ...)` 弹一次 UAC，跟主程序装哪不装哪脱钩（2026-08-03
从 `PrivilegesRequired=admin` 改过来：那样整个安装都要提权，用户每次都得自己把开始菜单
项从"所有用户"那堆里挪到自己账户下，体验更差）。构建这一步需要 Inno Setup 6 的
`ISCC.exe`（`winget install --id JRSoftware.InnoSetup -e`，纯开发机工具，不影响最终用户）。
- 安装包的发布目标（`dist/`）被正在运行的程序锁住时会重试 10 次然后失败——先关程序。
- `%LOCALAPPDATA%\ItamiTimer\`（settings.json/rules.json/log）和机器级单实例 Mutex
  都不认安装位置——装好的版本、项目内的 Debug/Release 编译产物，读写的是同一份配置，
  也不能同时运行（后启动的会把先启动的窗口拉到前台然后自己退出）。

---

# 第二部分 · 已知 Bug

> **编号是地址**：代码注释和 `DECISIONS.md` 里到处引用 `§15.x`，所以已修的条目保留
> 编号、只留一行结论。过程在 git 里。

## 已修（存档）

| | 现象 | 结局 |
|---|---|---|
| 15.1 | 休息起点落在过去，短任务下休息被吃光 | 第二版引擎消解（§4）。真正的成因是 afk 回填/收缩让跨阈值那一格后退，不是原文写的「末尾几秒不计入」 |
| 15.2 | Give Up 后按钮不恢复 `Start` | 已修：`RefreshGoalItems` 只改 `.Text`，控件树只在 `LoadRules` 建一次 |
| 15.3 | `duringByGroup` 未持久化 | 整套删除，改成 checkpoint 模型（§11.2） |
| 15.5 | 归档的结算范围偏了 180 秒 | `[0,3600)` → `[180,3780)`。**做过变异测试**：改回去当场变红（3600 → 3540） |
| 15.6 | 机器睡眠超过 2 小时 3 分 → 会话永久冻死 | `JudgmentBuffer.Cover` 先滚动到这一拍落得进 buffer |
| 15.8 | `README.md` 严重过时 | 整篇重写。四份面向用户的说明书要一起改，见 CLAUDE.md |
| 15.9 | `IconExport` 被清理误删，`pack-macos.sh` 跑不通 | 恢复。**`TomatoIcon.Render` / `IconExport` 绝不能删**——`.ico` 和 `.icns` 的唯一来源（DECISIONS D11） |

## 15.4 rules.json 只许有一个类型模型、一个解析器

原来 `executeCommand` 有**两条读取路径**，不同时间、不同 AI 各写一半造出来的，谁也
不知道对方那套设置。它咬过两次，症状一模一样——**文件的一半好用、另一半安静地不
工作，而程序照常启动**：

- 一次是那边没开 `JsonCommentHandling.Skip`（文档鼓励写注释 → 写了就失效）；
- 一次是 `TryGetProperty` **永远区分大小写**，而这边不区分。

现在整个 `rules.json` 只剩 `RulesFile` 一个类型模型、`GroupRules.Parse` 一个解析器，
`Command` 从 `GroupRules.CommandsFor(os)` 取，不再自己读文件。

⚠️ **往 rules.json 里加新的节，就往 `RulesFile` 加字段，别再另起一条路。**

## 15.7 验证工具和被验证对象必须是同一个引擎

`itami start` 曾经走 `Replay.Run` 全量重放，只有 `itami bench` 用 `JudgmentBuffer`——
**CLI 给的是另一套判定的账**，而 CLAUDE.md 当时管它叫「唯一给账单的地方」。同时
Core 的测试大半打在已经没人调用的 `Replay` 上：**护栏守着一段不跑的代码**。

已修：干跑跟实机走**同一个引擎、同一个节拍**（干跑只是把 `now` 喂进去），`Replay.cs` /
`TaskState.cs` 连同 5 个测试文件删除。代价如实记：测试从 125 条降到 71 条，后来补回。

⚠️ **这条事故的形状会反复出现**：任何「两个地方各算一遍同一件事」都是它的变体。
最近的两次——`Judgment` 的镜像被两个前端各驱动一遍（3.5.0 收进 `MirrorFeed`）、
「刚跑完那一分钟」被两个前端各取一遍且不等价（3.6.0 收进 `JudgmentBuffer.LastCompleted`）。

# 第三部分 · 待办需求

> ⚠️ **优先级规则（用户 2026-08-02）：第二、三部分的优先级高于第一部分和
> `DECISIONS.md` 的护栏。** 需求跟既有设计冲突时不要拿护栏去否决需求——按需求做，
> 做完把翻案补进 DECISIONS。但代价要先说清楚再动手。
>
> **惯例**：设计定下来就写进第一部分，这里只留一行已完成标记。编号是地址，代码里
> 到处在引用，所以不删号。

## 已完成（存档）

| | 需求 | 结局 |
|---|---|---|
| 16.0 | 第二版判定引擎 | 规格见 §3/§4，Core / CLI / 界面全部落地 |
| 16.1 | 骨牌左侧亮面宽度随倒下数量递减 | 按 `j + max(0, Fallen−1)` 移窗查表，DECISIONS D8 |
| 16.2 | 为什么全是矢量绘制而不用图片 / emoji | 维持矢量，DECISIONS D5（图标字体在 macOS 是豆腐块） |
| 16.3 | 启动 / 退出时的 AW 查询能否移除 | 已移除 |
| 16.4 | 单实例限制 | 机器级 Mutex，后启动的把先启动的窗口拉到前台然后自己退出 |
| 16.5.1 | AW 整个连不上时冻住整拍，不是 fail-open | 已修：连不上 = 空事件列表，走 §3.1 的 fail-open |
| 16.5.2 | 提交任务时要不要对 AW 探活 | 不加（用户 2026-09-02）：原则是尽量减少和 AW 的交互（§7.5），没有 AW 也不拦提交；回填（§11.2）连不上时的 WARN 日志已经是唯一、够用的痕迹 |
| 16.6 | exe / app 图标该画什么 | 用户澄清：不用换 |
| 17 | Alarms 清单：从外部文件读定时提醒 | 完整规格并入 §9.1，取舍见 DECISIONS J |

## 16.5 AW 异常检测：只记日志，不改判定

「没有任何记录 → 算专注」是知情的 fail-open（§3.1），代价是两条路一直开着：

1. `aw-watcher-window` 死掉而 server 还活着 → 每秒都没记录 → **全盘绿、白拿满分**；
2. `aw-watcher-afk` 死掉 → afk 查不到任何事件 → 一律落到窗口判定 →
   **「停在目标应用上起身走开」完全隐形**，A4 说那是最省力的作弊路径。

根子相同：`FindBucketIdAsync` 只检查 bucket **存在**，而 bucket 一旦建过就永远存在。

**结论：只记日志，判定逻辑不动**（用户 2026-08-02：「这个应用程序本意只是辅助…
用户自己故意偷懒，不予考虑」）。检测信号不需要额外查 AW——watcher 只要活着就会持续
心跳，某条事件的 `End` 会一直贴着 `now` 往前挪；某一拍的 4 分钟窗口里，`win`/`afk`
事件列表**连一条 `End` 贴近 `now` 的都没有**，watcher 大概率已经死了。落点在
`TaskSession`，不碰 Core——`JudgmentBuffer`/`Judgment.Paint` 保持零分支的纯函数状态。

一个已知的假阳性不打算处理：机器休眠时两个 watcher 也会真的停发心跳，跟被杀掉在这个
信号下看着一样。日志措辞直接写「过去 60 秒没有新事件」，不去猜是死是睡。

### 16.5.2 提交任务时要不要探活：不加

半独立的问题：中途断线判 fail-open 是合理的（不该拿 AW 的毛病惩罚用户），但**开局
就没有 AW** 是另一回事——那样整个约束从一开始就形同虚设。**结论：不加**（用户
2026-09-02）——原则是尽量减少和 AW 的交互（§7.5：空闲时一次 AW 查询都没有），没有
AW 也不该拦住提交。

- `AwClient.ProbeAsync` 曾经是为这个准备的，但从头到尾没有调用点，2026-08-06 已删除
  ——留着一个没人调的探活函数，只会让下一个读代码的人以为这条路走通了。
- **现状即结论**：回填（§11.2）挂在提交那一刻，连不上会写一条 WARN 日志。「开局就
  没有 AW」不是完全无痕的了，只是仍然不拦提交，这就是要的行为。

### 16.5.3 跑偏归因：日志里说清楚「为什么」

**跟 16.5 同一类，诊断专用，绝不反哺判定。** §8.1a「界面可以沉默，但原因不能消失」
这条原则本来就该覆盖到日志层——用户问「1:20 到 1:30 明明在看书，为什么表盘上是红的」
时，唯一的查证手段曾经是翻 AW 的原始事件。

- [`OffTaskAttribution.Attribute`](src/ItamiTimer.Core/OffTaskAttribution.cs)（Core，纯函数）：
  拿该分钟内**不命中**当前小目标的窗口事件，按 `(App, Title)` 累加重叠秒数，报占用最长的。
- 跟真实判定用**同一次** `GroupRules.GroupMatches` 调用（`group is null` 时永不命中），
  保证「为什么」和「算不算」永远对得上号。
- **结果只进日志**，不进 buffer，不影响任何一次判定：

  ```
  The minute just past had 55s off-task: chrome.exe "YouTube - some title"
  ```

- 取哪一分钟走 `JudgmentBuffer.LastCompleted`（**不是 `cells[^1]`**，那是承诺弧的灰色
  投影尾巴，`OffTaskSeconds` 恒为 0——这条日志因此在生产里一次都没触发过，DECISIONS L31）。
- 遵守 D3：**只在 `OffTaskSeconds` 占主导时记，AFK 不记**——人不在不是用户的错。

## 17.1 点小红圈瞄一眼下一条

红圈本来只是个纯装饰的角度指示（下一条 Alarms 什么时候到）。给它加了唯一一种交互：
**精确点在红圈上，弹一次提示条**，内容正是红圈代表的那一分钟（`AlarmsList.NextDue`）。
3.7.0 起拿的是**那一分钟上的全部条目**而不是单独一条——红圈画成双圈就是在说「这一刻不止
一件事」，点开只给一条等于说了一半（显示上限同 §9.1：正文 2 行 + `+N`）。

- **命中测试，不是 Button**：`DialControl` 是纯手绘控件，红圈没有子控件。
  [`DialControl.HitTestAlarmsDot`](src/ItamiTimer.App/Drawing/DialControl.cs) 的圆心和半径
  跟 `DrawAlarmsDot` **共用 `FaceGeometry()`**——画在哪就能点哪，两处不会各算各的。
  ⚠️ 特意不用 Button（DECISIONS E4：Button 内部把 `PointerPressed` 标 Handled，钟面上
  其它靠 `+=` 订阅的手势会收不到）。挂在 `MainWindow` 现有的 `dial.PointerPressed` 里。
- **命中区比画出来的圆大一圈**（固定 6px，不跟着表盘缩放）：红圈半径常见只有 7px 左右。
  这圈容差是隐形的，不改视觉。
- **按下即分岔，不是「松开才算」**：拖窗口必须在按下那一刻调 `BeginMoveDrag`，所以红圈
  这个分支也只能挂在按下上——点中的那一下就已经是「瞄一眼」，不会再退化成拖拽。
- **纯读**：不出声、不弹通知、**不推进 `_alarmsProcessedThrough` 水位线**，跟
  `CheckAlarmsList`（§9.1）完全隔离。这条不能妥协——点一下绝不能让后面真正到点的那次
  提醒被冲掉。
- **停留 3 秒起、每多一条 +1 秒、封顶 6 秒**，不是到点那次的 1 分钟（`ShowAlarmBanner`
  的 `visibleFor` 参数）。到点那次是「这件事现在该做了」，这一次只是扫一眼。
- **没有下一条闹钟时什么都不会发生**：`AlarmsDotMinutes` 为 null 时命中恒 false，
  那一下照旧走拖窗口——不是专门写的分支，是几何判断的自然结果。

`HitTestAlarmsDot` 依赖 `Bounds`（Avalonia 布局系统赋值），在 `ItamiTimer.App.Tests`
的边界内写不了单元测试；真实窗口上点过（用户 2026-09-02），命中区大小、按下即分岔的
手感、提示条内容都没有问题。
