# ItamiTimer（一袋米要扛几楼）—— 系统设计

> 本文描述**当前实现**的系统设计，按主题组织，不按时间。
> 历史上被推翻的方案、以及「为什么不要翻案」的护栏清单，见 [`DECISIONS.md`](./DECISIONS.md)。
> 对外介绍（英文）见 [`README.md`](./README.md)。

## 1. 定位：番茄工作法 + 客观审计

番茄工作法的骨架：选一个任务 → 承诺一段专注（经典 25 分钟）→ 挣得一段休息 → 停下。
它的两个已知弱点：计时器量的是**流逝时间**而不是**专注时间**；合规全靠自觉。

ItamiTimer 保留骨架，把自觉换成审计——**只有窗口活动命中已勾选的小目标、且人在座的
时间才计入**。审计数据来自本机 ActivityWatch（AW）的事件历史，程序自己不做任何行为监测。

与经典番茄钟的全部差异收敛为一句话：**计时器不存在，账本存在。** 程序内没有倒计时，
任何时刻的状态都是拿任务开始时刻去查 AW 历史、重放整段算出来的。偷懒的后果不是警告、
不是作废，而是**任务被拖长**——表盘上灰色的截止弧往前滑，你亲眼看着。

刻意不做的（v1 范围外）：长休息（每 4 轮一次）、违规宽限期、悬浮提醒窗、规则图形编辑器、
跨机器合并、统计面板。

## 2. 核心原则

0. **AW 是底座，不是零件。** 判定输入只来自 AW 本地 REST API，不调用任何系统原生窗口
   枚举/监听 API。AW 访问不了就直接说无法工作——不猜测、不降级、不找替代信号。
1. **任务一旦提交就已确定。** `startedAt` 固定、时长锁定。程序**永远不代替用户提交
   任务**——休息结束绝不自动开始下一轮。
2. **计时完全基于 AW。** 程序自己不累加时间。
3. **窗口只是外在表现，但进程就是任务本身。** 最小化、遮挡不影响任何东西；
   **退出程序 = 放弃任务**——任务只活在内存里，不落盘。
4. **状态是推导出来的，不是攒出来的。** 状态 = 纯函数(任务记录, AW 事件历史, now)。
   没有「剩余多少秒」这种可变累加值。

原则 4 的三个直接收益：轮询间隔不影响精度；临时连不上 AW 不损坏任何东西（历史在
aw-server 里，恢复后重查即补齐）；重放可以喂合成事件穷举测试，验 25 分钟不用等 25 分钟。

## 3. 两种模式

启动那一刻探测一次并**锁死**，整个会话不再变（`AppMode`）：

| | 约束模式 Constrained | 番茄钟模式 Pomodoro |
|---|---|---|
| 进入条件 | AW 探活成功（两个 bucket 都在）且 rules.json 读得出 | AW 连不上，**或** rules.json 读不了 |
| 计时依据 | 重放真实 AW 事件 | 重放**两条合成事件**（见下） |
| 小目标列表 | 显示，至少勾一个才能开始 | **整个隐藏**（不是变灰） |
| 盘面 | 绿/琥珀/红/空白/虚线，灰弧会滑 | 永远满格绿，灰弧不滑 |
| 键鼠空闲提醒 | 有（抢救即将被判 afk 的时间） | 无（没有 AW 就没什么可抢救） |
| 表盘、骨牌、休息扇形、三声通知、滴答、闹钟 | 照常 | 照常 |

**番茄钟模式的实现零侵入 Core**：喂一条 `app="ItamiTimer"` 的窗口事件和一条 `not-afk`
的 afk 事件覆盖 `[startedAt, now]`，自身豁免判 `Neutral`，`Neutral` 计入——整段重放出来
就是满格。`PomodoroFallbackTests` 钉住这条依赖链：动 `Neutral` 的计入语义或 `SelfApps`
名单，番茄钟会**静默地不再计时**，界面上看不出来（盘面本来就该全绿）。

**绝不做「AW 断了自动降级」**——那等于杀掉 aw-server 就能免费刷时长。任务中途 AW 挂掉
仍按 §7.4 报无法工作；AW 后启动就重开程序，不做重探和重试按钮。

## 4. 判定模型

> 一段区间计入累计专注 ⟺ on-task（命中已勾选组）**且** present（afk 数据说在座）

| 分类 | 条件 | 计入 | 盘面 |
|---|---|---|---|
| `OnTask` | 命中任一已勾选小目标 | ✔ | 满高绿板 |
| `Neutral` | 命中 ignore 名单，或是 ItamiTimer 自己 | ✔ | 同上 |
| `OffTask` | 其它（含规则文件里没有的应用，fail-closed） | ✘ | 短板 + 掺红 |
| `Absent` | afk 数据说 `afk` | ✘ | **什么都不画**（离开不怪你） |
| `Gap` | 这段区间 AW 没有数据 | ✘ | 虚线描边，不填色 |

判定顺序（`GroupRules.Classify`，四步，顺序即优先级）：

```
1. 是 ItamiTimer 自己？        → Neutral（硬编码 SelfApps，绝不进配置——
                                  否则用户看一眼进度就违规，死循环）
2. 命中任一已勾选组？          → OnTask（只对已勾选组求值，组间无优先级问题）
3. 命中 ignore？               → Neutral
4. 其它                        → OffTask（fail-closed：宁可误报让用户补规则）
```

`Absent` 在 `Replay.Slice` 里**优先于一切**：锁屏时 `LockApp.exe` 在 ignore 名单里
（本该计入）而 afk 说 `afk`——必须判 `Absent`，否则锁屏一小时时长照涨。

`OffTask` **不计入但也不惩罚**：没有作废、没有扣休息。惩罚已经发生了——截止线在滑。

## 5. 规则文件 `rules.json`

三级查找（`AppData.RulesPath`，绝不只看工作目录）：用户数据目录 → exe 旁 → 工作目录。
允许注释和结尾逗号。正则 `search` 语义，大小写敏感自理（`(?i)`）。

```
单条规则命中 ⟺ （无 app 或 app 命中）且（无 title 或 title 命中）
组命中       ⟺ 未 disabled 且【任一】条规则命中
```

三种规则形状缺一不可：只写 `app`（用这个工具就算）、只写 `title`（做这件事就算，
什么工具都行）、都写（用这些工具做这件事——卡浏览器标签页）。

- **空规则/空组是加载期错误**（匹配一切 = 关掉约束，fail-closed 要求拒绝加载）。
- `disabled: true` 屏蔽旧目标而不删除；屏蔽的组不出现在勾选列表、永不命中。
- 两个平台的 app 名写在**同一份文件**里，永不匹配的正则无害。
- `accumulatedMinutes`：每组的历史累计专注分钟数，任务终结时由 `GroupRules.Accumulate`
  读-加-写回（只累计 `OnTask`，`Neutral` 不归组）。写回会丢注释——已接受的代价。
- **一切改动追溯生效**：改文件、屏蔽组、增删勾选，一律对整段历史用**当前并集**重算。
  没有时间线、没有生效时刻。这允许「先玩 20 分钟 Blender 再补勾变绿」——用户知情
  并接受（约束的对象是「待在声明的学习范围里」，不是「不换脑子」）。

## 6. 任务生命周期

```
勾选小目标、拖时长滑块（10~50 步进 5，默认 25）、点 Start
  ↓  startedAt = 截断到当前整分钟（23:13:10 → 23:13:00，白送点击前 ≤59 秒，换来立刻开始）
  ↓  FocusMinutes 锁定；RestMinutes = ⌊focus/5⌋+1 推导锁定；界面立刻画整段灰弧，不查 AW
【专注】每个整分钟的计时点：查 AW → 重放 [startedAt, now) → 更新色块和截止弧
  ↓  累计 ≥ 承诺 → focusCompletedAt（区间内插值，精确到亚秒，不进位）
【休息】色环整个撤掉，画蓝色休息扇形 [达成时刻, +RestMinutes]；纯本地计时，零 AW 访问
  ↓  分针扫出扇形 → 响一声、扇形消失、会话终结
【空盘】停在这里等用户。空盘即邀请。
```

- **休息不是任务，也不被观察**：期间干什么都不重要，不进任何记录。约束休息会教人
  「别把任务做完」。
- 休息中点 Start（此时是 "New round"）= 终结当前任务 + 开新一轮，就是跳过休息。
- 专注中点 Start（此时是红色 "Give up"）或关窗口 → 同一个确认框；确认后 Abandoned。
- 崩溃 / 关机 / 进程被杀 = 放弃。程序区分不了「点了 X」和「被杀」，明知的代价。
- `RestMinutes` 的 **+1 是补偿不是慷慨**：达成发生在真实时刻，程序在下一个计时点才
  发现，休息从真实时刻起算——发现延迟被节拍封死在 60 秒内，补 1 分钟保证实歇永不少
  于名义。**+1 与 60 秒节拍绑死**，改节拍必须改这里（有测试守卫）。

## 7. AW 接口约定

### 7.1 两个 bucket，都必需

| bucket type | 用途 | 缺了会怎样 |
|---|---|---|
| `currentwindow` | 在不在做正事（`data.app`/`data.title`） | 无法判定 |
| `afkstatus` | 人在不在（`data.status`） | **「停在目标应用上走开」完全隐形**——窗口事件靠心跳续期自己长大，事件流不留空白。这是最省力的作弊路径，所以 afk 绝不可选、绝不降级为「视为在座」 |

### 7.2 硬知识（全部实测得来，实现时必须遵守）

| # | 事实 | 应对 |
|---|---|---|
| T1 | AW 区间查询只按事件**自己的开始时间**过滤，跨起点的事件静默消失 | 查询窗口向前放宽 6 小时再自己裁（`AwClient.FetchEventsAsync`）。失败模式良性（开头判 Gap），不为它加机制 |
| T3 | afk watcher 要安静满 180 秒才出结论 | 「此刻」的 afk 结论字面上还不存在，最后几个色块下一轮可能变色——正常现象，不要加稳定化 |
| T4 | 窗口标题每秒变时产生 `duration=0` 的事件 | `Replay.Bridge`：终点延伸到下一条起点，**空隙 ≤5 秒才桥接**（否则填平真窟窿） |
| T5 | afk 事件**回溯**写入，起点回填到最后一次输入 | 发现时损失已写死 → 唯一有效的提醒是**赶在截止线之前**自己读键鼠空闲（§11） |
| T6 | 人安静下来后 afk 桶末端有 ≤180 秒的空洞 | `Replay.PendingPresent`：末条 not-afk 结束未满 180 秒 → 暂定在座（会自我纠正，无作弊面）；且**每条 not-afk 的 End+180s 要加进边界集合**，否则整段空洞被起点判定裹走 |
| — | 系统代理会吞 localhost 请求 | `HttpClientHandler { UseProxy = false }` |
| — | `AddSeconds(double)` 四舍五入到毫秒，AW 是微秒精度 | `AwEvent.End` 用 `AddTicks`，否则连续偷懒段碎成几十次 |
| — | AW 时间戳是 UTC | **在边界上归一**：`AwClient` 解析时直接 `ToLocalTime()`，不让两种偏移量流进核心 |

### 7.3 时间模型

- `startedAt` 截断到整分钟 → 每个色块恒为完整 60 秒，纯度分母不失真。
- **核算区间** `[startedAt, floor(now))` 半开——正在走的那一分钟不画，防闪。
- 查询节拍**锚在整分钟**上（判据 `FloorToMinute(now) > 已查过的分钟`，初值 = startedAt），
  不锚在点击时刻。计时点可以漏，账漏不了——每轮全量重放，晚看只是晚看。
- 补勾小目标例外：把已查分钟设 `MinValue`，下一拍立刻重算。
- `focusCompletedAt` 不进位（进位意味着「账已结清还得撑 43 秒」，没有好答案），
  末格按实际长度画短一截。

### 7.4 不可达与窟窿

- 提交时不可达 → 约束模式下拒绝提交（但注意启动时就连不上会直接进番茄钟模式）。
- 进行中不可达 → 本拍跳过、写日志，不显示编造的进度。**不影响最终正确性**。
- `Gap`（真没数据：宕机、休眠边界）不计入也不惩罚，如实报告。
- **已知紧张**：`ProjectedEnd` 按「应该 − 实际」字面计算，Gap 也会推截止线——实质是
  惩罚，与「不惩罚」冲突。按用户给的公式字面实现，记录在案（`Replay.ProjectedEnd` 注释）。

## 8. 重放算法（Core 的本体）

纯函数，无 I/O、无时钟，`now` 是参数。`Replay.Run` 八步：

```
1. 裁剪两个事件列表到 [startedAt, now)（先 Bridge 处理 T4）
2. 收集边界排序去重：startedAt、now、每条事件两端、每条 not-afk 的 End+180s（T6）
3. 相邻边界构成区间，按起点判：afk 无数据且非暂定 → Gap；afk 说 afk → Absent；
   窗口无数据 → Gap；否则 GroupRules.Classify 四步
4. 累计 = Σ(OnTask ∪ Neutral)
5. 跨过阈值的区间内插值 → focusCompletedAt；之后的区间不参与专注核算
6. OffTask 极大连续段 → 违规记录（几次、各 app 多久——只有 CLI 用它）
7. Gap 合计
8. 末段分类 → Phase（跳过 ≤30 秒的尾部 Gap——AW 心跳恒滞后几秒，否则永远 NoData）
```

两个投影，同一纪律（状态与渲染分开）：

- `ToMinuteCells`：每分钟一格（Index、四类秒数、纯度）。**是投影，不是逐分钟累加的
  数组**——判据：关掉界面再打开，每格颜色能原样重建。CLI 画 ANSI 色块、表盘画色环，
  消费同一个列表。
- `ProjectedEnd`：预计结束 = startedAt + 承诺 + ⌈应该 − 实际⌉。缺口钳到 0；只往后滑
  不往回缩；达成后钉死在达成时刻。**欠账每次从头算，绝不写成增量**（增量在漏拍/补拍
  时会永久记错，且它就是原则 4 禁止的可变累加器）。

## 9. 表盘渲染规格

几何归一化到 `rFace = 1.0`，12 点为 0°，顺时针，分钟 × 6°。

| 层 | 半径 | 说明 |
|---|---|---|
| 木质边框 | 1.02 | 渐变模拟受光（光源统一在左上），外缘亮边 |
| 盘面 | 1.00 | 跟随主题：日面素白、夜面深灰（`DialPalette.Light/Dark`） |
| 色环 lane 0 | [0.50, 0.68] | 超圈螺旋内缩：lane 1 [0.31,0.46]、lane 2 [0.14,0.26]，>180 分钟原地覆盖 + 圈数标记 |
| 数字 1~12 | 基线 0.70 | 与色带、刻度互不侵占 |
| 刻度 | 0.90~0.965 | 分钟细、五分粗 |
| 指针 | 时 0.50 / 分 0.72 / 秒 0.80 | 带锥度，投影偏移在指针之前画 |
| 闹钟黄针 | 0.62 | 比分针短、比时针略长，画在时针之下 |

- **分针就是写入头**（第 i 格 = 钟面分钟 `[m₀+i, m₀+i+1)`）。表盘同时是钟、进度条、
  账本，三者共用一套坐标。这个约定反复白送东西：休息倒计时（分针扫出扇形即结束）、
  满格参照线（承诺弧同 lane）、无需任何进度指示器。
- **木桶短板**：`高度 = rIn + (rOut−rIn) × (0.5 + 0.5×纯度)`。颜色（绿→琥珀→红三段
  过渡）与高度编同一个量——一个给正常视觉，一个给所有人（色盲可读）。下限 1/2：
  纯度 0 也要是显眼的一块红板；**绝不取 0**——零高度会和「人不在（什么都不画）」
  撞在一起，而那是最不该混淆的一对：一个不怪你，一个全怪你。
- **承诺弧**：从写入头到预计结束，灰 22% 不透明；终点画径向截止线（眼睛要能追住
  一条**线**）。圈号必须用与色块同一套口径（任务已走分钟数），否则跨整点跳圈。
- **休息扇形**：从圆心切出的整块（不是又一条环带——形状是唯一还空着的通道），蓝色
  径向渐变（灰=欠账/离开、绿琥珀红=成绩，都有主了；蓝天然读作「歇一歇」），外缘 0.70。
  达成那一刻色环整个撤掉。`TaskSession.RemainingMinutes` 在达成后必须显式返回 0，
  否则色环清空导致写入头回退、灰弧整段重新冒出来。
- **秒针一秒一跳**，不是连续扫——扫秒针的钟不会响，滴答来自步进擒纵，两者互斥。
  33ms 渲染帧保留，用途是让跳变及时 + 在秒边界放滴答。
- **色环 = 当前任务的投影**这条不变量：没有任务就没有色环，实现里不许留任何跨任务
  缓存（有测试盯着「新任务的色环必须是空的」）。
- 骨牌：七块 = 星期几，倾角按凸多边形接触求解。启动时按当日初始化；**点击 Start 时
  补查一次日期**（跨午夜能更新，但检查密度低，保持神秘感——已知的刻意行为）。

## 10. 闹钟与到点关机

表盘即输入设备：**按压钟面**调闹钟，没有独立按钮。

- **模型**：黄针只能停在 144 个格子上（0~719 分钟，5 分钟 = 2.5° 一格）。短按 +5
  分钟；长按 500ms 后开始重复，步长每 1.6 秒翻倍、封顶 30 分钟/步，间隔渐进加速到
  150ms。短按与长按**分开计数**：按下不动作，释放时若重复定时器一次都没触发才算
  单击的 +5——否则点一下会走两步。
- **响铃时刻在拨针那一刻算死**（`NextRing`，三级判断全部**严格小于**）：黄针格子对应
  12 小时制时刻 T；`now < T` → 今天 T；`now < T+12h` → T+12；否则明天 T。**恰好重合
  意为 12 小时后**，不是立刻响——拨针时绝不当场触发。之后只单调比较
  `DateTime.Now >= 目标`，不做角度容差（0.5°/分钟，1.5° 容差=提前 3 分钟）。
- 一次性：响过即撤，不是每日闹钟。调整中（`_alarmAdjusting`，释放后 2 秒）不触发。
- 悬浮提示直接读同一个目标时刻（`HH:mm`）——显示的和会响的保证是同一个值。
  钟面不设静态 Tooltip（会盖住这个提示）。
- **Shutdown 开关**（设置里，与 Alarm 开关并排、各自独立）：命中时调系统关机命令
  （Windows `shutdown /s /t 0`，macOS `osascript … shut down`），收口在 `Shutdown.cs`。
  默认关闭。

## 11. 声音

**只用系统自带的音，不打包音频资源**（跟不放位图同一条纪律）。收口在 `Sound.cs`：
Windows 枚举 `C:\Windows\Media\*.wav` 走 winmm `PlaySound`；macOS 按 `~/Library/Sounds`
→ `/Library/Sounds` → `/System/Library/Sounds` 三级枚举 `*.aiff` 走 AudioToolbox
（`MacAudio.cs`——不用 `afplay`：滴答每秒一次，一小时 fork 三千六百个子进程，启动延迟
还会让钟听起来在喘）。找不到、放不出，一律安静收场——**提示音绝不能把程序搞挂**。

三声通知（各自可关，设置里选音色，选中立刻试听、**不看开关状态**）：

| 事件 | 性质 | 默认音色策略 |
|---|---|---|
| 专注达成 | 通报 | 清亮，「成了」 |
| 休息结束 | 通报 | 低沉，通报不催促 |
| 键鼠安静满 60 秒 | **补救** | 最轻——它一分钟可能响两次，问的是「你还在吗」 |

- 键鼠空闲声**只在 `[60, 180)` 秒窗口内响**：AW 判 afk 的起点回填到最后一次输入（T5），
  过了 180 秒已经救不回来，再响是噪音。判断在整分钟计时点上、查 AW **之前**做
  （AW 连不上时不能把它吞掉）。空闲信号必须自己读（`InputIdle.cs`）——AW 只存结论
  不存原始输入时间戳。
- **跑偏没有任何声音**，只写一行日志。表盘自己会说话。
- **滴答是运行时合成的**（`Tick.cs`）：白噪声 × 指数衰减 + 阻尼正弦 + 0.5ms 淡入，
  35ms/16bit/44.1kHz 写进内存 WAV。「滴」「答」是两个缓冲区（2800/2300Hz），按秒奇偶
  交替——真钟的擒纵两边不对称。音量烤进缓冲区（PlaySound 没有音量参数），改音量重合成。
  触发挂在 33ms 渲染帧的秒跨越上（天然对齐墙钟不漂移）。最小化照样响——钟不因为
  你不看就停。macOS 上合成段一个字不差，落一次临时 wav 再交给 AudioToolbox。
- **喇叭（右上角）只管滴答**，不是总静音——这个程序是**一个钟和一个督工装在同一个
  窗口里**：滴答、图钉属于钟（开关就在钟上）；三声通知属于督工（开关在设置窗口）。
  图钉是**手动**置顶开关，没有状态机（自动置顶那套已死，见 DECISIONS）。

## 12. 设置与数据

`%LOCALAPPDATA%\ItamiTimer\`（macOS `~/Library/Application Support/ItamiTimer`——
**不能用 `SpecialFolder.LocalApplicationData`**，.NET 在 Unix 上映射到 Finder 看不见的
`~/.local/share`）：

| 文件 | 谁写 | 说明 |
|---|---|---|
| `settings.json` | 程序 | 声音、音量、图钉、闹钟/关机开关、`awBaseUrl`。改一下存一下，读写失败吞掉走默认 |
| `rules.json` | 用户 + `Accumulate` | 用户自己那份，重新发布冲不掉 |
| `itami.log` | 程序 | Debug/Release 都写。界面全程沉默，日志是唯一能事后查的地方。1MB 滚动、留一份旧的。绝不能因为写日志把程序搞挂 |

「任务状态不落盘」（原则 3）与这三个文件不冲突：禁的是 current-task.json、累加值这类
**参与判定**的状态。设置和日志删掉不改变程序任何行为。

`UnhandledException` / `UnobservedTaskException` 都接住写日志——界面沉默，崩溃原因
更要留下来。

## 13. 模块与项目布局

```
src/ItamiTimer.Core/   net10.0  类库     判定与重放。无 UI、无平台调用
src/ItamiTimer.Cli/    net10.0  itami    命令行：start / replay，真实 AW 数据干跑
src/ItamiTimer.App/    net10.0  界面     Avalonia 12
tests/ItamiTimer.Core.Tests/    xUnit    纯函数测试
```

边界纪律，以及它们的**执行机制**：

- Core 无 UI —— csproj 不含任何 UI 包引用，塞进去**直接编不过**。保持 `net10.0`。
- Core 无 P/Invoke —— 纪律（编译器挡不住 `DllImport`），csproj 注释写明。
- App 的 TFM 是 `net10.0`（无 `-windows` 后缀）—— 后缀从没起过作用，去掉后 CA1416
  反而真的开始报没守住的平台调用（移植 macOS 时那 15 条警告就是待办清单本身）。
- 平台差异每一处收口在**单个文件**：`Sound` / `Tick` / `MacAudio` / `InputIdle` /
  `WindowPin` / `AppData` / `Shutdown`。没有散落的 `#if`。
- **不引入 `Process.GetProcesses()` / `EnumWindows`**——勾选列表是预定义活动组，
  不是实时进程列表；列表项和判定用同一份规则、同一个 `data.app` 来源。
- 状态与渲染分开：`Replay`/`ToMinuteCells` 在 Core，怎么画是 Cli/App 各自的事。

Core 的原子分层（L0 有 I/O，L1~L3 纯）：

| 层 | 原子 |
|---|---|
| L0 | `AwClient`: Probe / FindBucketId / FetchEvents（内部放宽 6h、UTC→本地、排序） |
| L1 | `Replay`: Clip / Boundaries / CoveringAt / Bridge / PendingPresent |
| L2 | `GroupRules`: RuleMatches / GroupMatches / Classify（+ Accumulate、TodayTomatoes） |
| L3 | `Replay`: Run / ToMinuteCells / ProjectedEnd；`TimeGrid`: Floor/CeilToMinute |

两条自我保护的关键身份标识（改错不报错、只把账算错）：

- `App.axaml` 的 **`Name="ItamiTimer"`**：macOS 上 AW 报的 `data.app` 就是它（不是进程
  名、不是 CFBundleName），不设则默认 `Avalonia Application`，自身豁免整个失效。
  修复方式永远是设 Name，**绝不是**往 `SelfApps` 加 `Avalonia Application`（那会豁免
  所有 Avalonia 应用）。
- csproj 的 **`AssemblyName=ItamiTimer`**：Windows 上 `data.app` 是 exe 名。

## 14. 跨平台（macOS）

判定层平台无关这条经受住了实测：**Core、Cli、全部测试一行没改就在 macOS（arm64）上
全过**。工作量全在 App 层，见 §13 的收口文件表。

- **必须打成 `.app`**（`pack-macos.sh`：发布 → 从 `TomatoIcon` 导 iconset → iconutil →
  组 bundle → ad-hoc 签名）。除图标外，bundle 用 `LSEnvironment` 把 `DOTNET_ROOT` 写进
  Info.plist——双击启动的 GUI 不继承 shell 环境变量，.NET 装在非标准位置时终端里能跑、
  Finder 点开报「You must install .NET」。挪动 .NET 后要重新打包。
- 三个调试出口（`--dial-specimens` / `--export-icon` / `--export-iconset`）走
  **headless**（`UseHeadless` + `UseSkia`，`UseHeadlessDrawing=false` 才是真像素），
  不初始化窗口平台——`pack-macos.sh` 的 `--export-iconset` 是构建的一环，构建不该
  依赖有没有人登录着桌面（无图形会话时 `UsePlatformDetect` 崩在 RenderTimer -6661）。
  **正常启动路径不走 headless。**
- 未验证风险：macOS 的 `localizedName` 可能随**系统**语言变（中文系统 `Finder`→`访达`），
  `ignore` 名单会失效。影响算账正确性、与界面语言无关，要支持非英文系统需先实测。

## 15. 界面语言：英文

不是 i18n——没有资源文件、没有切换，英文写死。四条边界：

| | 语言 |
|---|---|
| 界面、CLI、日志、异常消息 | 英文 |
| 窗口标题「一袋米要扛几楼」 | 中文（产品名，是梗不是短语） |
| `rules.json` | 用户数据，语言由用户读什么书决定 |
| 源码注释、本文档、DECISIONS、CLAUDE.md | 中文（开发记录） |

## 16. 构建与发布

```bash
dotnet build ItamiTimer.slnx
dotnet test ItamiTimer.slnx
dotnet publish src/ItamiTimer.App -c Release -r win-x64 --self-contained false -o "$LOCALAPPDATA/Programs/ItamiTimer"
./pack-macos.sh        # macOS
```

- **必须限定 RID**：否则所有平台的 Skia/HarfBuzz 原生库+符号全进来（实测 560MB）。
- csproj 的 `StripPdbFromPublish`（`AfterTargets="Publish"`）自动删掉
  `libSkiaSharp.pdb`(80MB) + `libHarfBuzzSharp.pdb`(20MB)：127MB → 27MB。
  **不要删这个 target，也不要退回文档里的手工步骤**——手工步骤已经被忘过一次。
- exe 图标由 `--export-icon` 从 `TomatoIcon` 导出，仓库里的 `.ico` 只是导出结果。

## 17. 手动验证清单

时长临时设 1~2 分钟（Core 不设下限，正是为了验证；量程约束只在滑块上）：

1. 最小化去摸鱼 1 分钟 → 回来那分钟是红的。
2. 专注中点关闭 → 必须确认；最小化不问。
3. 休息结束 → 停住等用户，绝不自动开下一轮。
4. 补勾小目标 → 之前的红格追溯变绿。
5. **停在目标应用上走开超过 180 秒 → 不计入**（最省力的作弊路径，光看窗口事件测不出）。
6. 看一眼 ItamiTimer 自己 → 不违规、不死循环。
7. 锁屏几分钟 → 不计入（Absent 压过 Neutral）。
8. 不开 AW 启动 → 番茄钟模式（列表消失、全绿）；任务中途停掉 aw-server → 那几分钟
   报无数据，恢复后补齐。
9. 闹钟：拨到恰好与时针重合 → 提示显示 12 小时后，不当场响。
